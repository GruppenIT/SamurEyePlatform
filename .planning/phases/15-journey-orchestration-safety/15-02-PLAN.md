---
phase: 15-journey-orchestration-safety
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - shared/schema.ts
  - server/storage/database-init.ts
autonomous: true
requirements:
  - JRNY-01
  - JRNY-02
  - JRNY-04

must_haves:
  truths:
    - "journeyTypeEnum em shared/schema.ts inclui 'api_security' como 5o valor"
    - "Tabela journeys tem coluna authorization_ack boolean NOT NULL DEFAULT false"
    - "Tipo TypeScript Journey (derivado de journeys.$inferSelect) inclui authorizationAck: boolean"
    - "insertJourneySchema (derivado via createInsertSchema) aceita authorizationAck no body"
    - "ensureJourneyApiSecurityColumns() é chamado em initializeDatabaseStructure() após ensureApiEndpointHttpxColumns e aplica ALTER TYPE + ALTER TABLE idempotentes"
    - "Re-rodar o boot do app não gera erro (IF NOT EXISTS + ADD VALUE IF NOT EXISTS)"
    - "Scheduler existente aceita schedule para journey_type=api_security sem alteração de código (enum extension é suficiente — JRNY-04)"
  artifacts:
    - path: "shared/schema.ts"
      provides: "journeyTypeEnum estendido + authorizationAck column + insertJourneySchema atualizado"
      contains: "api_security"
    - path: "server/storage/database-init.ts"
      provides: "ensureJourneyApiSecurityColumns() guard function + call site em initializeDatabaseStructure"
      contains: "ensureJourneyApiSecurityColumns"
  key_links:
    - from: "shared/schema.ts journeyTypeEnum"
      to: "server/services/journeyExecutor.ts switch(journey.type)"
      via: "TypeScript type narrowing — Plan 04 adiciona case 'api_security'"
      pattern: "'attack_surface'.*'ad_security'.*'edr_av'.*'web_application'.*'api_security'"
    - from: "journeys.authorizationAck column"
      to: "Plan 04 executeApiSecurity() guard check"
      via: "journey.authorizationAck === true check no executor"
      pattern: "authorization_ack.*boolean.*DEFAULT false"
---

<objective>
Estender o schema para suportar journey_type `api_security` e persistir o `authorizationAck` (JRNY-01, JRNY-02) — com guard idempotente `ensureJourneyApiSecurityColumns()` que aplica `ALTER TYPE ... ADD VALUE IF NOT EXISTS` + `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` no boot do app (padrão consolidado em Phases 9/10/11 — `ensureApiTables`, `ensureApiCredentialTables`, `ensureApiEndpointHttpxColumns`).

Purpose: Sem esse schema, o Plan 04 (`executeApiSecurity`) não pode ser chamado — TypeScript barraria `switch case 'api_security'` e Postgres rejeitaria insert de journey com type inválido. Também habilita JRNY-04: scheduler existente passa a aceitar api_security automaticamente.

Output: shared/schema.ts atualizado, database-init.ts com novo guard, e migration aplicada em tempo de boot (sem arquivo SQL separado — projeto não usa migration runner).
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/15-journey-orchestration-safety/15-CONTEXT.md
@.planning/phases/15-journey-orchestration-safety/15-RESEARCH.md

# Reference: padrão de guard estabelecido em Phases 9/10/11
@server/storage/database-init.ts

# Reference: schema atual
@shared/schema.ts
</context>

<interfaces>
<!-- SCHEMA ATUAL CONFIRMADO VIA LEITURA DIRETA -->

Linha 41 de shared/schema.ts (atual):
```typescript
export const journeyTypeEnum = pgEnum('journey_type', ['attack_surface', 'ad_security', 'edr_av', 'web_application']);
```

Linhas 176-189 de shared/schema.ts (atual):
```typescript
export const journeys = pgTable("journeys", {
  id: varchar("id").primaryKey().default(sql`gen_random_uuid()`),
  name: varchar("name").notNull(),
  type: journeyTypeEnum("type").notNull(),
  description: text("description"),
  params: jsonb("params").$type<Record<string, any>>().default({}).notNull(),
  targetSelectionMode: targetSelectionModeEnum("target_selection_mode").default('individual').notNull(),
  selectedTags: jsonb("selected_tags").$type<string[]>().default([]).notNull(),
  enableCveDetection: boolean("enable_cve_detection").default(true).notNull(),
  createdAt: timestamp("created_at").defaultNow().notNull(),
  createdBy: varchar("created_by").references(() => users.id).notNull(),
  updatedAt: timestamp("updated_at").defaultNow().notNull(),
});
```

Linhas 930-935 de shared/schema.ts (atual):
```typescript
export const insertJourneySchema = createInsertSchema(journeys).omit({
  id: true,
  createdAt: true,
  createdBy: true,
  updatedAt: true,
});
```

Padrão de guard idempotente em server/storage/database-init.ts (linhas 447-461):
```typescript
export async function ensureApiEndpointHttpxColumns(): Promise<void> {
  try {
    await db.execute(sql`
      ALTER TABLE api_endpoints
        ADD COLUMN IF NOT EXISTS httpx_status INTEGER,
        ...
    `);
    log.info('ensureApiEndpointHttpxColumns complete');
  } catch (error) {
    log.error({ err: error }, 'ensureApiEndpointHttpxColumns error');
  }
}
```

Call site em initializeDatabaseStructure (linhas 138-145):
```typescript
await ensureApiTables();
await ensureApiCredentialTables();
await ensureApiEndpointHttpxColumns();
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Estender journeyTypeEnum com 'api_security' + adicionar coluna authorizationAck na tabela journeys em shared/schema.ts</name>
  <files>shared/schema.ts</files>
  <behavior>
    - journeyTypeEnum.enumValues contém exatamente 5 strings na ordem: ['attack_surface', 'ad_security', 'edr_av', 'web_application', 'api_security']
    - journeys table tem campo authorizationAck: boolean('authorization_ack').notNull().default(false)
    - type Journey (inferido de journeys.$inferSelect) inclui propriedade authorizationAck: boolean
    - insertJourneySchema aceita { authorizationAck?: boolean } no parse (optional com default false via Drizzle)
    - insertJourneySchema.parse({ name: 'x', type: 'api_security', createdBy: 'u1' }) NÃO lança erro
  </behavior>
  <read_first>
    - shared/schema.ts (estado atual — linhas 41, 176-189, 930-935)
    - .planning/phases/15-journey-orchestration-safety/15-RESEARCH.md §Pattern 1 (pgEnum extension) + §Pitfall 5 (authorizationAck em Zod schema)
  </read_first>
  <action>
Fazer DUAS edições em `shared/schema.ts`:

### EDIT 1 — Linha 41 (journeyTypeEnum)

SUBSTITUIR exatamente:
```typescript
export const journeyTypeEnum = pgEnum('journey_type', ['attack_surface', 'ad_security', 'edr_av', 'web_application']);
```

POR:
```typescript
export const journeyTypeEnum = pgEnum('journey_type', ['attack_surface', 'ad_security', 'edr_av', 'web_application', 'api_security']);
```

Mudança: adicionar `'api_security'` como 5o elemento do array (ordem append-only porque Postgres não permite remover valores de enum).

### EDIT 2 — Linha 186 (antes de `createdAt`) da tabela `journeys`

LOCALIZAR o bloco da tabela `journeys` (começa em `export const journeys = pgTable("journeys", {` por volta da linha 177) e INSERIR a linha nova `authorizationAck` imediatamente ANTES da linha `createdAt: timestamp("created_at")...`:

```typescript
  // Phase 15 JRNY-02 — journey api_security requires explicit test-authorization ack.
  // Default false keeps existing journeys (attack_surface/ad_security/edr_av/web_application) unaffected.
  authorizationAck: boolean("authorization_ack").notNull().default(false),
```

Resultado esperado do bloco journeys após edição:
```typescript
export const journeys = pgTable("journeys", {
  id: varchar("id").primaryKey().default(sql`gen_random_uuid()`),
  name: varchar("name").notNull(),
  type: journeyTypeEnum("type").notNull(),
  description: text("description"),
  params: jsonb("params").$type<Record<string, any>>().default({}).notNull(),
  targetSelectionMode: targetSelectionModeEnum("target_selection_mode").default('individual').notNull(),
  selectedTags: jsonb("selected_tags").$type<string[]>().default([]).notNull(),
  enableCveDetection: boolean("enable_cve_detection").default(true).notNull(),
  // Phase 15 JRNY-02 — journey api_security requires explicit test-authorization ack.
  // Default false keeps existing journeys (attack_surface/ad_security/edr_av/web_application) unaffected.
  authorizationAck: boolean("authorization_ack").notNull().default(false),
  createdAt: timestamp("created_at").defaultNow().notNull(),
  createdBy: varchar("created_by").references(() => users.id).notNull(),
  updatedAt: timestamp("updated_at").defaultNow().notNull(),
});
```

### EDIT 3 — insertJourneySchema (linhas 930-935)

O `insertJourneySchema` já usa `createInsertSchema(journeys)`, então `authorizationAck` será auto-derivado como `boolean` opcional (por ter `.default(false)`). NÃO precisa de mudança — mas CONFIRMAR via search que `.omit({...})` na declaração NÃO inclui `authorizationAck` no omit set. Deve permanecer exatamente:

```typescript
export const insertJourneySchema = createInsertSchema(journeys).omit({
  id: true,
  createdAt: true,
  createdBy: true,
  updatedAt: true,
});
```

Se encontrar `authorizationAck: true` dentro do `.omit({...})` — REMOVER esse campo do omit (schema precisa aceitá-lo). (Defensivo — não deve ser necessário.)

### ADICIONAR teste unit no arquivo `server/__tests__/journeyOrchestration.test.ts`

Localizar o `describe('JRNY-01 —` criado no Plano 15-01 e SUBSTITUIR o primeiro `it.todo` por teste real:

```typescript
describe('JRNY-01 — journey_type enum contains api_security', () => {
  it('shared/schema.ts journeyTypeEnum array inclui "api_security" como 5o valor', async () => {
    const { journeyTypeEnum } = await import('@shared/schema');
    expect(journeyTypeEnum.enumValues).toEqual([
      'attack_surface', 'ad_security', 'edr_av', 'web_application', 'api_security'
    ]);
  });
  it.todo('switch statement em journeyExecutor.ts roteia case api_security para executeApiSecurity()');
});
```

E adicionar import no topo do arquivo de test:
```typescript
import { describe, it, expect } from 'vitest';
```

Localizar o `describe('JRNY-02 —` e SUBSTITUIR o segundo `it.todo` (a linha que diz "migration adiciona coluna authorization_ack boolean..."):

```typescript
  it('migration adiciona coluna authorization_ack boolean NOT NULL DEFAULT false em journeys', async () => {
    const { journeys } = await import('@shared/schema');
    // Drizzle table object exposes column metadata
    expect(journeys.authorizationAck).toBeDefined();
    expect(journeys.authorizationAck.notNull).toBe(true);
    expect(journeys.authorizationAck.default).toBe(false);
    expect(journeys.authorizationAck.name).toBe('authorization_ack');
  });
  it('insertJourneySchema (derivado via createInsertSchema) inclui authorizationAck como boolean', async () => {
    const { insertJourneySchema } = await import('@shared/schema');
    const result = insertJourneySchema.safeParse({
      name: 'test', type: 'api_security', authorizationAck: true, params: {}, targetSelectionMode: 'individual', selectedTags: [], enableCveDetection: false
    });
    expect(result.success).toBe(true);
  });
```

(Mantém os demais `it.todo` intactos — serão promovidos por Plan 04.)
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/journeyOrchestration.test.ts -t "JRNY-01" --reporter=verbose &amp;&amp; npx vitest run server/__tests__/journeyOrchestration.test.ts -t "JRNY-02" --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "journeyTypeEnum = pgEnum" shared/schema.ts` mostra linha com 5 valores incluindo `'api_security'`
    - `grep -n "authorization_ack" shared/schema.ts` retorna 1+ linhas dentro do bloco `export const journeys = pgTable`
    - `grep -c "'api_security'" shared/schema.ts` retorna pelo menos 1
    - `grep -c "authorizationAck: boolean" shared/schema.ts` retorna 1 ou mais
    - Command `npx tsc --noEmit -p .` (ou `npm run check` se existir) retorna exit 0 — nenhum erro de tipo em shared/
    - Command `npx vitest run server/__tests__/journeyOrchestration.test.ts -t "JRNY-01"` passa 1 test (não mais .todo)
    - Command `npx vitest run server/__tests__/journeyOrchestration.test.ts -t "JRNY-02"` passa 2 tests (não mais .todo para migration + insertJourneySchema)
    - `insertJourneySchema.omit` NÃO contém `authorizationAck: true`
  </acceptance_criteria>
  <done>journeyTypeEnum contém api_security, tabela journeys tem authorization_ack boolean NOT NULL DEFAULT false, insertJourneySchema aceita authorizationAck, testes JRNY-01 e 2/3 de JRNY-02 passam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar ensureJourneyApiSecurityColumns() guard em database-init.ts + wire no initializeDatabaseStructure</name>
  <files>server/storage/database-init.ts</files>
  <behavior>
    - Função exportada `ensureJourneyApiSecurityColumns(): Promise<void>` existe em database-init.ts
    - Função executa DOIS statements: `ALTER TYPE journey_type ADD VALUE IF NOT EXISTS 'api_security'` + `ALTER TABLE journeys ADD COLUMN IF NOT EXISTS authorization_ack boolean NOT NULL DEFAULT false`
    - Função tem estrutura try/catch com log.info no sucesso e log.error no erro (NÃO re-throw — segue padrão de `ensureApiEndpointHttpxColumns`)
    - `initializeDatabaseStructure()` chama `ensureJourneyApiSecurityColumns()` DEPOIS de `ensureApiEndpointHttpxColumns()`
    - Re-rodar initializeDatabaseStructure é idempotente (nenhum erro Postgres)
  </behavior>
  <read_first>
    - server/storage/database-init.ts (linhas 138-151 initializeDatabaseStructure call order, linhas 447-461 ensureApiEndpointHttpxColumns template)
    - .planning/phases/15-journey-orchestration-safety/15-RESEARCH.md §Pattern 1 + §Pitfall 1 (Drizzle NÃO auto-gera ALTER TYPE ADD VALUE)
  </read_first>
  <action>
Fazer DUAS edições em `server/storage/database-init.ts`:

### EDIT 1 — Adicionar chamada em initializeDatabaseStructure

LOCALIZAR (linhas 138-145 aproximadamente):
```typescript
    // Phase 9: API Discovery tables (HIER-01, HIER-02, FIND-01)
    await ensureApiTables();

    // Phase 10: API Credentials table (CRED-01..04)
    await ensureApiCredentialTables();

    // Phase 11: httpx enrichment columns on api_endpoints (ENRH-01)
    await ensureApiEndpointHttpxColumns();  // Phase 11 ENRH-01

  } catch (error) {
```

INSERIR antes do `} catch (error) {`:
```typescript
    // Phase 15: journey_type enum extension + authorizationAck column (JRNY-01, JRNY-02)
    await ensureJourneyApiSecurityColumns();

```

### EDIT 2 — Adicionar a função guard no final do arquivo (após `ensureApiEndpointHttpxColumns`, antes de `ensureSystemUserExists`)

LOCALIZAR (final da função ensureApiEndpointHttpxColumns, linha ~461):
```typescript
    log.info('ensureApiEndpointHttpxColumns complete');
  } catch (error) {
    log.error({ err: error }, 'ensureApiEndpointHttpxColumns error');
  }
}

export async function ensureSystemUserExists(): Promise<void> {
```

INSERIR a nova função ENTRE essas duas:
```typescript
    log.info('ensureApiEndpointHttpxColumns complete');
  } catch (error) {
    log.error({ err: error }, 'ensureApiEndpointHttpxColumns error');
  }
}

// Phase 15 JRNY-01, JRNY-02 — runtime idempotent guard for journey_type enum extension +
// journeys.authorization_ack column. Drizzle does NOT auto-generate ALTER TYPE ADD VALUE
// migrations (RESEARCH.md §Pitfall 1), so this guard handles it at boot.
// Pattern mirrors ensureApiEndpointHttpxColumns: try/catch with logged failure, no re-throw.
// Safe to re-run; idempotent via IF NOT EXISTS clauses.
export async function ensureJourneyApiSecurityColumns(): Promise<void> {
  try {
    // Extend journey_type enum with 'api_security' value (append-only per Postgres semantics).
    await db.execute(sql`
      ALTER TYPE journey_type ADD VALUE IF NOT EXISTS 'api_security'
    `);

    // Add authorization_ack boolean column with NOT NULL DEFAULT false.
    // Default false keeps existing journeys unaffected (attack_surface/ad_security/edr_av/web_application).
    await db.execute(sql`
      ALTER TABLE journeys
        ADD COLUMN IF NOT EXISTS authorization_ack boolean NOT NULL DEFAULT false
    `);

    log.info('ensureJourneyApiSecurityColumns complete');
  } catch (error) {
    log.error({ err: error }, 'ensureJourneyApiSecurityColumns error');
  }
}

export async function ensureSystemUserExists(): Promise<void> {
```

### EDIT 3 — Adicionar teste no server/__tests__/journeyOrchestration.test.ts (opcional — se vitest test helper permitir DB stub)

Para este task, a verificação primária é TypeScript compila + função exportada. Sem mock de DB, não executamos o SQL no test. (O teste real acontece no smoke manual de boot.) Pular adição de teste neste task — verify usa compilation + grep.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p . 2>&amp;1 | head -20 &amp;&amp; grep -c "ensureJourneyApiSecurityColumns" server/storage/database-init.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function ensureJourneyApiSecurityColumns" server/storage/database-init.ts` returns 1
    - `grep -c "await ensureJourneyApiSecurityColumns()" server/storage/database-init.ts` returns 1 (call site em initializeDatabaseStructure)
    - `grep -c "ADD VALUE IF NOT EXISTS 'api_security'" server/storage/database-init.ts` returns 1
    - `grep -c "ADD COLUMN IF NOT EXISTS authorization_ack" server/storage/database-init.ts` returns 1
    - O texto da função contém `try {` e `} catch (error) {` (segue padrão idempotente)
    - A função NÃO contém `throw error` dentro do catch (segue padrão swallow)
    - Call site em initializeDatabaseStructure aparece DEPOIS da linha com `ensureApiEndpointHttpxColumns()` (ordem posterior)
    - `npx tsc --noEmit -p .` retorna exit 0 — sem erros de tipo
  </acceptance_criteria>
  <done>Função ensureJourneyApiSecurityColumns exportada + chamada no initializeDatabaseStructure logo após ensureApiEndpointHttpxColumns, TypeScript compila sem erros, SQL usa IF NOT EXISTS em ambos os statements.</done>
</task>

</tasks>

<verification>
# Verificação integrada: schema + guard + tipo Journey
npx tsc --noEmit -p . 2>&1 | head -30

# Testes cobertos neste plano
npx vitest run server/__tests__/journeyOrchestration.test.ts -t "JRNY-01" --reporter=verbose
npx vitest run server/__tests__/journeyOrchestration.test.ts -t "JRNY-02" --reporter=verbose

# Verificação estrutural
grep -n "'api_security'" shared/schema.ts
grep -n "authorization_ack\|authorizationAck" shared/schema.ts
grep -n "ensureJourneyApiSecurityColumns" server/storage/database-init.ts
</verification>

<success_criteria>
- journeyTypeEnum tem 5 valores: ['attack_surface', 'ad_security', 'edr_av', 'web_application', 'api_security']
- Coluna authorization_ack existe como boolean NOT NULL DEFAULT false
- ensureJourneyApiSecurityColumns() aplica ambas as migrations idempotentemente
- Chamada no initializeDatabaseStructure após ensureApiEndpointHttpxColumns
- tsc --noEmit retorna exit 0
- Tests JRNY-01 (1) + JRNY-02 (2/3) passam — o 3o de JRNY-02 (executor guard) é do Plan 04
</success_criteria>

<output>
Após completar, criar `.planning/phases/15-journey-orchestration-safety/15-02-SUMMARY.md` com:
- Diff resumido de shared/schema.ts (antes/depois)
- Diff resumido de database-init.ts (novo export + call site)
- Tests promovidos de it.todo → it (contagem)
- Confirmação de idempotência do guard via boot simulado (ou afirmação de que IF NOT EXISTS protege)
</output>
