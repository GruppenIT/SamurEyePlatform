---
phase: 01-parser-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - server/services/scanners/networkScanner.ts
  - server/__tests__/fixtures/nmap/smb-open-no-vuln.xml
  - server/__tests__/fixtures/nmap/rdp-with-os-detection.xml
  - server/__tests__/fixtures/nmap/vuln-ms17-010.xml
  - server/__tests__/fixtures/nmap/multi-host-cidr.xml
  - server/__tests__/fixtures/nmap/single-host-all-fields.xml
  - server/__tests__/fixtures/nmap/filtered-ports-only.xml
  - server/__tests__/fixtures/nmap/os-detection-cpe.xml
  - server/__tests__/fixtures/nmap/service-version-cpe.xml
  - server/__tests__/nmapParser.test.ts
  - package.json
autonomous: true
requirements:
  - PARS-01
  - PARS-03
  - PARS-04
  - PARS-10

must_haves:
  truths:
    - "nmap XML output is parsed into typed NmapFinding objects without any regex"
    - "OS detection data (name, accuracy, CPE) is captured in NmapFinding fields"
    - "Service version details (product, version, extrainfo, CPE) are captured in NmapFinding fields"
    - "Single-host and multi-host scans both produce arrays of NmapFinding"
    - "Invalid nmap XML lines are logged and skipped, not thrown"
  artifacts:
    - path: "shared/schema.ts"
      provides: "BaseFinding, NmapFinding, NseScript interfaces and Zod schemas"
      contains: "NmapFindingSchema"
    - path: "server/services/scanners/networkScanner.ts"
      provides: "parseNmapXml method replacing parseNmapOutput"
      contains: "XMLParser"
    - path: "server/__tests__/nmapParser.test.ts"
      provides: "Snapshot tests for nmap parser output"
      contains: "toMatchSnapshot"
  key_links:
    - from: "server/services/scanners/networkScanner.ts"
      to: "shared/schema.ts"
      via: "import NmapFindingSchema"
      pattern: "NmapFindingSchema\\.safeParse"
    - from: "server/services/scanners/networkScanner.ts"
      to: "fast-xml-parser"
      via: "XMLParser import"
      pattern: "XMLParser"
---

<objective>
Rewrite the nmap parser from text/regex to XML output, define the NormalizedFinding type system, and install fast-xml-parser.

Purpose: Establishes the type foundation (BaseFinding discriminated union + Zod schemas) that all 4 parsers will share, and replaces the fragile 600-line text parser in networkScanner.ts with structured XML parsing. This is the foundation for PARS-01, PARS-03, PARS-04, and begins PARS-10.

Output: NmapFinding Zod schema in shared/schema.ts, parseNmapXml method in networkScanner.ts, nmap fixture files, snapshot tests.
</objective>

<execution_context>
@C:/Users/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-parser-foundation/01-CONTEXT.md
@.planning/phases/01-parser-foundation/01-RESEARCH.md

@shared/schema.ts
@server/services/scanners/networkScanner.ts
@server/services/threatEngine.ts

<interfaces>
<!-- Key types from the codebase that the executor needs -->

From shared/schema.ts (existing Zod/Drizzle setup):
- Already imports `z` from 'zod'
- Already imports drizzle-zod; existing schemas like `insertThreatSchema`, `threatSchema`
- Use `@shared/*` path alias for imports

From server/services/scanners/networkScanner.ts (existing exports):
```typescript
export interface PortScanResult { /* existing port scan result */ }
export interface AliveHostResult { /* existing alive host check */ }
export interface ServiceInfo { /* existing service info */ }
export class NetworkScanner { /* singleton class with scanPorts, scanCIDR */ }
export const networkScanner = new NetworkScanner();
```

From server/services/threatEngine.ts (threat rules consuming parser output):
- 25 rules with `id` field; nmap-relevant rules: 'exposed-service', 'cve-detected'
- Rules match on `finding.type === 'port'` and `finding.type === 'nmap_vuln'`
- `detectThreats()` is the main entry point
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define NormalizedFinding type system and NmapFinding Zod schema</name>
  <files>shared/schema.ts, package.json</files>
  <behavior>
    - BaseFindingSchema validates objects with type (string), target (string), severity (enum: low/medium/high/critical), optional timestamp
    - NseScriptSchema validates objects with id (string), output (string), optional cves (string[]), optional exploitState (string), optional tables (Record)
    - NmapFindingSchema extends BaseFinding with type literal 'port', ip, port, state (open/closed/filtered), service, plus optional product/version/extrainfo/serviceCpe/osName/osAccuracy/osCpe/nseScripts/banner/osInfo
    - NmapFindingSchema.strip() removes unknown fields
    - safeParse rejects objects missing required fields (type, target, severity, port, state, service)
    - safeParse accepts objects with all optional fields populated
    - Also define NmapVulnFindingSchema with type literal 'nmap_vuln' (same shape, different discriminator for threat engine compatibility)
  </behavior>
  <action>
    1. Run `npm install fast-xml-parser` to add the dependency.
    2. In `shared/schema.ts`, add new Zod schemas AFTER the existing Drizzle table definitions (do NOT modify any existing code). Add:
       - `BaseFindingSchema` with type, target, severity enum, optional timestamp
       - `NseScriptSchema` with id, output, optional cves/exploitState/tables
       - `NmapFindingSchema` extending BaseFinding with type literal 'port', fields: ip (optional), port (string), state enum, service (string), plus optional product/version/extrainfo/serviceCpe/osName/osAccuracy(number)/osCpe(string[])/nseScripts(NseScript[])/banner/osInfo. Call `.strip()`.
       - `NmapVulnFindingSchema` — same shape as NmapFindingSchema but with `type: z.literal('nmap_vuln')`. This preserves compatibility with threatEngine rule 'cve-detected' which matches on `type === 'nmap_vuln'`.
       - Export inferred types: `NmapFinding`, `NmapVulnFinding`, `NseScript`, `BaseFinding`
    3. Name schemas carefully to avoid collision with existing `threatSchema`, `insertThreatSchema` etc. Use the `*FindingSchema` suffix pattern.
    4. Write a small test in `server/__tests__/nmapParser.test.ts` that validates safeParse accepts a valid NmapFinding and rejects an invalid one.
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/nmapParser.test.ts</automated>
  </verify>
  <done>
    - NmapFindingSchema and NmapVulnFindingSchema exported from shared/schema.ts
    - fast-xml-parser in package.json dependencies
    - safeParse accepts valid NmapFinding objects, rejects invalid ones
    - TypeScript compiles without errors (`npx tsc --noEmit`)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite nmap parser to XML and create snapshot tests with fixtures</name>
  <files>
    server/services/scanners/networkScanner.ts,
    server/__tests__/nmapParser.test.ts,
    server/__tests__/fixtures/nmap/smb-open-no-vuln.xml,
    server/__tests__/fixtures/nmap/rdp-with-os-detection.xml,
    server/__tests__/fixtures/nmap/vuln-ms17-010.xml,
    server/__tests__/fixtures/nmap/multi-host-cidr.xml,
    server/__tests__/fixtures/nmap/single-host-all-fields.xml,
    server/__tests__/fixtures/nmap/filtered-ports-only.xml,
    server/__tests__/fixtures/nmap/os-detection-cpe.xml,
    server/__tests__/fixtures/nmap/service-version-cpe.xml
  </files>
  <behavior>
    - parseNmapXml('valid XML with one host, one open port') returns array with one NmapFinding
    - parseNmapXml('XML with multiple hosts') returns findings for each host's open ports
    - parseNmapXml('XML with OS detection') populates osName, osAccuracy, osCpe fields
    - parseNmapXml('XML with service version') populates product, version, extrainfo, serviceCpe fields
    - parseNmapXml('XML with NSE scripts') populates nseScripts array with id, output, cves
    - parseNmapXml('XML with only filtered ports') returns empty array (no open ports)
    - parseNmapXml('empty/malformed XML') returns empty array and logs warning
    - All nmap args builders include '-oX -' and do NOT include '-oN' or '-oG'
    - Each finding passes NmapFindingSchema.safeParse validation
  </behavior>
  <action>
    1. Create synthetic nmap XML fixture files under `server/__tests__/fixtures/nmap/`. Each file is a valid nmap XML document (`<nmaprun>` root) crafted to test specific scenarios:
       - `smb-open-no-vuln.xml`: single host, port 445 open, SMB service, no scripts (tests 'exposed-service' rule)
       - `rdp-with-os-detection.xml`: single host, port 3389 open, RDP service, OS detection block with Windows Server 2019, 98% accuracy, CPE (tests PARS-03)
       - `vuln-ms17-010.xml`: single host, port 445, NSE script `smb-vuln-ms17-010` with CVE-2017-0144 in `<table>/<elem>` (tests PARS-02 in plan 01-02, but fixture needed now)
       - `multi-host-cidr.xml`: 3 hosts with various ports (tests CIDR array handling)
       - `single-host-all-fields.xml`: one host with OS, service version (product, version, extrainfo, serviceCpe CPE), hostname, NSE scripts — maximum field coverage
       - `filtered-ports-only.xml`: one host where all ports are state="filtered" — expect empty results
       - `os-detection-cpe.xml`: one host with `<os><osmatch>` containing multiple `<osclass>` with CPE strings (tests PARS-03 CPE array)
       - `service-version-cpe.xml`: one host with service element containing product, version, extrainfo, and `<cpe>` child (tests PARS-04)

    2. In `networkScanner.ts`, add a new public method `parseNmapXml(stdout: string, originalTarget: string): NmapFinding[]` following the pattern from RESEARCH.md:
       - Import `XMLParser` from `fast-xml-parser`
       - Configure with `isArray` for: host, port, script, osmatch, osclass, cpe, elem, table, hostname, address
       - Set `ignoreAttributes: false`, `attributeNamePrefix: '@_'`, `parseAttributeValue: true`
       - Iterate `doc.nmaprun.host[]`, extract IP from address array, hostname from hostnames
       - For each open port: build raw object, `NmapFindingSchema.safeParse()`, push valid findings
       - Extract OS from `host.os.osmatch[0]`: osName, osAccuracy, osCpe (flatMap from osclass.cpe)
       - Extract service: product, version, extrainfo, serviceCpe from `port.service`
       - Extract NSE scripts: map `port.script[]` to NseScript objects, extract CVEs from `table/elem`
       - On parse error: log.warn and return empty array (do not throw)

    3. Update all `buildNmapArgs`-style methods in networkScanner.ts:
       - Add `'-oX', '-'` to args
       - Remove any `-oN` or `-oG` flags if present
       - Keep existing `-sV`, `-O`, port lists, timing args

    4. Replace internal calls to `parseNmapOutput(stdout)` with `parseNmapXml(stdout, target)`. Keep the old `parseNmapOutput` method marked as `@deprecated` but do NOT delete it yet (journeyExecutor may still reference it; that wiring happens in plan 01-02).

    5. Add snapshot tests in `server/__tests__/nmapParser.test.ts`:
       - For each fixture: load XML, call `parseNmapXml`, `expect(findings).toMatchSnapshot()`
       - Specific field assertions: OS fields populated for `os-detection-cpe.xml`, service version fields for `service-version-cpe.xml`
       - Edge case: filtered-ports fixture returns empty array
       - Edge case: malformed XML string returns empty array

    6. Ensure `commonPorts` array, `AliveHostResult` interface, and `PortScanResult` interface are preserved — they are used by other parts of the codebase.
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/nmapParser.test.ts</automated>
  </verify>
  <done>
    - parseNmapXml parses all 8 fixture files correctly
    - OS detection fields (osName, osAccuracy, osCpe) populated from XML (PARS-03)
    - Service version fields (product, version, extrainfo, serviceCpe) populated from XML (PARS-04)
    - nmap spawn args include '-oX -' and no '-oN'/'-oG' (PARS-01)
    - Single-host and multi-host fixtures both produce valid NmapFinding arrays
    - All snapshot tests pass
    - TypeScript compiles: `npx tsc --noEmit`
  </done>
</task>

</tasks>

<verification>
```bash
# All nmap parser tests pass
npx vitest run server/__tests__/nmapParser.test.ts

# TypeScript compiles
npx tsc --noEmit

# fast-xml-parser installed
node -e "require('fast-xml-parser')"

# NmapFindingSchema exported
node -e "const s = require('./shared/schema'); console.log(typeof s.NmapFindingSchema)"
```
</verification>

<success_criteria>
- fast-xml-parser installed and imported in networkScanner.ts
- NmapFindingSchema and NmapVulnFindingSchema in shared/schema.ts with .strip()
- parseNmapXml method on NetworkScanner class parses XML with OS/service/NSE data
- 8 nmap XML fixture files under server/__tests__/fixtures/nmap/
- Snapshot tests pass for all fixtures
- Existing PortScanResult, AliveHostResult, ServiceInfo interfaces unchanged
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-parser-foundation/01-01-SUMMARY.md`
</output>
