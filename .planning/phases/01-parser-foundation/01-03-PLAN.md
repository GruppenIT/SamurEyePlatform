---
phase: 01-parser-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - shared/schema.ts
  - server/services/scanners/adScanner.ts
  - server/services/scanners/edrAvScanner.ts
  - server/__tests__/fixtures/ad/password-never-expires.json
  - server/__tests__/fixtures/ad/privileged-group-members.json
  - server/__tests__/fixtures/ad/obsolete-os.json
  - server/__tests__/fixtures/ad/inactive-users.json
  - server/__tests__/fixtures/ad/weak-password-policy.json
  - server/__tests__/fixtures/ad/domain-controller-not-found.json
  - server/__tests__/fixtures/ad/inactive-computers.json
  - server/__tests__/fixtures/ad/bidirectional-trust.json
  - server/__tests__/fixtures/ad/old-passwords.json
  - server/__tests__/fixtures/ad/domain-admin-critical.json
  - server/__tests__/fixtures/ad/password-complexity-disabled.json
  - server/__tests__/fixtures/ad/password-history-insufficient.json
  - server/__tests__/fixtures/ad/generic-ad-finding.json
  - server/__tests__/fixtures/ad/specific-inactive-user.json
  - server/__tests__/fixtures/ad/privileged-group-too-many.json
  - server/__tests__/fixtures/ad/password-never-expires-user.json
  - server/__tests__/fixtures/edr/detection-success.json
  - server/__tests__/fixtures/edr/detection-failure.json
  - server/__tests__/fixtures/edr/timeout-error.json
  - server/__tests__/adParser.test.ts
  - server/__tests__/edrParser.test.ts
  - server/__tests__/threatRuleSnapshots.test.ts
autonomous: true
requirements:
  - PARS-07
  - PARS-08
  - PARS-09
  - PARS-10
  - PARS-11

must_haves:
  truths:
    - "AD PowerShell commands use -Depth 10 so nested group memberships and GPO structures are fully serialized"
    - "AD parser outputs AdFinding with group membership chains as string arrays, GPO links as structured objects, trust attributes as typed records"
    - "EDR parser outputs EdrFinding with per-host timeline array showing deploy/detect/cleanup events with timestamps"
    - "All 4 parser types (NmapFinding, NucleiFinding, AdFinding, EdrFinding) are typed discriminated unions validated by Zod"
    - "Snapshot tests cover all 25 threat engine rules with at least one fixture scenario each"
  artifacts:
    - path: "shared/schema.ts"
      provides: "AdFindingSchema, EdrFindingSchema, NormalizedFinding discriminated union"
      contains: "AdFindingSchema"
    - path: "server/services/scanners/adScanner.ts"
      provides: "AD parser with -Depth 10 and typed AdFinding output"
      contains: "ConvertTo-Json -Depth 10"
    - path: "server/services/scanners/edrAvScanner.ts"
      provides: "EDR parser with timeline events"
      contains: "EdrTimelineEvent"
    - path: "server/__tests__/threatRuleSnapshots.test.ts"
      provides: "Full threat rule coverage snapshot tests (PARS-11)"
      contains: "toMatchSnapshot"
  key_links:
    - from: "server/services/scanners/adScanner.ts"
      to: "shared/schema.ts"
      via: "import AdFindingSchema"
      pattern: "AdFindingSchema\\.safeParse"
    - from: "server/services/scanners/edrAvScanner.ts"
      to: "shared/schema.ts"
      via: "import EdrFindingSchema"
      pattern: "EdrFindingSchema\\.safeParse"
    - from: "server/__tests__/threatRuleSnapshots.test.ts"
      to: "server/services/threatEngine.ts"
      via: "import detectThreats or analyzeFindings"
      pattern: "threatEngine.*detect|analyze"
---

<objective>
Complete the AD and EDR parsers with typed output, define the full NormalizedFinding discriminated union, and write comprehensive snapshot tests covering all 25 threat engine rules.

Purpose: Closes the parser foundation by: (1) patching AD PowerShell commands with -Depth 10 and outputting typed AdFinding (PARS-07, PARS-08), (2) adding timeline events to EDR output (PARS-09), (3) completing the NormalizedFinding discriminated union across all 4 parser types (PARS-10), and (4) writing the definitive snapshot test suite that locks the threat engine data contract before any future refactoring (PARS-11).

Output: AdFindingSchema + EdrFindingSchema + NormalizedFinding union in shared/schema.ts, patched AD/EDR scanners, ~29 fixture files, comprehensive snapshot test suite.
</objective>

<execution_context>
@C:/Users/rodrigo/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/rodrigo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-parser-foundation/01-CONTEXT.md
@.planning/phases/01-parser-foundation/01-RESEARCH.md
@.planning/phases/01-parser-foundation/01-01-SUMMARY.md
@.planning/phases/01-parser-foundation/01-02-SUMMARY.md

@shared/schema.ts
@server/services/scanners/adScanner.ts
@server/services/scanners/edrAvScanner.ts
@server/services/threatEngine.ts

<interfaces>
<!-- Types from prior plans that this plan depends on -->

From shared/schema.ts (after plans 01-01 and 01-02):
```typescript
export const BaseFindingSchema = z.object({
  type: z.string(),
  target: z.string(),
  severity: z.enum(['low', 'medium', 'high', 'critical']),
  timestamp: z.string().datetime().optional(),
});
export type BaseFinding = z.infer<typeof BaseFindingSchema>;

export const NmapFindingSchema = BaseFindingSchema.extend({ type: z.literal('port'), ... }).strip();
export type NmapFinding = z.infer<typeof NmapFindingSchema>;

export const NmapVulnFindingSchema = BaseFindingSchema.extend({ type: z.literal('nmap_vuln'), ... }).strip();
export type NmapVulnFinding = z.infer<typeof NmapVulnFindingSchema>;

export const NucleiFindingSchema = BaseFindingSchema.extend({ type: z.literal('nuclei'), ... }).strip();
export type NucleiFinding = z.infer<typeof NucleiFindingSchema>;
```

From server/services/scanners/adScanner.ts (existing exports to preserve):
```typescript
// Existing decoders — PRESERVE all of these:
// decodeUacFlags(uac: number): { flag, risk }[]
// WELL_KNOWN_GROUPS: Record<string, string>
// decodeKerberosEtypes(value: number): string[]
// decodeTrustDirection(value: number): string
// decodeTrustType(value: number): string
// PrimaryGroupID decoder
```

From server/services/scanners/edrAvScanner.ts (existing exports):
```typescript
export class EDRAVScanner {
  // testSingleHost returns a finding object — needs timeline array added
}
```

From server/services/threatEngine.ts (all 25 rule IDs for PARS-11 coverage):
Nmap rules: exposed-service, cve-detected
Nuclei rules: nuclei-vulnerability, web-vulnerability
EDR rules: edr-av-failure
AD rules (20): ad-security-generic, ad-users-password-never-expires, ad-domain-controller-not-found,
  ad-inactive-users, ad-users-old-passwords, ad-privileged-group-members, ad-obsolete-os,
  ad-inactive-computers, ad-weak-password-policy, domain-admin-critical-password-expired,
  specific-inactive-user, privileged-group-too-many-members, password-complexity-disabled,
  password-history-insufficient, passwords-never-expire, inactive-computer-detected,
  obsolete-operating-system, bidirectional-trust-detected, domain-admin-old-password,
  password-never-expires
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define AdFinding/EdrFinding schemas, patch AD -Depth 10, add EDR timeline, create NormalizedFinding union</name>
  <files>
    shared/schema.ts,
    server/services/scanners/adScanner.ts,
    server/services/scanners/edrAvScanner.ts
  </files>
  <behavior>
    - AdFindingSchema validates objects with type literal 'ad_finding', target, severity, plus: checkId (string), checkName (string), details (string), groupMembership (string[] optional), gpoLinks (array of objects optional), trustAttributes (object optional), uacFlags (array optional), rawData (Record optional)
    - AdFindingSchema.strip() removes unknown fields
    - EdrFindingSchema validates objects with type literal 'edr_test', target, severity, plus: hostname, eicarRemoved (boolean|null), testDuration (number), deploymentMethod (string), timeline (EdrTimelineEvent[]), detected (boolean|null), optional filePath/share/error/sampleRate
    - EdrTimelineEventSchema validates: timestamp (string), action (enum of 6 event types), detail (string), optional share
    - NormalizedFinding discriminated union = NmapFinding | NmapVulnFinding | NucleiFinding | AdFinding | EdrFinding
    - NormalizedFindingSchema = z.discriminatedUnion('type', [...])
    - Every ConvertTo-Json in adScanner.ts has -Depth 10
    - EDR testSingleHost returns finding with timeline array
  </behavior>
  <action>
    1. In `shared/schema.ts`, add after the existing NucleiFindingSchema:

       **EdrTimelineEventSchema:**
       - `timestamp: z.string()` (ISO-8601)
       - `action: z.enum(['deploy_attempt', 'deploy_success', 'detected', 'not_detected', 'timeout', 'cleanup'])`
       - `detail: z.string()`
       - `share: z.string().optional()`

       **EdrFindingSchema** extending BaseFinding:
       - `type: z.literal('edr_test')`
       - `hostname: z.string()`
       - `eicarRemoved: z.boolean().nullable()`
       - `testDuration: z.number()`
       - `deploymentMethod: z.string()`
       - `filePath: z.string().optional()`
       - `share: z.string().optional()`
       - `error: z.string().optional()`
       - `timeline: z.array(EdrTimelineEventSchema)` (PARS-09: the timeline)
       - `sampleRate: z.number().optional()`
       - `detected: z.boolean().nullable()`
       - Call `.strip()`

       **AdFindingSchema** extending BaseFinding:
       - `type: z.literal('ad_finding')`
       - `checkId: z.string()`
       - `checkName: z.string()`
       - `details: z.string().optional()`
       - `groupMembership: z.array(z.string()).optional()` (PARS-08: ordered chain)
       - `gpoLinks: z.array(z.object({ name: z.string(), path: z.string(), enabled: z.boolean().optional() })).optional()` (PARS-08)
       - `trustAttributes: z.object({ direction: z.string(), type: z.string(), transitivity: z.string().optional() }).optional()` (PARS-08)
       - `uacFlags: z.array(z.object({ flag: z.string(), risk: z.string() })).optional()`
       - `rawData: z.record(z.unknown()).optional()` (fallback for full PS output)
       - Call `.strip()`

       **NormalizedFindingSchema** as discriminated union:
       - `z.discriminatedUnion('type', [NmapFindingSchema, NmapVulnFindingSchema, NucleiFindingSchema, AdFindingSchema, EdrFindingSchema])`
       - Export type `NormalizedFinding = z.infer<typeof NormalizedFindingSchema>`

    2. In `adScanner.ts`, grep for ALL occurrences of `ConvertTo-Json` and patch each one:
       - `ConvertTo-Json` -> `ConvertTo-Json -Depth 10`
       - `ConvertTo-Json -Compress` -> `ConvertTo-Json -Depth 10 -Compress`
       - Do a thorough search: there may be 10+ occurrences across different PowerShell command strings.
       - PRESERVE all existing decoders: `decodeUacFlags`, `WELL_KNOWN_GROUPS`, `decodeKerberosEtypes`, `decodeTrustDirection`, `decodeTrustType`.

    3. In `adScanner.ts`, add a public method `parseAdResults(rawResults: any[], checkId: string, checkName: string): AdFinding[]` that:
       - Takes raw PowerShell JSON output and wraps each record into an AdFinding
       - Maps group membership to ordered string arrays
       - Maps GPO links to structured objects
       - Maps trust attributes using existing decoders
       - Applies `AdFindingSchema.safeParse()` — log+skip invalid
       - Preserves `rawData` for full PS output as fallback

    4. In `edrAvScanner.ts`, modify `testSingleHost` (or its return type) to include a `timeline: EdrTimelineEvent[]` array:
       - At each significant step (deploy attempt, deploy success, detection check, cleanup), push a timeline event with `{ timestamp: new Date().toISOString(), action, detail, share? }`
       - The existing `finding` object returned gets the `timeline` array added
       - Apply `EdrFindingSchema.safeParse()` before returning

    5. Verify TypeScript compiles: `npx tsc --noEmit`
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>
    - AdFindingSchema, EdrFindingSchema, NormalizedFindingSchema exported from shared/schema.ts (PARS-10)
    - All ConvertTo-Json calls in adScanner.ts use -Depth 10 (PARS-07)
    - adScanner.parseAdResults produces typed AdFinding with group chains, GPO links, trust attributes (PARS-08)
    - edrAvScanner.testSingleHost returns EdrFinding with timeline array (PARS-09)
    - NormalizedFinding is a discriminated union of all 4 scanner types (PARS-10)
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create all fixture files and comprehensive threat rule snapshot tests</name>
  <files>
    server/__tests__/fixtures/ad/password-never-expires.json,
    server/__tests__/fixtures/ad/privileged-group-members.json,
    server/__tests__/fixtures/ad/obsolete-os.json,
    server/__tests__/fixtures/ad/inactive-users.json,
    server/__tests__/fixtures/ad/weak-password-policy.json,
    server/__tests__/fixtures/ad/domain-controller-not-found.json,
    server/__tests__/fixtures/ad/inactive-computers.json,
    server/__tests__/fixtures/ad/bidirectional-trust.json,
    server/__tests__/fixtures/ad/old-passwords.json,
    server/__tests__/fixtures/ad/domain-admin-critical.json,
    server/__tests__/fixtures/ad/password-complexity-disabled.json,
    server/__tests__/fixtures/ad/password-history-insufficient.json,
    server/__tests__/fixtures/ad/generic-ad-finding.json,
    server/__tests__/fixtures/ad/specific-inactive-user.json,
    server/__tests__/fixtures/ad/privileged-group-too-many.json,
    server/__tests__/fixtures/ad/password-never-expires-user.json,
    server/__tests__/fixtures/edr/detection-success.json,
    server/__tests__/fixtures/edr/detection-failure.json,
    server/__tests__/fixtures/edr/timeout-error.json,
    server/__tests__/adParser.test.ts,
    server/__tests__/edrParser.test.ts,
    server/__tests__/threatRuleSnapshots.test.ts
  </files>
  <behavior>
    - Each AD fixture is a JSON file containing an array of AdFinding-shaped objects that trigger a specific threat rule
    - Each EDR fixture is a JSON file containing an EdrFinding-shaped object with timeline events
    - adParser.test.ts: parsing each AD fixture through parseAdResults produces valid AdFinding objects
    - edrParser.test.ts: parsing each EDR fixture through EdrFindingSchema produces valid EdrFinding objects with timeline
    - threatRuleSnapshots.test.ts: for each of the 25 threat rules, loads the relevant fixture, parses it, feeds to threatEngine, and snapshots the result
    - All 25 rules have at least one snapshot test scenario
  </behavior>
  <action>
    1. Create AD fixture files under `server/__tests__/fixtures/ad/`. Each JSON file contains an array of findings matching the AdFinding shape. Create one fixture per AD threat rule (16 AD rules total). Each fixture should contain the minimum data needed to trigger the specific rule in `threatEngine.ts`:
       - `password-never-expires.json`: AD users with `userAccountControl` flag for DONT_EXPIRE_PASSWORD
       - `privileged-group-members.json`: Users in Domain Admins / Enterprise Admins groups
       - `obsolete-os.json`: Computers with Windows Server 2008/XP operating systems
       - `inactive-users.json`: Users with `lastLogon` > 90 days ago
       - `weak-password-policy.json`: Password policy with minLength < 8
       - `domain-controller-not-found.json`: AD scan that found no DCs
       - `inactive-computers.json`: Computers with `lastLogon` > 90 days ago
       - `bidirectional-trust.json`: Trust with bidirectional direction
       - `old-passwords.json`: Users with `passwordLastSet` > 365 days
       - `domain-admin-critical.json`: Domain admin with expired/old password
       - `password-complexity-disabled.json`: Policy with complexity disabled
       - `password-history-insufficient.json`: Policy with history < 12
       - `generic-ad-finding.json`: Generic AD check result (catch-all rule)
       - `specific-inactive-user.json`: Specific named inactive user
       - `privileged-group-too-many.json`: Group with > 5 members
       - `password-never-expires-user.json`: Specific user with password-never-expires flag

       Read each AD threat rule in `threatEngine.ts` (lines ~269-740) to understand exactly what field values and structure each rule's matcher expects. The fixtures must produce findings that match.

    2. Create EDR fixture files under `server/__tests__/fixtures/edr/`:
       - `detection-success.json`: EdrFinding with `detected: true`, `eicarRemoved: true`, timeline with deploy_attempt -> deploy_success -> detected -> cleanup
       - `detection-failure.json`: EdrFinding with `detected: false`, `eicarRemoved: false`, timeline with deploy_attempt -> deploy_success -> not_detected -> cleanup (triggers 'edr-av-failure' rule)
       - `timeout-error.json`: EdrFinding with `detected: null`, `error: 'timeout'`, timeline with deploy_attempt -> timeout

    3. Write `server/__tests__/adParser.test.ts`:
       - For each AD fixture: load JSON, parse through `adScanner.parseAdResults()`, assert all findings pass `AdFindingSchema.safeParse()`
       - Snapshot each parsed result
       - Assert group membership arrays are ordered string arrays (not flattened strings)
       - Assert GPO links are structured objects where present
       - Assert trust attributes are typed where present

    4. Write `server/__tests__/edrParser.test.ts`:
       - For each EDR fixture: load JSON, validate against `EdrFindingSchema`
       - Assert timeline array has correct event sequence
       - Assert detection-success has `detected: true`, detection-failure has `detected: false`
       - Snapshot each result

    5. Write `server/__tests__/threatRuleSnapshots.test.ts` — the PARS-11 comprehensive test:
       - Import `threatEngine` (or the specific `detectThreats` / `analyzeFindings` function)
       - For EACH of the 25 threat rules, write at least one test:
         - Load the relevant fixture (nmap XML fixtures from plan 01-01, nuclei JSONL from plan 01-02, AD/EDR JSON from this task)
         - Parse through the appropriate parser (networkScanner.parseNmapXml, vulnScanner.parseNuclei, adScanner.parseAdResults, EdrFindingSchema.safeParse)
         - Feed the `NormalizedFinding[]` to the threat engine
         - `expect(threats).toMatchSnapshot()` — captures the exact threat object shape
       - Group tests by describe blocks: 'nmap rules', 'nuclei rules', 'AD rules', 'EDR rules'
       - The 25 rules and their fixture mappings:
         * `exposed-service` -> smb-open-no-vuln.xml
         * `cve-detected` -> vuln-ms17-010.xml (as nmap_vuln type)
         * `nuclei-vulnerability` -> cve-with-classification.jsonl
         * `web-vulnerability` -> xss-with-matcher.jsonl
         * `edr-av-failure` -> detection-failure.json
         * 20 AD rules -> corresponding AD fixture files

       NOTE: This is the critical PARS-11 deliverable. The snapshots lock the data contract between parsers and threat engine. They MUST be committed and act as regression guards.

    6. Run full test suite: `npx vitest run`
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>
    - 16 AD fixture files created covering all AD threat rules
    - 3 EDR fixture files created covering EDR rule scenarios
    - adParser.test.ts passes: all AD findings validate against AdFindingSchema
    - edrParser.test.ts passes: all EDR findings have timeline arrays (PARS-09)
    - threatRuleSnapshots.test.ts passes: all 25 threat rules have snapshot coverage (PARS-11)
    - Full vitest suite green
    - Group membership chains are string arrays, not flattened strings (PARS-08)
    - All ConvertTo-Json calls have -Depth 10 (verified by grep) (PARS-07)
  </done>
</task>

</tasks>

<verification>
```bash
# Full test suite
npx vitest run

# Verify -Depth 10 applied everywhere
grep -c "ConvertTo-Json" server/services/scanners/adScanner.ts
grep -c "ConvertTo-Json -Depth 10" server/services/scanners/adScanner.ts
# Both counts should be equal

# Verify NormalizedFinding union exists
grep "NormalizedFindingSchema" shared/schema.ts

# TypeScript compiles
npx tsc --noEmit

# Snapshot count check — should have snapshots for all 25 rules
grep -c "toMatchSnapshot" server/__tests__/threatRuleSnapshots.test.ts
# Should be >= 25
```
</verification>

<success_criteria>
- AdFindingSchema and EdrFindingSchema in shared/schema.ts with .strip() (PARS-10)
- NormalizedFinding discriminated union covering all 4 parser types (PARS-10)
- All ConvertTo-Json calls in adScanner.ts use -Depth 10 (PARS-07)
- AD parser captures group chains, GPO links, trust attributes as structured types (PARS-08)
- EDR parser captures per-host timeline with granular events (PARS-09)
- Snapshot tests cover all 25 threat engine rules (PARS-11)
- Full vitest suite passes
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-parser-foundation/01-03-SUMMARY.md`
</output>
