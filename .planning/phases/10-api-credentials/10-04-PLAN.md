---
phase: 10-api-credentials
plan: 04
type: execute
wave: 2
depends_on: [10-02, 10-03]
files_modified:
  - server/storage/apiCredentials.ts
  - server/storage/database-init.ts
  - server/storage/interface.ts
  - server/storage/index.ts
autonomous: true
requirements: [CRED-01, CRED-02, CRED-03, CRED-04]
must_haves:
  truths:
    - "Storage facade `server/storage/apiCredentials.ts` expõe 7 funções: listApiCredentials, getApiCredential, getApiCredentialWithSecret, createApiCredential, updateApiCredential, deleteApiCredential, resolveApiCredential"
    - "createApiCredential cifra o secret via encryptionService.encryptCredential() (KEK/DEK reuso, sem nova crypto)"
    - "createApiCredential trata mTLS via JSON.stringify({cert,key,ca}) antes de cifrar"
    - "createApiCredential popula bearerExpiresAt via decodeJwtExp para auth type bearer_jwt"
    - "listApiCredentials e getApiCredential SEMPRE excluem secretEncrypted/dekEncrypted (via SAFE_FIELDS explicit list)"
    - "getApiCredentialWithSecret é o ÚNICO método que retorna campos cifrados — reservado para executor Phase 11+"
    - "resolveApiCredential aplica algoritmo: priority ASC → specificity (mais literais ganha) → createdAt ASC, com escopo apiId OR (apiId IS NULL)"
    - "ensureApiCredentialTables() cria enum + tabela + indexes idempotentemente, é chamado APÓS ensureApiTables() em initializeDatabaseStructure"
    - "IStorage ganha 7 assinaturas e DatabaseStorage namespace-importa apiCredentials"
  artifacts:
    - path: "server/storage/apiCredentials.ts"
      provides: "Facade de domínio para api_credentials (8 funções incluindo SAFE_FIELDS)"
      exports: ["listApiCredentials", "getApiCredential", "getApiCredentialWithSecret", "createApiCredential", "updateApiCredential", "deleteApiCredential", "resolveApiCredential"]
    - path: "server/storage/database-init.ts"
      provides: "Guard idempotente ensureApiCredentialTables() invocado após ensureApiTables()"
      contains: "export async function ensureApiCredentialTables"
    - path: "server/storage/interface.ts"
      provides: "IStorage com 7 novas assinaturas para api_credentials"
      contains: "listApiCredentials("
    - path: "server/storage/index.ts"
      provides: "DatabaseStorage com namespace import de apiCredentials"
      contains: "import * as apiCredentialOps from"
  key_links:
    - from: "server/storage/apiCredentials.ts"
      to: "server/services/encryption.ts"
      via: "import { encryptionService } — calls encryptCredential() and decryptCredential()"
      pattern: "encryptionService\\.(encryptCredential|decryptCredential)"
    - from: "server/storage/apiCredentials.ts"
      to: "server/services/credentials/matchUrlPattern.ts"
      via: "import { matchUrlPattern } in resolveApiCredential"
      pattern: "from ['\"]\\.\\./services/credentials/matchUrlPattern['\"]"
    - from: "server/storage/apiCredentials.ts"
      to: "server/services/credentials/decodeJwtExp.ts"
      via: "import { decodeJwtExp } in createApiCredential for bearer_jwt"
      pattern: "from ['\"]\\.\\./services/credentials/decodeJwtExp['\"]"
    - from: "server/storage/database-init.ts initializeDatabaseStructure()"
      to: "ensureApiCredentialTables()"
      via: "await ensureApiCredentialTables() chamado APÓS await ensureApiTables()"
      pattern: "ensureApiCredentialTables\\(\\)"
    - from: "server/storage/index.ts DatabaseStorage class"
      to: "server/storage/apiCredentials.ts"
      via: "import * as apiCredentialOps from './apiCredentials'"
      pattern: "import \\* as apiCredentialOps"
---

<objective>
Wave 2 — Implementar TODA a camada de persistência do Phase 10: facade de domínio + guard idempotente + integração no IStorage/DatabaseStorage.

Purpose: Após este plan, todas as operações de banco para api_credentials estão funcionais e o boot do app cria as tabelas automaticamente. O Plan 05 (route) só precisa fazer Zod parse + chamar storage.
Output: 1 arquivo novo (`apiCredentials.ts`) + 3 arquivos modificados aditivamente. ~300 linhas de código.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-api-credentials/10-CONTEXT.md
@.planning/phases/10-api-credentials/10-RESEARCH.md
@.planning/phases/10-api-credentials/10-VALIDATION.md
@.planning/phases/10-api-credentials/10-02-SUMMARY.md
@.planning/phases/10-api-credentials/10-03-SUMMARY.md

# Templates Phase 9 (precedente exato)
@server/storage/apis.ts
@server/storage/database-init.ts
@server/storage/interface.ts
@server/storage/index.ts

# Crypto reusada SEM mudança
@server/services/encryption.ts

# Helpers consumidos
@server/services/credentials/matchUrlPattern.ts
@server/services/credentials/decodeJwtExp.ts

# Schema do Phase 10 (exporta tudo que precisamos)
@shared/schema.ts

# Stubs Wave 0 que este plan ATIVA
@server/__tests__/apiCredentials/storage.test.ts
@server/__tests__/apiCredentials/resolveCredential.test.ts
@server/__tests__/apiCredentials/guard.test.ts

<interfaces>
<!-- Contratos exatos a implementar -->

// server/storage/apiCredentials.ts
export const SAFE_FIELDS = { /* todos os campos exceto secretEncrypted, dekEncrypted */ };

export async function listApiCredentials(
  filter?: { apiId?: string; authType?: ApiAuthType }
): Promise<ApiCredentialSafe[]>;

export async function getApiCredential(id: string): Promise<ApiCredentialSafe | undefined>;

// INTERNO — só para executor Phase 11+
export async function getApiCredentialWithSecret(id: string): Promise<ApiCredentialWithSecret | undefined>;

export async function createApiCredential(
  input: InsertApiCredential,
  userId: string,
): Promise<ApiCredentialSafe>;

export async function updateApiCredential(
  id: string,
  patch: PatchApiCredential,
  userId: string,
): Promise<ApiCredentialSafe>;

export async function deleteApiCredential(id: string): Promise<void>;

export async function resolveApiCredential(
  apiId: string,
  endpointUrl: string, // URL absoluta = api.baseUrl + endpoint.path
): Promise<ApiCredentialSafe | null>;

// server/storage/database-init.ts
export async function ensureApiCredentialTables(): Promise<void>;
// chamada após `await ensureApiTables();` em initializeDatabaseStructure (linha ~139)

// server/services/encryption.ts (REUSO, NÃO MODIFICAR)
encryptionService.encryptCredential(secret: string): { secretEncrypted: string; dekEncrypted: string };
encryptionService.decryptCredential(secretEncrypted: string, dekEncrypted: string): string;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar storage facade server/storage/apiCredentials.ts (8 funções incluindo SAFE_FIELDS + resolveApiCredential)</name>
  <files>server/storage/apiCredentials.ts, server/__tests__/apiCredentials/storage.test.ts, server/__tests__/apiCredentials/resolveCredential.test.ts</files>
  <read_first>
    - server/storage/apis.ts (template Phase 9 de facade — header, imports, organização de funções)
    - server/storage/assets.ts (linhas 175-230 — `getCredentials()` é o template canônico de SAFE_FIELDS explícito que exclui secret*/dek*)
    - server/services/encryption.ts (confirmar assinatura exata de encryptCredential/decryptCredential)
    - shared/schema.ts (procurar `apiCredentials` table + `ApiCredentialSafe` tipo + `InsertApiCredential` + `PatchApiCredential`)
    - server/lib/logger.ts (confirmar createLogger signature + redaction paths já cobrindo secretEncrypted/dekEncrypted)
    - server/services/credentials/matchUrlPattern.ts (consumir matchUrlPattern)
    - server/services/credentials/decodeJwtExp.ts (consumir decodeJwtExp)
    - .planning/phases/10-api-credentials/10-RESEARCH.md (seções "Padrão 3" linhas 295-368, "Padrão 6" linhas 447-479 — código verbatim das 8 funções)
    - .planning/phases/10-api-credentials/10-CONTEXT.md (seção "Storage facade" linhas 153-163 — métodos esperados)
    - server/__tests__/apiCredentials/storage.test.ts (stubs a ativar)
    - server/__tests__/apiCredentials/resolveCredential.test.ts (stubs a ativar)
    - server/__tests__/apiStorage.test.ts (template Phase 9 de teste de storage com DB real)
  </read_first>
  <behavior>
    - Test (CRED-01, CRED-02): cada um dos 7 auth types pode ser criado e recuperado via getApiCredentialWithSecret + decryptCredential com round-trip exato
    - Test (CRED-02): mtls cria secretEncrypted contendo `JSON.stringify({cert,key,ca})`; decrypt + JSON.parse retorna objeto correto
    - Test (CRED-02): listApiCredentials e getApiCredential NUNCA retornam secretEncrypted/dekEncrypted (Object.keys não inclui esses campos)
    - Test (CRED-02): getApiCredentialWithSecret RETORNA secretEncrypted/dekEncrypted (uso interno)
    - Test (CRED-01): bearer_jwt com `exp` no JWT popula bearerExpiresAt; bearer_jwt opaco aceita sem erro com bearerExpiresAt null
    - Test (UNIQUE): create com (name, createdBy) duplicado lança erro `code === '23505'`
    - Test (FK): deletar API referenciada faz apiId virar NULL na credencial
    - Test (CRED-04): resolveApiCredential com 2 credenciais que casam → menor priority ganha
    - Test (CRED-04): mesma priority → mais específica (mais literais no pattern) ganha
    - Test (CRED-04): mesma priority + specificity → mais antiga ganha (createdAt ASC)
    - Test (CRED-04): credencial global (apiId IS NULL) é candidata para qualquer apiId
    - Test (CRED-04): credencial scoped de outro apiId NÃO é candidata
    - Test (CRED-04): nenhuma credencial casa → retorna null (não lança)
  </behavior>
  <action>
**Sub-task A: Criar `server/storage/apiCredentials.ts`** com EXATAMENTE este conteúdo (montado a partir de 10-RESEARCH.md §Padrão 3 + Padrão 6):

```ts
// Phase 10 — CRED-01..04: facade de domínio para api_credentials.
// Padrão simétrico com server/storage/apis.ts (Phase 9).
// Reusa encryptionService (KEK/DEK existente) — zero nova crypto.

import { db } from "../db";
import {
  apiCredentials,
  apis,
  type ApiCredential,
  type ApiCredentialSafe,
  type ApiCredentialWithSecret,
  type InsertApiCredential,
  type PatchApiCredential,
  type ApiAuthType,
} from "@shared/schema";
import { encryptionService } from "../services/encryption";
import { matchUrlPattern } from "../services/credentials/matchUrlPattern";
import { decodeJwtExp } from "../services/credentials/decodeJwtExp";
import { eq, asc, and, isNull, or } from "drizzle-orm";
import { createLogger } from "../lib/logger";

const log = createLogger("storage:api-credentials");

// SAFE_FIELDS: campos retornados por list/get/resolve — NUNCA inclui secret*/dek*.
// Padrão exato de getCredentials() em assets.ts:180.
const SAFE_FIELDS = {
  id: apiCredentials.id,
  name: apiCredentials.name,
  description: apiCredentials.description,
  authType: apiCredentials.authType,
  urlPattern: apiCredentials.urlPattern,
  priority: apiCredentials.priority,
  apiId: apiCredentials.apiId,
  apiKeyHeaderName: apiCredentials.apiKeyHeaderName,
  apiKeyQueryParam: apiCredentials.apiKeyQueryParam,
  basicUsername: apiCredentials.basicUsername,
  bearerExpiresAt: apiCredentials.bearerExpiresAt,
  oauth2ClientId: apiCredentials.oauth2ClientId,
  oauth2TokenUrl: apiCredentials.oauth2TokenUrl,
  oauth2Scope: apiCredentials.oauth2Scope,
  oauth2Audience: apiCredentials.oauth2Audience,
  hmacKeyId: apiCredentials.hmacKeyId,
  hmacAlgorithm: apiCredentials.hmacAlgorithm,
  hmacSignatureHeader: apiCredentials.hmacSignatureHeader,
  hmacSignedHeaders: apiCredentials.hmacSignedHeaders,
  hmacCanonicalTemplate: apiCredentials.hmacCanonicalTemplate,
  createdAt: apiCredentials.createdAt,
  updatedAt: apiCredentials.updatedAt,
  createdBy: apiCredentials.createdBy,
  updatedBy: apiCredentials.updatedBy,
};

export async function listApiCredentials(
  filter?: { apiId?: string; authType?: ApiAuthType },
): Promise<ApiCredentialSafe[]> {
  const conds = [];
  if (filter?.apiId) conds.push(eq(apiCredentials.apiId, filter.apiId));
  if (filter?.authType) conds.push(eq(apiCredentials.authType, filter.authType));

  let query = db.select(SAFE_FIELDS).from(apiCredentials);
  if (conds.length > 0) {
    query = query.where(and(...conds)) as typeof query;
  }
  const rows = await query.orderBy(asc(apiCredentials.priority), asc(apiCredentials.createdAt));
  return rows as ApiCredentialSafe[];
}

export async function getApiCredential(id: string): Promise<ApiCredentialSafe | undefined> {
  const [row] = await db.select(SAFE_FIELDS).from(apiCredentials).where(eq(apiCredentials.id, id));
  return row as ApiCredentialSafe | undefined;
}

// INTERNO — uso exclusivo do executor (Phase 11+) que precisa do secret cifrado para decrypt.
export async function getApiCredentialWithSecret(
  id: string,
): Promise<ApiCredentialWithSecret | undefined> {
  const [row] = await db.select().from(apiCredentials).where(eq(apiCredentials.id, id));
  return row;
}

// Extrai o secret do input por auth type — função pura (sem I/O).
function extractSecret(input: InsertApiCredential): string {
  if (input.authType === "mtls") {
    return JSON.stringify({
      cert: input.mtlsCert,
      key: input.mtlsKey,
      ca: input.mtlsCa ?? null,
    });
  }
  // Demais tipos: campo `secret` direto
  return (input as { secret: string }).secret;
}

// Remove campos de "input wrapper" (secret, mtls*) para que `db.insert` receba só colunas válidas.
function stripSecretFields<T extends Record<string, unknown>>(input: T): Omit<T, "secret" | "mtlsCert" | "mtlsKey" | "mtlsCa"> {
  const { secret: _s, mtlsCert: _c, mtlsKey: _k, mtlsCa: _a, ...rest } = input as Record<string, unknown>;
  return rest as Omit<T, "secret" | "mtlsCert" | "mtlsKey" | "mtlsCa">;
}

export async function createApiCredential(
  input: InsertApiCredential,
  userId: string,
): Promise<ApiCredentialSafe> {
  const secretToEncrypt = extractSecret(input);
  const { secretEncrypted, dekEncrypted } = encryptionService.encryptCredential(secretToEncrypt);

  // Derivar bearerExpiresAt para bearer_jwt (CONTEXT.md: falha silenciosa)
  let bearerExpiresAt: Date | null = null;
  if (input.authType === "bearer_jwt") {
    bearerExpiresAt = decodeJwtExp((input as { secret: string }).secret);
  }

  const cleaned = stripSecretFields(input);
  const [created] = await db
    .insert(apiCredentials)
    .values({
      ...cleaned,
      secretEncrypted,
      dekEncrypted,
      bearerExpiresAt,
      createdBy: userId,
    })
    .returning(SAFE_FIELDS);

  log.info(
    { apiCredentialId: created.id, authType: created.authType, apiId: created.apiId },
    "api credential created",
  );
  return created as ApiCredentialSafe;
}

export async function updateApiCredential(
  id: string,
  patch: PatchApiCredential,
  userId: string,
): Promise<ApiCredentialSafe> {
  // Se patch tem secret novo (ou cert/key/ca para mTLS), re-criptografa.
  const updates: Record<string, unknown> = stripSecretFields(patch);
  updates.updatedBy = userId;
  updates.updatedAt = new Date();

  const hasNewSecret = patch.secret || patch.mtlsCert || patch.mtlsKey || patch.mtlsCa;
  if (hasNewSecret) {
    // Para re-criptografar, precisamos saber o authType. Fetch row atual.
    const current = await getApiCredentialWithSecret(id);
    if (!current) {
      throw new Error("Credencial não encontrada");
    }
    let newSecret: string;
    if (current.authType === "mtls") {
      newSecret = JSON.stringify({
        cert: patch.mtlsCert ?? null,
        key: patch.mtlsKey ?? null,
        ca: patch.mtlsCa ?? null,
      });
    } else {
      newSecret = patch.secret as string;
    }
    const { secretEncrypted, dekEncrypted } = encryptionService.encryptCredential(newSecret);
    updates.secretEncrypted = secretEncrypted;
    updates.dekEncrypted = dekEncrypted;

    if (current.authType === "bearer_jwt" && patch.secret) {
      updates.bearerExpiresAt = decodeJwtExp(patch.secret);
    }
  }

  const [updated] = await db
    .update(apiCredentials)
    .set(updates)
    .where(eq(apiCredentials.id, id))
    .returning(SAFE_FIELDS);

  log.info(
    { apiCredentialId: id, reEncrypted: !!hasNewSecret },
    "api credential updated",
  );
  return updated as ApiCredentialSafe;
}

export async function deleteApiCredential(id: string): Promise<void> {
  await db.delete(apiCredentials).where(eq(apiCredentials.id, id));
  log.info({ apiCredentialId: id }, "api credential deleted");
}

// CRED-04 — resolução por priority + specificity + createdAt.
// Algoritmo (CONTEXT.md linhas 98-103):
//   1. Candidatos: apiId === apiId OR apiId IS NULL
//   2. Filtra por matchUrlPattern(c.urlPattern, endpointUrl)
//   3. Sort: priority ASC → specificity DESC (mais literais ganha) → createdAt ASC
//   4. Retorna top 1 ou null.
export async function resolveApiCredential(
  apiId: string,
  endpointUrl: string,
): Promise<ApiCredentialSafe | null> {
  const candidates = await db
    .select(SAFE_FIELDS)
    .from(apiCredentials)
    .where(or(eq(apiCredentials.apiId, apiId), isNull(apiCredentials.apiId)))
    .orderBy(asc(apiCredentials.priority), asc(apiCredentials.createdAt));

  const matching = (candidates as ApiCredentialSafe[]).filter((c) =>
    matchUrlPattern(c.urlPattern, endpointUrl),
  );
  if (matching.length === 0) return null;

  const countLiterals = (p: string) => p.replace(/\*/g, "").length;
  matching.sort((a, b) => {
    if (a.priority !== b.priority) return a.priority - b.priority;
    const specDiff = countLiterals(b.urlPattern) - countLiterals(a.urlPattern);
    if (specDiff !== 0) return specDiff;
    const aTime = a.createdAt ? new Date(a.createdAt).getTime() : 0;
    const bTime = b.createdAt ? new Date(b.createdAt).getTime() : 0;
    return aTime - bTime;
  });

  return matching[0];
}
```

**Sub-task B: Ativar stubs em `server/__tests__/apiCredentials/storage.test.ts`**

Substituir TODOS os `it.todo` por `it` real. Setup: usar a infra de teste do projeto (`server/__tests__/apiStorage.test.ts` é o template — mesmo banco real, beforeEach truncate de api_credentials). Imports:
```ts
import { describe, it, expect, beforeAll, beforeEach } from "vitest";
import { db } from "../../db";
import { apiCredentials, apis, users } from "@shared/schema";
import { eq, sql } from "drizzle-orm";
import {
  listApiCredentials, getApiCredential, getApiCredentialWithSecret,
  createApiCredential, updateApiCredential, deleteApiCredential,
} from "../../storage/apiCredentials";
import { encryptionService } from "../../services/encryption";
import { createTestApiCredential, TEST_PEM_CERT, TEST_PEM_KEY } from "../helpers/apiCredentialFactory";
```

Implementar (no mínimo):
- 7 testes de round-trip (1 por auth type) — assert `decryptCredential(secretEncrypted, dekEncrypted)` retorna o secret original
- 3 testes de mTLS multi-part (com ca, sem ca, decrypt + JSON.parse)
- 3 testes de sanitização: list/get sem secret*; getWithSecret COM secret*
- 2 testes de bearerExpiresAt: JWT com exp válido popula data; JWT opaco aceita sem erro
- 2 testes de UNIQUE: duplicado lança 23505; mesmo nome users distintos OK
- 1 teste de FK: deletar API → apiId vira NULL

**Sub-task C: Ativar stubs em `server/__tests__/apiCredentials/resolveCredential.test.ts`**

Implementar todos `it.todo` como `it` real, criando 3+ credenciais por cenário:
- Priority diferente (50 vs 100)
- Mesma priority + specificity diferente
- Mesma priority + mesma specificity + createdAt diferente
- apiId scoped vs global (apiId IS NULL)
- apiId Y vs query X
- Nenhum match → null
- Shape ApiCredentialSafe (sem secret/dek)

Rodar `npm run test -- server/__tests__/apiCredentials/storage.test.ts server/__tests__/apiCredentials/resolveCredential.test.ts` e confirmar exit 0.
  </action>
  <verify>
    <automated>npm run test -- server/__tests__/apiCredentials/storage.test.ts server/__tests__/apiCredentials/resolveCredential.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `server/storage/apiCredentials.ts` exists
    - Grep `export async function listApiCredentials\(` returns 1 match
    - Grep `export async function getApiCredential\(` returns 1 match
    - Grep `export async function getApiCredentialWithSecret\(` returns 1 match
    - Grep `export async function createApiCredential\(` returns 1 match
    - Grep `export async function updateApiCredential\(` returns 1 match
    - Grep `export async function deleteApiCredential\(` returns 1 match
    - Grep `export async function resolveApiCredential\(` returns 1 match
    - Grep `encryptionService\.encryptCredential\(secretToEncrypt\)` returns 1 match
    - Grep `JSON\.stringify\(\{[^}]*cert:[^}]*key:[^}]*ca:` returns ≥ 1 match (mTLS composite)
    - Grep `decodeJwtExp\(` returns ≥ 1 match
    - Grep `matchUrlPattern\(` returns ≥ 1 match
    - Grep `const SAFE_FIELDS = \{` returns 1 match
    - Grep `secretEncrypted` in SAFE_FIELDS section returns 0 matches (must NOT include secret in safe shape) — verify via reading SAFE_FIELDS block specifically
    - File `server/__tests__/apiCredentials/storage.test.ts` no longer contains `it\.todo`
    - File `server/__tests__/apiCredentials/resolveCredential.test.ts` no longer contains `it\.todo`
    - `npm run test -- server/__tests__/apiCredentials/storage.test.ts server/__tests__/apiCredentials/resolveCredential.test.ts` exits 0
    - Test output shows ≥ 25 passing tests across the two files
  </acceptance_criteria>
  <done>Facade completa com 7 funções + SAFE_FIELDS, todos os testes Wave 0 de storage e resolve ativos e verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar ensureApiCredentialTables() guard em database-init.ts + integrar no boot</name>
  <files>server/storage/database-init.ts, server/__tests__/apiCredentials/guard.test.ts</files>
  <read_first>
    - server/storage/database-init.ts (LER COMPLETO — entender a função `initializeDatabaseStructure` em ~linha 132 e o template `ensureApiTables` em linhas 151-339)
    - .planning/phases/10-api-credentials/10-RESEARCH.md (seção "Padrão 4" linhas 370-417 — código verbatim do guard)
    - .planning/phases/10-api-credentials/10-RESEARCH.md (seção "Armadilha 1" linhas 528-536 — quoted identifiers)
    - .planning/phases/10-api-credentials/10-CONTEXT.md (seção "Migration pattern" linhas 142-150)
    - server/__tests__/ensureApiTables.test.ts (template Phase 9 de teste de guard)
    - server/__tests__/apiCredentials/guard.test.ts (stubs a ativar)
  </read_first>
  <behavior>
    - Test (idempotência): primeira execução cria enum `api_auth_type` (CREATE TYPE)
    - Test (idempotência): segunda execução detecta enum em pg_type e NÃO recria
    - Test (idempotência): primeira execução cria tabela `api_credentials`
    - Test (idempotência): segunda execução detecta tabela em pg_tables e NÃO recria
    - Test (idempotência): primeira execução cria 3 indexes (IDX_api_credentials_api_id, IDX_api_credentials_priority, UQ_api_credentials_name_created_by)
    - Test (idempotência): segunda execução detecta cada index em pg_indexes e NÃO recria
    - Test (fallback): erro durante criação NÃO propaga — log.error + return (app continua)
    - Test (ordem): `ensureApiCredentialTables()` é chamado APÓS `ensureApiTables()` em `initializeDatabaseStructure`
  </behavior>
  <action>
**Sub-task A: Adicionar `ensureApiCredentialTables()` em `server/storage/database-init.ts`**

LOCALIZAR a função existente `ensureApiTables()` (linha ~151). ADICIONAR a nova função `ensureApiCredentialTables()` IMEDIATAMENTE APÓS `ensureApiTables()` mas ANTES de `ensureSystemUserExists()` (linha ~341).

```ts
// Phase 10 — API Credentials table (CRED-01..04)
// Runtime idempotent guard. Replicates ensureApiTables pattern exactly.
// Safe to re-run; safe to run after `db:push` has already created the objects.
// Identifiers are quoted in CREATE statements AND in pg_indexes lookups (Pitfall 1 avoidance).
export async function ensureApiCredentialTables(): Promise<void> {
  try {
    // ---- pgEnum api_auth_type ----
    const enumCheck = await db.execute(sql`
      SELECT typname FROM pg_type WHERE typname = 'api_auth_type'
    `);
    if ((enumCheck.rowCount ?? 0) === 0) {
      log.info("creating api_auth_type enum");
      await db.execute(sql`
        CREATE TYPE api_auth_type AS ENUM (
          'api_key_header',
          'api_key_query',
          'bearer_jwt',
          'basic',
          'oauth2_client_credentials',
          'hmac',
          'mtls'
        )
      `);
    }

    // ---- table api_credentials ----
    const tableCheck = await db.execute(sql`
      SELECT tablename FROM pg_tables WHERE schemaname = 'public' AND tablename = 'api_credentials'
    `);
    if ((tableCheck.rowCount ?? 0) === 0) {
      log.info("creating api_credentials table");
      await db.execute(sql`
        CREATE TABLE IF NOT EXISTS api_credentials (
          id VARCHAR PRIMARY KEY DEFAULT gen_random_uuid(),
          name TEXT NOT NULL,
          description TEXT,
          auth_type api_auth_type NOT NULL,
          url_pattern TEXT NOT NULL DEFAULT '*',
          priority INTEGER NOT NULL DEFAULT 100,
          api_id VARCHAR REFERENCES apis(id) ON DELETE SET NULL,
          secret_encrypted TEXT NOT NULL,
          dek_encrypted TEXT NOT NULL,
          api_key_header_name TEXT,
          api_key_query_param TEXT,
          basic_username TEXT,
          bearer_expires_at TIMESTAMP,
          oauth2_client_id TEXT,
          oauth2_token_url TEXT,
          oauth2_scope TEXT,
          oauth2_audience TEXT,
          hmac_key_id TEXT,
          hmac_algorithm TEXT,
          hmac_signature_header TEXT,
          hmac_signed_headers TEXT[],
          hmac_canonical_template TEXT,
          created_at TIMESTAMP NOT NULL DEFAULT NOW(),
          updated_at TIMESTAMP NOT NULL DEFAULT NOW(),
          created_by VARCHAR NOT NULL REFERENCES users(id),
          updated_by VARCHAR REFERENCES users(id)
        )
      `);
    }

    // ---- indexes ----
    const INDEXES: Array<[string, string]> = [
      [
        "IDX_api_credentials_api_id",
        `CREATE INDEX "IDX_api_credentials_api_id" ON api_credentials (api_id)`,
      ],
      [
        "IDX_api_credentials_priority",
        `CREATE INDEX "IDX_api_credentials_priority" ON api_credentials (priority)`,
      ],
      [
        "UQ_api_credentials_name_created_by",
        `CREATE UNIQUE INDEX "UQ_api_credentials_name_created_by" ON api_credentials (name, created_by)`,
      ],
    ];

    for (const [idxName, ddl] of INDEXES) {
      const idxCheck = await db.execute(sql`
        SELECT indexname FROM pg_indexes
        WHERE tablename = 'api_credentials' AND indexname = ${idxName}
      `);
      if ((idxCheck.rowCount ?? 0) === 0) {
        log.info({ idxName }, "creating api_credentials index");
        await db.execute(sql.raw(ddl));
      }
    }

    log.info("ensureApiCredentialTables complete");
  } catch (error) {
    log.error({ err: error }, "ensureApiCredentialTables error");
    // Não relançar — app continua em modo fallback (padrão Phase 9)
  }
}
```

**Sub-task B: Invocar o guard no boot**

LOCALIZAR a linha `await ensureApiTables();` (linha ~139 em `initializeDatabaseStructure`). ADICIONAR a chamada IMEDIATAMENTE APÓS:

```ts
    // Phase 9: API Discovery tables (HIER-01, HIER-02, FIND-01)
    await ensureApiTables();

    // Phase 10: API Credentials table (CRED-01..04)
    await ensureApiCredentialTables();
```

**Sub-task C: Ativar stubs em `server/__tests__/apiCredentials/guard.test.ts`**

Imports:
```ts
import { describe, it, expect, beforeAll, beforeEach } from "vitest";
import { db } from "../../db";
import { sql } from "drizzle-orm";
import { ensureApiCredentialTables } from "../../storage/database-init";
```

Implementar (mínimo):
- Idempotência: rodar 2x e confirmar via pg_type/pg_tables/pg_indexes que não há erro
- Fallback: mock `db.execute` para lançar; confirmar `await expect(ensureApiCredentialTables()).resolves.toBeUndefined()` (não relança)
- Ordem no boot: ler `server/storage/database-init.ts` como string e confirmar que `ensureApiCredentialTables` aparece DEPOIS de `ensureApiTables` no source (regex em `initializeDatabaseStructure`):
  ```ts
  it("ensureApiCredentialTables e chamado apos ensureApiTables em initializeDatabaseStructure", async () => {
    const fs = await import("node:fs/promises");
    const src = await fs.readFile("server/storage/database-init.ts", "utf8");
    const initBlock = src.match(/initializeDatabaseStructure[\s\S]+?\n\}/)?.[0] ?? "";
    const apiIdx = initBlock.indexOf("ensureApiTables()");
    const credIdx = initBlock.indexOf("ensureApiCredentialTables()");
    expect(apiIdx).toBeGreaterThan(-1);
    expect(credIdx).toBeGreaterThan(apiIdx);
  });
  ```

Rodar `npm run test -- server/__tests__/apiCredentials/guard.test.ts` e confirmar exit 0.
  </action>
  <verify>
    <automated>npm run test -- server/__tests__/apiCredentials/guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Grep `export async function ensureApiCredentialTables\(\): Promise<void>` in `server/storage/database-init.ts` returns 1 match
    - Grep `await ensureApiCredentialTables\(\);` in `server/storage/database-init.ts` returns 1 match (invocação no boot)
    - Order check: line number of `await ensureApiCredentialTables();` MUST be greater than line number of `await ensureApiTables();` in same file (use grep -n on both, compare line numbers)
    - Grep `CREATE TYPE api_auth_type AS ENUM` returns 1 match
    - Grep `CREATE TABLE IF NOT EXISTS api_credentials` returns 1 match
    - Grep `IDX_api_credentials_api_id` returns ≥ 1 match
    - Grep `IDX_api_credentials_priority` returns ≥ 1 match
    - Grep `UQ_api_credentials_name_created_by` returns ≥ 1 match
    - Grep `hmac_signed_headers TEXT\[\]` returns 1 match (array column DDL correto)
    - Grep `api_id VARCHAR REFERENCES apis\(id\) ON DELETE SET NULL` returns 1 match
    - Grep `} catch \(error\)` in ensureApiCredentialTables function returns ≥ 1 (fallback mode)
    - File `server/__tests__/apiCredentials/guard.test.ts` no longer contains `it\.todo`
    - `npm run test -- server/__tests__/apiCredentials/guard.test.ts` exits 0
    - Test output shows ≥ 8 passing tests
  </acceptance_criteria>
  <done>Guard idempotente criado, invocado no boot APÓS ensureApiTables, fallback mode confirmado, testes verdes.</done>
</task>

<task type="auto">
  <name>Task 3: Adicionar 7 assinaturas em IStorage e namespace import em DatabaseStorage</name>
  <files>server/storage/interface.ts, server/storage/index.ts</files>
  <read_first>
    - server/storage/interface.ts (LER COMPLETO — adicionar assinaturas no bloco de credentials existente ou em bloco novo "API Credentials operations")
    - server/storage/index.ts (LER linhas 1-50 e 195-220 — confirmar padrão de `import * as apiOps from "./apis"` + atribuição em DatabaseStorage)
    - server/storage/apiCredentials.ts (criado na Task 1 — imports a refletir)
    - shared/schema.ts (confirmar exports `ApiCredentialSafe`, `ApiCredentialWithSecret`, `InsertApiCredential`, `PatchApiCredential`, `ApiAuthType`)
  </read_first>
  <action>
**Sub-task A: Adicionar imports + assinaturas em `server/storage/interface.ts`**

ADICIONAR ao bloco de imports do tipo `@shared/schema` no topo do arquivo:
```ts
  type ApiCredential,
  type ApiCredentialSafe,
  type ApiCredentialWithSecret,
  type InsertApiCredential,
  type PatchApiCredential,
  type ApiAuthType,
```

ADICIONAR DENTRO da `interface IStorage` um novo bloco de assinaturas (logo após o bloco de credentials legacy, antes de "Journey operations"):

```ts
  // Phase 10 — API Credentials operations (CRED-01..04)
  listApiCredentials(filter?: { apiId?: string; authType?: ApiAuthType }): Promise<ApiCredentialSafe[]>;
  getApiCredential(id: string): Promise<ApiCredentialSafe | undefined>;
  getApiCredentialWithSecret(id: string): Promise<ApiCredentialWithSecret | undefined>;
  createApiCredential(input: InsertApiCredential, userId: string): Promise<ApiCredentialSafe>;
  updateApiCredential(id: string, patch: PatchApiCredential, userId: string): Promise<ApiCredentialSafe>;
  deleteApiCredential(id: string): Promise<void>;
  resolveApiCredential(apiId: string, endpointUrl: string): Promise<ApiCredentialSafe | null>;
```

**Sub-task B: Wirar namespace import em `server/storage/index.ts`**

ADICIONAR import (após `import * as apiOps from "./apis";` na linha 13):
```ts
import * as apiCredentialOps from "./apiCredentials";
```

ADICIONAR atribuições dentro da classe `DatabaseStorage` (logo após o bloco do Phase 9 que começa em ~linha 199 — `getApi = apiOps.getApi;`):

```ts
  // Phase 10 — API Credentials (CRED-01..04)
  listApiCredentials = apiCredentialOps.listApiCredentials;
  getApiCredential = apiCredentialOps.getApiCredential;
  getApiCredentialWithSecret = apiCredentialOps.getApiCredentialWithSecret;
  createApiCredential = apiCredentialOps.createApiCredential;
  updateApiCredential = apiCredentialOps.updateApiCredential;
  deleteApiCredential = apiCredentialOps.deleteApiCredential;
  resolveApiCredential = apiCredentialOps.resolveApiCredential;
```

NÃO TOCAR em outras assinaturas/atribuições. Apenas adições aditivas.

Rodar typecheck e confirmar que `DatabaseStorage` satisfaz `IStorage` sem erro.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "DatabaseStorage|IStorage|apiCredential" | grep "error TS" | head -10; npm run test -- server/__tests__/apiCredentials</automated>
  </verify>
  <acceptance_criteria>
    - Grep `listApiCredentials\(filter\?:` in `server/storage/interface.ts` returns 1 match
    - Grep `getApiCredentialWithSecret\(id: string\): Promise<ApiCredentialWithSecret \| undefined>` in `server/storage/interface.ts` returns 1 match
    - Grep `resolveApiCredential\(apiId: string, endpointUrl: string\): Promise<ApiCredentialSafe \| null>` in `server/storage/interface.ts` returns 1 match
    - Grep `import \* as apiCredentialOps from ['"]\./apiCredentials['"]` in `server/storage/index.ts` returns 1 match
    - Grep `listApiCredentials = apiCredentialOps\.listApiCredentials` in `server/storage/index.ts` returns 1 match
    - Grep `createApiCredential = apiCredentialOps\.createApiCredential` returns 1 match
    - Grep `resolveApiCredential = apiCredentialOps\.resolveApiCredential` returns 1 match
    - Grep `getApiCredentialWithSecret = apiCredentialOps\.getApiCredentialWithSecret` returns 1 match
    - `npx tsc --noEmit -p tsconfig.json 2>&1 | grep "apiCredential" | grep "error TS"` returns 0 matches
    - `npm run test -- server/__tests__/apiCredentials` exits 0
  </acceptance_criteria>
  <done>IStorage com 7 assinaturas, DatabaseStorage com 7 atribuições, typecheck verde, suite Wave 0 ainda passa.</done>
</task>

</tasks>

<verification>
- `npm run test -- server/__tests__/apiCredentials` exit 0 (storage + resolve + guard ATIVOS; schema + route + jwtExp como antes)
- `npm run test` exit 0 (full suite)
- `npx tsc --noEmit` sem erros relacionados a apiCredentials
- App boot manual (opcional, smoke): `await initializeDatabaseStructure()` cria a tabela e indexes sem erro
</verification>

<success_criteria>
- Facade completo (`apiCredentials.ts`) com 7 funções + SAFE_FIELDS
- Guard idempotente integrado no boot
- IStorage + DatabaseStorage refletem as 7 funções
- ~30+ testes ativos no Phase 10 (storage + resolve + guard)
- Plan 05 (rota) só precisa fazer Zod parse + chamar storage.createApiCredential etc
</success_criteria>

<output>
Após completion, criar `.planning/phases/10-api-credentials/10-04-SUMMARY.md` documentando:
- Lista exata de funções exportadas em `apiCredentials.ts`
- Linhas modificadas em `database-init.ts` (referência de ordem)
- Linhas adicionadas em `interface.ts` e `index.ts`
- Quantidade de testes verdes do phase
</output>
