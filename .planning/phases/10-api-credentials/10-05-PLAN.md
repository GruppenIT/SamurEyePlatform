---
phase: 10-api-credentials
plan: 05
type: execute
wave: 3
depends_on: [10-02, 10-04]
files_modified:
  - server/routes/apiCredentials.ts
  - server/routes/index.ts
autonomous: true
requirements: [CRED-01, CRED-05]
must_haves:
  truths:
    - "Rota POST /api/v1/api-credentials aceita os 7 auth types (validados via insertApiCredentialSchema discriminated union) e retorna 201 com ApiCredentialSafe"
    - "POST com nome duplicado para o mesmo createdBy retorna 409 com message pt-BR 'Credencial já cadastrada com esse nome'"
    - "POST sem autenticação retorna 401; com role read_only retorna 403; com role operator OU global_administrator retorna 201"
    - "GET /api/v1/api-credentials lista sanitizado (sem secret*/dek*) com filtros opcionais ?apiId= e ?authType="
    - "GET /api/v1/api-credentials/:id retorna 200 sanitizado ou 404"
    - "PATCH /api/v1/api-credentials/:id valida via patchApiCredentialSchema e re-encripta secret quando presente"
    - "DELETE /api/v1/api-credentials/:id retorna 204"
    - "Resposta 201 NUNCA contém secretEncrypted, dekEncrypted, secret, mtlsCert, mtlsKey, mtlsCa em texto puro"
    - "log.info de criação inclui apiCredentialId + authType + apiId; nunca o body completo (Armadilha 3)"
    - "registerApiCredentialsRoutes(app) é registrado em server/routes/index.ts no barrel"
    - "Wizard inline-create (CRED-05) reaproveita esta mesma rota POST — backend contract documentado"
  artifacts:
    - path: "server/routes/apiCredentials.ts"
      provides: "5 endpoints CRUD para api_credentials (POST, GET list, GET :id, PATCH, DELETE)"
      exports: ["registerApiCredentialsRoutes"]
      min_lines: 150
    - path: "server/routes/index.ts"
      provides: "Barrel registra registerApiCredentialsRoutes(app) ao lado dos demais módulos"
      contains: "registerApiCredentialsRoutes(app)"
  key_links:
    - from: "server/routes/apiCredentials.ts"
      to: "shared/schema.ts"
      via: "import { insertApiCredentialSchema, patchApiCredentialSchema }"
      pattern: "from ['\"]@shared/schema['\"]"
    - from: "server/routes/apiCredentials.ts"
      to: "server/storage (DatabaseStorage)"
      via: "storage.createApiCredential / storage.listApiCredentials etc"
      pattern: "storage\\.(create|list|get|update|delete|resolve)ApiCredential"
    - from: "server/routes/apiCredentials.ts"
      to: "server/services/credentials/matchUrlPattern.ts"
      via: "isValidUrlPattern para validar urlPattern no POST/PATCH antes de gravar"
      pattern: "isValidUrlPattern"
    - from: "server/routes/index.ts"
      to: "server/routes/apiCredentials.ts"
      via: "import + register call"
      pattern: "registerApiCredentialsRoutes"
---

<objective>
Wave 3 — Implementar a rota CRUD `POST|GET|PATCH|DELETE /api/v1/api-credentials` que serve como entrypoint backend tanto para a UI dedicada (Phase 16) quanto para criação inline no wizard (CRED-05).

Purpose: Único ponto de entrada HTTP para CRUD de credenciais — Phase 16 reusa sem código duplicado. A rota delega para `storage.createApiCredential` etc; toda regra de negócio vive no facade do Plan 04.
Output: 1 arquivo novo (`server/routes/apiCredentials.ts`) + 2 linhas em `server/routes/index.ts`. Stubs de rota Wave 0 ativados.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-api-credentials/10-CONTEXT.md
@.planning/phases/10-api-credentials/10-RESEARCH.md
@.planning/phases/10-api-credentials/10-VALIDATION.md
@.planning/phases/10-api-credentials/10-02-SUMMARY.md
@.planning/phases/10-api-credentials/10-04-SUMMARY.md

# Template Phase 9 (precedente exato de rota /api/v1/)
@server/routes/apis.ts

# Barrel + middleware patterns
@server/routes/index.ts
@server/routes/middleware.ts

# Schema + storage que esta rota consome
@shared/schema.ts
@server/storage/apiCredentials.ts
@server/services/credentials/matchUrlPattern.ts

# Stub Wave 0 que este plan ATIVA
@server/__tests__/apiCredentials/route.test.ts
@server/__tests__/helpers/apiCredentialFactory.ts

# Template de teste de rota Phase 9
@server/__tests__/apisRoute.test.ts

<interfaces>
<!-- Contratos exatos -->

// server/routes/apiCredentials.ts
import type { Express } from "express";
export function registerApiCredentialsRoutes(app: Express): void;

// Endpoints expostos:
//   POST   /api/v1/api-credentials      → 201 ApiCredentialSafe | 400 Zod fail | 409 dup | 401/403 auth
//   GET    /api/v1/api-credentials      → 200 ApiCredentialSafe[] (filtros: ?apiId=, ?authType=)
//   GET    /api/v1/api-credentials/:id  → 200 ApiCredentialSafe | 404
//   PATCH  /api/v1/api-credentials/:id  → 200 ApiCredentialSafe | 400 | 404 | 409
//   DELETE /api/v1/api-credentials/:id  → 204 | 404

// Middleware order (verificar em apis.ts Phase 9):
//   isAuthenticatedWithPasswordCheck → requireOperator (cobre operator + global_administrator)

// Mensagens de erro pt-BR (CONTEXT.md decisão):
//   400 Zod  → "Dados de credencial inválidos"
//   400 pat  → "URL pattern inválido"
//   409 dup  → "Credencial já cadastrada com esse nome"
//   404      → "Credencial não encontrada"
//   500      → "Falha ao processar credencial"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar server/routes/apiCredentials.ts com 5 endpoints CRUD</name>
  <files>server/routes/apiCredentials.ts, server/__tests__/apiCredentials/route.test.ts</files>
  <read_first>
    - server/routes/apis.ts (template Phase 9 — copiar header, imports, padrão de error handling, RBAC, log.info)
    - server/routes/middleware.ts (procurar `isAuthenticatedWithPasswordCheck`, `requireOperator`, `requireAdmin` — confirmar nomes exatos e exports)
    - server/storage/apiCredentials.ts (Plan 04 — confirmar assinaturas das 7 funções)
    - shared/schema.ts (procurar `insertApiCredentialSchema`, `patchApiCredentialSchema`, `ApiCredentialSafe`)
    - server/services/credentials/matchUrlPattern.ts (consumir `isValidUrlPattern`)
    - server/__tests__/apisRoute.test.ts (template Phase 9 de teste de rota — setup express, supertest, fixtures de usuário, RBAC mock)
    - server/__tests__/apiCredentials/route.test.ts (stubs a ativar)
    - .planning/phases/10-api-credentials/10-RESEARCH.md (seção "Padrão Rota POST" linhas 640-664 — código verbatim)
    - .planning/phases/10-api-credentials/10-CONTEXT.md (seção "Route" linhas 165-178 — endpoints + RBAC + status codes esperados)
  </read_first>
  <behavior>
    - Test (CRED-01, CRED-05): POST com payload válido para CADA UM dos 7 auth types retorna 201 e response inclui id, name, authType, urlPattern, priority, createdAt
    - Test (CRED-05): Response 201 NUNCA contém secretEncrypted, dekEncrypted, secret, mtlsCert, mtlsKey, mtlsCa
    - Test (CRED-01): POST com payload inválido (Zod fail) retorna 400 com message "Dados de credencial inválidos" e details
    - Test (CRED-01): POST com urlPattern inválido (`**`) retorna 400 com message "URL pattern inválido"
    - Test (CRED-05): POST duplicado (mesmo name + createdBy) retorna 409 com message "Credencial já cadastrada com esse nome"
    - Test (RBAC): POST sem autenticação retorna 401
    - Test (RBAC): POST com role `read_only` retorna 403
    - Test (RBAC): POST com role `operator` retorna 201
    - Test (RBAC): POST com role `global_administrator` retorna 201
    - Test (CRED-01): POST bearer_jwt com `exp` válido popula `bearerExpiresAt` na resposta como ISO string
    - Test (CRED-01): POST bearer_jwt opaco retorna 201 com bearerExpiresAt null
    - Test (CRED-05): GET /api/v1/api-credentials lista todas as credenciais sanitizadas
    - Test (CRED-05): GET com filter ?apiId=X retorna só com apiId=X
    - Test (CRED-05): GET com filter ?authType=basic retorna só basic
    - Test: GET por id retorna 200 sanitizado; id inexistente retorna 404
    - Test: PATCH atualiza name/description sem tocar secret
    - Test: PATCH com secret novo re-criptografa (pode ser confirmado via getApiCredentialWithSecret + decrypt)
    - Test: PATCH com urlPattern inválido retorna 400
    - Test: DELETE retorna 204; DELETE de id inexistente retorna 404
    - Test (Armadilha 3): log.info NÃO contém mtlsCert/mtlsKey/secret no body — apenas { apiCredentialId, authType, apiId }
  </behavior>
  <action>
**Sub-task A: Criar `server/routes/apiCredentials.ts`**

Usar `server/routes/apis.ts` como template estrutural. Conteúdo exato:

```ts
// Phase 10 — CRED-01, CRED-05: rota CRUD para api_credentials.
// Mesma rota usada pelo wizard inline-create (Phase 16).
// RBAC: operator + global_administrator (mesmo nivel de POST /api/v1/apis).

import type { Express, Request, Response } from "express";
import { z } from "zod";
import { storage } from "../storage";
import {
  insertApiCredentialSchema,
  patchApiCredentialSchema,
  type ApiAuthType,
} from "@shared/schema";
import { isValidUrlPattern } from "../services/credentials/matchUrlPattern";
import {
  isAuthenticatedWithPasswordCheck,
  requireOperator,
} from "./middleware";
import { createLogger } from "../lib/logger";

const log = createLogger("routes:api-credentials");

export function registerApiCredentialsRoutes(app: Express): void {
  // ── POST /api/v1/api-credentials ─────────────────────────────────────
  app.post(
    "/api/v1/api-credentials",
    isAuthenticatedWithPasswordCheck,
    requireOperator,
    async (req: any, res: Response) => {
      // 1. Zod parse
      let body: z.infer<typeof insertApiCredentialSchema>;
      try {
        body = insertApiCredentialSchema.parse(req.body);
      } catch (err: any) {
        log.info({ err: err?.errors }, "api credential rejected by Zod");
        return res
          .status(400)
          .json({ message: "Dados de credencial inválidos", details: err?.errors });
      }

      // 2. Validar urlPattern (whitelist conservadora)
      if (body.urlPattern && !isValidUrlPattern(body.urlPattern)) {
        return res.status(400).json({ message: "URL pattern inválido" });
      }

      // 3. Persistir via storage
      try {
        const cred = await storage.createApiCredential(body, req.user.id);
        log.info(
          { apiCredentialId: cred.id, authType: cred.authType, apiId: cred.apiId },
          "api credential created via route",
        );
        return res.status(201).json(cred);
      } catch (error: any) {
        if (error?.code === "23505") {
          return res
            .status(409)
            .json({ message: "Credencial já cadastrada com esse nome" });
        }
        log.error({ err: error }, "failed to create api credential");
        return res.status(500).json({ message: "Falha ao processar credencial" });
      }
    },
  );

  // ── GET /api/v1/api-credentials ──────────────────────────────────────
  app.get(
    "/api/v1/api-credentials",
    isAuthenticatedWithPasswordCheck,
    requireOperator,
    async (req: Request, res: Response) => {
      const filter: { apiId?: string; authType?: ApiAuthType } = {};
      if (typeof req.query.apiId === "string") filter.apiId = req.query.apiId;
      if (typeof req.query.authType === "string") {
        filter.authType = req.query.authType as ApiAuthType;
      }
      try {
        const list = await storage.listApiCredentials(filter);
        return res.status(200).json(list);
      } catch (error) {
        log.error({ err: error }, "failed to list api credentials");
        return res.status(500).json({ message: "Falha ao listar credenciais" });
      }
    },
  );

  // ── GET /api/v1/api-credentials/:id ──────────────────────────────────
  app.get(
    "/api/v1/api-credentials/:id",
    isAuthenticatedWithPasswordCheck,
    requireOperator,
    async (req: Request, res: Response) => {
      try {
        const cred = await storage.getApiCredential(req.params.id);
        if (!cred) {
          return res.status(404).json({ message: "Credencial não encontrada" });
        }
        return res.status(200).json(cred);
      } catch (error) {
        log.error({ err: error }, "failed to get api credential");
        return res.status(500).json({ message: "Falha ao buscar credencial" });
      }
    },
  );

  // ── PATCH /api/v1/api-credentials/:id ────────────────────────────────
  app.patch(
    "/api/v1/api-credentials/:id",
    isAuthenticatedWithPasswordCheck,
    requireOperator,
    async (req: any, res: Response) => {
      let patch: z.infer<typeof patchApiCredentialSchema>;
      try {
        patch = patchApiCredentialSchema.parse(req.body);
      } catch (err: any) {
        return res
          .status(400)
          .json({ message: "Dados de credencial inválidos", details: err?.errors });
      }
      if (patch.urlPattern && !isValidUrlPattern(patch.urlPattern)) {
        return res.status(400).json({ message: "URL pattern inválido" });
      }
      try {
        const exists = await storage.getApiCredential(req.params.id);
        if (!exists) {
          return res.status(404).json({ message: "Credencial não encontrada" });
        }
        const updated = await storage.updateApiCredential(req.params.id, patch, req.user.id);
        log.info({ apiCredentialId: req.params.id }, "api credential patched");
        return res.status(200).json(updated);
      } catch (error: any) {
        if (error?.code === "23505") {
          return res
            .status(409)
            .json({ message: "Credencial já cadastrada com esse nome" });
        }
        log.error({ err: error }, "failed to patch api credential");
        return res.status(500).json({ message: "Falha ao processar credencial" });
      }
    },
  );

  // ── DELETE /api/v1/api-credentials/:id ───────────────────────────────
  app.delete(
    "/api/v1/api-credentials/:id",
    isAuthenticatedWithPasswordCheck,
    requireOperator,
    async (req: Request, res: Response) => {
      try {
        const exists = await storage.getApiCredential(req.params.id);
        if (!exists) {
          return res.status(404).json({ message: "Credencial não encontrada" });
        }
        await storage.deleteApiCredential(req.params.id);
        log.info({ apiCredentialId: req.params.id }, "api credential deleted via route");
        return res.status(204).send();
      } catch (error) {
        log.error({ err: error }, "failed to delete api credential");
        return res.status(500).json({ message: "Falha ao remover credencial" });
      }
    },
  );
}
```

NOTA: se em `server/routes/middleware.ts` o nome do middleware operator for diferente (ex.: `requireOperatorOrAdmin`), AJUSTAR o import. O executor deve ler middleware.ts ANTES e usar o nome exato.

**Sub-task B: Ativar stubs em `server/__tests__/apiCredentials/route.test.ts`**

Usar `server/__tests__/apisRoute.test.ts` como template (mesmo padrão de express + supertest + setup de user com role + truncate de tabelas).

Implementar (mínimo):
- 8 testes de POST por auth type (1 por tipo + 1 caso adicional) confirmando 201 + shape sanitizado
- 2 testes de sanitização (Object.keys não inclui campos proibidos)
- 5 testes de erro (400 Zod, 400 urlPattern, 409 dup, 401, 403)
- 2 testes de RBAC sucesso (operator OK, global_administrator OK)
- 2 testes de bearer_jwt (exp válido popula data; opaco aceita)
- 3 testes de GET (lista, filter apiId, filter authType)
- 2 testes de GET :id (200 vs 404)
- 3 testes de PATCH (update name, re-encrypt secret, urlPattern inválido)
- 2 testes de DELETE (204 vs 404)
- 1 teste de logging seguro (capturar stream pino e confirmar que mtlsCert NÃO aparece em nenhum log de criação de mtls)

Rodar `npm run test -- server/__tests__/apiCredentials/route.test.ts` e confirmar exit 0.
  </action>
  <verify>
    <automated>npm run test -- server/__tests__/apiCredentials/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `server/routes/apiCredentials.ts` exists
    - Grep `export function registerApiCredentialsRoutes\(app: Express\): void` returns 1 match
    - Grep `app\.post\(['"]\/api\/v1\/api-credentials['"]` returns 1 match
    - Grep `app\.get\(['"]\/api\/v1\/api-credentials['"]` returns 1 match (list)
    - Grep `app\.get\(['"]\/api\/v1\/api-credentials\/:id['"]` returns 1 match
    - Grep `app\.patch\(['"]\/api\/v1\/api-credentials\/:id['"]` returns 1 match
    - Grep `app\.delete\(['"]\/api\/v1\/api-credentials\/:id['"]` returns 1 match
    - Grep `insertApiCredentialSchema\.parse\(req\.body\)` returns 1 match
    - Grep `patchApiCredentialSchema\.parse\(req\.body\)` returns 1 match
    - Grep `isValidUrlPattern\(` returns ≥ 2 matches (POST + PATCH)
    - Grep `["']Credencial já cadastrada com esse nome["']` returns ≥ 2 matches (POST + PATCH 409)
    - Grep `["']Credencial não encontrada["']` returns ≥ 3 matches (GET, PATCH, DELETE 404)
    - Grep `["']URL pattern inválido["']` returns ≥ 2 matches
    - Grep `["']Dados de credencial inválidos["']` returns ≥ 2 matches
    - Grep `error\?\.code === ["']23505["']` returns ≥ 1 match
    - Grep `requireOperator` returns ≥ 5 matches (1 por endpoint)
    - Grep `isAuthenticatedWithPasswordCheck` returns ≥ 5 matches
    - Grep `log\.info\(\s*\{[^}]*apiCredentialId` returns ≥ 1 match (logging seguro com IDs)
    - Grep `req\.body` inside `log\.` calls returns 0 matches (Armadilha 3 — nunca logar body inteiro)
    - File `server/__tests__/apiCredentials/route.test.ts` no longer contains `it\.todo`
    - `npm run test -- server/__tests__/apiCredentials/route.test.ts` exits 0
    - Test output shows ≥ 25 passing tests
  </acceptance_criteria>
  <done>5 endpoints CRUD funcionais com RBAC + Zod + 409 dup + sanitização garantida; testes Wave 0 de rota ativos e verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Registrar registerApiCredentialsRoutes(app) em server/routes/index.ts</name>
  <files>server/routes/index.ts</files>
  <read_first>
    - server/routes/index.ts (LER linhas 1-80 — ver exatamente onde está o import + chamada de `registerApiRoutes` Phase 9 para inserir simetria)
    - server/routes/apiCredentials.ts (criado na Task 1 — confirmar export `registerApiCredentialsRoutes`)
  </read_first>
  <action>
**Sub-task A: Adicionar import**

LOCALIZAR em `server/routes/index.ts` a linha exata `import { registerApiRoutes } from "./apis";` (linha ~25).

ADICIONAR IMEDIATAMENTE APÓS:
```ts
import { registerApiCredentialsRoutes } from "./apiCredentials";
```

**Sub-task B: Adicionar chamada de registro**

LOCALIZAR a linha exata `registerApiRoutes(app);` (linha ~74) na função `registerRoutes`.

ADICIONAR IMEDIATAMENTE APÓS (mesma indentação):
```ts
  registerApiCredentialsRoutes(app);
```

NÃO TOCAR em outras linhas. Apenas 2 inserções aditivas.

Após editar, rodar a suíte completa para garantir que nenhuma rota existente regrediu e que a nova rota está registrada (a route.test.ts da Task 1 monta seu próprio express, mas a integração no barrel principal é o que faz a rota aparecer no app real).

Confirmar via grep:
```bash
grep -n "registerApiCredentialsRoutes" server/routes/index.ts
```
Deve retornar EXATAMENTE 2 linhas (1 import + 1 chamada).
  </action>
  <verify>
    <automated>npm run test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Grep `import \{ registerApiCredentialsRoutes \} from ["']\./apiCredentials["']` in `server/routes/index.ts` returns 1 match
    - Grep `registerApiCredentialsRoutes\(app\);` in `server/routes/index.ts` returns 1 match
    - Order check: line number of `registerApiCredentialsRoutes(app);` MUST be greater than line number of `registerApiRoutes(app);` in `server/routes/index.ts` (verify via `grep -n` on both)
    - `npm run test` exits 0 (suite completa verde)
    - `npx tsc --noEmit -p tsconfig.json 2>&1 | grep "error TS"` returns 0 matches related to apiCredentials/registerApi
  </acceptance_criteria>
  <done>Barrel atualizado com 2 inserções; suite completa verde; rota acessível via app principal.</done>
</task>

</tasks>

<verification>
- `npm run test` exit 0 (full suite passes — incluindo Phase 10 completo + regressões)
- `npm run test -- server/__tests__/apiCredentials` exit 0 com TODOS os 6 stubs Wave 0 ativos (schema, storage, route, urlPattern, resolveCredential, guard, jwtExp)
- `npx tsc --noEmit` sem erros relacionados a apiCredentials
- `grep -n "api-credentials" server/routes/index.ts` retorna 0 matches (a rota é registrada via função, não string literal direta)
- `grep -n "registerApiCredentialsRoutes" server/routes/index.ts` retorna 2 matches
</verification>

<success_criteria>
**Phase 10 completo após este plan:**

- 5 endpoints CRUD em `/api/v1/api-credentials` operacionais
- RBAC enforced (operator/global_administrator) em todos os endpoints
- Zod discriminated union valida shape correto por auth type
- urlPattern validado via whitelist conservadora antes de gravar
- Mensagens pt-BR consistentes com o restante do projeto
- Logging seguro (apenas IDs + authType + apiId; nunca body)
- Wizard inline-create (CRED-05) tem o backend contract pronto — Phase 16 só precisa chamar POST e injetar `{id, name, authType}` no select
- Suíte Nyquist do phase: ≥ 80 testes ativos cobrindo CRED-01..05 exaustivamente
</success_criteria>

<output>
Após completion, criar `.planning/phases/10-api-credentials/10-05-SUMMARY.md` documentando:
- Endpoints expostos com método + path + códigos de status possíveis
- Linha exata adicionada em `server/routes/index.ts` (import + register call)
- Confirmação de número total de testes verdes em `server/__tests__/apiCredentials`
- Contrato consumido pelo Phase 16 (POST request body + 201 response shape)
- Confirmação que toda Phase 10 está fechada (CRED-01..05 ✓)
</output>
