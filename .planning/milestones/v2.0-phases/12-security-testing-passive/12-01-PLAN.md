---
phase: 12-security-testing-passive
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - shared/schema.ts
  - server/__tests__/apiPassive/nucleiArgs.test.ts
  - server/__tests__/apiPassive/jsonlMapper.test.ts
  - server/__tests__/apiPassive/api9Inventory.test.ts
  - server/__tests__/apiPassive/jwtAlgNone.test.ts
  - server/__tests__/apiPassive/kidInjection.test.ts
  - server/__tests__/apiPassive/tokenReuse.test.ts
  - server/__tests__/apiPassive/apiKeyLeakage.test.ts
  - server/__tests__/apiPassive/dedupeUpsert.test.ts
  - server/__tests__/apiPassive/orchestrator.test.ts
  - server/__tests__/apiPassive/route.test.ts
  - server/__tests__/fixtures/api-passive/nuclei-passive-mock.jsonl
  - server/__tests__/fixtures/api-passive/jwt-alg-none-response.json
  - server/__tests__/fixtures/api-passive/jwt-kid-injection-response.json
  - server/__tests__/fixtures/api-passive/jwt-expired-response.json
  - server/__tests__/fixtures/api-passive/api-key-leakage-body.json
autonomous: true
requirements:
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "Test harness exists: 10 test files em server/__tests__/apiPassive/ com it.todo stubs"
    - "Fixtures determinísticas existem em server/__tests__/fixtures/api-passive/ (5 arquivos)"
    - "apiPassiveTestOptsSchema Zod exportada de shared/schema.ts com stages + dryRun + overrides + .strict()"
    - "Tipos ApiPassiveTestOpts + PassiveTestResult exportados de shared/schema.ts"
    - "Vitest consegue coletar stubs sem erros: `npx vitest list server/__tests__/apiPassive` retorna 10 files"
  artifacts:
    - path: "server/__tests__/apiPassive/nucleiArgs.test.ts"
      provides: "Stub for Nuclei CLI arg builder (TEST-01)"
      contains: "it.todo"
    - path: "server/__tests__/apiPassive/jsonlMapper.test.ts"
      provides: "Stub for Nuclei JSONL → ApiFindingEvidence mapper (TEST-01)"
      contains: "it.todo"
    - path: "server/__tests__/apiPassive/api9Inventory.test.ts"
      provides: "Stub for DB-derived API9 signal queries (TEST-01)"
      contains: "it.todo"
    - path: "server/__tests__/apiPassive/jwtAlgNone.test.ts"
      provides: "Stub for alg:none forge (TEST-02)"
      contains: "it.todo"
    - path: "server/__tests__/apiPassive/kidInjection.test.ts"
      provides: "Stub for 4 canonical kid payloads (TEST-02)"
      contains: "it.todo"
    - path: "server/__tests__/apiPassive/tokenReuse.test.ts"
      provides: "Stub for exp check + skip-opaque (TEST-02)"
      contains: "it.todo"
    - path: "server/__tests__/apiPassive/apiKeyLeakage.test.ts"
      provides: "Stub for substring detection + mask-at-source (TEST-02)"
      contains: "it.todo"
    - path: "server/__tests__/apiPassive/dedupeUpsert.test.ts"
      provides: "Stub for insert/update/reopen dedupe (TEST-01+TEST-02)"
      contains: "it.todo"
    - path: "server/__tests__/apiPassive/orchestrator.test.ts"
      provides: "Stub for stages + dryRun + cancel orchestration (TEST-01+TEST-02)"
      contains: "it.todo"
    - path: "server/__tests__/apiPassive/route.test.ts"
      provides: "Stub for POST /test/passive RBAC + Zod (TEST-01+TEST-02)"
      contains: "it.todo"
    - path: "server/__tests__/fixtures/api-passive/nuclei-passive-mock.jsonl"
      provides: "3-5 representative Nuclei findings (1 misconfig, 1 exposure, 1 graphql, 1 cors)"
      contains: "info"
    - path: "server/__tests__/fixtures/api-passive/jwt-alg-none-response.json"
      provides: "Mocked response for accepted alg:none JWT"
    - path: "server/__tests__/fixtures/api-passive/jwt-kid-injection-response.json"
      provides: "Mocked response for accepted kid-manipulated JWT"
    - path: "server/__tests__/fixtures/api-passive/jwt-expired-response.json"
      provides: "Mocked response for accepted expired JWT"
    - path: "server/__tests__/fixtures/api-passive/api-key-leakage-body.json"
      provides: "Mocked response body with API key leaking"
    - path: "shared/schema.ts"
      provides: "apiPassiveTestOptsSchema Zod + ApiPassiveTestOpts + PassiveTestResult types"
      contains: "apiPassiveTestOptsSchema"
  key_links:
    - from: "server/__tests__/apiPassive/*.test.ts"
      to: "vitest.config.ts"
      via: "glob include server/**/*.test.ts"
      pattern: "apiPassive"
    - from: "shared/schema.ts apiPassiveTestOptsSchema"
      to: "Wave 3 route handler + Wave 2 orchestrator"
      via: "Zod import + z.infer"
      pattern: "apiPassiveTestOptsSchema"
---

<objective>
Wave 0 — Nyquist foundation para Phase 12. Criar 10 test stubs (it.todo) em `server/__tests__/apiPassive/`, 5 fixtures determinísticas para dryRun em `server/__tests__/fixtures/api-passive/`, e a Zod schema `apiPassiveTestOptsSchema` + types `ApiPassiveTestOpts`/`PassiveTestResult` em `shared/schema.ts`. Nada de implementação em src/server; apenas andaimes que Waves 1-3 consomem.

Purpose: **Sampling continuity**. Waves 1-3 não podem adicionar `<automated>` verify commands sem arquivos de teste pré-existindo. Fixtures permitem dryRun determinístico (success criteria #4) sem spawn Nuclei real. Zod schema é o contrato público do opts — Wave 2 (orchestrator) e Wave 3 (route) importam ambos.

Output: 10 stub files + 5 fixture files + 1 Zod schema + 2 types derivados. Zero lógica de negócio.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/12-security-testing-passive/12-CONTEXT.md
@.planning/phases/12-security-testing-passive/12-RESEARCH.md
@.planning/phases/12-security-testing-passive/12-VALIDATION.md

@.planning/phases/11-discovery-enrichment/11-01-PLAN.md

<interfaces>
<!-- From shared/schema.ts — types consumed by the new opts/result schema. -->

```typescript
// Already in shared/schema.ts (Phase 9):
export const owaspApiCategoryEnum = pgEnum('owasp_api_category', [
  'api1_bola_2023', 'api2_broken_auth_2023', 'api3_bopla_2023',
  'api4_rate_limit_2023', 'api5_bfla_2023', 'api6_business_flow_2023',
  'api7_ssrf_2023', 'api8_misconfiguration_2023', 'api9_inventory_2023',
  'api10_unsafe_consumption_2023',
]);
export const apiFindingStatusEnum = pgEnum('api_finding_status',
  ['open', 'triaged', 'false_positive', 'closed']);

// Already in shared/schema.ts (Phase 11) — TEMPLATE for apiPassiveTestOptsSchema shape:
export const discoverApiOptsSchema = z.object({
  stages: z.object({ /* ... */ }).strict().partial().optional(),
  // ...
}).strict();
export type DiscoverApiOpts = z.infer<typeof discoverApiOptsSchema>;

// Already in shared/schema.ts (Phase 9):
export const NucleiFindingSchema = /* ... */;  // line 1741
export type NucleiFinding = z.infer<typeof NucleiFindingSchema>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zod schema apiPassiveTestOptsSchema + types em shared/schema.ts</name>
  <files>shared/schema.ts</files>
  <read_first>
    - shared/schema.ts (linhas 1854-1889 — discoverApiOptsSchema como template)
    - shared/schema.ts (linhas 94-109 — owaspApiCategoryEnum + apiFindingStatusEnum)
    - .planning/phases/12-security-testing-passive/12-CONTEXT.md (§"Entrypoint + contract + dryRun + preflight")
    - .planning/phases/12-security-testing-passive/12-RESEARCH.md (§"Ready for Planning")
  </read_first>
  <behavior>
    - Test 1: apiPassiveTestOptsSchema aceita `{}` (todos opts default) e retorna objeto vazio válido
    - Test 2: apiPassiveTestOptsSchema aceita `{ stages: { nucleiPassive: false, authFailure: false, api9Inventory: false } }` (explícito off)
    - Test 3: apiPassiveTestOptsSchema rejeita campo desconhecido em root (`.strict()`) — ex: `{ foo: 'bar' }` deve falhar
    - Test 4: apiPassiveTestOptsSchema rejeita campo desconhecido em stages (`.strict()`) — ex: `{ stages: { bolaActive: true } }` deve falhar
    - Test 5: apiPassiveTestOptsSchema aceita `{ credentialIdOverride: 'uuid-string' }` e `{ endpointIds: ['a','b'] }`
    - Test 6: apiPassiveTestOptsSchema aceita `{ dryRun: true }` e `{ nuclei: { rateLimit: 5, timeoutSec: 15 } }`
    - Test 7: type PassiveTestResult tem campos: apiId, stagesRun, stagesSkipped, findingsCreated, findingsUpdated, findingsByCategory, findingsBySeverity, cancelled, dryRun, durationMs
  </behavior>
  <action>
Adicionar ao final de `shared/schema.ts` (após o bloco `discoverApiOptsSchema`, procurar âncora `export type DiscoverApiOpts = z.infer<typeof discoverApiOptsSchema>;` na linha ~1889 e inserir APÓS):

```typescript
// ============================================================================
// Phase 12: Security Testing — Passive — TEST-01, TEST-02
// ============================================================================

/**
 * apiPassiveTestOptsSchema — input contract for runApiPassiveTests() and POST
 * /api/v1/apis/:id/test/passive. Uses `.strict()` on root and stages sub-object
 * to reject unknown fields (mirrors discoverApiOptsSchema Phase 11 decision).
 */
export const apiPassiveTestOptsSchema = z.object({
  stages: z.object({
    nucleiPassive: z.boolean().optional(),   // default true (applied at call site)
    authFailure: z.boolean().optional(),     // default true
    api9Inventory: z.boolean().optional(),   // default true
  }).strict().optional(),
  credentialIdOverride: z.string().uuid().optional(),
  endpointIds: z.array(z.string().uuid()).optional(),
  dryRun: z.boolean().optional(),            // default false
  nuclei: z.object({
    rateLimit: z.number().int().min(1).max(50).optional(),  // default 10 req/s
    timeoutSec: z.number().int().min(1).max(120).optional(), // default 10s
  }).strict().optional(),
}).strict();

export type ApiPassiveTestOpts = z.infer<typeof apiPassiveTestOptsSchema>;

/**
 * PassiveTestResult — public contract returned by runApiPassiveTests().
 * Consumed by Phase 15 journey executor. Extend without breaking.
 */
export interface PassiveTestResult {
  apiId: string;
  stagesRun: Array<'nuclei_passive' | 'auth_failure' | 'api9_inventory'>;
  stagesSkipped: Array<{ stage: string; reason: string }>;
  findingsCreated: number;
  findingsUpdated: number;
  findingsByCategory: Record<string, number>;
  findingsBySeverity: Record<string, number>;
  cancelled: boolean;
  dryRun: boolean;
  durationMs: number;
}
```

NÃO modificar outras partes do arquivo. Inserir APENAS o bloco acima. Verificar import `z` já existente no topo (já existe — `import { z } from "zod";`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "shared/schema\.ts" | head -5 ; npx vitest run shared/schema.test.ts 2>&1 | tail -20 || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "apiPassiveTestOptsSchema" shared/schema.ts` retorna ≥ 2 matches (export const + type inference)
    - `grep -n "export type ApiPassiveTestOpts" shared/schema.ts` retorna 1 match
    - `grep -n "export interface PassiveTestResult" shared/schema.ts` retorna 1 match
    - `grep -n "\.strict()" shared/schema.ts` inclui 2 novas ocorrências (root + stages + nuclei)
    - `npx tsc --noEmit` NÃO produz erros novos em `shared/schema.ts`
    - `node -e "const s = require('./shared/schema.ts'); console.log(typeof s.apiPassiveTestOptsSchema);"` não aplicável (arquivo TS) — confirma via tsc
  </acceptance_criteria>
  <done>apiPassiveTestOptsSchema Zod exportado + ApiPassiveTestOpts type + PassiveTestResult interface; `tsc --noEmit` limpo; Waves 2-3 conseguem importar via `@shared/schema`.</done>
</task>

<task type="auto">
  <name>Task 2: 10 test stubs it.todo em server/__tests__/apiPassive/</name>
  <files>
    server/__tests__/apiPassive/nucleiArgs.test.ts,
    server/__tests__/apiPassive/jsonlMapper.test.ts,
    server/__tests__/apiPassive/api9Inventory.test.ts,
    server/__tests__/apiPassive/jwtAlgNone.test.ts,
    server/__tests__/apiPassive/kidInjection.test.ts,
    server/__tests__/apiPassive/tokenReuse.test.ts,
    server/__tests__/apiPassive/apiKeyLeakage.test.ts,
    server/__tests__/apiPassive/dedupeUpsert.test.ts,
    server/__tests__/apiPassive/orchestrator.test.ts,
    server/__tests__/apiPassive/route.test.ts
  </files>
  <read_first>
    - .planning/phases/12-security-testing-passive/12-VALIDATION.md (§"Wave 0 Requirements" — lista exata 10 stubs)
    - .planning/phases/12-security-testing-passive/12-CONTEXT.md (§"Claude's Discretion" item "Cobertura Nyquist Wave 0")
    - .planning/codebase/TESTING.md (§"Test Structure" — import pattern, describe, it)
    - server/__tests__/cors.test.ts (exemplo canonical de teste existente)
  </read_first>
  <action>
Criar 10 arquivos stub, um por arquivo. Cada arquivo segue o pattern:

```typescript
/**
 * Phase 12 Wave 0 — Nyquist stub for {subject}.
 * Implementation comes in Wave {W} (see 12-VALIDATION.md).
 * Requirement: {TEST-XX}
 */
import { describe, it } from 'vitest';

describe('{subject}', () => {
  // it.todo stubs — Wave {W} turns these into real assertions.
  {stubs}
});
```

Conteúdo exato por arquivo (copiar integralmente):

**1. `server/__tests__/apiPassive/nucleiArgs.test.ts`** (subject: `nuclei arg builder`; TEST-01; Wave 1):
```typescript
/**
 * Phase 12 Wave 0 — Nyquist stub for Nuclei arg builder.
 * Implementation comes in Wave 1 (12-02-PLAN — scanners/api/nucleiApi.ts).
 * Requirement: TEST-01
 */
import { describe, it } from 'vitest';

describe('nucleiApi: arg builder', () => {
  it.todo('includes -tags misconfig,exposure,graphql,cors exactly');
  it.todo('sets -rl 10 by default, honors opts.rateLimit override');
  it.todo('sets -timeout 10 by default, honors opts.timeoutSec override');
  it.todo('includes -jsonl -silent -retries 0 defensive defaults');
  it.todo('passes -l /dev/stdin (endpoint list via stdin batching)');
  it.todo('sets -t /tmp/nuclei/nuclei-templates (preflight-managed dir)');
});
```

**2. `server/__tests__/apiPassive/jsonlMapper.test.ts`** (subject: `Nuclei JSONL → ApiFindingEvidence`; TEST-01; Wave 1):
```typescript
/**
 * Phase 12 Wave 0 — Nyquist stub for Nuclei JSONL mapper.
 * Implementation comes in Wave 1 (12-02-PLAN — scanners/api/nucleiApi.ts).
 * Requirement: TEST-01
 */
import { describe, it } from 'vitest';

describe('nucleiApi: JSONL → ApiFindingEvidence mapper', () => {
  it.todo('parses NucleiFindingSchema.safeParse per line (rejects malformed lines)');
  it.todo('maps kebab-case (matched-at, template-id, extracted-results) to camelCase');
  it.todo('truncates request.body and response.body to 8192 chars (bodySnippet)');
  it.todo('maps Nuclei severity info→low, low→low, medium→medium, high→high, critical→critical');
  it.todo('sets owaspCategory api8_misconfiguration_2023 for tags misconfig/exposure/cors');
  it.todo('sets owaspCategory api9_inventory_2023 for tag graphql');
  it.todo('extracts evidence.extractedValues = { matcherName, extractedResults, templateId }');
  it.todo('copies nuclei.info.description into evidence.context');
});
```

**3. `server/__tests__/apiPassive/api9Inventory.test.ts`** (subject: `API9 DB-derived signals`; TEST-01; Wave 1):
```typescript
/**
 * Phase 12 Wave 0 — Nyquist stub for API9 Inventory DB queries.
 * Implementation comes in Wave 1 (12-02-PLAN — scanners/api/api9Inventory.ts).
 * Requirement: TEST-01
 */
import { describe, it } from 'vitest';

describe('api9Inventory: DB-derived signals', () => {
  it.todo('detectSpecPubliclyExposed: finds apis with specUrl IS NOT NULL AND specHash IS NOT NULL');
  it.todo('detectSpecPubliclyExposed: emits severity=medium, title="Especificação de API exposta publicamente"');
  it.todo('detectGraphqlIntrospection: finds apis with apiType=graphql and endpoints from spec source');
  it.todo('detectGraphqlIntrospection: emits severity=medium, title="GraphQL introspection habilitado em produção"');
  it.todo('detectHiddenKiterunnerEndpoints: finds api_endpoints with discoverySources=[kiterunner] exclusive');
  it.todo('detectHiddenKiterunnerEndpoints: filters httpxStatus IN (200, 401, 403)');
  it.todo('detectHiddenKiterunnerEndpoints: emits severity=low per endpoint, title="Endpoint oculto descoberto por brute-force"');
  it.todo('returns empty array when API has none of the 3 signals');
});
```

**4. `server/__tests__/apiPassive/jwtAlgNone.test.ts`** (subject: `JWT alg:none forge`; TEST-02; Wave 1):
```typescript
/**
 * Phase 12 Wave 0 — Nyquist stub for JWT alg:none forge.
 * Implementation comes in Wave 1 (12-02-PLAN — scanners/api/authFailure.ts).
 * Requirement: TEST-02
 */
import { describe, it } from 'vitest';

describe('authFailure: forgeJwtAlgNone', () => {
  it.todo('replaces header.alg with "none", preserves payload verbatim');
  it.todo('emits three-segment token (header.payload. — signature empty string)');
  it.todo('returns originalAlg from decoded original header (e.g., RS256)');
  it.todo('throws "JWT opaco" on tokens with < 2 segments');
  it.todo('returns originalAlg=null when header.alg is not a string');
  it.todo('round-trips standard JWT claims (iss/sub/exp) in payload unchanged');
});
```

**5. `server/__tests__/apiPassive/kidInjection.test.ts`** (subject: `kid injection payloads`; TEST-02; Wave 1):
```typescript
/**
 * Phase 12 Wave 0 — Nyquist stub for kid injection.
 * Implementation comes in Wave 1 (12-02-PLAN — scanners/api/authFailure.ts).
 * Requirement: TEST-02
 */
import { describe, it } from 'vitest';

describe('authFailure: injectKid + KID_INJECTION_PAYLOADS', () => {
  it.todo('exports exactly 4 canonical payloads: path-traversal-dev-null, path-traversal-etc-passwd, sql-injection-tautology, url-injection-external-jwks');
  it.todo('injectKid replaces header.kid with payload value, preserves payload + signature');
  it.todo('injectKid emits three-segment token (header.payload.signature)');
  it.todo('injectKid with empty original signature yields trailing dot (header.payload.)');
});
```

**6. `server/__tests__/apiPassive/tokenReuse.test.ts`** (subject: `token reuse / expired JWT`; TEST-02; Wave 1):
```typescript
/**
 * Phase 12 Wave 0 — Nyquist stub for token reuse detection.
 * Implementation comes in Wave 1 (12-02-PLAN — scanners/api/authFailure.ts).
 * Requirement: TEST-02
 */
import { describe, it } from 'vitest';

describe('authFailure: checkTokenReuse', () => {
  it.todo('returns skip={reason:"opaque_token"} when decodeJwtExp returns undefined');
  it.todo('returns skip={reason:"not_expired"} when exp > now()');
  it.todo('proceeds with probe when exp < now()');
  it.todo('emits severity=high finding when probe status < 400');
  it.todo('extractedValues includes tokenExpiredAt (ISO) and acceptedAt (ISO)');
});
```

**7. `server/__tests__/apiPassive/apiKeyLeakage.test.ts`** (subject: `API key leakage`; TEST-02; Wave 1):
```typescript
/**
 * Phase 12 Wave 0 — Nyquist stub for API key leakage heuristic.
 * Implementation comes in Wave 1 (12-02-PLAN — scanners/api/authFailure.ts).
 * Requirement: TEST-02
 */
import { describe, it } from 'vitest';

describe('authFailure: detectApiKeyLeakage', () => {
  it.todo('probes up to 5 GET endpoints per API');
  it.todo('returns match when API key substring appears in any response body');
  it.todo('mask-at-source: leakedKeyPrefix = first 3 chars + "***" (never full key)');
  it.todo('emits severity=high finding with leakedInEndpointId UUID');
  it.todo('returns no match when key does not appear in any probed body');
  it.todo('respects 1s delay between requests (rate cap)');
});
```

**8. `server/__tests__/apiPassive/dedupeUpsert.test.ts`** (subject: `upsertApiFindingByKey`; TEST-01+TEST-02; Wave 2):
```typescript
/**
 * Phase 12 Wave 0 — Nyquist stub for findings dedupe upsert.
 * Implementation comes in Wave 2 (12-03-PLAN — storage/apiFindings.ts).
 * Requirements: TEST-01, TEST-02
 */
import { describe, it } from 'vitest';

describe('storage/apiFindings: upsertApiFindingByKey', () => {
  it.todo('inserts new row when (endpointId, owaspCategory, title) does not exist');
  it.todo('updates existing row when match found and status != closed');
  it.todo('preserves status when updating (does not reset to open)');
  it.todo('refreshes evidence + jobId + updatedAt on update');
  it.todo('inserts NEW row when match exists but status=closed (reopen)');
  it.todo('returns { finding, action: "inserted" | "updated" }');
  it.todo('uses db.transaction to serialize SELECT + INSERT/UPDATE');
});
```

**9. `server/__tests__/apiPassive/orchestrator.test.ts`** (subject: `runApiPassiveTests orchestrator`; TEST-01+TEST-02; Wave 2):
```typescript
/**
 * Phase 12 Wave 0 — Nyquist stub for orchestrator.
 * Implementation comes in Wave 2 (12-03-PLAN — journeys/apiPassiveTests.ts).
 * Requirements: TEST-01, TEST-02
 */
import { describe, it } from 'vitest';

describe('journeys/apiPassiveTests: runApiPassiveTests', () => {
  it.todo('runs 3 stages in order: api9Inventory → nucleiPassive → authFailure');
  it.todo('honors opts.stages.X=false to skip stage (stagesSkipped populated)');
  it.todo('dryRun=true: reads fixtures, no spawn/fetch, findings prefixed [DRY-RUN]');
  it.todo('cancellation via jobQueue.isJobCancelled between stages sets cancelled=true');
  it.todo('preserves partial findings on cancel (no rollback)');
  it.todo('preflightNuclei failure: skip nucleiPassive, continue authFailure + api9Inventory');
  it.todo('durationMs populated, findingsByCategory counts match upsert actions');
  it.todo('authFailure stage skips endpoints where requiresAuth != true');
  it.todo('authFailure stage skips endpoints without compatible resolveApiCredential match');
});
```

**10. `server/__tests__/apiPassive/route.test.ts`** (subject: `POST /test/passive + GET /api/v1/api-findings`; TEST-01+TEST-02; Wave 3):
```typescript
/**
 * Phase 12 Wave 0 — Nyquist stub for route handlers.
 * Implementation comes in Wave 3 (12-04-PLAN — routes/apis.ts + routes/apiFindings.ts).
 * Requirements: TEST-01, TEST-02
 */
import { describe, it } from 'vitest';

describe('POST /api/v1/apis/:id/test/passive', () => {
  it.todo('requires authentication (401 if not logged)');
  it.todo('requires operator or global_administrator role (403 if readonly)');
  it.todo('rejects body with unknown field (.strict() Zod)');
  it.todo('returns 404 when apiId does not exist');
  it.todo('calls runApiPassiveTests with parsed opts + jobId');
  it.todo('returns 201 with PassiveTestResult shape');
});

describe('GET /api/v1/api-findings', () => {
  it.todo('requires at least one of apiId/endpointId/jobId (400 otherwise)');
  it.todo('allows readonly_analyst role (RBAC expanded)');
  it.todo('filters by owaspCategory/severity/status/jobId');
  it.todo('applies limit (default 50) + offset pagination');
  it.todo('returns ApiFinding[] sanitized (no secret fields — api_findings has none natively)');
});
```

NÃO adicionar `import { expect }` ou assertions reais. Apenas `it.todo` literais (Wave 0 é intencionalmente vazio).
  </action>
  <verify>
    <automated>npx vitest list server/__tests__/apiPassive 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `ls server/__tests__/apiPassive/ | wc -l` retorna `10`
    - `grep -l "it.todo" server/__tests__/apiPassive/*.test.ts | wc -l` retorna `10`
    - `grep -c "it.todo" server/__tests__/apiPassive/*.test.ts` soma total ≥ 60 (cobertura Nyquist)
    - `grep -L "it\s*(" server/__tests__/apiPassive/*.test.ts | wc -l` retorna `10` (NENHUM arquivo tem `it(` real — só `it.todo`)
    - `grep -l "import.*expect" server/__tests__/apiPassive/*.test.ts | wc -l` retorna `0` (sem expect em stubs)
    - `npx vitest list server/__tests__/apiPassive 2>&1` enumera 10 arquivos sem erros de parse
  </acceptance_criteria>
  <done>10 test files criados com ≥6 `it.todo` cada; vitest coleta sem erros; Waves 1-3 podem referenciar como `<automated>` alvos.</done>
</task>

<task type="auto">
  <name>Task 3: 5 fixtures determinísticas para dryRun em server/__tests__/fixtures/api-passive/</name>
  <files>
    server/__tests__/fixtures/api-passive/nuclei-passive-mock.jsonl,
    server/__tests__/fixtures/api-passive/jwt-alg-none-response.json,
    server/__tests__/fixtures/api-passive/jwt-kid-injection-response.json,
    server/__tests__/fixtures/api-passive/jwt-expired-response.json,
    server/__tests__/fixtures/api-passive/api-key-leakage-body.json
  </files>
  <read_first>
    - .planning/phases/12-security-testing-passive/12-VALIDATION.md (§"Fixtures — determinístico dryRun")
    - .planning/phases/12-security-testing-passive/12-CONTEXT.md (§"Entrypoint + contract + dryRun + preflight" — shape das fixtures)
    - shared/schema.ts linhas 1737-1764 (NucleiFindingSchema — shape de cada linha do JSONL)
    - server/__tests__/fixtures/nuclei/ (exemplo de fixture Nuclei existente v1.0 — se existir)
  </read_first>
  <action>
Criar 5 fixtures. Conteúdo exato:

**1. `server/__tests__/fixtures/api-passive/nuclei-passive-mock.jsonl`** (5 linhas, cada uma válida NucleiFindingSchema):
```
{"template":"http/misconfiguration/server-status.yaml","template-id":"apache-server-status","template-url":"https://cloud.projectdiscovery.io/public/apache-server-status","template-path":"/tmp/nuclei/nuclei-templates/http/misconfiguration/server-status.yaml","info":{"name":"Apache Server Status - Detect","author":["pdteam"],"tags":["misconfig","apache","exposure"],"description":"Apache mod_status endpoint is exposed publicly.","severity":"low","reference":["https://httpd.apache.org/docs/2.4/mod/mod_status.html"]},"type":"http","host":"https://example.internal","matched-at":"https://example.internal/server-status","matcher-name":"word-match","extracted-results":["Server Version: Apache/2.4.52"],"request":"GET /server-status HTTP/1.1\nHost: example.internal\n","response":"HTTP/1.1 200 OK\nContent-Type: text/html\n\n<h1>Apache Server Status</h1>","ip":"10.0.0.5","timestamp":"2026-04-20T10:00:00Z","curl-command":"curl -X GET https://example.internal/server-status","matcher-status":true,"matched-line":null}
{"template":"http/exposures/configs/git-config.yaml","template-id":"git-config","template-url":"https://cloud.projectdiscovery.io/public/git-config","template-path":"/tmp/nuclei/nuclei-templates/http/exposures/configs/git-config.yaml","info":{"name":".git/config Exposure","author":["pdteam"],"tags":["exposure","config","git"],"description":"A .git/config file is publicly accessible.","severity":"medium","reference":["https://owasp.org/www-community/attacks/git-exposure"]},"type":"http","host":"https://example.internal","matched-at":"https://example.internal/.git/config","matcher-name":"git-config-match","extracted-results":["[core]"],"request":"GET /.git/config HTTP/1.1\nHost: example.internal\n","response":"HTTP/1.1 200 OK\nContent-Type: text/plain\n\n[core]\n\trepositoryformatversion = 0","ip":"10.0.0.5","timestamp":"2026-04-20T10:00:01Z","curl-command":"curl https://example.internal/.git/config","matcher-status":true,"matched-line":null}
{"template":"http/technologies/graphql-detect.yaml","template-id":"graphql-detect","template-url":"https://cloud.projectdiscovery.io/public/graphql-detect","template-path":"/tmp/nuclei/nuclei-templates/http/technologies/graphql-detect.yaml","info":{"name":"GraphQL Endpoint Detect","author":["pdteam"],"tags":["graphql","tech","detect"],"description":"GraphQL endpoint is exposed.","severity":"info","reference":["https://graphql.org"]},"type":"http","host":"https://example.internal","matched-at":"https://example.internal/graphql","matcher-name":"graphql-probe","extracted-results":[],"request":"POST /graphql HTTP/1.1\nHost: example.internal\nContent-Type: application/json\n\n{\"query\":\"{__typename}\"}","response":"HTTP/1.1 200 OK\nContent-Type: application/json\n\n{\"data\":{\"__typename\":\"Query\"}}","ip":"10.0.0.5","timestamp":"2026-04-20T10:00:02Z","curl-command":"curl -X POST https://example.internal/graphql -d '{\"query\":\"{__typename}\"}'","matcher-status":true,"matched-line":null}
{"template":"http/misconfiguration/cors-misconfig.yaml","template-id":"cors-misconfig","template-url":"https://cloud.projectdiscovery.io/public/cors-misconfig","template-path":"/tmp/nuclei/nuclei-templates/http/misconfiguration/cors-misconfig.yaml","info":{"name":"CORS Misconfiguration - Arbitrary Origin Reflection","author":["pdteam"],"tags":["cors","misconfig"],"description":"Server reflects any Origin header in Access-Control-Allow-Origin with credentials.","severity":"high","reference":["https://portswigger.net/web-security/cors"]},"type":"http","host":"https://example.internal","matched-at":"https://example.internal/api/v1/users","matcher-name":"cors-reflect-credentials","extracted-results":["Access-Control-Allow-Origin: https://attacker.example"],"request":"GET /api/v1/users HTTP/1.1\nHost: example.internal\nOrigin: https://attacker.example\n","response":"HTTP/1.1 200 OK\nAccess-Control-Allow-Origin: https://attacker.example\nAccess-Control-Allow-Credentials: true\n","ip":"10.0.0.5","timestamp":"2026-04-20T10:00:03Z","curl-command":"curl -H 'Origin: https://attacker.example' https://example.internal/api/v1/users","matcher-status":true,"matched-line":null}
{"template":"http/exposures/apis/swagger-api.yaml","template-id":"swagger-api","template-url":"https://cloud.projectdiscovery.io/public/swagger-api","template-path":"/tmp/nuclei/nuclei-templates/http/exposures/apis/swagger-api.yaml","info":{"name":"Swagger UI Exposure","author":["pdteam"],"tags":["exposure","api","swagger"],"description":"Swagger UI is publicly accessible.","severity":"info","reference":["https://swagger.io"]},"type":"http","host":"https://example.internal","matched-at":"https://example.internal/swagger-ui.html","matcher-name":"swagger-match","extracted-results":[],"request":"GET /swagger-ui.html HTTP/1.1\nHost: example.internal\n","response":"HTTP/1.1 200 OK\nContent-Type: text/html\n\n<title>Swagger UI</title>","ip":"10.0.0.5","timestamp":"2026-04-20T10:00:04Z","curl-command":"curl https://example.internal/swagger-ui.html","matcher-status":true,"matched-line":null}
```

**2. `server/__tests__/fixtures/api-passive/jwt-alg-none-response.json`**:
```json
{
  "status": 200,
  "headers": {
    "content-type": "application/json",
    "x-request-id": "test-alg-none-001"
  },
  "body": "{\"user\":{\"id\":42,\"email\":\"admin@example.internal\",\"role\":\"admin\"}}",
  "describedBy": "Server accepted JWT with alg=none and empty signature — critical auth bypass"
}
```

**3. `server/__tests__/fixtures/api-passive/jwt-kid-injection-response.json`**:
```json
{
  "status": 200,
  "headers": {
    "content-type": "application/json",
    "x-request-id": "test-kid-inject-001"
  },
  "body": "{\"data\":{\"access\":\"granted\",\"scope\":\"admin\"}}",
  "describedBy": "Server accepted JWT with manipulated kid header (path traversal payload)",
  "payloadUsed": "../../../../../../../dev/null"
}
```

**4. `server/__tests__/fixtures/api-passive/jwt-expired-response.json`**:
```json
{
  "status": 200,
  "headers": {
    "content-type": "application/json",
    "x-request-id": "test-token-reuse-001"
  },
  "body": "{\"authenticated\":true,\"user\":\"alice\"}",
  "describedBy": "Server accepted expired JWT — token reuse not rejected",
  "tokenExpiredAt": "2024-01-01T00:00:00Z"
}
```

**5. `server/__tests__/fixtures/api-passive/api-key-leakage-body.json`**:
```json
{
  "status": 200,
  "headers": {
    "content-type": "application/json",
    "x-request-id": "test-key-leakage-001"
  },
  "body": "{\"debug\":{\"authorization_header\":\"Bearer sk_test_abc123xyz456\",\"api_key\":\"sk_test_abc123xyz456\"},\"user\":\"bob\"}",
  "describedBy": "Response body contains the caller's API key — severity high leakage",
  "leakedKey": "sk_test_abc123xyz456"
}
```

Criar pasta `server/__tests__/fixtures/api-passive/` se não existe. Fixtures são versionadas via git SHA (sem checksum explícito — não são binários).
  </action>
  <verify>
    <automated>ls -la server/__tests__/fixtures/api-passive/ && echo "--- JSONL lines ---" && wc -l server/__tests__/fixtures/api-passive/nuclei-passive-mock.jsonl && echo "--- JSON validity ---" && for f in server/__tests__/fixtures/api-passive/*.json; do node -e "JSON.parse(require('fs').readFileSync('$f','utf8'));console.log('OK: $f');"; done</automated>
  </verify>
  <acceptance_criteria>
    - `ls server/__tests__/fixtures/api-passive/ | wc -l` retorna `5`
    - `wc -l server/__tests__/fixtures/api-passive/nuclei-passive-mock.jsonl` retorna ≥ 5
    - Cada linha do `nuclei-passive-mock.jsonl` é JSON válido — `cat ... | while read l; do echo "$l" | node -e 'JSON.parse(require("fs").readFileSync(0,"utf8"))'; done` não gera erro
    - `grep -c '"severity"' server/__tests__/fixtures/api-passive/nuclei-passive-mock.jsonl` retorna ≥ 5 (cada finding tem severity)
    - `grep -l '"tags":\["misconfig"' server/__tests__/fixtures/api-passive/nuclei-passive-mock.jsonl` retorna match (cobre tag misconfig)
    - `grep -l '"tags":\["exposure"' server/__tests__/fixtures/api-passive/nuclei-passive-mock.jsonl` retorna match (cobre exposure)
    - `grep -l '"tags":\["graphql"' server/__tests__/fixtures/api-passive/nuclei-passive-mock.jsonl` retorna match (cobre graphql)
    - `grep -l '"tags":\["cors"' server/__tests__/fixtures/api-passive/nuclei-passive-mock.jsonl` retorna match (cobre cors)
    - Todos 4 arquivos `.json` passam `JSON.parse` sem erro
    - `grep "sk_test_abc123xyz456" server/__tests__/fixtures/api-passive/api-key-leakage-body.json` retorna ≥ 1 match (string completa presente para teste de substring match)
  </acceptance_criteria>
  <done>5 fixtures criadas e versionadas; JSONL tem ≥5 linhas cobrindo 4 tags oficiais (misconfig/exposure/graphql/cors); 4 JSON válidos com shape consumível por Wave 2 orchestrator.</done>
</task>

</tasks>

<verification>
Run after all tasks complete:

```bash
# 1) Type check
npx tsc --noEmit

# 2) Vitest coleta os stubs
npx vitest list server/__tests__/apiPassive

# 3) Vitest executa os stubs (todos it.todo = 0 passed, 0 failed, N skipped)
npx vitest run server/__tests__/apiPassive --reporter=default

# 4) Contagem final
echo "stubs=$(ls server/__tests__/apiPassive/ | wc -l) fixtures=$(ls server/__tests__/fixtures/api-passive/ | wc -l)"
# Expected: stubs=10 fixtures=5
```
</verification>

<success_criteria>
- [ ] `shared/schema.ts` exporta `apiPassiveTestOptsSchema`, `ApiPassiveTestOpts`, `PassiveTestResult`
- [ ] `npx tsc --noEmit` sem erros novos em `shared/schema.ts`
- [ ] 10 arquivos de teste em `server/__tests__/apiPassive/` (apenas `it.todo`)
- [ ] 5 fixtures em `server/__tests__/fixtures/api-passive/` (1 JSONL + 4 JSON)
- [ ] `npx vitest run server/__tests__/apiPassive` passa (0 fail, N todo)
- [ ] JSONL cobre 4 tags oficiais Nuclei TEST-01 (misconfig/exposure/graphql/cors)
- [ ] Waves 1-3 podem referenciar stubs como `<automated>` alvos
</success_criteria>

<output>
After completion, create `.planning/phases/12-security-testing-passive/12-01-SUMMARY.md`
</output>
