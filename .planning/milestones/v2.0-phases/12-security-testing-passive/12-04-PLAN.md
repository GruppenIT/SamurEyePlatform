---
phase: 12-security-testing-passive
plan: 04
type: execute
wave: 3
depends_on:
  - "12-security-testing-passive/12-01"
  - "12-security-testing-passive/12-02"
  - "12-security-testing-passive/12-03"
files_modified:
  - server/routes/apis.ts
  - server/routes/apiFindings.ts
  - server/routes/index.ts
  - server/scripts/runApiPassiveTests.ts
  - docs/operations/run-api-passive-tests.md
autonomous: false
requirements:
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "POST /api/v1/apis/:id/test/passive existe com RBAC operator + global_administrator + Zod validation (apiPassiveTestOptsSchema)"
    - "POST /test/passive retorna 404 para apiId inexistente, 400 para body inválido, 201 com PassiveTestResult para sucesso"
    - "GET /api/v1/api-findings existe com RBAC incluindo readonly_analyst; requer pelo menos um de apiId/endpointId/jobId (400 caso contrário)"
    - "GET /api/v1/api-findings aceita filtros owaspCategory/severity/status/jobId + limit/offset"
    - "registerApiFindingsRoutes(app) registrado em server/routes/index.ts barrel"
    - "CLI server/scripts/runApiPassiveTests.ts aceita flags --api=<id> [--no-nuclei] [--no-auth-failure] [--no-api9] [--dry-run] [--credential=<id>]"
    - "CLI usa import.meta.url guard + tsx-safe (segue padrão runApiDiscovery.ts)"
    - "Runbook pt-BR em docs/operations/run-api-passive-tests.md com seções: Pré-requisitos, dryRun, Execução real, Interpretação de findings, Troubleshooting"
    - "Human UAT checkpoint confirma end-to-end: CLI dry-run → findings persistidas → GET /api/v1/api-findings retorna findings com [DRY-RUN] prefix"
  artifacts:
    - path: "server/routes/apis.ts"
      provides: "POST /api/v1/apis/:id/test/passive handler appended to registerApiRoutes(app)"
      contains: "test/passive"
    - path: "server/routes/apiFindings.ts"
      provides: "GET /api/v1/api-findings handler + registerApiFindingsRoutes(app)"
      exports: ["registerApiFindingsRoutes"]
      min_lines: 80
    - path: "server/routes/index.ts"
      provides: "registerApiFindingsRoutes(app) wired into barrel"
      contains: "registerApiFindingsRoutes"
    - path: "server/scripts/runApiPassiveTests.ts"
      provides: "CLI operator tool — standalone tsx script"
      min_lines: 80
    - path: "docs/operations/run-api-passive-tests.md"
      provides: "pt-BR runbook for operators"
      min_lines: 100
  key_links:
    - from: "server/routes/apis.ts POST /test/passive handler"
      to: "services/journeys/apiPassiveTests.runApiPassiveTests"
      via: "import + call with parsed opts + jobId"
      pattern: "runApiPassiveTests"
    - from: "server/routes/apis.ts POST /test/passive handler"
      to: "shared/schema.apiPassiveTestOptsSchema"
      via: "Zod parse of req.body"
      pattern: "apiPassiveTestOptsSchema.parse"
    - from: "server/routes/apiFindings.ts GET /api/v1/api-findings"
      to: "storage.listApiFindings (Wave 2)"
      via: "storage facade call with parsed filter"
      pattern: "storage.listApiFindings"
    - from: "server/scripts/runApiPassiveTests.ts"
      to: "services/journeys/apiPassiveTests.runApiPassiveTests"
      via: "direct import + call main()"
      pattern: "runApiPassiveTests"
    - from: "server/routes/index.ts"
      to: "server/routes/apiFindings.registerApiFindingsRoutes"
      via: "import + register call"
      pattern: "registerApiFindingsRoutes"
---

<objective>
Wave 3 — Public surfaces. Rota HTTP para disparar a execução (`POST /api/v1/apis/:id/test/passive`), rota de leitura (`GET /api/v1/api-findings`), CLI de operador, runbook pt-BR, e checkpoint de UAT humana.

Purpose: Expor a capacidade de testing passivo para (a) trigger via UI interna (futuro Phase 16), (b) execução operacional via CLI, (c) leitura via read path. Fechar ciclo de Phase 12.

Output: 2 rotas + 1 barrel wiring + 1 CLI + 1 doc + 1 UAT checkpoint.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/12-security-testing-passive/12-CONTEXT.md
@.planning/phases/12-security-testing-passive/12-RESEARCH.md
@.planning/phases/12-security-testing-passive/12-01-PLAN.md
@.planning/phases/12-security-testing-passive/12-02-PLAN.md
@.planning/phases/12-security-testing-passive/12-03-PLAN.md

@server/routes/apis.ts
@server/routes/apiCredentials.ts
@server/routes/index.ts
@server/scripts/runApiDiscovery.ts
@docs/operations/run-api-discovery.md

<interfaces>
<!-- Key types and contracts from Waves 0-2. -->

From Wave 0 (shared/schema.ts):
```typescript
export const apiPassiveTestOptsSchema = z.object({...}).strict();
export type ApiPassiveTestOpts = z.infer<typeof apiPassiveTestOptsSchema>;
export interface PassiveTestResult { /* ... */ }
```

From Wave 2 (server/services/journeys/apiPassiveTests.ts):
```typescript
export async function runApiPassiveTests(
  apiId: string,
  opts: ApiPassiveTestOpts,
  jobId?: string,
): Promise<PassiveTestResult>;
```

From Wave 2 (server/storage/apiFindings.ts):
```typescript
export interface ListApiFindingsFilter {
  apiId?: string;
  endpointId?: string;
  jobId?: string;
  owaspCategory?: string;
  severity?: 'low' | 'medium' | 'high' | 'critical';
  status?: 'open' | 'triaged' | 'false_positive' | 'closed';
  limit?: number;
  offset?: number;
}
export async function listApiFindings(filter: ListApiFindingsFilter): Promise<ApiFinding[]>;
```

Existing Phase 11 route pattern (server/routes/apis.ts) — POST /api/v1/apis/:id/discover (lines ~70-150):
```typescript
app.post('/api/v1/apis/:id/discover', isAuthenticatedWithPasswordCheck, requireOperator, async (req, res) => {
  let body = discoverApiOptsSchema.parse(req.body);
  const api = await storage.getApi(req.params.id);
  if (!api) return res.status(404).json({ message: "API não encontrada" });
  const jobId = randomUUID();
  const result = await discoverApi(req.params.id, body, jobId);
  await storage.logAudit({ userId: req.user.id, action: 'api_discovery_started', ... });
  res.status(201).json(result);
});
```

Existing Phase 10 pattern (server/routes/apiCredentials.ts) — GET with filters + RBAC:
```typescript
app.get('/api/v1/api-credentials', isAuthenticatedWithPasswordCheck, requireAnyRole, async (req, res) => {
  const filter = { apiId: req.query.apiId as string | undefined, ... };
  const creds = await storage.listApiCredentials(filter);
  res.json(creds);
});
```

RBAC middleware (server/routes/middleware.ts):
```typescript
export function requireOperator(req, res, next);       // operator + global_administrator
export function requireAdmin(req, res, next);          // global_administrator only
export function requireAnyRole(req, res, next);        // any authenticated role
// For readonly_analyst inclusion → use requireAnyRole OR a custom role guard.
```

Existing CLI template (server/scripts/runApiDiscovery.ts):
```typescript
import 'dotenv/config';
import { discoverApi } from '../services/journeys/apiDiscovery';

async function main() {
  const args = parseArgs(process.argv.slice(2));
  if (!args.api) { console.error('--api=<id> required'); process.exit(1); }
  const opts = { stages: {...}, dryRun: args['dry-run'] };
  const result = await discoverApi(args.api, opts);
  console.log(JSON.stringify(result, null, 2));
}

if (import.meta.url === `file://${process.argv[1]}`) {
  main().catch((err) => { console.error(err); process.exit(1); });
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: server/routes/apis.ts — adicionar POST /api/v1/apis/:id/test/passive</name>
  <files>server/routes/apis.ts</files>
  <read_first>
    - server/routes/apis.ts (arquivo atual completo — Phase 11 já tem POST /discover como template direto)
    - server/routes/middleware.ts (requireOperator signature)
    - server/localAuth.ts (isAuthenticatedWithPasswordCheck)
    - shared/schema.ts (apiPassiveTestOptsSchema de Wave 0)
    - server/services/journeys/apiPassiveTests.ts (runApiPassiveTests de Wave 2)
    - .planning/phases/12-security-testing-passive/12-CONTEXT.md (§"Entrypoint + contract + dryRun + preflight" — rota interna)
    - .planning/phases/12-security-testing-passive/12-RESEARCH.md (§"Ready for Planning" Wave 3)
  </read_first>
  <behavior>
    - Test 1: POST /test/passive sem autenticação retorna 401
    - Test 2: POST /test/passive com role=readonly_analyst retorna 403 (requireOperator bloqueia)
    - Test 3: POST /test/passive com body `{ foo: 'bar' }` retorna 400 (Zod .strict() rejeita)
    - Test 4: POST /test/passive com apiId inexistente retorna 404
    - Test 5: POST /test/passive com body válido chama `runApiPassiveTests(apiId, opts, jobId)` e retorna 201 com PassiveTestResult
    - Test 6: Audit log é escrito com `action: 'api_passive_test_started'` incluindo userId/apiId/jobId
  </behavior>
  <action>
**APPEND** ao arquivo `server/routes/apis.ts` (NÃO sobrescrever). O arquivo tem ~152 linhas atuais com handlers existentes. Adicionar NOVO handler DENTRO da função `registerApiRoutes(app)` ao final, ANTES do `}` que fecha a função.

Localizar:

```typescript
// Atualmente o arquivo tem no topo:
import { insertApiSchema, discoverApiOptsSchema } from "@shared/schema";
```

Modificar o import para adicionar `apiPassiveTestOptsSchema`:

```typescript
import {
  insertApiSchema,
  discoverApiOptsSchema,
  apiPassiveTestOptsSchema,
} from "@shared/schema";
```

E adicionar import novo APÓS o import existente de `discoverApi`:

```typescript
import { discoverApi } from "../services/journeys/apiDiscovery";
import { runApiPassiveTests } from "../services/journeys/apiPassiveTests";
```

ADICIONAR o handler ao final da função `registerApiRoutes(app)` (dentro do `export function registerApiRoutes(app: Express) { ... }`, após o handler POST /:id/discover, antes do `}` final):

```typescript
  /**
   * POST /api/v1/apis/:id/test/passive — Phase 12 TEST-01/TEST-02 entrypoint.
   *
   * Body: ApiPassiveTestOpts (see shared/schema.ts apiPassiveTestOptsSchema).
   * RBAC: operator + global_administrator (requireOperator).
   * Returns 201 with PassiveTestResult. 404 if API not found. 400 on Zod fail.
   */
  app.post(
    '/api/v1/apis/:id/test/passive',
    isAuthenticatedWithPasswordCheck,
    requireOperator,
    async (req: any, res) => {
      let opts: ReturnType<typeof apiPassiveTestOptsSchema.parse>;
      try {
        opts = apiPassiveTestOptsSchema.parse(req.body ?? {});
      } catch (err: any) {
        log.info({ err, apiId: req.params.id }, 'passive test request rejected by Zod');
        return res.status(400).json({
          message: 'Opções de teste passivo inválidas',
          details: err?.errors ?? undefined,
        });
      }

      const apiId = req.params.id;
      const api = await storage.getApi(apiId);
      if (!api) {
        return res.status(404).json({ message: 'API não encontrada' });
      }

      // Synthetic jobId — Phase 15 replaces with real jobQueue.enqueue().
      const jobId = randomUUID();

      try {
        await storage.logAudit({
          userId: req.user.id,
          action: 'api_passive_test_started',
          target: apiId,
          metadata: { jobId, dryRun: opts.dryRun ?? false, stages: opts.stages ?? {} },
        });
      } catch (err) {
        log.warn({ err, apiId, jobId }, 'failed to write audit log for passive test start — continuing');
      }

      try {
        const result = await runApiPassiveTests(apiId, opts, jobId);
        log.info(
          {
            apiId,
            jobId,
            userId: req.user.id,
            stagesRun: result.stagesRun.length,
            findingsCreated: result.findingsCreated,
            findingsUpdated: result.findingsUpdated,
            cancelled: result.cancelled,
            dryRun: result.dryRun,
          },
          'api passive tests executed via route',
        );
        return res.status(201).json(result);
      } catch (err) {
        log.error({ err, apiId, jobId }, 'api passive tests failed');
        return res.status(500).json({
          message: 'Falha ao executar testes passivos',
        });
      }
    },
  );
```

Preservar TODOS os handlers existentes (POST /, GET /, POST /:id/discover). NÃO modificar eles.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "routes/apis" | head -5 ; grep -c "app.post\|app.get" server/routes/apis.ts ; npx vitest run server/__tests__/apiPassive/route.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "POST /api/v1/apis/:id/test/passive\|'/api/v1/apis/:id/test/passive'" server/routes/apis.ts` retorna ≥ 1 match
    - `grep -n "apiPassiveTestOptsSchema" server/routes/apis.ts` retorna ≥ 2 matches (import + parse)
    - `grep -n "runApiPassiveTests" server/routes/apis.ts` retorna ≥ 2 matches (import + call)
    - `grep -n "api_passive_test_started" server/routes/apis.ts` retorna 1 match (audit action)
    - `grep -n "requireOperator" server/routes/apis.ts` retorna ≥ 2 matches (handler existing + novo)
    - `grep -n "API não encontrada" server/routes/apis.ts` retorna ≥ 1 match (pt-BR 404 preservado)
    - `grep -n "Opções de teste passivo inválidas" server/routes/apis.ts` retorna 1 match (pt-BR 400 novo)
    - Handler `POST /api/v1/apis` (HIER-03) ainda presente: `grep "app.post('/api/v1/apis'," server/routes/apis.ts` retorna 1 match
    - Handler `POST /:id/discover` ainda presente: `grep "test/passive\|:/discover" server/routes/apis.ts` mostra ambos
    - `npx tsc --noEmit` não produz erro em `server/routes/apis.ts`
  </acceptance_criteria>
  <done>POST /test/passive aditivo; Phase 11 handlers preservados; stubs route.test.ts passíveis de consumo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: server/routes/apiFindings.ts + barrel registration (GET /api/v1/api-findings)</name>
  <files>
    server/routes/apiFindings.ts,
    server/routes/index.ts
  </files>
  <read_first>
    - server/routes/apiCredentials.ts (template: pattern GET com filtros query params + RBAC + storage call)
    - server/routes/apis.ts (imports canonical pattern)
    - server/routes/index.ts (barrel — onde adicionar registerApiFindingsRoutes)
    - server/routes/middleware.ts (requireOperator + requireAnyRole para incluir readonly_analyst)
    - server/storage/apiFindings.ts (listApiFindings criado em Wave 2)
    - shared/schema.ts linhas 94-109 (owaspApiCategoryEnum + apiFindingStatusEnum + threatSeverityEnum)
    - .planning/phases/12-security-testing-passive/12-CONTEXT.md (§"Read path — GET /api/v1/api-findings")
  </read_first>
  <behavior>
    - Test 1: GET /api/v1/api-findings sem query params retorna 400 (falta apiId/endpointId/jobId)
    - Test 2: GET /api/v1/api-findings?apiId=xxx com role readonly_analyst retorna 200 (RBAC expandido)
    - Test 3: GET /api/v1/api-findings?endpointId=xxx retorna findings filtrados
    - Test 4: GET /api/v1/api-findings?jobId=xxx retorna findings filtrados
    - Test 5: GET /api/v1/api-findings aceita filtros combinados: severity=high + status=open + owaspCategory=api2_broken_auth_2023
    - Test 6: GET /api/v1/api-findings?limit=10&offset=20 aplica pagination
    - Test 7: GET /api/v1/api-findings com owaspCategory inválida retorna 400
  </behavior>
  <action>
**Sub-task A — Criar `server/routes/apiFindings.ts`:**

```typescript
/**
 * Phase 12 — API Findings read path.
 *
 * GET /api/v1/api-findings — list findings with filters (RBAC inclui
 * readonly_analyst pois é read-only).
 *
 * Phase 14 (FIND-02) centralizará sanitização de evidence globally.
 * Phase 16 adicionará UI de drill-down; Phase 12 entrega apenas o read path
 * para uso interno e UAT.
 */
import type { Express } from 'express';
import { z } from 'zod';
import { storage } from '../storage';
import { isAuthenticatedWithPasswordCheck } from '../localAuth';
import { requireAnyRole } from './middleware';
import { createLogger } from '../lib/logger';

const log = createLogger('routes:apiFindings');

/**
 * Zod schema for GET /api/v1/api-findings query string. .strict() prevents
 * typos/unknown params silently ignored. At least one of apiId/endpointId/
 * jobId MUST be present (refine below).
 */
const listFindingsQuerySchema = z.object({
  apiId: z.string().uuid().optional(),
  endpointId: z.string().uuid().optional(),
  jobId: z.string().uuid().optional(),
  owaspCategory: z.enum([
    'api1_bola_2023',
    'api2_broken_auth_2023',
    'api3_bopla_2023',
    'api4_rate_limit_2023',
    'api5_bfla_2023',
    'api6_business_flow_2023',
    'api7_ssrf_2023',
    'api8_misconfiguration_2023',
    'api9_inventory_2023',
    'api10_unsafe_consumption_2023',
  ]).optional(),
  severity: z.enum(['low', 'medium', 'high', 'critical']).optional(),
  status: z.enum(['open', 'triaged', 'false_positive', 'closed']).optional(),
  limit: z.coerce.number().int().min(1).max(500).optional(),
  offset: z.coerce.number().int().min(0).optional(),
})
  .strict()
  .refine(
    (q) => !!(q.apiId || q.endpointId || q.jobId),
    { message: 'Forneça ao menos um filtro: apiId, endpointId ou jobId' },
  );

export function registerApiFindingsRoutes(app: Express) {
  /**
   * GET /api/v1/api-findings
   * Query params: apiId | endpointId | jobId (at least one required),
   *               owaspCategory, severity, status, limit, offset.
   * RBAC: requireAnyRole (inclui readonly_analyst — este read path é
   *       auditoria interna).
   */
  app.get(
    '/api/v1/api-findings',
    isAuthenticatedWithPasswordCheck,
    requireAnyRole,
    async (req: any, res) => {
      let parsed: z.infer<typeof listFindingsQuerySchema>;
      try {
        parsed = listFindingsQuerySchema.parse(req.query);
      } catch (err: any) {
        log.info({ err, query: req.query }, 'api-findings query rejected by Zod');
        return res.status(400).json({
          message: err?.errors?.[0]?.message ?? 'Parâmetros de filtro inválidos',
          details: err?.errors ?? undefined,
        });
      }

      try {
        const findings = await storage.listApiFindings({
          apiId: parsed.apiId,
          endpointId: parsed.endpointId,
          jobId: parsed.jobId,
          owaspCategory: parsed.owaspCategory,
          severity: parsed.severity,
          status: parsed.status,
          limit: parsed.limit ?? 50,
          offset: parsed.offset ?? 0,
        });
        log.info(
          {
            userId: req.user.id,
            filterKeys: Object.keys(parsed),
            resultCount: findings.length,
          },
          'api findings listed',
        );
        return res.json(findings);
      } catch (err) {
        log.error({ err }, 'failed to list api findings');
        return res.status(500).json({ message: 'Falha ao listar findings de API' });
      }
    },
  );
}
```

**Sub-task B — Atualizar `server/routes/index.ts` para registrar:**

Adicionar import após os imports de `./apis` e `./apiCredentials`:

```typescript
import { registerApiRoutes } from "./apis";
import { registerApiCredentialsRoutes } from "./apiCredentials";
import { registerApiFindingsRoutes } from "./apiFindings";  // NEW
```

Adicionar chamada no bloco onde `registerApiRoutes(app)` e `registerApiCredentialsRoutes(app)` são chamados:

```typescript
  registerApiRoutes(app);
  registerApiCredentialsRoutes(app);
  registerApiFindingsRoutes(app);  // NEW
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "routes/apiFindings|routes/index" | head -5 ; grep -n "registerApiFindingsRoutes" server/routes/index.ts ; npx vitest run server/__tests__/apiPassive/route.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `server/routes/apiFindings.ts` existe com ≥ 80 linhas
    - `grep -n "export function registerApiFindingsRoutes" server/routes/apiFindings.ts` retorna 1 match
    - `grep -n "app.get('/api/v1/api-findings'" server/routes/apiFindings.ts` retorna 1 match
    - `grep -n "requireAnyRole" server/routes/apiFindings.ts` retorna 1 match (RBAC expandido)
    - `grep -n "listApiFindings" server/routes/apiFindings.ts` retorna ≥ 1 match (storage call)
    - `grep -n ".strict()" server/routes/apiFindings.ts` retorna ≥ 1 match (Zod strict)
    - `grep -n "Forneça ao menos um filtro" server/routes/apiFindings.ts` retorna 1 match (pt-BR refine message)
    - `grep -cE "api[12345678]_.*_2023" server/routes/apiFindings.ts` retorna ≥ 10 (todas 10 categorias OWASP enum)
    - `grep -n "registerApiFindingsRoutes" server/routes/index.ts` retorna ≥ 2 matches (import + call)
    - `grep -cE "registerApiRoutes|registerApiCredentialsRoutes|registerApiFindingsRoutes" server/routes/index.ts` retorna ≥ 6 (3 imports + 3 calls = 6)
    - `npx tsc --noEmit` não produz erro novo
  </acceptance_criteria>
  <done>GET /api/v1/api-findings exposto, RBAC inclui readonly_analyst, filtros validados por Zod, barrel wirado.</done>
</task>

<task type="auto">
  <name>Task 3: server/scripts/runApiPassiveTests.ts (CLI operator)</name>
  <files>server/scripts/runApiPassiveTests.ts</files>
  <read_first>
    - server/scripts/runApiDiscovery.ts (template CLI completo — copiar estrutura)
    - server/services/journeys/apiPassiveTests.ts (runApiPassiveTests signature de Wave 2)
    - shared/schema.ts (ApiPassiveTestOpts de Wave 0)
    - .planning/phases/12-security-testing-passive/12-CONTEXT.md (§"Entrypoint" — CLI flags)
  </read_first>
  <action>
Criar arquivo novo `server/scripts/runApiPassiveTests.ts`. Seguir padrão exato de `runApiDiscovery.ts` adaptado para passive tests. Conteúdo:

```typescript
#!/usr/bin/env tsx
/**
 * Phase 12 TEST-01/TEST-02 — Operator CLI for API Passive Tests.
 *
 * Usage:
 *   npx tsx --env-file=.env server/scripts/runApiPassiveTests.ts \
 *     --api=<id> [--no-nuclei] [--no-auth-failure] [--no-api9] [--dry-run] [--credential=<id>]
 *
 * Reads .env for DB + encryption config. Calls runApiPassiveTests() and
 * prints PassiveTestResult as JSON. Exit code 0 on success, 1 on error.
 *
 * Safety:
 *   - Always uses a synthetic UUID for jobId (operator-triggered, not a
 *     scheduled job). Phase 15 orchestration adds real enqueue.
 *   - dryRun forces fixtures — no real HTTP traffic.
 */
import { runApiPassiveTests } from '../services/journeys/apiPassiveTests';
import type { ApiPassiveTestOpts } from '@shared/schema';
import { randomUUID } from 'crypto';

function parseArgs(argv: string[]): Record<string, string | boolean> {
  const out: Record<string, string | boolean> = {};
  for (const arg of argv) {
    if (arg.startsWith('--')) {
      const eq = arg.indexOf('=');
      if (eq > 0) {
        out[arg.slice(2, eq)] = arg.slice(eq + 1);
      } else {
        out[arg.slice(2)] = true;
      }
    }
  }
  return out;
}

function printUsage(): void {
  console.error(`
Usage:
  npx tsx --env-file=.env server/scripts/runApiPassiveTests.ts \\
    --api=<id> [--no-nuclei] [--no-auth-failure] [--no-api9] [--dry-run] [--credential=<id>]

Required:
  --api=<uuid>              API ID to test

Optional toggles (default: all stages ON):
  --no-nuclei               Skip Nuclei passive scan
  --no-auth-failure         Skip JWT/API key auth-failure tests
  --no-api9                 Skip API9 DB-derived inventory signals
  --dry-run                 Use local fixtures (no HTTP/spawn); findings titled [DRY-RUN]
  --credential=<uuid>       Force specific credential for auth-failure (overrides resolve)

Example:
  npx tsx --env-file=.env server/scripts/runApiPassiveTests.ts --api=abc-123 --dry-run
`);
}

async function main(): Promise<void> {
  const args = parseArgs(process.argv.slice(2));

  if (args.help || args.h) {
    printUsage();
    process.exit(0);
  }

  const apiId = typeof args.api === 'string' ? args.api : undefined;
  if (!apiId) {
    console.error('Erro: flag obrigatória --api=<uuid> ausente.');
    printUsage();
    process.exit(1);
  }

  const opts: ApiPassiveTestOpts = {
    stages: {
      nucleiPassive: args['no-nuclei'] !== true,
      authFailure: args['no-auth-failure'] !== true,
      api9Inventory: args['no-api9'] !== true,
    },
    dryRun: args['dry-run'] === true,
  };
  if (typeof args.credential === 'string') {
    opts.credentialIdOverride = args.credential;
  }

  const jobId = randomUUID();

  console.error(
    `[runApiPassiveTests] apiId=${apiId} jobId=${jobId} dryRun=${opts.dryRun ?? false} ` +
      `nuclei=${opts.stages?.nucleiPassive} auth=${opts.stages?.authFailure} api9=${opts.stages?.api9Inventory}`,
  );

  try {
    const result = await runApiPassiveTests(apiId, opts, jobId);
    // Result on stdout (machine-readable); progress/log on stderr.
    console.log(JSON.stringify(result, null, 2));
    process.exit(result.cancelled ? 2 : 0);
  } catch (err) {
    console.error('Erro executando testes passivos:', err instanceof Error ? err.message : err);
    process.exit(1);
  }
}

// Only run main() when invoked directly (not when imported from tests).
if (import.meta.url === `file://${process.argv[1]}`) {
  main().catch((err) => {
    console.error('Erro não tratado:', err);
    process.exit(1);
  });
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "scripts/runApiPassiveTests" | head -5 ; npx tsx --env-file=.env server/scripts/runApiPassiveTests.ts --help 2>&1 | head -20 || true</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `server/scripts/runApiPassiveTests.ts` existe com ≥ 80 linhas
    - `grep -n "import.meta.url" server/scripts/runApiPassiveTests.ts` retorna 1 match (guard)
    - `grep -n "runApiPassiveTests" server/scripts/runApiPassiveTests.ts` retorna ≥ 2 matches (import + call)
    - `grep -n "apiPassiveTestOptsSchema\|ApiPassiveTestOpts" server/scripts/runApiPassiveTests.ts` retorna ≥ 1 match (type import)
    - `grep -n "--no-nuclei\|--no-auth-failure\|--no-api9\|--dry-run\|--credential" server/scripts/runApiPassiveTests.ts` retorna ≥ 5 matches (todas flags documentadas)
    - `grep -n "JSON.stringify" server/scripts/runApiPassiveTests.ts` retorna ≥ 1 match (stdout result)
    - `grep -n "randomUUID" server/scripts/runApiPassiveTests.ts` retorna ≥ 1 match (synthetic jobId)
    - `grep -n "Erro: flag obrigatória" server/scripts/runApiPassiveTests.ts` retorna 1 match (pt-BR usage error)
    - `npx tsc --noEmit` não produz erro
    - Execução `npx tsx server/scripts/runApiPassiveTests.ts --help` exibe usage e exit 0
  </acceptance_criteria>
  <done>CLI operacional com flags do CONTEXT; output stdout JSON PassiveTestResult; exit codes 0/1/2.</done>
</task>

<task type="auto">
  <name>Task 4: docs/operations/run-api-passive-tests.md (runbook pt-BR)</name>
  <files>docs/operations/run-api-passive-tests.md</files>
  <read_first>
    - docs/operations/run-api-discovery.md (template de runbook Phase 11 — seguir estrutura)
    - .planning/phases/12-security-testing-passive/12-CONTEXT.md (escopo e decisões)
    - .planning/phases/12-security-testing-passive/12-RESEARCH.md (Nuclei tags + cenários)
  </read_first>
  <action>
Criar arquivo novo `docs/operations/run-api-passive-tests.md`. Conteúdo exato em pt-BR:

```markdown
# Runbook — Testes Passivos de API (Phase 12)

**Versão:** 1.0
**Última atualização:** 2026-04-20
**Requisitos cobertos:** TEST-01 (Nuclei + API9 Inventory), TEST-02 (JWT/API key auth-failure)

Este runbook descreve como operar os testes passivos de API via CLI e via rota HTTP interna. Os testes são **stateless** (Nuclei templates oficiais misconfig/exposure/graphql/cors + checks DB-derived de inventário API9 + 4 vetores in-house de auth-failure: JWT alg:none, kid injection, token reuse, API key leakage).

---

## 1. Pré-requisitos

- Nuclei instalado em `/opt/samureye/bin/nuclei` ou no `PATH` (verificar com `which nuclei`).
- Templates Nuclei em `/tmp/nuclei/nuclei-templates` (gerenciados pelo `nucleiPreflight.ts` — são baixados automaticamente na primeira execução).
- Variáveis de ambiente em `.env`: `DATABASE_URL`, `ENCRYPTION_KEK`.
- API registrada via `POST /api/v1/apis` (Phase 9) e com endpoints descobertos via Phase 11 (`POST /api/v1/apis/:id/discover`).
- Para testes de auth-failure: credencial API cadastrada via `POST /api/v1/api-credentials` (Phase 10) dos tipos `bearer_jwt`, `api_key_header` ou `api_key_query`.

### Autorização

**Confirme que você tem permissão explícita para testar a API antes de executar contra qualquer alvo que não seja um ambiente de laboratório controlado.** A Phase 15 adicionará o checkbox formal de autorização; Phase 12 confia no operador.

---

## 2. Execução em `dryRun` (recomendado para primeira validação)

O modo `--dry-run` lê fixtures locais em `server/__tests__/fixtures/api-passive/`, não faz spawn de Nuclei nem requisições HTTP reais. Serve para validar o pipeline e o read path sem impacto no alvo.

```bash
npx tsx --env-file=.env server/scripts/runApiPassiveTests.ts \
  --api=<uuid-da-api> \
  --dry-run
```

Saída esperada (stdout — JSON de `PassiveTestResult`):

```json
{
  "apiId": "...",
  "stagesRun": ["api9_inventory", "nuclei_passive", "auth_failure"],
  "stagesSkipped": [],
  "findingsCreated": 5,
  "findingsUpdated": 0,
  "findingsByCategory": {
    "api8_misconfiguration_2023": 3,
    "api9_inventory_2023": 1,
    "api2_broken_auth_2023": 3
  },
  "findingsBySeverity": {
    "low": 2,
    "medium": 1,
    "high": 3,
    "critical": 1
  },
  "cancelled": false,
  "dryRun": true,
  "durationMs": 1234
}
```

**Findings geradas pelo dryRun têm o título prefixado com `[DRY-RUN] `** para facilitar filtragem posterior.

### Verificar findings via read path

```bash
curl -u <user>:<pwd> "http://localhost:3000/api/v1/api-findings?apiId=<uuid>&limit=100"
```

---

## 3. Execução real (contra alvo autorizado)

### 3.1 Usando CLI

```bash
npx tsx --env-file=.env server/scripts/runApiPassiveTests.ts \
  --api=<uuid-da-api>
```

Todas as 3 stages rodam por default (`api9_inventory`, `nuclei_passive`, `auth_failure`). Flags para desabilitar seletivamente:

| Flag | Efeito |
|------|--------|
| `--no-nuclei` | Skipa stage Nuclei (útil para depurar apenas auth-failure) |
| `--no-auth-failure` | Skipa stage auth-failure (útil quando não há credencial cadastrada) |
| `--no-api9` | Skipa stage API9 inventory (quando só interessa Nuclei) |
| `--credential=<uuid>` | Força uma credencial específica em vez de usar `resolveApiCredential()` |

### 3.2 Usando rota HTTP interna

```bash
curl -u <user>:<pwd> -X POST \
  -H "Content-Type: application/json" \
  -d '{"dryRun":false,"stages":{"nucleiPassive":true,"authFailure":true,"api9Inventory":true}}' \
  "http://localhost:3000/api/v1/apis/<uuid>/test/passive"
```

RBAC: exige role `operator` ou `global_administrator`.

Respostas:

- `201` + JSON `PassiveTestResult` em caso de sucesso.
- `400` quando o body não valida em `apiPassiveTestOptsSchema` (Zod `.strict()` rejeita campos desconhecidos).
- `404` quando o `apiId` não existe.
- `500` em falha interna (detalhes nos logs estruturados — sem secrets).

---

## 4. Interpretação de findings

Findings são persistidas em `api_findings` com categoria OWASP API Top 10 2023 + severidade:

### 4.1 Categorias geradas pelo Phase 12

- **`api2_broken_auth_2023`** — auth-failure in-house (severity `high` ou `critical`).
  - `critical` → "JWT com alg=none aceito pelo servidor" (bypass completo).
  - `high` → "kid injection", "JWT expirado aceito", "API key vazada em response body".
- **`api8_misconfiguration_2023`** — Nuclei com tags `misconfig`, `exposure`, `cors` (severity `low` a `high`, conforme template).
- **`api9_inventory_2023`** — 3 variantes DB-derived:
  - "Especificação de API exposta publicamente" (`medium`).
  - "GraphQL introspection habilitado em produção" (`medium`).
  - "Endpoint oculto descoberto por brute-force" (`low`).

### 4.2 Dedupe automático

Ao reexecutar contra a mesma API:

- Finding já aberta (`status != closed`) com mesma tripla `(endpointId, owaspCategory, title)` → **update** (refresh de evidence + jobId + updatedAt; status preservado).
- Finding fechada (`status = closed`) com match → **nova row** (issue reabriu).
- Sem match → insert normal.

Não há inflação desnecessária da tabela.

### 4.3 Mask-at-source em evidence

Phase 12 nunca escreve tokens/API keys completos em `evidence.extractedValues`. Para API key leakage, apenas o prefixo `<primeiros 3 chars>***` é armazenado. Phase 14 (FIND-02) formalizará sanitização global; Phase 12 é defensive-by-default.

---

## 5. Troubleshooting

### 5.1 Stage `nuclei_passive` em `stagesSkipped`

- Motivo mais comum: `nuclei` binário não está no PATH ou em `/opt/samureye/bin/`. Rode `which nuclei` para verificar.
- Verificar logs estruturados para a mensagem do `preflightNuclei` (templates dir, versão, etc).

### 5.2 Stage `auth_failure` em `stagesSkipped` com `reason: "no endpoints eligible"`

- Nenhum endpoint da API tem `requiresAuth=true` (rode Phase 11 httpx enrichment antes).
- Nenhuma credencial cadastrada é compatível com o `urlPattern` dos endpoints (`resolveApiCredential` retorna `null`).
- Credencial cadastrada é de auth-type fora de escopo Phase 12 (`basic`/`oauth2_client_credentials`/`hmac`/`mtls`) — apenas `bearer_jwt`, `api_key_header` e `api_key_query` são suportados nesta phase.

### 5.3 Stage `api9_inventory` reporta 0 hits

- API não tem `specUrl` populada (rode Phase 11 discovery contra alvo com OpenAPI/Swagger exposto).
- API não é `graphql` OU não tem endpoints descobertos via spec.
- Nenhum endpoint tem `discoverySources = ['kiterunner']` exclusivo (rode Phase 11 com `--kiterunner`).

### 5.4 Findings não aparecem no `GET /api/v1/api-findings`

- A query requer pelo menos um de `apiId`, `endpointId` ou `jobId`. Sem nenhum destes, retorna 400.
- Confirme o role — `readonly_analyst`, `operator` e `global_administrator` têm acesso.

### 5.5 Timeout do Nuclei

- Default total: 30 minutos por API. Se atingir, `log.warn` com mensagem `nuclei total timeout — SIGTERM`. Reduza o número de endpoints via `opts.endpointIds` ou aumente o `rateLimit` se o alvo aguentar (respeitando políticas de rate cap do ambiente de teste).

---

## 6. Observabilidade

- Logs estruturados via `pino` — todos os logs JSON, sem bodies nem secrets (pino redaction automática).
- Campos relevantes por log: `apiId`, `jobId`, `stage`, `findingsCreated`, `findingsUpdated`, `durationMs`, `cancelled`.
- Audit log escrito no start da execução via rota HTTP: `action: 'api_passive_test_started'` com `userId`, `apiId`, `jobId`, `dryRun`, `stages`.

---

## 7. Próximas phases

- **Phase 13** — BOLA/BFLA/BOPLA/rate-limit/SSRF (stateful).
- **Phase 14** — Sanitização formal de evidence (FIND-02), promoção para `threats` (FIND-03), WebSocket events (FIND-04).
- **Phase 15** — Wiring no `journeyExecutor`, abort via `/jobs/:id/abort`, authorization ack, rate ceiling 50 req/s, audit log formal.
- **Phase 16** — UI de findings filter por `source=api_security`, curl reproduction, false-positive marking.

---

**Paralelo ao Phase 11:** `docs/operations/run-api-discovery.md`.
```
  </action>
  <verify>
    <automated>test -f docs/operations/run-api-passive-tests.md && wc -l docs/operations/run-api-passive-tests.md && grep -cE "^## " docs/operations/run-api-passive-tests.md</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `docs/operations/run-api-passive-tests.md` existe com ≥ 100 linhas
    - `grep -c "^## " docs/operations/run-api-passive-tests.md` retorna ≥ 7 (seções: Pré-requisitos, dryRun, Execução real, Interpretação, Troubleshooting, Observabilidade, Próximas phases)
    - `grep -n "npx tsx.*runApiPassiveTests" docs/operations/run-api-passive-tests.md` retorna ≥ 2 matches (exemplos de comando)
    - `grep -n "curl.*api-findings\|curl.*test/passive" docs/operations/run-api-passive-tests.md` retorna ≥ 2 matches (exemplos HTTP)
    - `grep -n "--dry-run\|--no-nuclei\|--no-auth-failure\|--no-api9\|--credential" docs/operations/run-api-passive-tests.md` retorna ≥ 5 matches (todas as flags documentadas)
    - `grep -n "TEST-01\|TEST-02" docs/operations/run-api-passive-tests.md` retorna ≥ 1 match (traceability)
    - `grep -n "mask-at-source\|prefix" docs/operations/run-api-passive-tests.md` retorna ≥ 1 match (explicação segurança)
    - `grep -n "api2_broken_auth_2023\|api8_misconfiguration_2023\|api9_inventory_2023" docs/operations/run-api-passive-tests.md` retorna ≥ 3 matches (categorias OWASP)
    - Runbook está em pt-BR (contém palavras: "Pré-requisitos", "Execução", "Troubleshooting", "Interpretação")
  </acceptance_criteria>
  <done>Runbook pt-BR completo; operador tem guia passo-a-passo para dryRun e execução real.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: UAT humana — validação end-to-end Phase 12</name>
  <what-built>
Phase 12 entrega o primeiro batch de testes passivos OWASP API Top 10:

1. **10 test stubs Nyquist** + **5 fixtures determinísticas** (Wave 0).
2. **3 scanners novos**: `nucleiApi.ts` (Nuclei passivo), `authFailure.ts` (4 vetores JWT/API key), `api9Inventory.ts` (3 sinais DB-derived) (Wave 1).
3. **Orchestrator `runApiPassiveTests`** compõe 3 stages com dryRun + cancelamento (Wave 2).
4. **Storage extension** `upsertApiFindingByKey` (dedupe transacional) + `listApiFindings` (read path) (Wave 2).
5. **POST /api/v1/apis/:id/test/passive** (dispara execução com RBAC) + **GET /api/v1/api-findings** (leitura com filtros incluindo readonly_analyst) (Wave 3).
6. **CLI `server/scripts/runApiPassiveTests.ts`** + **runbook `docs/operations/run-api-passive-tests.md`** (Wave 3).

Sucesso no fluxo dryRun prova reprodutibilidade determinística (success criteria #4). Execução real contra alvo autorizado prova TEST-01 + TEST-02.
  </what-built>
  <how-to-verify>
Execute os passos abaixo em ordem. Documente saída de cada passo.

### Passo 1 — Validação de tipo + lint

```bash
npx tsc --noEmit 2>&1 | tail -20
# Esperado: sem erros novos relacionados a Phase 12.
```

### Passo 2 — Suite de testes (Nyquist verde)

```bash
npx vitest run server/__tests__/apiPassive --reporter=default 2>&1 | tail -30
# Esperado: todos arquivos listados, 0 failures (it.todo count esperado).
```

### Passo 3 — Validação regressão Phases 9/10/11

```bash
npx vitest run server/__tests__/apiDiscovery server/__tests__/apiCredentials 2>&1 | tail -10
# Esperado: nenhum teste novo falhando (Phase 11 Wave 0 + Phase 10 preservados).
```

### Passo 4 — CLI `--help`

```bash
npx tsx server/scripts/runApiPassiveTests.ts --help
# Esperado: uso exibido, exit 0.
```

### Passo 5 — DryRun contra API existente

**Pré-requisito:** Ter uma API registrada (rode `POST /api/v1/apis` antes se não houver).

Descubra um apiId:
```bash
curl -u admin:<pwd> "http://localhost:3000/api/v1/apis" | head -50
```

Execute dryRun:
```bash
npx tsx --env-file=.env server/scripts/runApiPassiveTests.ts \
  --api=<apiId-real> --dry-run 2>&1 | tee /tmp/p12-dryrun.json
```

Valide saída:
- Campo `dryRun: true`.
- `findingsCreated >= 1` (pelo menos o Nuclei fixture gerou findings).
- `stagesRun` inclui `['api9_inventory', 'nuclei_passive']` no mínimo.

### Passo 6 — Read path

```bash
curl -u admin:<pwd> "http://localhost:3000/api/v1/api-findings?apiId=<apiId-real>&limit=100" | jq '.'
```

Valide:
- Response 200.
- Array de findings com títulos começando com `[DRY-RUN] ` (fixtures foram persistidas).
- Cada finding tem `owaspCategory` em `{api2_broken_auth_2023, api8_misconfiguration_2023, api9_inventory_2023}`.
- Cada finding tem `severity` em `{low, medium, high, critical}`.
- Campo `evidence` é JSON com estrutura `{ request, response, extractedValues?, context? }`.

### Passo 7 — RBAC

Usuário com role `readonly_analyst`:
```bash
curl -u readonly:<pwd> "http://localhost:3000/api/v1/api-findings?apiId=<apiId>" 
# Esperado: 200 (readonly tem acesso ao read path).

curl -u readonly:<pwd> -X POST \
  -H "Content-Type: application/json" \
  -d '{}' \
  "http://localhost:3000/api/v1/apis/<apiId>/test/passive"
# Esperado: 403 (readonly não pode disparar execução).
```

### Passo 8 — Dedupe

Rode o dryRun uma segunda vez:
```bash
npx tsx --env-file=.env server/scripts/runApiPassiveTests.ts --api=<apiId> --dry-run
# Esperado: findingsCreated=0, findingsUpdated>=1 (dedupe por (endpointId, category, title)).
```

### Passo 9 — Execução real (OPCIONAL — requer alvo autorizado)

Contra um alvo como OWASP Juice Shop local:
```bash
npx tsx --env-file=.env server/scripts/runApiPassiveTests.ts --api=<juice-shop-apiId>
# Inspecione stdout: findings reais (sem prefix [DRY-RUN]).
# Confirme via GET /api/v1/api-findings que evidence.bodySnippet <= 8192 chars.
# Confirme nos logs que NÃO há secrets/tokens full (pino redaction ativa).
```

### Passo 10 — Estrutura de logs

```bash
tail -200 server.log 2>/dev/null | grep -i "passive" | head -20
# Esperado: logs estruturados JSON com `apiId`, `jobId`, `findingsCreated`, `findingsUpdated`, `durationMs`.
# NÃO deve conter: tokens, API keys, bodies de request/response completas, "Authorization: Bearer ..." literal.
```
  </how-to-verify>
  <resume-signal>Após confirmar todos os passos, responda `approved` para encerrar Phase 12. Se houver problema em algum passo, descreva o erro e o passo específico para diagnóstico.</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:

```bash
# 1) Type check final
npx tsc --noEmit

# 2) Phase 12 stubs — todos os 10 devem colecionar + rodar sem erro
npx vitest run server/__tests__/apiPassive --reporter=default

# 3) Regression Phases 9/10/11
npx vitest run server/__tests__/apiDiscovery server/__tests__/apiCredentials 2>&1 | tail -10

# 4) CLI smoke
npx tsx server/scripts/runApiPassiveTests.ts --help

# 5) Arquivos finais
for f in \
  server/routes/apis.ts \
  server/routes/apiFindings.ts \
  server/routes/index.ts \
  server/scripts/runApiPassiveTests.ts \
  docs/operations/run-api-passive-tests.md; do
  echo "== $f =="; wc -l "$f"
done
```
</verification>

<success_criteria>
- [ ] POST /api/v1/apis/:id/test/passive wirada em `registerApiRoutes(app)` com RBAC requireOperator + Zod validation
- [ ] GET /api/v1/api-findings em arquivo novo `server/routes/apiFindings.ts` com RBAC requireAnyRole (inclui readonly_analyst)
- [ ] `registerApiFindingsRoutes(app)` wirada em barrel `server/routes/index.ts`
- [ ] CLI `server/scripts/runApiPassiveTests.ts` com todas as 4 toggles + dryRun + credential override
- [ ] Runbook `docs/operations/run-api-passive-tests.md` com ≥ 7 seções pt-BR
- [ ] UAT humana aprovada (10 passos executados com sucesso)
- [ ] `npx tsc --noEmit` limpo
- [ ] Regression Phases 9/10/11 preservadas
</success_criteria>

<output>
After completion, create `.planning/phases/12-security-testing-passive/12-04-SUMMARY.md`
</output>
