---
phase: 14-findings-runtime-threat-integration
plan: 03
type: execute
wave: 2
depends_on:
  - "14-01"
files_modified:
  - shared/schema.ts
  - server/services/jobEventBroadcaster.ts
  - server/routes/jobs.ts
  - server/__tests__/jobEventBroadcaster.test.ts
autonomous: true
requirements:
  - FIND-04
must_haves:
  truths:
    - "shared/schema.ts exporta `jobEventSchema` Zod discriminated union com 3 variants: stage_progress, findings_batch, journey_complete"
    - "shared/schema.ts exporta `type JobEvent = z.infer<typeof jobEventSchema>`"
    - "findings_batch variant limita `findings` array a max 20 items; `message` em stage_progress limita a 200 chars"
    - "Arquivo `server/services/jobEventBroadcaster.ts` exporta singleton `jobEventBroadcaster` com métodos `subscribe(jobId, ws)`, `unsubscribe(jobId, ws)`, `emit(jobId, event)`"
    - "`emit` valida payload via jobEventSchema.parse antes de enviar; eventos inválidos disparam warning + são droppados (não lançam)"
    - "`emit` aplica rate limit 10 events/sec per jobId (throttle simples via timestamp tracking); eventos excedendo são droppados com warning"
    - "`emit` itera sobre subscribers do jobId e chama ws.send(JSON.stringify(event)); se ws.send falha (disconnect), unsubscribe o client automaticamente"
    - "`server/routes/jobs.ts` ganha handler WebSocket `GET /api/v1/jobs/:jobId/ws` que chama `jobEventBroadcaster.subscribe(jobId, ws)` on upgrade + unsubscribe on close (se rota já existe, apenas integra o broadcaster)"
    - "Arquivo `server/__tests__/jobEventBroadcaster.test.ts` contém exatamente 6 stubs it.todo"
  artifacts:
    - path: "shared/schema.ts"
      provides: "jobEventSchema Zod discriminated union + JobEvent type"
      contains: "jobEventSchema"
    - path: "server/services/jobEventBroadcaster.ts"
      provides: "jobEventBroadcaster singleton + subscribe/unsubscribe/emit methods"
      exports: ["jobEventBroadcaster", "JobEventBroadcaster"]
    - path: "server/routes/jobs.ts"
      provides: "WebSocket upgrade handler wired to jobEventBroadcaster"
      contains: "jobEventBroadcaster.subscribe"
    - path: "server/__tests__/jobEventBroadcaster.test.ts"
      provides: "6 it.todo stubs: schema validation, subscribe/unsubscribe, emit basic, rate limit, ws.send failure disconnect, payload size cap"
      contains: "it.todo"
  key_links:
    - from: "server/services/jobEventBroadcaster.ts"
      to: "Wave 3 (14-04) route handlers + Phase 15 journeyExecutor"
      via: "import jobEventBroadcaster + call .emit(jobId, event) after stage/batch"
      pattern: "jobEventBroadcaster\\.emit"
    - from: "server/routes/jobs.ts GET /ws upgrade"
      to: "jobEventBroadcaster.subscribe"
      via: "WebSocket connection handler wires to singleton"
      pattern: "subscribe\\(jobId"
    - from: "shared/schema.ts jobEventSchema"
      to: "Frontend client (future Phase 16) + broadcaster.emit validation"
      via: "Zod parse contract on both sides"
      pattern: "jobEventSchema"
---

<objective>
Wave 2 — Criar real-time event infrastructure para FIND-04. Entrega 4 artifacts: (1) Zod discriminated union `jobEventSchema` em `shared/schema.ts` com 3 variants (stage_progress, findings_batch, journey_complete); (2) service novo `server/services/jobEventBroadcaster.ts` com subscribe/unsubscribe/emit + rate limit 10/sec; (3) wiring na rota WebSocket existente `server/routes/jobs.ts`; (4) scaffold de testes com 6 it.todo.

Purpose: **Real-time signaling, não data tunnel**. UI recebe eventos enquanto journey roda — IDs + counts, não full finding payloads (client polls GET /api/v1/api-findings para details se quiser). Rate limit 10/sec previne WebSocket saturation de bursts. ws.send failure faz auto-unsubscribe (client disconnected). Fail-open: emit NUNCA quebra journey — eventos inválidos/droppados só logam warning.

Output: 1 arquivo novo (service) + 1 arquivo novo de teste + extensão shared/schema.ts + extensão server/routes/jobs.ts. Contrato consumido por Wave 3 plan 14-04 (route handlers emit findings_batch) e Phase 15 (journeyExecutor emit stage_progress + journey_complete).
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/14-findings-runtime-threat-integration/14-CONTEXT.md

@shared/schema.ts
@server/routes/jobs.ts
@server/services/jobQueue.ts

<interfaces>
<!-- jobEventSchema exact shape from 14-CONTEXT.md §"WebSocket Events (FIND-04)". -->
<!-- Implementor MUST use this exact discriminated union — downstream consumers (Phase 16 UI) will parse. -->

```typescript
// Target Zod schema to add in shared/schema.ts (Task 1):
export const jobEventSchema = z.discriminatedUnion('type', [
  z.object({
    type: z.literal('stage_progress'),
    stage: z.string(),
    status: z.enum(['started', 'completed', 'failed']),
    findingsDiscovered: z.number().int().min(0),
    durationMs: z.number().int().min(0),
    message: z.string().max(200),
  }),
  z.object({
    type: z.literal('findings_batch'),
    findings: z.array(z.object({
      id: z.string(),
      owaspCategory: z.string(),
      severity: z.string(),
      endpointPath: z.string(),
      title: z.string(),
    })).max(20),
    batchNumber: z.number().int().min(1),
    totalNewInBatch: z.number().int().min(0),
  }),
  z.object({
    type: z.literal('journey_complete'),
    jobId: z.string(),
    apiId: z.string(),
    totalFindings: z.number().int().min(0),
    totalThreatsPromoted: z.number().int().min(0),
    durationMs: z.number().int().min(0),
    status: z.enum(['success', 'cancelled']),
  }),
]);
export type JobEvent = z.infer<typeof jobEventSchema>;
```

```typescript
// server/routes/jobs.ts — assumed pattern for WebSocket upgrade (verify actual file):
// If existing: `GET /api/v1/jobs/:jobId/ws` exists → integrate broadcaster in connection handler
// If missing: add minimal upgrade handler that wires to broadcaster
//
// Expected signature after Task 3:
// wss.on('connection', (ws, req) => {
//   const jobId = extractJobIdFromUrl(req.url);
//   jobEventBroadcaster.subscribe(jobId, ws);
//   ws.on('close', () => jobEventBroadcaster.unsubscribe(jobId, ws));
// });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar jobEventSchema Zod discriminated union em shared/schema.ts</name>
  <files>shared/schema.ts</files>
  <read_first>
    - shared/schema.ts (últimos 200 linhas — localizar onde Phase 12/13 adicionou apiPassiveTestOptsSchema + PassiveTestResult + apiActiveTestOptsSchema como template de inserção)
    - .planning/phases/14-findings-runtime-threat-integration/14-CONTEXT.md §"WebSocket Events (FIND-04)" — event schema Zod (linhas ~135-175)
    - .planning/phases/14-findings-runtime-threat-integration/14-CONTEXT.md §"Event payload limits" — max 20 findings per batch, max 200 chars em message
  </read_first>
  <behavior>
    - jobEventSchema aceita input `{ type: 'stage_progress', stage: 'bola', status: 'completed', findingsDiscovered: 3, durationMs: 12345, message: 'BOLA: 6 pares testados' }` → válido
    - jobEventSchema rejeita `{ type: 'stage_progress' }` sem required fields
    - jobEventSchema rejeita `message` > 200 chars
    - jobEventSchema variant findings_batch rejeita findings array com 21 elementos (max 20)
    - jobEventSchema variant journey_complete valida status ∈ {success, cancelled}
    - Type JobEvent = z.infer<typeof jobEventSchema> correctly representa 3 variantes discriminadas
  </behavior>
  <action>
Adicionar ao final de `shared/schema.ts` (após o último bloco de Phase 13 — localizar âncora `export interface ActiveTestResult` e inserir APÓS fechamento `}` dessa interface). Se schema do arquivo já tem >2000 linhas, manter organização por phase.

```typescript

// ============================================================================
// Phase 14: Findings Runtime & Threat Integration — FIND-04 WebSocket events
// ============================================================================

/**
 * jobEventSchema — Zod discriminated union for real-time job events pushed
 * over WebSocket (`/api/v1/jobs/:jobId/ws`). 3 variants:
 *
 *   - stage_progress: per-stage completion event with count + duration
 *   - findings_batch: batch of newly-discovered findings (max 20 per event)
 *   - journey_complete: final event for the whole journey (jobId)
 *
 * Carry-forward from 14-CONTEXT.md §"WebSocket Events (FIND-04)":
 *   - findings_batch.findings.max(20) — avoids massive JSON per event
 *   - stage_progress.message.max(200) — pt-BR human-readable summary
 *   - rate limit 10 events/sec/jobId — enforced by broadcaster (not schema)
 */
export const jobEventSchema = z.discriminatedUnion('type', [
  z.object({
    type: z.literal('stage_progress'),
    stage: z.string(),
    status: z.enum(['started', 'completed', 'failed']),
    findingsDiscovered: z.number().int().min(0),
    durationMs: z.number().int().min(0),
    message: z.string().max(200),
  }),
  z.object({
    type: z.literal('findings_batch'),
    findings: z.array(z.object({
      id: z.string(),
      owaspCategory: z.string(),
      severity: z.string(),
      endpointPath: z.string(),
      title: z.string(),
    })).max(20),
    batchNumber: z.number().int().min(1),
    totalNewInBatch: z.number().int().min(0),
  }),
  z.object({
    type: z.literal('journey_complete'),
    jobId: z.string(),
    apiId: z.string(),
    totalFindings: z.number().int().min(0),
    totalThreatsPromoted: z.number().int().min(0),
    durationMs: z.number().int().min(0),
    status: z.enum(['success', 'cancelled']),
  }),
]);

export type JobEvent = z.infer<typeof jobEventSchema>;
```

**AVOID:**
- Não adicionar outros event types (deferred em 14-CONTEXT.md §"Deferred Ideas" — per-severity channels, webhook, etc).
- Não adicionar constants de rate limit aqui (são internal detail do broadcaster).
- Não renomear `findingsDiscovered` → `findingsCount` etc — 14-CONTEXT.md é source of truth do nome.
- Não mover schemas Phase 12/13 — aditivo no final do arquivo.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "schema\\.ts" | grep -i error | head -5; grep -qE "export const jobEventSchema" shared/schema.ts && grep -qE "export type JobEvent" shared/schema.ts && npx tsx -e "import { jobEventSchema } from './shared/schema'; const r = jobEventSchema.safeParse({ type: 'stage_progress', stage: 'bola', status: 'completed', findingsDiscovered: 3, durationMs: 100, message: 'ok' }); if (!r.success) process.exit(1); console.log('OK');"</automated>
  </verify>
  <done>`shared/schema.ts` exporta `jobEventSchema` (Zod discriminated union 3 variantes) + `JobEvent` type; TypeScript compila limpo; smoke parse do exemplo stage_progress returns success; Zero breaking change em schemas existentes (aditivo).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test scaffold — server/__tests__/jobEventBroadcaster.test.ts com 6 it.todo stubs</name>
  <files>server/__tests__/jobEventBroadcaster.test.ts</files>
  <read_first>
    - .planning/phases/14-findings-runtime-threat-integration/14-CONTEXT.md §"WebSocket Events (FIND-04)" (toda a seção)
    - shared/schema.ts §jobEventSchema (criado em Task 1)
    - server/__tests__/sanitization.test.ts (plan 14-01) — template file estrutura + padrão it.todo
  </read_first>
  <behavior>
    Stubs (it.todo):
    - Test 1: 'jobEventSchema valida stage_progress + findings_batch + journey_complete variantes corretamente via safeParse'
    - Test 2: 'subscribe/unsubscribe — adicionar ws a jobId gera subscriber count=1; unsubscribe volta a 0; resubscribe de ws diferente gera count=2'
    - Test 3: 'emit envia JSON.stringify(event) via ws.send para todos subscribers do jobId (broadcast); outros jobIds NÃO recebem'
    - Test 4: 'emit rate limit 10 events/sec per jobId: 11º evento no mesmo segundo é droppado + log warning; contador reseta ao segundo seguinte'
    - Test 5: 'ws.send failure (simulated throw) — broadcaster auto-unsubscribes o client com erro; outros subscribers continuam recebendo'
    - Test 6: 'emit com payload inválido (ex: findings array de 21 items) — Zod parse falha; evento é droppado com warning; ws.send NÃO é chamado'
  </behavior>
  <action>
Criar `server/__tests__/jobEventBroadcaster.test.ts` com exatamente 6 `it.todo` stubs:

```typescript
import { describe, it, expect } from 'vitest';
import { jobEventBroadcaster } from '../services/jobEventBroadcaster';
import { jobEventSchema } from '../../shared/schema';

describe('jobEventBroadcaster (FIND-04)', () => {
  // Schema validation
  it.todo('jobEventSchema valida stage_progress + findings_batch + journey_complete variantes corretamente via safeParse');

  // Subscribe lifecycle
  it.todo('subscribe/unsubscribe — adicionar ws a jobId gera subscriber count=1; unsubscribe volta a 0; resubscribe de ws diferente gera count=2');

  // Broadcasting
  it.todo('emit envia JSON.stringify(event) via ws.send para todos subscribers do jobId (broadcast); outros jobIds NÃO recebem');

  // Rate limiting
  it.todo('emit rate limit 10 events/sec per jobId: 11º evento no mesmo segundo é droppado + log warning; contador reseta ao segundo seguinte');

  // Resilience
  it.todo('ws.send failure (simulated throw) — broadcaster auto-unsubscribes o client com erro; outros subscribers continuam recebendo');

  // Payload validation
  it.todo('emit com payload inválido (ex: findings array de 21 items) — Zod parse falha; evento é droppado com warning; ws.send NÃO é chamado');
});
```

**AVOID:** Não implementar bodies (todos it.todo). Não testar Phase 15 journeyExecutor integration aqui (escopo 14-04 ou futuro).
  </action>
  <verify>
    <automated>npx vitest list server/__tests__/jobEventBroadcaster.test.ts 2>&1 | grep -c todo | grep -q "^6$" && echo OK || (echo "FAIL: esperado 6 it.todo"; exit 1)</automated>
  </verify>
  <done>Arquivo existe; vitest lista exatamente 6 it.todo; import de `../services/jobEventBroadcaster` unresolved até Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implementação server/services/jobEventBroadcaster.ts + wiring em server/routes/jobs.ts</name>
  <files>server/services/jobEventBroadcaster.ts, server/routes/jobs.ts</files>
  <read_first>
    - .planning/phases/14-findings-runtime-threat-integration/14-CONTEXT.md §"WebSocket Events (FIND-04)" (completo — granularity, subscriber model, rate limiting, resilience)
    - .planning/phases/14-findings-runtime-threat-integration/14-CONTEXT.md §"Claude's Discretion" — broadcaster novo service vs extensão jobQueue (decisão: novo service)
    - server/routes/jobs.ts (arquivo completo — localizar WebSocket handler existente ou pattern de route para adicionar upgrade)
    - server/services/jobQueue.ts (padrão de singleton service export)
    - shared/schema.ts §jobEventSchema (Task 1)
    - package.json — verificar se `ws` library está instalada (import WebSocket from 'ws')
  </read_first>
  <behavior>
    - jobEventBroadcaster é singleton (default export instance)
    - Internal state: Map<jobId, Set<WebSocket>> subscribers; Map<jobId, { count, windowStart }> rate tracking
    - subscribe(jobId, ws): adiciona ws ao Set; idempotente se ws já subscribed
    - unsubscribe(jobId, ws): remove ws do Set; cleanup Set vazio
    - emit(jobId, event): 1) valida via jobEventSchema.safeParse; 2) aplica rate limit check; 3) itera subscribers e ws.send; 4) ws.send error → auto-unsubscribe
    - Rate limit: windowStart = now; se windowStart > 1000ms ago, reset count=0; se count >= 10, drop; senão count++
    - Routes: server/routes/jobs.ts tem upgrade handler (se existir) ou adicionar nova rota; handler conecta ws ao broadcaster.subscribe + close → unsubscribe
  </behavior>
  <action>
**Passo A — Criar `server/services/jobEventBroadcaster.ts`:**

```typescript
import type { WebSocket } from 'ws';
import { jobEventSchema, type JobEvent } from '../../shared/schema';

// ============================================================================
// Constants
// ============================================================================

const RATE_LIMIT_EVENTS_PER_SEC = 10;   // per 14-CONTEXT.md §"Rate limiting WebSocket events"
const RATE_LIMIT_WINDOW_MS = 1000;

// ============================================================================
// JobEventBroadcaster
// ============================================================================

export class JobEventBroadcaster {
  private subscribers = new Map<string, Set<WebSocket>>();
  private rateTracker = new Map<string, { count: number; windowStart: number }>();

  subscribe(jobId: string, ws: WebSocket): void {
    let set = this.subscribers.get(jobId);
    if (!set) {
      set = new Set<WebSocket>();
      this.subscribers.set(jobId, set);
    }
    set.add(ws);
  }

  unsubscribe(jobId: string, ws: WebSocket): void {
    const set = this.subscribers.get(jobId);
    if (!set) return;
    set.delete(ws);
    if (set.size === 0) {
      this.subscribers.delete(jobId);
      this.rateTracker.delete(jobId);  // cleanup rate window too
    }
  }

  subscriberCount(jobId: string): number {
    return this.subscribers.get(jobId)?.size ?? 0;
  }

  emit(jobId: string, event: JobEvent): void {
    // Layer 1: Validate payload (Zod parse)
    const parsed = jobEventSchema.safeParse(event);
    if (!parsed.success) {
      // eslint-disable-next-line no-console
      console.warn('[jobEventBroadcaster] invalid event dropped', {
        jobId,
        errors: parsed.error.issues,
      });
      return;
    }

    // Layer 2: Rate limit (10 events/sec per jobId)
    if (!this.checkRateLimit(jobId)) {
      // eslint-disable-next-line no-console
      console.warn('[jobEventBroadcaster] rate limit exceeded, event dropped', {
        jobId,
        limit: RATE_LIMIT_EVENTS_PER_SEC,
      });
      return;
    }

    // Layer 3: Broadcast
    const subscribers = this.subscribers.get(jobId);
    if (!subscribers || subscribers.size === 0) return;

    const payload = JSON.stringify(parsed.data);
    const deadSockets: WebSocket[] = [];

    for (const ws of subscribers) {
      try {
        ws.send(payload);
      } catch (err) {
        // client disconnected or send error — schedule unsubscribe
        deadSockets.push(ws);
        // eslint-disable-next-line no-console
        console.warn('[jobEventBroadcaster] ws.send failed, auto-unsubscribing', {
          jobId,
          error: err instanceof Error ? err.message : String(err),
        });
      }
    }

    // Cleanup dead sockets outside the iteration
    for (const ws of deadSockets) {
      this.unsubscribe(jobId, ws);
    }
  }

  private checkRateLimit(jobId: string): boolean {
    const now = Date.now();
    let tracker = this.rateTracker.get(jobId);
    if (!tracker || now - tracker.windowStart >= RATE_LIMIT_WINDOW_MS) {
      tracker = { count: 0, windowStart: now };
      this.rateTracker.set(jobId, tracker);
    }
    if (tracker.count >= RATE_LIMIT_EVENTS_PER_SEC) {
      return false;
    }
    tracker.count++;
    return true;
  }
}

// Singleton export — all routes + services share this instance
export const jobEventBroadcaster = new JobEventBroadcaster();
```

**Passo B — Wire em `server/routes/jobs.ts`:**

Ler arquivo atual primeiro. Cenário A (rota WebSocket `/api/v1/jobs/:jobId/ws` já existe): localizar o handler `wss.on('connection', ...)` ou equivalente e adicionar `jobEventBroadcaster.subscribe(jobId, ws)` + `ws.on('close', () => jobEventBroadcaster.unsubscribe(jobId, ws))`.

Cenário B (rota não existe ainda): adicionar nova rota minimalista ANTES do fim do arquivo (import + registration):

```typescript
// server/routes/jobs.ts — adicionar perto das rotas existentes:
import { jobEventBroadcaster } from '../services/jobEventBroadcaster';

// In the Express router setup where WebSocket upgrade is handled:
// (depends on whether project uses ws library directly, or express-ws, or socket.io)
//
// Minimal pattern if using `ws` WebSocket.Server attached to HTTP server:
//   wss.on('connection', (ws, req) => {
//     const url = new URL(req.url ?? '', `http://${req.headers.host}`);
//     const match = url.pathname.match(/\/api\/v1\/jobs\/([^/]+)\/ws$/);
//     if (!match) { ws.close(); return; }
//     const jobId = match[1];
//     // RBAC check (Phase 14 extends existing auth middleware — verify ownership)
//     jobEventBroadcaster.subscribe(jobId, ws);
//     ws.on('close', () => jobEventBroadcaster.unsubscribe(jobId, ws));
//     ws.on('error', () => jobEventBroadcaster.unsubscribe(jobId, ws));
//   });
```

Se `jobs.ts` não tem pattern claro de WebSocket (rota REST apenas), limitar Passo B a ADICIONAR o import `jobEventBroadcaster` no topo do arquivo como "anchor" para Wave 3 / Phase 15 completarem o upgrade handler. Deixar comentário TODO pointing para 14-04 ou Phase 15. Justificativa: 14-CONTEXT.md diz "(likely exists)" — não podemos afirmar sem inspeção; fail-forward permitido aqui.

**AVOID:**
- Não usar `socket.io` se projeto usa `ws` (e vice-versa) — detectar e usar o existente.
- Não adicionar autenticação robusta aqui (RBAC é Phase 15 SAFE-04 concern; reusar middleware existente se houver).
- Não expor `subscribers` Map publicamente (private — teste via `subscriberCount(jobId)` public method).
- Não bloquear journey execution em rate limit hit — é drop + warn, não exception.
- Não persistir eventos em DB (CONTEXT: "real-time signaling não data tunnel" — queries GET para data).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "jobEventBroadcaster\\.ts|jobs\\.ts" | grep -i error | head -5; grep -qE "export class JobEventBroadcaster" server/services/jobEventBroadcaster.ts && grep -qE "export const jobEventBroadcaster" server/services/jobEventBroadcaster.ts && grep -qE "jobEventBroadcaster" server/routes/jobs.ts && echo OK</automated>
  </verify>
  <done>`server/services/jobEventBroadcaster.ts` exporta classe `JobEventBroadcaster` + singleton `jobEventBroadcaster`; TypeScript compila limpo; `server/routes/jobs.ts` tem import do broadcaster (wiring completo se WebSocket route existe; import-only anchor se não); 6 it.todo em `jobEventBroadcaster.test.ts` agora têm imports resolvidos.</done>
</task>

</tasks>

<verification>
- `ls server/services/jobEventBroadcaster.ts server/__tests__/jobEventBroadcaster.test.ts` retorna ambos
- `grep -qE "export const jobEventSchema" shared/schema.ts` confirma schema adicionado
- `grep -qE "export const jobEventBroadcaster" server/services/jobEventBroadcaster.ts` confirma singleton
- `grep -q "jobEventBroadcaster" server/routes/jobs.ts` confirma wiring (ou import anchor)
- `npx tsc --noEmit` passa sem erros
- `npx vitest list server/__tests__/jobEventBroadcaster.test.ts` lista 6 it.todo
- Smoke tsx: `jobEventSchema.safeParse({ type: 'stage_progress', ... })` success
- Zero quebra em rotas REST existentes (aditivo)
</verification>

<success_criteria>
- FIND-04 foundation: jobEventSchema + jobEventBroadcaster shipped
- 6 it.todo stubs coletados
- TypeScript strict clean
- Rate limit 10/sec per jobId implementado
- Auto-unsubscribe on ws.send failure (client disconnect resilience)
- Zod validation antes de send (invalid events droppados com warning)
- WebSocket route wired OR import anchor placed para Wave 3 / Phase 15
- Zero change em jobQueue.ts ou em outros services existentes
- Ready para Wave 3 (14-04) emit findings_batch após upsertApiFindingByKey
</success_criteria>

<output>
After completion, create `.planning/phases/14-findings-runtime-threat-integration/14-03-SUMMARY.md` with:
- jobEventSchema final Zod shape (3 variants com campos exatos)
- JobEventBroadcaster class API (subscribe, unsubscribe, emit, subscriberCount)
- Rate limit config (RATE_LIMIT_EVENTS_PER_SEC=10, window=1000ms)
- WebSocket route status: A) fully wired OR B) import-only anchor (documentar qual)
- Invocation pattern Wave 3 (14-04): `jobEventBroadcaster.emit(jobId, { type: 'findings_batch', ... })`
- Known limitations (sem retry/replay de eventos perdidos; client deve poll GET /api/v1/jobs/:jobId se quiser reconciliar state)
</output>
