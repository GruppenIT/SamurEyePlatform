---
phase: 14-findings-runtime-threat-integration
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - shared/sanitization.ts
  - server/__tests__/sanitization.test.ts
autonomous: true
requirements:
  - FIND-02
must_haves:
  truths:
    - "Arquivo `shared/sanitization.ts` existe e exporta `sanitizeApiFinding(evidence, options?): ApiFindingEvidence` como função pura (não muta input)"
    - "Constantes `PII_PATTERNS` (CPF/CNPJ/email/creditCard) e `REDACT_HEADERS` (lista case-insensitive de 9 auth headers) exportadas de `shared/sanitization.ts`"
    - "`sanitizeApiFinding` remove headers sensíveis (Authorization, X-API-Key, X-Auth-Token, Cookie, etc) de `evidence.request.headers` em modo case-insensitive"
    - "`sanitizeApiFinding` trunca `evidence.request.bodySnippet` e `evidence.response.bodySnippet` para ≤8KB com marcador `[... truncated ...]` quando excedido"
    - "`sanitizeApiFinding` aplica PII masking (CPF → `***.***.***-**`, CNPJ, email preserving domain, credit card preserving last-4) em bodySnippets + extractedValues"
    - "`sanitizeApiFinding` é fail-open: em caso de erro de regex/parse, retorna input original + não lança exception"
    - "Arquivo `server/__tests__/sanitization.test.ts` contém exatamente 10 stubs `it.todo` cobrindo as 10 behaviors abaixo, e `npx vitest list server/__tests__/sanitization.test.ts` executa sem erros"
  artifacts:
    - path: "shared/sanitization.ts"
      provides: "sanitizeApiFinding() pure function + PII_PATTERNS + REDACT_HEADERS consts for Waves 1-3"
      exports: ["sanitizeApiFinding", "PII_PATTERNS", "REDACT_HEADERS", "BODY_TRUNCATE_LIMIT"]
    - path: "server/__tests__/sanitization.test.ts"
      provides: "10 it.todo stubs — Nyquist scaffold for Phase 14 FIND-02 verification"
      contains: "it.todo"
  key_links:
    - from: "shared/sanitization.ts sanitizeApiFinding"
      to: "Wave 3 (14-04) server/routes/apis.ts POST /test/passive + POST /test/active handlers"
      via: "import sanitizeApiFinding from shared/sanitization"
      pattern: "sanitizeApiFinding\\(.*evidence"
    - from: "server/__tests__/sanitization.test.ts"
      to: "vitest.config.ts"
      via: "glob include server/**/*.test.ts"
      pattern: "sanitization"
---

<objective>
Wave 0 — Criar a foundation de sanitização para FIND-02. Entrega a função pura `sanitizeApiFinding(evidence)` em `shared/sanitization.ts` com 3 comportamentos: (1) redação de auth headers, (2) truncagem de body ≤8KB, (3) masking de PII brasileira (CPF/CNPJ/email/cartão). Cria também o scaffold de testes `server/__tests__/sanitization.test.ts` com 10 `it.todo` stubs que Waves 1-3 + CI podem rodar.

Purpose: **Última linha de defesa antes da persistência**. Scanners Phase 12/13 já mascaram-at-source, mas `sanitizeApiFinding` é o guard final invocado pelas rotas Wave 3 ANTES de `upsertApiFindingByKey`. Função pura → testável isoladamente, sem mocks de DB nem HTTP. Fail-open design: erro de regex nunca bloqueia journey (findings ainda persistem, apenas com sanitização best-effort).

Output: 1 arquivo novo `shared/sanitization.ts` (≈150-200 linhas) + 1 arquivo novo de teste com 10 `it.todo`. Zero mudança em schema ou em rotas. Contrato público consumido por Wave 3 plan 14-04.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/14-findings-runtime-threat-integration/14-CONTEXT.md

@shared/schema.ts
@server/storage/apiFindings.ts

<interfaces>
<!-- From shared/schema.ts Phase 9 — ApiFindingEvidence shape that sanitizeApiFinding receives. -->
<!-- Executor MUST match this exact shape; do not introduce alternative evidence types. -->

```typescript
// shared/schema.ts (Phase 9) — apiFindingEvidenceSchema Zod + type:
export const apiFindingEvidenceSchema = z.object({
  request: z.object({
    method: z.string().optional(),
    url: z.string().optional(),
    headers: z.record(z.string()).optional(),
    bodySnippet: z.string().optional(),  // ≤8KB after Phase 14 sanitization
  }).optional(),
  response: z.object({
    status: z.number().optional(),
    headers: z.record(z.string()).optional(),
    bodySnippet: z.string().optional(),  // ≤8KB after Phase 14 sanitization
  }).optional(),
  extractedValues: z.record(z.unknown()).optional(),  // keys may contain PII
  matchedPattern: z.string().optional(),
  notes: z.string().optional(),
}).passthrough();

export type ApiFindingEvidence = z.infer<typeof apiFindingEvidenceSchema>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test scaffold — server/__tests__/sanitization.test.ts com 10 it.todo stubs</name>
  <files>server/__tests__/sanitization.test.ts</files>
  <read_first>
    - .planning/phases/14-findings-runtime-threat-integration/14-CONTEXT.md §"Sanitização (FIND-02)" (decisions block) — enumera exatamente as 4 camadas de PII, REDACT_HEADERS list, truncagem 8KB
    - .planning/phases/12-security-testing-passive/12-01-PLAN.md (Task 2 stub file como template estrutural para it.todo)
    - server/__tests__/apiSchema.test.ts (primeiras 40 linhas — padrão de imports + describe/it usado no projeto)
    - shared/schema.ts §apiFindingEvidenceSchema (Zod + type ApiFindingEvidence)
  </read_first>
  <behavior>
    Stubs (it.todo, sem implementação — Wave 3 ou futuras pode promover para it() real):
    - Test 1: 'remove headers sensíveis case-insensitive de evidence.request.headers (Authorization, X-API-Key, X-Auth-Token, Cookie, X-CSRF-Token, Token, API-Key, ApiKey, X-Access-Token)'
    - Test 2: 'preserva headers não-sensíveis de evidence.request.headers (Content-Type, User-Agent, Accept)'
    - Test 3: 'trunca evidence.response.bodySnippet para 8192 bytes + append marker [... truncated ...] quando input > 8KB'
    - Test 4: 'mantém evidence.response.bodySnippet unchanged quando input ≤ 8KB'
    - Test 5: 'mascara CPF formatado (###.###.###-##) para ***.***.***-** e CPF sem pontuação (11 dígitos) para 11 asteriscos'
    - Test 6: 'mascara CNPJ formatado (##.###.###/####-##) para **.***.***/****-** em bodySnippet + extractedValues'
    - Test 7: 'mascara email (user@domain.com → ***@domain.com) preservando o domínio para context investigativo'
    - Test 8: 'mascara credit card (PAN) preservando last-4 dígitos (e.g., 4532-1234-5678-9090 → ****-****-****-9090)'
    - Test 9: 'é função pura — não muta o input evidence (verifica via deep equal do objeto original antes/depois)'
    - Test 10: 'fail-open — em erro de regex (input malformado) retorna evidence original sem lançar exception (catch + log warning)'
  </behavior>
  <action>
Criar `server/__tests__/sanitization.test.ts` com exatamente 10 `it.todo` stubs agrupados em um único `describe('sanitizeApiFinding (FIND-02)')`. Use o padrão do projeto (import { describe, it, expect } from 'vitest'). Importe o tipo e função do caminho público `../../shared/sanitization` (relative import — não path alias).

Estrutura do arquivo:

```typescript
import { describe, it, expect } from 'vitest';
import { sanitizeApiFinding, PII_PATTERNS, REDACT_HEADERS, BODY_TRUNCATE_LIMIT } from '../../shared/sanitization';
import type { ApiFindingEvidence } from '../../shared/schema';

describe('sanitizeApiFinding (FIND-02)', () => {
  // Header redaction
  it.todo('remove headers sensíveis case-insensitive de evidence.request.headers (Authorization, X-API-Key, X-Auth-Token, Cookie, X-CSRF-Token, Token, API-Key, ApiKey, X-Access-Token)');
  it.todo('preserva headers não-sensíveis de evidence.request.headers (Content-Type, User-Agent, Accept)');

  // Body truncation
  it.todo('trunca evidence.response.bodySnippet para 8192 bytes + append marker [... truncated ...] quando input > 8KB');
  it.todo('mantém evidence.response.bodySnippet unchanged quando input ≤ 8KB');

  // PII masking — 4 patterns
  it.todo('mascara CPF formatado (###.###.###-##) para ***.***.***-** e CPF sem pontuação (11 dígitos) para 11 asteriscos');
  it.todo('mascara CNPJ formatado (##.###.###/####-##) para **.***.***/****-** em bodySnippet + extractedValues');
  it.todo('mascara email (user@domain.com → ***@domain.com) preservando o domínio para context investigativo');
  it.todo('mascara credit card (PAN) preservando last-4 dígitos (e.g., 4532-1234-5678-9090 → ****-****-****-9090)');

  // Purity + resilience
  it.todo('é função pura — não muta o input evidence (verifica via deep equal do objeto original antes/depois)');
  it.todo('fail-open — em erro de regex (input malformado) retorna evidence original sem lançar exception (catch + log warning)');
});
```

**AVOID:** Não implementar o corpo dos testes (todos it.todo). Não adicionar testes para `promoteHighCriticalFindings` (escopo de 14-02). Não testar WebSocket (14-03). Não importar DB ou fixtures HTTP — função é pura, não precisa mock.
  </action>
  <verify>
    <automated>npx vitest list server/__tests__/sanitization.test.ts 2>&1 | grep -c "todo" | grep -q "^10$" && echo OK || (echo "FAIL: esperado 10 it.todo"; exit 1)</automated>
  </verify>
  <done>Arquivo `server/__tests__/sanitization.test.ts` existe; vitest coleta exatamente 10 `it.todo`; zero testes executáveis (todos todo); imports usam `../../shared/sanitization` (que será criado em Task 2, então este teste ficará em estado "unresolved import" até Task 2 completar — aceitável para Wave 0).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementação shared/sanitization.ts — sanitizeApiFinding + consts PII_PATTERNS + REDACT_HEADERS</name>
  <files>shared/sanitization.ts</files>
  <read_first>
    - .planning/phases/14-findings-runtime-threat-integration/14-CONTEXT.md §"Sanitização (FIND-02)" — decisions block completo (linhas ~30-100 do CONTEXT.md): signatures, regex patterns, lista de headers exata, scope fields
    - shared/schema.ts §apiFindingEvidenceSchema + type ApiFindingEvidence (Phase 9 evidence contract — shape que função recebe)
    - server/lib/logger.ts (se existir) — padrão de logger usado por fail-open warnings (ou pino import direto)
    - server/__tests__/sanitization.test.ts (criado em Task 1) — behaviors que função deve satisfazer
  </read_first>
  <behavior>
    - Test 1-10 mesmos que Task 1 (função deve ser implementada de forma que passaria se stubs fossem promovidos para `it()` — mas nesta Wave 0 ficam como it.todo, implementação valida via compilação TypeScript + self-test manual do executor)
    - Função é pura: não usa Date.now, Math.random, ou I/O
    - Função retorna **nova instância** (shallow clone no top-level, deep clone nos campos modificados)
    - Regex compilados 1x no module init (performance; const fora da função)
    - Case-insensitive header match via `.toLowerCase()` comparison
  </behavior>
  <action>
Criar `shared/sanitization.ts` com as seguintes exports e implementação. Seguir exatamente o contrato documentado em 14-CONTEXT.md §"Sanitização (FIND-02)".

```typescript
import type { ApiFindingEvidence } from './schema';

// ============================================================================
// Constants (FIND-02)
// ============================================================================

/**
 * BODY_TRUNCATE_LIMIT — max bytes for evidence.request.bodySnippet and
 * evidence.response.bodySnippet. Carry-forward de Phase 9 ApiFindingEvidence
 * constraint (bodySnippet ≤ 8KB).
 */
export const BODY_TRUNCATE_LIMIT = 8192;

export const BODY_TRUNCATE_MARKER = '[... truncated ...]';

/**
 * REDACT_HEADERS — lista case-insensitive de headers de auth/token a serem
 * removidos de evidence.request.headers antes de persistência. Match é feito
 * via .toLowerCase() comparison (casos como "Authorization" e "authorization"
 * ambos removidos). Exato 9 entries conforme 14-CONTEXT.md decisions.
 */
export const REDACT_HEADERS = [
  'authorization',
  'x-api-key',
  'x-auth-token',
  'api-key',
  'apikey',
  'x-access-token',
  'cookie',
  'x-csrf-token',
  'token',
] as const;

/**
 * PII_PATTERNS — regex const compilados 1x no module init. Preserve context
 * onde aplicável (email preserva domain, credit card preserva last-4).
 *   - cpf: 11 dígitos formatado ###.###.###-## ou crú 11 dígitos
 *   - cnpj: 14 dígitos formatado ##.###.###/####-##
 *   - email: user@domain → ***@domain (preserva após @)
 *   - creditCard: PAN 16 dígitos → preserve last 4
 */
export const PII_PATTERNS = {
  cpfFormatted: /\b\d{3}\.\d{3}\.\d{3}-\d{2}\b/g,
  cpfPlain: /\b\d{11}\b/g,
  cnpj: /\b\d{2}\.\d{3}\.\d{3}\/\d{4}-\d{2}\b/g,
  email: /([A-Za-z0-9._%+-]+)@([A-Za-z0-9.-]+\.[A-Za-z]{2,})/g,
  creditCardDashed: /\b(\d{4})[- ](\d{4})[- ](\d{4})[- ](\d{4})\b/g,
  creditCardPlain: /\b\d{12}(\d{4})\b/g,
} as const;

// ============================================================================
// sanitizeApiFinding (FIND-02)
// ============================================================================

export interface SanitizeOptions {
  piiMaskPatterns?: RegExp[];  // override default patterns (optional, rarely used)
}

/**
 * sanitizeApiFinding — 3-layer protection before persistence:
 *   1. Redact auth headers (REDACT_HEADERS case-insensitive)
 *   2. Truncate bodySnippets to BODY_TRUNCATE_LIMIT (8KB) + append marker
 *   3. Mask PII (CPF, CNPJ, email, credit card) in bodySnippets + extractedValues
 *
 * Pure function: returns new object, does not mutate input.
 * Fail-open: on regex/parse error, logs warning (console.warn) and returns
 *   the original evidence unmodified. Findings MUST persist even if
 *   sanitization fails; blocking the journey is worse than best-effort sanitize.
 *
 * Scope fields processed:
 *   - evidence.request.headers (redaction only; no PII mask)
 *   - evidence.request.bodySnippet (truncate + PII mask)
 *   - evidence.response.bodySnippet (truncate + PII mask)
 *   - evidence.extractedValues.* (PII mask on string values; keys preserved)
 */
export function sanitizeApiFinding(
  evidence: ApiFindingEvidence,
  options: SanitizeOptions = {},
): ApiFindingEvidence {
  try {
    const sanitized: ApiFindingEvidence = { ...evidence };

    // Layer 1: Header redaction (request.headers)
    if (evidence.request?.headers) {
      sanitized.request = {
        ...evidence.request,
        headers: redactAuthHeaders(evidence.request.headers),
      };
    }

    // Layer 2+3: Truncate + mask bodySnippets
    if (evidence.request?.bodySnippet !== undefined) {
      sanitized.request = {
        ...sanitized.request,
        bodySnippet: truncateAndMask(evidence.request.bodySnippet),
      };
    }

    if (evidence.response?.bodySnippet !== undefined) {
      sanitized.response = {
        ...evidence.response,
        bodySnippet: truncateAndMask(evidence.response.bodySnippet),
      };
    }

    // Layer 3 only: extractedValues (no truncation — usually small keyed values)
    if (evidence.extractedValues) {
      sanitized.extractedValues = maskExtractedValues(evidence.extractedValues);
    }

    return sanitized;
  } catch (err) {
    // Fail-open: log + return original (findings must not be lost)
    // eslint-disable-next-line no-console
    console.warn('[sanitizeApiFinding] sanitization failed, using original evidence (fail-open)', {
      error: err instanceof Error ? err.message : String(err),
    });
    return evidence;
  }
}

// ============================================================================
// Internal helpers (not exported)
// ============================================================================

function redactAuthHeaders(headers: Record<string, string>): Record<string, string> {
  const result: Record<string, string> = {};
  const redactSet = new Set<string>(REDACT_HEADERS.map((h) => h.toLowerCase()));
  for (const [key, value] of Object.entries(headers)) {
    if (!redactSet.has(key.toLowerCase())) {
      result[key] = value;
    }
    // sensitive headers simply omitted (not replaced with '***' — avoids accidental leak in logs)
  }
  return result;
}

function truncateAndMask(input: string): string {
  let output = input;
  if (output.length > BODY_TRUNCATE_LIMIT) {
    output = output.slice(0, BODY_TRUNCATE_LIMIT) + BODY_TRUNCATE_MARKER;
  }
  return maskPiiInString(output);
}

function maskPiiInString(input: string): string {
  let output = input;
  // Order matters: formatted patterns first so plain \d{11} doesn't swallow CPF.
  output = output.replace(PII_PATTERNS.cpfFormatted, '***.***.***-**');
  output = output.replace(PII_PATTERNS.cnpj, '**.***.***/****-**');
  output = output.replace(PII_PATTERNS.creditCardDashed, '****-****-****-$4');
  output = output.replace(PII_PATTERNS.creditCardPlain, '************$1');
  output = output.replace(PII_PATTERNS.email, '***@$2');
  output = output.replace(PII_PATTERNS.cpfPlain, '***********');
  return output;
}

function maskExtractedValues(values: Record<string, unknown>): Record<string, unknown> {
  const result: Record<string, unknown> = {};
  for (const [key, value] of Object.entries(values)) {
    if (typeof value === 'string') {
      result[key] = maskPiiInString(value);
    } else {
      // leave non-string values (numbers, booleans, nested objects) as-is;
      // deep traversal deferred (14-CONTEXT.md explicit — extractedValues is shallow by convention)
      result[key] = value;
    }
  }
  return result;
}
```

**AVOID:**
- Não importar `pino` ou project logger — `console.warn` em fail-open path é aceitável (função é shared/, agnóstica de runtime).
- Não fazer deep clone recursivo em `extractedValues` values quando não são strings (shallow intencional — CONTEXT.md §decisions explicit).
- Não registrar função como Express middleware aqui (Wave 3 invoca diretamente em route handler).
- Não chamar `upsertApiFindingByKey` ou qualquer storage (função é pura + agnóstica).
- Não adicionar `async` — função é síncrona; fail-open via try/catch, não via rejected Promise.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "shared/sanitization\\.ts|sanitization\\.test\\.ts" | grep -i error | head -5; npx vitest list server/__tests__/sanitization.test.ts 2>&1 | grep -c todo | grep -q "^10$" && echo OK</automated>
  </verify>
  <done>`shared/sanitization.ts` exporta `sanitizeApiFinding`, `PII_PATTERNS`, `REDACT_HEADERS`, `BODY_TRUNCATE_LIMIT`, `BODY_TRUNCATE_MARKER`; TypeScript compila limpo (zero erros de tipo); o import `from '../../shared/sanitization'` em `sanitization.test.ts` resolve (unresolved import antes do Task 2 estava esperado); vitest continua listando 10 it.todo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Manual dry-run — invocar sanitizeApiFinding com evidência real (smoke check)</name>
  <files>shared/sanitization.ts</files>
  <read_first>
    - shared/sanitization.ts (criado em Task 2)
    - server/__tests__/fixtures/api-passive/ (qualquer fixture JSON existente com evidence → usar como exemplo de shape)
    - .planning/phases/14-findings-runtime-threat-integration/14-CONTEXT.md §"Sanitização (FIND-02)" — validar que implementation cobre todos cases listados
  </read_first>
  <behavior>
    - Dry-run manual: executar um node/tsx one-liner que chama `sanitizeApiFinding` com uma evidência sintética contendo 1 de cada: Authorization header, body com CPF + email + CC, extractedValues com CNPJ
    - Output observável confirma: header removido, bodySnippet com PII mascarada, extractedValues com CNPJ mascarado, input não mutado (imutabilidade verificada via JSON.stringify comparison)
  </behavior>
  <action>
Rodar dry-run direto via `tsx` (instalado no projeto) para validar que a implementação funciona end-to-end com evidência real. Comando a executar:

```bash
npx tsx -e "
import { sanitizeApiFinding } from './shared/sanitization';

const input = {
  request: {
    method: 'POST',
    url: 'https://api.example.com/users',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': 'Bearer secret_token_12345',
      'X-API-Key': 'apikey-xyz',
      'User-Agent': 'test',
    },
    bodySnippet: 'email: foo@example.com, cpf: 123.456.789-00, cc: 4532-1234-5678-9090',
  },
  response: {
    status: 200,
    bodySnippet: 'x'.repeat(10000),  // 10KB → deve truncar para 8192 + marker
  },
  extractedValues: {
    company_cnpj: '12.345.678/0001-99',
    user_id: 42,  // non-string — preserved
  },
};

const frozen = JSON.stringify(input);
const out = sanitizeApiFinding(input);

// Checks:
console.log('1. Authorization removed?', !('Authorization' in (out.request?.headers ?? {})));
console.log('2. X-API-Key removed?', !('X-API-Key' in (out.request?.headers ?? {})));
console.log('3. User-Agent preserved?', out.request?.headers?.['User-Agent'] === 'test');
console.log('4. Request body CPF masked?', out.request?.bodySnippet?.includes('***.***.***-**'));
console.log('5. Request body email masked?', out.request?.bodySnippet?.includes('***@example.com'));
console.log('6. Request body CC masked?', out.request?.bodySnippet?.includes('****-****-****-9090'));
console.log('7. Response body truncated?', (out.response?.bodySnippet?.length ?? 0) <= 8192 + 20);
console.log('8. Response has truncate marker?', out.response?.bodySnippet?.includes('[... truncated ...]'));
console.log('9. extractedValues CNPJ masked?', out.extractedValues?.company_cnpj === '**.***.***/****-**');
console.log('10. extractedValues non-string preserved?', out.extractedValues?.user_id === 42);
console.log('11. Input NOT mutated?', JSON.stringify(input) === frozen);
"
```

Todos 11 checks devem imprimir `true`. Se qualquer `false`, voltar a Task 2 e corrigir a regex/helper correspondente.

**AVOID:**
- Não commitar o one-liner como arquivo permanente (é smoke test one-shot de dev).
- Não adicionar o dry-run como CI step (testes it.todo farão o trabalho em Wave 3 quando promovidos para `it()`).
  </action>
  <verify>
    <automated>npx tsx -e "import { sanitizeApiFinding } from './shared/sanitization'; const input = { request: { headers: { 'Authorization': 'Bearer x', 'Content-Type': 'app/json' }, bodySnippet: 'cpf: 123.456.789-00' }, response: { bodySnippet: 'x'.repeat(10000) }, extractedValues: { cnpj: '12.345.678/0001-99' } }; const out = sanitizeApiFinding(input); const ok = !('Authorization' in (out.request?.headers ?? {})) && out.request?.bodySnippet?.includes('***.***.***-**') && (out.response?.bodySnippet?.length ?? 0) <= 8220 && out.extractedValues?.cnpj === '**.***.***/****-**'; if (!ok) { console.error('SMOKE FAILED'); process.exit(1); } console.log('SMOKE OK');"</automated>
  </verify>
  <done>Smoke dry-run imprime `SMOKE OK`. Todos os 4 pilares (header redaction, body truncate, PII mask em body, PII mask em extractedValues) validados em input real. Função pronta para Wave 3 (14-04) invocar em route handlers.</done>
</task>

</tasks>

<verification>
- `ls shared/sanitization.ts server/__tests__/sanitization.test.ts` retorna ambos arquivos
- `npx tsc --noEmit` passa sem erros relacionados a sanitization
- `npx vitest list server/__tests__/sanitization.test.ts` lista exatamente 10 it.todo
- Smoke tsx command imprime `SMOKE OK`
- Função `sanitizeApiFinding` export é acessível por `import { sanitizeApiFinding } from '../../shared/sanitization'` (relative) ou alias configurado
- Zero change em `shared/schema.ts` (aditivo em arquivo separado)
- Zero change em rotas (Wave 3 integra)
</verification>

<success_criteria>
- FIND-02 foundation: ✅ sanitizeApiFinding pure function shipped
- 10 it.todo stubs coletados por vitest
- TypeScript strict mode clean (zero type errors)
- Smoke dry-run confirma header redaction + body truncate + PII mask (CPF/CNPJ/email/CC) + imutabilidade
- Fail-open path testado manualmente (passar evidência malformada → retorna original sem throw)
- Contratos públicos documentados via JSDoc (executor Wave 3 pode usar sem ler implementation)
</success_criteria>

<output>
After completion, create `.planning/phases/14-findings-runtime-threat-integration/14-01-SUMMARY.md` with:
- Function signature + options interface
- 4 PII pattern regex final forms (post-implementation — podem divergir ligeiramente do CONTEXT.md)
- REDACT_HEADERS final list (9 headers)
- BODY_TRUNCATE_LIMIT = 8192, marker = `[... truncated ...]`
- Invocation pattern for Wave 3 (route handler snippet)
- Known limitations (shallow extractedValues, no recursive object traversal, Brazil-specific PII only)
</output>
