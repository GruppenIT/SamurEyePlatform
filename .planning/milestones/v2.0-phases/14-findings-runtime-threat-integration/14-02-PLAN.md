---
phase: 14-findings-runtime-threat-integration
plan: 02
type: execute
wave: 1
depends_on:
  - "14-01"
files_modified:
  - server/services/threatPromotion.ts
  - server/storage/apiFindings.ts
  - server/__tests__/threatPromotion.test.ts
autonomous: true
requirements:
  - FIND-03
must_haves:
  truths:
    - "Arquivo `server/services/threatPromotion.ts` existe e exporta `promoteHighCriticalFindings(apiId, newFindingIds)` async + helper `findDuplicateThreat(criteria)`"
    - "`promoteHighCriticalFindings` filtra findings por severity ∈ {high, critical} AND status=open AND promotedThreatId IS NULL antes de promover"
    - "`findDuplicateThreat` aplica estratégia dupla: (1) exact match por parentAssetId + source='api_security' + threatTitle LIKE %owaspCategory%; (2) temporal fallback — threat mais recente nos últimos 60 min"
    - "Promoção criando nova threat usa `db.transaction()` — threat insert + apiFindings.promotedThreatId update são atômicos (rollback em falha)"
    - "Quando dup detectado, finding é linkado via `apiFindings.promotedThreatId = existing.id` (sem criar nova threat)"
    - "Função retorna `{ promoted: number, linked: number, skipped: number }` — skipped quando finding não qualifica (baixa severity, já promovido, closed)"
    - "Fail-open: DB error em promotion NÃO lança — loga warning + marca finding com promotedThreatId=null (findings persistem, dashboard mostra menos threats)"
    - "Storage facade `server/storage/apiFindings.ts` ganha método `listFindingsForPromotion(findingIds): Promise<ApiFinding[]>` retornando findings + campo promotedThreatId atual"
    - "Arquivo `server/__tests__/threatPromotion.test.ts` contém exatamente 8 stubs it.todo + `npx vitest list` retorna 8"
  artifacts:
    - path: "server/services/threatPromotion.ts"
      provides: "promoteHighCriticalFindings + findDuplicateThreat para Wave 3 route invocation"
      exports: ["promoteHighCriticalFindings", "findDuplicateThreat", "PromotionResult"]
    - path: "server/storage/apiFindings.ts"
      provides: "listFindingsForPromotion + updateFindingPromotedThreatId storage methods"
      contains: "listFindingsForPromotion"
    - path: "server/__tests__/threatPromotion.test.ts"
      provides: "8 it.todo stubs cobrindo severity filter, exact dedupe, temporal dedupe, transaction atomicity, fail-open, link-on-dup, new-threat-shape, race condition"
      contains: "it.todo"
  key_links:
    - from: "server/services/threatPromotion.ts"
      to: "Wave 3 (14-04) server/routes/apis.ts POST /test/passive + /test/active"
      via: "async call after upsertApiFindingByKey (fire-and-forget Promise, no await blocks)"
      pattern: "promoteHighCriticalFindings\\(.*apiId"
    - from: "server/services/threatPromotion.ts"
      to: "shared/schema.ts threats table + apiFindings.promotedThreatId FK"
      via: "drizzle db.insert(threats) + db.update(apiFindings)"
      pattern: "promotedThreatId"
    - from: "server/storage/apiFindings.ts listFindingsForPromotion"
      to: "threatPromotion.ts"
      via: "import from storage facade (not direct drizzle query in service)"
      pattern: "listFindingsForPromotion"
---

<objective>
Wave 1 — Criar a promoção automática de findings high/critical para a tabela `threats` (FIND-03). Entrega 3 artifacts: (1) service novo `server/services/threatPromotion.ts` com lógica de promoção + dedupe dupla (exact + temporal 60min); (2) extensão storage facade `server/storage/apiFindings.ts` com métodos `listFindingsForPromotion` + `updateFindingPromotedThreatId`; (3) scaffold de testes `threatPromotion.test.ts` com 8 it.todo.

Purpose: **Surface findings críticos no dashboard executivo existente (v1.0)**. Findings existem em `api_findings` isolado; sem promoção, não aparecem em `/api/v1/threats/summary`. Dedupe dupla previne alarm fatigue quando mesmo teste roda 2x em 1h. Async + fail-open design: promotion é "advisory" — se falha, findings ainda persistem + queryable via filter. Phase 15 pode retry.

Output: 2 arquivos novos (service + test) + 1 extensão storage. Zero schema change (usa tabela `threats` + FK `apiFindings.promotedThreatId` já existentes). Contrato consumido por Wave 3 plan 14-04.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/14-findings-runtime-threat-integration/14-CONTEXT.md
@.planning/phases/14-findings-runtime-threat-integration/14-01-PLAN.md

@shared/schema.ts
@server/storage/apiFindings.ts
@server/storage/interface.ts

<interfaces>
<!-- threats table + apiFindings.promotedThreatId shape. Wave 1 implementor uses these directly. -->
<!-- NO codebase exploration needed — contracts below are authoritative. -->

```typescript
// shared/schema.ts — threats table (v1.0 pre-existing; Phase 14 inserts rows, zero schema change):
export const threats = pgTable('threats', {
  id: uuid('id').primaryKey().defaultRandom(),
  assetId: uuid('asset_id').references(() => assets.id),           // null for API findings
  parentAssetId: uuid('parent_asset_id').references(() => assets.id), // = apiId for Phase 14 promotions
  threatTitle: text('threat_title').notNull(),
  threatSeverity: varchar('threat_severity', { length: 16 }).notNull(),  // 'critical' | 'high' | ...
  source: varchar('source', { length: 64 }).notNull(),              // 'api_security' for Phase 14
  threatData: jsonb('threat_data'),                                 // { apiEndpointId, owaspCategory, findingIds[] }
  resolvedCount: integer('resolved_count').default(0).notNull(),
  verifiedCount: integer('verified_count').default(0).notNull(),
  lastSeen: timestamp('last_seen').defaultNow().notNull(),
  createdAt: timestamp('created_at').defaultNow().notNull(),
  updatedAt: timestamp('updated_at').defaultNow().notNull(),
});

// shared/schema.ts — apiFindings.promotedThreatId (Phase 9 added this FK column):
export const apiFindings = pgTable('api_findings', {
  // ... existing fields ...
  promotedThreatId: uuid('promoted_threat_id'),  // FK to threats.id, nullable, NOT cascade delete
  severity: varchar('severity', { length: 16 }).notNull(),  // 'critical' | 'high' | 'medium' | 'low' | 'info'
  status: varchar('status', { length: 16 }).notNull().default('open'),  // 'open' | 'closed' | 'false_positive'
  owaspCategory: varchar('owasp_category', { length: 64 }).notNull(),  // e.g., 'api1_bola_2023'
  endpointPath: text('endpoint_path'),
  apiEndpointId: uuid('api_endpoint_id'),
  // ... rest ...
});

// shared/schema.ts — owasp category enum (for threatTitle construction):
export const owaspApiCategoryEnum = pgEnum('owasp_api_category', [
  'api1_bola_2023', 'api2_broken_auth_2023', 'api3_bopla_2023',
  'api4_rate_limit_2023', 'api5_bfla_2023', 'api6_business_flow_2023',
  'api7_ssrf_2023', 'api8_misconfiguration_2023', 'api9_inventory_2023',
  'api10_unsafe_consumption_2023',
]);
```

```typescript
// server/storage/apiFindings.ts (Phase 12 existing — will be extended):
// Already exported:
export async function upsertApiFindingByKey(input: {
  apiEndpointId: string;
  owaspCategory: string;
  title: string;
  data: Partial<InsertApiFinding>;
}): Promise<ApiFinding>;

export async function listApiFindings(filter: {
  apiId?: string;
  severity?: string[];
  status?: string[];
}): Promise<ApiFinding[]>;

// NEW (Phase 14 Task 2 adds):
// export async function listFindingsForPromotion(findingIds: string[]): Promise<ApiFinding[]>;
// export async function updateFindingPromotedThreatId(findingId: string, threatId: string | null, tx?: Transaction): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test scaffold — server/__tests__/threatPromotion.test.ts com 8 it.todo stubs</name>
  <files>server/__tests__/threatPromotion.test.ts</files>
  <read_first>
    - .planning/phases/14-findings-runtime-threat-integration/14-CONTEXT.md §"Promoção (FIND-03) — Findings → Threats table" (decisions block)
    - .planning/phases/14-findings-runtime-threat-integration/14-CONTEXT.md §"Threat Engine Integration — Atomicity & Safety" (linhas ~180-210)
    - server/__tests__/apiStorage.test.ts primeiras 60 linhas (padrão test + mock de db)
    - server/__tests__/sanitization.test.ts (plan 14-01 Task 1 — padrão de file estrutural)
  </read_first>
  <behavior>
    Stubs (it.todo):
    - Test 1: 'filtra findings: apenas severity ∈ {high, critical} AND status=open AND promotedThreatId IS NULL qualificam para promoção'
    - Test 2: 'skipped counter incrementa para findings com severity=medium/low/info (não qualificam)'
    - Test 3: 'findDuplicateThreat — exact match: threat existente com source=api_security + parentAssetId=apiId + threatTitle contém owaspCategory → retorna o threat existente'
    - Test 4: 'findDuplicateThreat — temporal fallback: sem exact match mas threat com source=api_security criado < 60min atrás → retorna o mais recente'
    - Test 5: 'findDuplicateThreat — retorna null quando não há exact nem temporal match (caminho "create new threat")'
    - Test 6: 'promotion criando threat nova executa em db.transaction(): rollback se apiFindings.update falha depois de threats.insert → nenhum registro persiste (atomicity)'
    - Test 7: 'quando dup detectado via findDuplicateThreat, apiFindings.promotedThreatId é atualizado com existing.id + linked counter incrementa (sem INSERT em threats)'
    - Test 8: 'fail-open: db error (simulated via mock throw) NÃO lança — função retorna { promoted:0, linked:0, skipped:N, error: "..." }; finding permanece com promotedThreatId=null'
  </behavior>
  <action>
Criar `server/__tests__/threatPromotion.test.ts` com exatamente 8 `it.todo` stubs agrupados em `describe('promoteHighCriticalFindings + findDuplicateThreat (FIND-03)')`. Padrão do projeto: import vitest + service path relativo.

```typescript
import { describe, it, expect } from 'vitest';
import { promoteHighCriticalFindings, findDuplicateThreat } from '../services/threatPromotion';

describe('promoteHighCriticalFindings + findDuplicateThreat (FIND-03)', () => {
  // Severity filter
  it.todo('filtra findings: apenas severity ∈ {high, critical} AND status=open AND promotedThreatId IS NULL qualificam para promoção');
  it.todo('skipped counter incrementa para findings com severity=medium/low/info (não qualificam)');

  // Dedupe strategies
  it.todo('findDuplicateThreat — exact match: threat existente com source=api_security + parentAssetId=apiId + threatTitle contém owaspCategory → retorna o threat existente');
  it.todo('findDuplicateThreat — temporal fallback: sem exact match mas threat com source=api_security criado < 60min atrás → retorna o mais recente');
  it.todo('findDuplicateThreat — retorna null quando não há exact nem temporal match (caminho "create new threat")');

  // Atomicity + linking
  it.todo('promotion criando threat nova executa em db.transaction(): rollback se apiFindings.update falha depois de threats.insert → nenhum registro persiste (atomicity)');
  it.todo('quando dup detectado via findDuplicateThreat, apiFindings.promotedThreatId é atualizado com existing.id + linked counter incrementa (sem INSERT em threats)');

  // Resilience
  it.todo('fail-open: db error (simulated via mock throw) NÃO lança — função retorna { promoted:0, linked:0, skipped:N, error: "..." }; finding permanece com promotedThreatId=null');
});
```

**AVOID:** Não implementar o corpo dos testes (todos it.todo). Não importar DB real (scaffold; implementação/mocks ficam para CI futuro). Não testar sanitização (14-01 escopo).
  </action>
  <verify>
    <automated>npx vitest list server/__tests__/threatPromotion.test.ts 2>&1 | grep -c todo | grep -q "^8$" && echo OK || (echo "FAIL: esperado 8 it.todo"; exit 1)</automated>
  </verify>
  <done>Arquivo `server/__tests__/threatPromotion.test.ts` existe; vitest coleta exatamente 8 `it.todo`; import `../services/threatPromotion` ficará unresolved até Task 2 — aceitável para Wave 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extensão server/storage/apiFindings.ts — listFindingsForPromotion + updateFindingPromotedThreatId</name>
  <files>server/storage/apiFindings.ts</files>
  <read_first>
    - server/storage/apiFindings.ts (arquivo completo — padrões de export + drizzle query usados em Phase 12)
    - server/storage/interface.ts — interface IStorage (verificar se precisa adicionar novos métodos lá também)
    - shared/schema.ts §apiFindings table — campo promotedThreatId + FK shape
    - server/storage/apis.ts (exemplo de padrão update + transaction em outros storage facades)
  </read_first>
  <behavior>
    - listFindingsForPromotion(findingIds: string[]) retorna ApiFinding[] contendo todos os finding records com os IDs dados (inclusive severity, status, promotedThreatId, apiEndpointId, owaspCategory, endpointPath — campos necessários para promotion logic)
    - updateFindingPromotedThreatId(findingId, threatId, tx?) atualiza apenas a coluna promotedThreatId; aceita transaction opcional para caller usar db.transaction()
    - Quando tx passado, usa tx ao invés de db global (driz padrão)
    - Erros de DB propagam (não fail-open no storage layer — fail-open é responsabilidade do service caller)
  </behavior>
  <action>
Adicionar ao final de `server/storage/apiFindings.ts` (após export `listApiFindings`). Localizar o padrão de imports drizzle existente (e.g., `import { db } from '../db'`, `import { apiFindings } from '../../shared/schema'`, `import { eq, inArray } from 'drizzle-orm'`) — reusar identicamente.

```typescript
// ============================================================================
// FIND-03: Promotion support — Phase 14
// ============================================================================

/**
 * listFindingsForPromotion — batch fetch findings by ID for the promotion
 * service. Returns records with ALL fields needed to decide promotion eligibility
 * (severity, status, promotedThreatId) and to construct the threat record
 * (apiEndpointId, owaspCategory, endpointPath, title).
 *
 * Throws on DB error (service layer handles fail-open; storage is transparent).
 */
export async function listFindingsForPromotion(findingIds: string[]): Promise<ApiFinding[]> {
  if (findingIds.length === 0) {
    return [];
  }
  const rows = await db
    .select()
    .from(apiFindings)
    .where(inArray(apiFindings.id, findingIds));
  return rows;
}

/**
 * updateFindingPromotedThreatId — update only the promotedThreatId column on
 * api_findings. Accepts an optional drizzle Transaction so the caller can
 * atomically compose: BEGIN → insert threats → update api_findings → COMMIT.
 *
 * Pass threatId=null to clear the link (e.g., in retry/compensation flows).
 */
export async function updateFindingPromotedThreatId(
  findingId: string,
  threatId: string | null,
  tx?: typeof db,
): Promise<void> {
  const runner = tx ?? db;
  await runner
    .update(apiFindings)
    .set({ promotedThreatId: threatId, updatedAt: new Date() })
    .where(eq(apiFindings.id, findingId));
}
```

Se `server/storage/interface.ts` tem IStorage interface listando métodos por storage facade, adicionar assinaturas:

```typescript
// server/storage/interface.ts — IStorage
listFindingsForPromotion(findingIds: string[]): Promise<ApiFinding[]>;
updateFindingPromotedThreatId(findingId: string, threatId: string | null): Promise<void>;
// (tx parameter é internal detail — interface expõe apenas contrato público)
```

**AVOID:**
- Não inventar índice novo em apiFindings.promotedThreatId (se não existe ainda, aceitável — volumes baixos).
- Não criar trigger ou procedure DB — pure TS-side.
- Não fazer deep-join com threats table aqui (service Task 3 faz explicit `findDuplicateThreat`).
- Não mover `upsertApiFindingByKey` — aditivo em mesmo arquivo.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep "server/storage/apiFindings\\.ts" | grep -i error | head -5; grep -q "listFindingsForPromotion" server/storage/apiFindings.ts && grep -q "updateFindingPromotedThreatId" server/storage/apiFindings.ts && echo OK</automated>
  </verify>
  <done>`server/storage/apiFindings.ts` exporta 2 novos métodos `listFindingsForPromotion` e `updateFindingPromotedThreatId`; TypeScript strict compila; se IStorage interface existe, assinaturas adicionadas em `interface.ts`; nenhum teste Phase 12 quebra (aditivo).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implementação server/services/threatPromotion.ts — promoteHighCriticalFindings + findDuplicateThreat</name>
  <files>server/services/threatPromotion.ts</files>
  <read_first>
    - .planning/phases/14-findings-runtime-threat-integration/14-CONTEXT.md §"Promoção (FIND-03) — Findings → Threats table" (completo, decisions block)
    - .planning/phases/14-findings-runtime-threat-integration/14-CONTEXT.md §"Threat Engine Integration — Atomicity & Safety"
    - .planning/phases/14-findings-runtime-threat-integration/14-CONTEXT.md §"Dedupe transaction" (pseudo-code fornecido linhas ~190-210)
    - shared/schema.ts §threats table + apiFindings (campos usados)
    - server/storage/apiFindings.ts (Task 2 — métodos `listFindingsForPromotion` + `updateFindingPromotedThreatId`)
    - shared/owaspApiCategories.ts (labels pt-BR que aparecem em threatTitle)
    - server/services/scoringEngine.ts (NÃO chamar — Wave 1 só cria threat records, scoring recalc automático)
    - server/__tests__/threatPromotion.test.ts (Task 1 — behaviors esperados)
  </read_first>
  <behavior>
    - promoteHighCriticalFindings(apiId, newFindingIds) → Promise<PromotionResult>
    - Fetch findings via listFindingsForPromotion → filtra severity ∈ {high, critical} AND status=open AND promotedThreatId IS NULL
    - Para cada finding qualificado: findDuplicateThreat(apiId, owaspCategory) → se match, link; se null, insert new threat em db.transaction
    - Transaction: insert threats + update apiFindings.promotedThreatId juntos; rollback atomico em falha
    - threatTitle formato: `{OwaspLabelPtBR}: {endpointPath || 'endpoint desconhecido'}`
    - threatData JSONB: `{ apiEndpointId, owaspCategory, findingIds: [id] }` (single finding inicialmente; dup link adiciona ao array via append futuro — simples por ora)
    - source: 'api_security' (string literal — não novo enum entry)
    - parentAssetId: apiId (hierarchy reuse)
    - Fail-open: try/catch toda a operação; em erro loga + retorna result com error message
  </behavior>
  <action>
Criar `server/services/threatPromotion.ts` com a seguinte estrutura. Seguir EXATAMENTE as decisões em 14-CONTEXT.md §"Promoção (FIND-03)" — não inventar variações.

```typescript
import { and, eq, gte, like, desc } from 'drizzle-orm';
import { db } from '../db';
import { threats, apiFindings } from '../../shared/schema';
import type { ApiFinding } from '../../shared/schema';
import {
  listFindingsForPromotion,
  updateFindingPromotedThreatId,
} from '../storage/apiFindings';
import { OWASP_API_CATEGORIES_PT_BR } from '../../shared/owaspApiCategories';

// ============================================================================
// Public types
// ============================================================================

export interface PromotionResult {
  promoted: number;   // new threats created
  linked: number;     // findings linked to existing threats (dedupe hit)
  skipped: number;    // findings did not qualify (severity/status/already promoted)
  error?: string;     // fail-open: set when DB error short-circuits the batch
}

// ============================================================================
// findDuplicateThreat
// ============================================================================

const TEMPORAL_DUP_WINDOW_MS = 60 * 60 * 1000; // 60 minutes per 14-CONTEXT.md

/**
 * findDuplicateThreat — 2-strategy dedupe (first match wins):
 *   1. Exact: threat with source='api_security' AND parentAssetId=apiId
 *      AND threatTitle LIKE '%{owaspCategory}%'.
 *   2. Temporal fallback: threat with source='api_security' AND
 *      parentAssetId=apiId AND createdAt > now-60min (most recent).
 *
 * Returns matching threat row or null. Consumed by promoteHighCriticalFindings.
 */
export async function findDuplicateThreat(
  apiId: string,
  owaspCategory: string,
): Promise<typeof threats.$inferSelect | null> {
  // Strategy 1: exact match (parentAssetId + source + title contains owaspCategory key)
  const exact = await db
    .select()
    .from(threats)
    .where(
      and(
        eq(threats.parentAssetId, apiId),
        eq(threats.source, 'api_security'),
        like(threats.threatTitle, `%${owaspCategory}%`),
      ),
    )
    .orderBy(desc(threats.createdAt))
    .limit(1);
  if (exact.length > 0) {
    return exact[0];
  }

  // Strategy 2: temporal fallback (last 60min, same apiId + source)
  const since = new Date(Date.now() - TEMPORAL_DUP_WINDOW_MS);
  const temporal = await db
    .select()
    .from(threats)
    .where(
      and(
        eq(threats.parentAssetId, apiId),
        eq(threats.source, 'api_security'),
        gte(threats.createdAt, since),
      ),
    )
    .orderBy(desc(threats.createdAt))
    .limit(1);
  return temporal[0] ?? null;
}

// ============================================================================
// promoteHighCriticalFindings
// ============================================================================

const PROMOTE_SEVERITIES = new Set(['high', 'critical']);

export async function promoteHighCriticalFindings(
  apiId: string,
  newFindingIds: string[],
): Promise<PromotionResult> {
  const result: PromotionResult = { promoted: 0, linked: 0, skipped: 0 };

  try {
    if (newFindingIds.length === 0) {
      return result;
    }

    const findings = await listFindingsForPromotion(newFindingIds);

    for (const finding of findings) {
      // Qualification gate
      if (!PROMOTE_SEVERITIES.has(finding.severity)) {
        result.skipped++;
        continue;
      }
      if (finding.status !== 'open') {
        result.skipped++;
        continue;
      }
      if (finding.promotedThreatId) {
        result.skipped++;
        continue;
      }

      // Dedupe check (outside transaction — read-only)
      const dup = await findDuplicateThreat(apiId, finding.owaspCategory);

      if (dup) {
        // Link path: update finding only, no new threat insert
        await updateFindingPromotedThreatId(finding.id, dup.id);
        result.linked++;
        continue;
      }

      // New threat path: atomic insert + link
      await db.transaction(async (tx) => {
        const label = OWASP_API_CATEGORIES_PT_BR[finding.owaspCategory] ?? finding.owaspCategory;
        const title = `${label}: ${finding.endpointPath ?? 'endpoint desconhecido'}`;
        const [inserted] = await tx
          .insert(threats)
          .values({
            parentAssetId: apiId,
            threatTitle: title,
            threatSeverity: finding.severity,
            source: 'api_security',
            threatData: {
              apiEndpointId: finding.apiEndpointId,
              owaspCategory: finding.owaspCategory,
              findingIds: [finding.id],
            },
            resolvedCount: 0,
            verifiedCount: 0,
            lastSeen: new Date(),
          })
          .returning();

        await updateFindingPromotedThreatId(finding.id, inserted.id, tx);
        result.promoted++;
      });
    }

    return result;
  } catch (err) {
    // Fail-open: findings persistem; log warning + return result with error field
    // eslint-disable-next-line no-console
    console.warn('[promoteHighCriticalFindings] failed, findings persist without promotion', {
      apiId,
      error: err instanceof Error ? err.message : String(err),
    });
    result.error = err instanceof Error ? err.message : String(err);
    return result;
  }
}
```

**AVOID:**
- Não chamar `scoringEngine.recalculate()` — existing scoring formula aplica automaticamente quando threat record aparece (14-CONTEXT.md explicit).
- Não criar `audit_log` entry direto — Phase 15 SAFE-04 owns audit logging (14-CONTEXT.md explicit).
- Não adicionar unique constraint em threats tabela aqui — race condition é "tolerável" (14-CONTEXT.md §"Race condition mitigation" — loser cria threat similar; dashboard dedup on read futuro).
- Não chamar pino/project logger — `console.warn` aceitável em service layer (consistent com 14-01 sanitization).
- Não fazer await em Wave 3 caller — é fire-and-forget (wave 3 usa `void promoteHighCriticalFindings(...)` ou `.catch()` wrapper).
- Não mover a invocação para dentro de storage/apiFindings.ts — service layer é o nível certo (14-CONTEXT.md §"Integration Points").
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep "threatPromotion\\.ts" | grep -i error | head -5; grep -qE "export async function promoteHighCriticalFindings" server/services/threatPromotion.ts && grep -qE "export async function findDuplicateThreat" server/services/threatPromotion.ts && grep -qE "db\\.transaction" server/services/threatPromotion.ts && echo OK</automated>
  </verify>
  <done>`server/services/threatPromotion.ts` exporta `promoteHighCriticalFindings` (async), `findDuplicateThreat` (async), `PromotionResult` type; TypeScript compila limpo; usa `db.transaction` para atomicity; fail-open path testável via mock de listFindingsForPromotion que throws; 8 it.todo em `threatPromotion.test.ts` agora têm imports resolvidos (unresolved → resolved).</done>
</task>

</tasks>

<verification>
- `ls server/services/threatPromotion.ts server/__tests__/threatPromotion.test.ts` retorna ambos
- `grep -c "listFindingsForPromotion\|updateFindingPromotedThreatId" server/storage/apiFindings.ts` retorna ≥2
- `npx tsc --noEmit` passa (zero type errors relacionados aos novos arquivos)
- `npx vitest list server/__tests__/threatPromotion.test.ts` lista 8 it.todo
- `grep -q "source: 'api_security'" server/services/threatPromotion.ts` confirma source literal correto
- Zero modificação em shared/schema.ts (aditivo em arquivos novos)
- Zero modificação em server/routes/apis.ts (Wave 3 integra)
- Zero modificação em scoringEngine.ts (CONTEXT explicit)
</verification>

<success_criteria>
- FIND-03 foundation: ✅ promoteHighCriticalFindings + findDuplicateThreat shipped
- 8 it.todo stubs coletados por vitest, imports resolvem pós-Task 3
- TypeScript strict clean
- db.transaction() atomicity — threat insert + finding update atomic
- Dedupe 2-strategy: exact (threatTitle LIKE %owaspCategory%) + temporal (60min window)
- Fail-open: DB error não propaga; PromotionResult.error captura + log warning
- Zero quebra de contrato Phase 12 upsertApiFindingByKey (aditivo)
- Storage facade estendido sem breaking changes
- Ready para Wave 3 (14-04) invocar fire-and-forget no route handler
</success_criteria>

<output>
After completion, create `.planning/phases/14-findings-runtime-threat-integration/14-02-SUMMARY.md` with:
- PromotionResult interface final shape
- Signature final de promoteHighCriticalFindings + findDuplicateThreat
- Dedupe strategy pseudo-code (quais queries exatas)
- threatTitle construction pattern (e.g., "BOLA 2023: /api/users/:id")
- Invocation pattern for Wave 3 (14-04): `void promoteHighCriticalFindings(apiId, newIds).catch(...)` fire-and-forget
- Known limitations (single finding per threat inicialmente; dup link sobrescreve promotedThreatId mas não deep-merges findingIds array — deferido)
</output>
