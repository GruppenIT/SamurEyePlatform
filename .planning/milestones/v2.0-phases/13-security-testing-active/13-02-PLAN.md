---
phase: 13-security-testing-active
plan: 02
type: execute
wave: 1
depends_on:
  - "13-security-testing-active/13-01"
files_modified:
  - shared/apiRemediationTemplates.ts
  - server/services/scanners/api/bola.ts
  - server/services/scanners/api/bfla.ts
  - server/services/scanners/api/bopla.ts
  - server/services/scanners/api/rateLimit.ts
  - server/services/scanners/api/ssrfNuclei.ts
autonomous: true
requirements:
  - TEST-03
  - TEST-04
  - TEST-05
  - TEST-06
  - TEST-07
must_haves:
  truths:
    - "API_REMEDIATION_TEMPLATES estendido com 5 novas entries pt-BR: api1_bola_2023, api3_bopla_2023, api4_rate_limit_2023, api5_bfla_2023, api7_ssrf_2023 (entradas Phase 12 preservadas)"
    - "bola.ts exporta pairCredentials (C(n,2) sem espelhamento), harvestObjectIds (regex /^(id|uuid|pk)$/i, cap 3), testCrossAccess (finding severity=high quando status<400 sem forbidden-text)"
    - "bfla.ts exporta identifyLowPrivCreds (3-signal OR: priority int alto, description regex, skip-se-1-cred), matchAdminEndpoint (regex exata), testPrivEscalation (severity high/medium)"
    - "bopla.ts exporta fetchSeedBody, injectSensitiveKey (spread sem substituição), verifyReflection (deep key-path, NÃO regex) — gate destructiveEnabled na entrada"
    - "rateLimit.ts exporta buildBurst (Promise.all, burstSize default 20 max 50) e detectThrottling (ALL-3-signals: no-429 + no-Retry-After + no-X-RateLimit-* + >=90% status<400)"
    - "ssrfNuclei.ts exporta identifyUrlParams (3 heurísticas OR), buildSsrfNucleiArgs (sem -ni, com -interactions-poll-duration 5s etc), runSsrfNuclei (adapta nucleiApi.ts)"
    - "Todos scanners usam createLogger + nunca logam secrets completos (SAFE-06)"
    - "Zero novas deps npm — fetch nativo + node:crypto + nuclei binary existente"
  artifacts:
    - path: "shared/apiRemediationTemplates.ts"
      provides: "5 novas entries Phase 13 preservando 3 entries Phase 12"
      contains: "api1_bola_2023"
    - path: "server/services/scanners/api/bola.ts"
      provides: "BOLA cross-identity object access scanner (TEST-03)"
      exports: ["pairCredentials", "harvestObjectIds", "testCrossAccess", "BolaHit"]
      min_lines: 100
    - path: "server/services/scanners/api/bfla.ts"
      provides: "BFLA admin privilege escalation scanner (TEST-04)"
      exports: ["identifyLowPrivCreds", "matchAdminEndpoint", "testPrivEscalation", "BflaHit"]
      min_lines: 100
    - path: "server/services/scanners/api/bopla.ts"
      provides: "BOPLA/Mass Assignment sensitive property injection scanner (TEST-05)"
      exports: ["fetchSeedBody", "injectSensitiveKey", "verifyReflection", "BoplaHit"]
      min_lines: 120
    - path: "server/services/scanners/api/rateLimit.ts"
      provides: "Rate-limit absence scanner (TEST-06)"
      exports: ["buildBurst", "detectThrottling", "RateLimitHit"]
      min_lines: 80
    - path: "server/services/scanners/api/ssrfNuclei.ts"
      provides: "SSRF scanner via Nuclei + interactsh (TEST-07)"
      exports: ["identifyUrlParams", "buildSsrfNucleiArgs", "runSsrfNuclei", "SsrfHit"]
      min_lines: 120
  key_links:
    - from: "bola.ts pairCredentials"
      to: "listApiCredentials({apiId}) from server/storage/apiCredentials.ts"
      via: "import + call (inline, no new storage method)"
      pattern: "listApiCredentials"
    - from: "bopla.ts"
      to: "shared/schema.ts BOPLA_SENSITIVE_KEYS"
      via: "import const — source of truth for 10 sensitive keys"
      pattern: "BOPLA_SENSITIVE_KEYS"
    - from: "ssrfNuclei.ts"
      to: "server/services/journeys/nucleiPreflight.ts"
      via: "preflightNuclei(log) — reuse antes de spawn"
      pattern: "preflightNuclei"
    - from: "ssrfNuclei.ts"
      to: "NucleiFindingSchema (shared/schema.ts)"
      via: "safeParse per JSONL line — camelCase fields (matchedAt, templateId)"
      pattern: "NucleiFindingSchema"
    - from: "shared/apiRemediationTemplates.ts"
      to: "scanners bola/bfla/bopla/rateLimit/ssrfNuclei"
      via: "import API_REMEDIATION_TEMPLATES para hit.remediation"
      pattern: "API_REMEDIATION_TEMPLATES"
---

<objective>
Wave 1 — Scanners + extensão de remediation templates. 5 arquivos de scanner em `server/services/scanners/api/` + extensão de constantes pt-BR em `shared/apiRemediationTemplates.ts`. Cada scanner é um conjunto de funções puras (ou async) que retorna hits tipados para o orchestrator (Wave 2) persistir.

Purpose: Separar concerns — BOLA/BFLA/BOPLA/rateLimit como scanners TypeScript nativos usando fetch; SSRF como wrapper Nuclei com interactsh. Cada scanner testável isoladamente (stubs Wave 0 são os alvos). Wave 2 compõe.

Output: 6 arquivos (1 extensão + 5 novos). Zero mudança em schema, storage, rotas, CLI, docs.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/13-security-testing-active/13-CONTEXT.md
@.planning/phases/13-security-testing-active/13-RESEARCH.md
@.planning/phases/13-security-testing-active/13-01-PLAN.md

@.planning/phases/12-security-testing-passive/12-02-PLAN.md
@server/services/scanners/api/authFailure.ts
@server/services/scanners/api/nucleiApi.ts
@server/services/journeys/nucleiPreflight.ts
@shared/apiRemediationTemplates.ts

<interfaces>
<!-- Tipos e contratos que os scanners consomem. Extraídos da codebase. -->

From shared/schema.ts (Wave 0 — 13-01-PLAN):
```typescript
export const BOPLA_SENSITIVE_KEYS = [
  'is_admin', 'isAdmin', 'admin', 'role', 'roles', 'permissions',
  'superuser', 'owner', 'verified', 'email_verified',
] as const;
export type BoplaSensitiveKey = typeof BOPLA_SENSITIVE_KEYS[number];

export const apiActiveTestOptsSchema = z.object({
  stages: z.object({ bola, bfla, bopla, rateLimit, ssrf }).strict().optional(),
  destructiveEnabled: z.boolean().optional(),
  credentialIds: z.array(z.string().uuid()).optional(),
  endpointIds: z.array(z.string().uuid()).optional(),
  dryRun: z.boolean().optional(),
  rateLimit: z.object({ burstSize (max 50), windowMs, endpointIds (max 5) }).strict().optional(),
  ssrf: z.object({ interactshUrl }).strict().optional(),
  bola: z.object({ maxCredentials (max 6), maxIdsPerEndpoint (max 5) }).strict().optional(),
}).strict();

export interface ActiveTestResult { /* ... credentialsUsed: number */ }
```

From shared/schema.ts (Phase 9):
```typescript
export interface ApiFindingEvidence {
  request: { method: string; url: string; headers?: Record<string, string>; bodySnippet?: string };
  response: { status: number; headers?: Record<string, string>; bodySnippet?: string };
  extractedValues?: Record<string, unknown>;
  context?: string;
}
// owaspApiCategoryEnum includes: api1_bola_2023, api3_bopla_2023, api4_rate_limit_2023, api5_bfla_2023, api7_ssrf_2023
```

From server/storage/apiCredentials.ts (Phase 10):
```typescript
export interface ApiCredentialSafe {
  id: string;
  apiId: string | null;
  urlPattern: string;
  authType: 'bearer_jwt' | 'api_key_header' | 'api_key_query' | 'basic' | 'hmac' | 'oauth2_client_credentials' | 'mtls';
  priority: number;
  description: string | null;
  apiKeyHeaderName: string | null;
  apiKeyQueryParam: string | null;
  basicUsername: string | null;
}
export async function listApiCredentials(filter?: { apiId?: string; authType?: ApiAuthType }): Promise<ApiCredentialSafe[]>;
export async function getApiCredentialWithSecret(id: string): Promise<ApiCredentialWithSecret | undefined>;
// ApiCredentialWithSecret includes: secret (decrypted), secretEncrypted, dekEncrypted, plus all ApiCredentialSafe fields
```

From server/services/scanners/api/nucleiApi.ts (Phase 12 — template for ssrfNuclei.ts):
```typescript
export function buildNucleiArgs(opts: NucleiPassiveOpts): string[];
// Pattern: spawn('nuclei', args, { stdio: ['pipe','pipe','pipe'], env: { HOME:'/tmp/nuclei', ... } })
// JSONL streaming via NucleiFindingSchema.safeParse per line
// processTracker.register(jobId, 'nuclei', child, 'api-ssrf:nuclei')
// child.stdin.write(urls.join('\n') + '\n'); child.stdin.end();
```

From server/services/journeys/nucleiPreflight.ts:
```typescript
export async function preflightNuclei(log: Logger): Promise<{ ok: boolean; reason?: string }>;
```

From shared/schema.ts (Phase 12):
```typescript
// NucleiFindingSchema uses camelCase after parse (CRITICAL — Phase 12 STATE.md decision):
// safe.data.matchedAt (not 'matched-at'), safe.data.templateId (not 'template-id')
// safe.data.info.severity, safe.data.info.tags, safe.data['extracted-results']
export const NucleiFindingSchema: z.ZodType<NucleiFinding>;
```

From shared/apiRemediationTemplates.ts (Phase 12 — PRESERVE these):
```typescript
export const API_REMEDIATION_TEMPLATES = {
  api8_misconfiguration_2023: '...',
  api9_inventory_2023: { spec_exposed, graphql_introspection, hidden_endpoint },
  api2_broken_auth_2023: { alg_none, kid_injection, token_reuse, api_key_leakage },
} as const;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: shared/apiRemediationTemplates.ts — estender com 5 entries Phase 13</name>
  <files>shared/apiRemediationTemplates.ts</files>
  <read_first>
    - shared/apiRemediationTemplates.ts (arquivo atual completo — PRESERVAR as 3 entries Phase 12 intactas)
    - .planning/phases/13-security-testing-active/13-CONTEXT.md §"Findings dedupe + evidence + remediation" — texto exato pt-BR das 5 novas entries
    - .planning/phases/12-security-testing-passive/12-02-PLAN.md (Task 1 — template de estrutura)
  </read_first>
  <behavior>
    - Test 1: API_REMEDIATION_TEMPLATES ainda exporta api2_broken_auth_2023, api8_misconfiguration_2023, api9_inventory_2023 (Phase 12 entries preservadas)
    - Test 2: API_REMEDIATION_TEMPLATES exporta api1_bola_2023 com texto pt-BR sobre object-level ACL
    - Test 3: API_REMEDIATION_TEMPLATES exporta api3_bopla_2023 com texto pt-BR sobre allow-list explícita
    - Test 4: API_REMEDIATION_TEMPLATES exporta api4_rate_limit_2023 com texto pt-BR sobre 429 + Retry-After
    - Test 5: API_REMEDIATION_TEMPLATES exporta api5_bfla_2023 com texto pt-BR sobre RBAC backend validation
    - Test 6: API_REMEDIATION_TEMPLATES exporta api7_ssrf_2023 com texto pt-BR sobre URL allow-list + 169.254.169.254
  </behavior>
  <action>
MODIFICAR `shared/apiRemediationTemplates.ts`. Adicionar as 5 novas entries DENTRO do objeto existente `API_REMEDIATION_TEMPLATES`, ANTES do `} as const;` final. Preservar integralmente as 3 entries Phase 12.

Adicionar ao objeto (após `api2_broken_auth_2023: { ... },`):

```typescript
  // ── Phase 13: Security Testing — Active ──────────────────────────────────

  api1_bola_2023:
    'Implemente verificação de autorização por objeto (object-level ACL) antes de servir recursos. ' +
    'Nunca confie apenas no ID fornecido pelo cliente — valide que o principal autenticado tem ' +
    'permissão no objeto específico.',

  api3_bopla_2023:
    'Use allow-list explícita de campos aceitáveis em PUT/PATCH. Rejeite ou ignore silenciosamente ' +
    'propriedades sensíveis (role, is_admin, permissions) mesmo se presentes no payload.',

  api4_rate_limit_2023:
    'Implemente rate limiting com respostas 429 Too Many Requests + header Retry-After. ' +
    'Use limites diferenciados por tier de usuário e endpoint.',

  api5_bfla_2023:
    'Aplique autorização por função (role-based access control) em todos endpoints administrativos. ' +
    'Valide privilégios no backend mesmo quando a UI não expõe a ação — nunca confie no cliente.',

  api7_ssrf_2023:
    'Valide URLs fornecidas pelo usuário contra allow-list explícita de destinos. Bloqueie ranges ' +
    'privados (RFC 1918), localhost, link-local, e cloud metadata endpoints (169.254.169.254). ' +
    'Use client HTTP dedicado sem seguir redirects para metadata.',
```

NÃO alterar a export type `ApiRemediationTemplate` (já inferida via `typeof`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "apiRemediationTemplates" | head -5 ; grep -cE "api[12345789]_" shared/apiRemediationTemplates.ts ; npx vitest run server/__tests__/apiActive/remediation.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "api1_bola_2023" shared/apiRemediationTemplates.ts` retorna 1 match
    - `grep -n "api3_bopla_2023" shared/apiRemediationTemplates.ts` retorna 1 match
    - `grep -n "api4_rate_limit_2023" shared/apiRemediationTemplates.ts` retorna 1 match
    - `grep -n "api5_bfla_2023" shared/apiRemediationTemplates.ts` retorna 1 match
    - `grep -n "api7_ssrf_2023" shared/apiRemediationTemplates.ts` retorna 1 match
    - `grep -n "api2_broken_auth_2023\|api8_misconfiguration_2023\|api9_inventory_2023" shared/apiRemediationTemplates.ts` retorna ≥ 3 matches (Phase 12 preservados)
    - `grep -c "169.254.169.254" shared/apiRemediationTemplates.ts` retorna ≥ 1 (SSRF cloud metadata)
    - `grep -n "as const" shared/apiRemediationTemplates.ts` retorna 1 match (estrutura não quebrada)
    - `npx tsc --noEmit` sem erros em apiRemediationTemplates.ts
  </acceptance_criteria>
  <done>5 entries Phase 13 adicionadas; entries Phase 12 preservadas; scanners bola/bfla/bopla/rateLimit/ssrfNuclei podem importar via `API_REMEDIATION_TEMPLATES`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: server/services/scanners/api/bola.ts (TEST-03 — BOLA cross-identity)</name>
  <files>server/services/scanners/api/bola.ts</files>
  <read_first>
    - server/services/scanners/api/authFailure.ts (Phase 12 — template 1:1 para estrutura e padrão fetch nativo)
    - .planning/phases/13-security-testing-active/13-CONTEXT.md §"BOLA (TEST-03 / API1)" — todos os valores concretos
    - .planning/phases/13-security-testing-active/13-RESEARCH.md §"Pattern 3: Path-template substitution" + §"harvestObjectIds" code example
    - server/__tests__/apiActive/bola.test.ts (stubs Wave 0 — alvos dos testes)
    - shared/apiRemediationTemplates.ts (api1_bola_2023)
  </read_first>
  <behavior>
    - Test 1: pairCredentials([A,B,C,D]) gera exatamente C(4,2)=6 pares únicos sem (B,A) espelhado
    - Test 2: pairCredentials caps em maxCredentials (default 4) → max C(4,2)=6 pares
    - Test 3: pairCredentials([A]) retorna [] (menos de 2 creds)
    - Test 4: harvestObjectIds extrai até 3 IDs de campos com chave /^(id|uuid|pk)$/i de array ou objeto
    - Test 5: harvestObjectIds retorna [] para body não-objeto, null, sem matching keys
    - Test 6: testCrossAccess emite BolaHit severity=high quando cred B obtém status < 400 AND body.length > 0
    - Test 7: testCrossAccess supprime finding quando body contém "forbidden"/"unauthorized"/"permission denied" (case-insensitive)
    - Test 8: evidence.extractedValues inclui credentialAId, credentialBId, objectId, endpointPath
    - Test 9: substitutePathId substitui {id}/{userId}/{anyParam} com harvested ID via regex /\{(\w+)\}/g
    - Test 10: fallback para ?id=<val> quando path não tem {param} template
  </behavior>
  <action>
Criar arquivo novo `server/services/scanners/api/bola.ts`. Seguir padrão de `authFailure.ts` (Phase 12). Implementação:

```typescript
/**
 * Phase 13 TEST-03 — BOLA (Broken Object Level Authorization) scanner.
 *
 * Strategy:
 *   1. pairCredentials: C(n,2) ordered unique pairs (A→B only, no B→A mirror).
 *   2. harvestObjectIds: GET list-like endpoints with cred A; extract ≤3 IDs
 *      from JSON fields matching /^(id|uuid|pk)$/i.
 *   3. testCrossAccess: for each harvested ID, substitute into path template
 *      and GET with cred B. Finding when status < 400 AND body non-empty AND
 *      no forbidden-text.
 *
 * CONTEXT.md constraints:
 *   - maxCredentials = 4 default, max 6 (C(6,2) = 15 pairs max)
 *   - maxIdsPerEndpoint = 3 default, max 5
 *   - Only GET endpoints with requiresAuth=true are tested
 *   - Only "list-like" endpoints (path has no {param}) are used for harvest
 *   - Path template: OpenAPI {id} style only (Regex /\{(\w+)\}/g)
 *   - Severity: always 'high' (cross-identity object read confirmed)
 *   - Title (deterministic): "Acesso não autorizado a objeto via credencial secundária"
 *   - SAFE-06: never log harvested IDs or credential secrets; log only counts + endpointIds
 */
import { createLogger } from '../../../lib/logger';
import type { ApiFindingEvidence } from '@shared/schema';
import { API_REMEDIATION_TEMPLATES } from '@shared/apiRemediationTemplates';

const log = createLogger('scanners:api:bola');

const BOLA_TITLE = 'Acesso não autorizado a objeto via credencial secundária';
const FORBIDDEN_PATTERNS = /forbidden|unauthorized|permission denied/i;
const ID_FIELD_REGEX = /^(id|uuid|pk)$/i;
const PATH_TEMPLATE_REGEX = /\{(\w+)\}/g;

export interface BolaHit {
  endpointId: string;
  owaspCategory: 'api1_bola_2023';
  severity: 'high';
  title: string;
  description: string;
  remediation: string;
  evidence: ApiFindingEvidence;
}

export interface BolaCredential {
  id: string;
  authType: string;
  authHeaders: Record<string, string>;
  queryParams?: Record<string, string>;
}

/**
 * Generate C(n,2) ordered unique pairs without mirroring.
 * e.g. [A,B,C] → [(A,B), (A,C), (B,C)]  — no (B,A) etc.
 */
export function pairCredentials(
  creds: Array<{ id: string }>,
  maxCredentials: number = 4,
): Array<[{ id: string }, { id: string }]> {
  const capped = creds.slice(0, Math.min(maxCredentials, 6));
  const pairs: Array<[{ id: string }, { id: string }]> = [];
  for (let i = 0; i < capped.length; i++) {
    for (let j = i + 1; j < capped.length; j++) {
      pairs.push([capped[i], capped[j]]);
    }
  }
  return pairs;
}

/**
 * Extract up to maxIds unique IDs from a JSON response body.
 * Scans for keys matching /^(id|uuid|pk)$/i in objects/arrays.
 */
export function harvestObjectIds(
  body: unknown,
  maxIds: number = 3,
): Array<string | number> {
  if (typeof body !== 'object' || body === null) return [];
  const ids: Array<string | number> = [];

  function scan(obj: Record<string, unknown>): void {
    for (const [key, val] of Object.entries(obj)) {
      if (ID_FIELD_REGEX.test(key) && (typeof val === 'string' || typeof val === 'number')) {
        if (!ids.includes(val)) {
          ids.push(val);
          if (ids.length >= maxIds) return;
        }
      }
    }
  }

  if (Array.isArray(body)) {
    for (const item of body) {
      if (typeof item === 'object' && item !== null) {
        scan(item as Record<string, unknown>);
      }
      if (ids.length >= maxIds) break;
    }
  } else {
    scan(body as Record<string, unknown>);
  }

  return ids.slice(0, maxIds);
}

/**
 * Substitute OpenAPI {param} tokens in pathTemplate with id.
 * Falls back to ?id=<val> when no template tokens found.
 */
export function buildAccessUrl(baseUrl: string, pathTemplate: string, id: string | number): string {
  const substituted = pathTemplate.replace(PATH_TEMPLATE_REGEX, String(id));
  if (substituted === pathTemplate) {
    // No {param} tokens found — append as query param
    const sep = pathTemplate.includes('?') ? '&' : '?';
    return `${baseUrl}${pathTemplate}${sep}id=${encodeURIComponent(String(id))}`;
  }
  return `${baseUrl}${substituted}`;
}

/**
 * Determine if a path is "list-like" (suitable for harvest).
 * List-like: path has no {param} template AND (ends in plural noun pattern OR ends in /list).
 * Non-list: /users/{id} or /items/{itemId} — detail endpoint, not useful for harvest.
 */
export function isListLikePath(path: string): boolean {
  if (PATH_TEMPLATE_REGEX.test(path)) return false;
  // Reset lastIndex since regex is stateful with 'g' flag
  PATH_TEMPLATE_REGEX.lastIndex = 0;
  return true; // any path without {param} qualifies (harvest is opportunistic)
}

/**
 * Build fetch headers from a decrypted credential.
 * Supports: bearer_jwt, api_key_header, api_key_query, basic.
 * Throws for unsupported types (hmac/oauth2/mtls) — caller logs + skips.
 */
export function buildAuthHeaders(cred: {
  authType: string;
  secret: string;
  apiKeyHeaderName?: string | null;
  basicUsername?: string | null;
}): { headers: Record<string, string>; queryParam?: { name: string; value: string } } {
  switch (cred.authType) {
    case 'bearer_jwt':
      return { headers: { Authorization: `Bearer ${cred.secret}` } };
    case 'api_key_header':
      if (!cred.apiKeyHeaderName) throw new Error('api_key_header requires apiKeyHeaderName');
      return { headers: { [cred.apiKeyHeaderName]: cred.secret } };
    case 'basic': {
      const b64 = Buffer.from(`${cred.basicUsername ?? ''}:${cred.secret}`).toString('base64');
      return { headers: { Authorization: `Basic ${b64}` } };
    }
    case 'api_key_query':
      return { headers: {}, queryParam: { name: cred.apiKeyHeaderName ?? 'api_key', value: cred.secret } };
    default:
      throw new Error(`Auth type ${cred.authType} not supported by BOLA scanner (Phase 13)`);
  }
}

/**
 * Test cross-access: cred B fetches the object harvested by cred A.
 * Returns BolaHit when status < 400 AND body non-empty AND no forbidden text.
 */
export async function testCrossAccess(params: {
  endpointId: string;
  endpointPath: string;
  baseUrl: string;
  objectId: string | number;
  credentialAId: string;
  credentialBHeaders: Record<string, string>;
  credentialBQueryParam?: { name: string; value: string };
}): Promise<BolaHit | null> {
  let url = buildAccessUrl(params.baseUrl, params.endpointPath, params.objectId);
  if (params.credentialBQueryParam) {
    const sep = url.includes('?') ? '&' : '?';
    url = `${url}${sep}${encodeURIComponent(params.credentialBQueryParam.name)}=${encodeURIComponent(params.credentialBQueryParam.value)}`;
  }

  let status: number;
  let body: string;
  try {
    const resp = await fetch(url, { method: 'GET', headers: params.credentialBHeaders });
    status = resp.status;
    body = (await resp.text()).slice(0, 8192);
  } catch (err) {
    log.debug({ err: err instanceof Error ? err.message : err, endpointId: params.endpointId }, 'bola cross-access fetch failed');
    return null;
  }

  if (status >= 400) return null;
  if (!body || body.length === 0) return null;
  if (FORBIDDEN_PATTERNS.test(body)) return null;

  const maskedObjectId = String(params.objectId).slice(0, 3) + '***';
  log.info(
    { endpointId: params.endpointId, status, credentialAId: params.credentialAId },
    'bola cross-access finding',
  );

  return {
    endpointId: params.endpointId,
    owaspCategory: 'api1_bola_2023',
    severity: 'high',
    title: BOLA_TITLE,
    description: `Credencial B acessou objeto de propriedade de credencial A com status ${status}. Cross-identity object access confirmado em ${params.endpointPath}.`,
    remediation: API_REMEDIATION_TEMPLATES.api1_bola_2023,
    evidence: {
      request: { method: 'GET', url },
      response: { status, bodySnippet: body },
      extractedValues: {
        credentialAId: params.credentialAId,
        credentialBId: 'resolved-at-call-site',
        objectId: maskedObjectId,
        endpointPath: params.endpointPath,
      },
      context: 'BOLA cross-identity object access — cred B obtained object owned by cred A',
    },
  };
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "scanners/api/bola" | head -5 ; npx vitest run server/__tests__/apiActive/bola.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function pairCredentials" server/services/scanners/api/bola.ts` retorna 1 match
    - `grep -n "export function harvestObjectIds" server/services/scanners/api/bola.ts` retorna 1 match
    - `grep -n "export async function testCrossAccess" server/services/scanners/api/bola.ts` retorna 1 match
    - `grep -n "export function buildAccessUrl" server/services/scanners/api/bola.ts` retorna 1 match
    - `grep -n "Acesso não autorizado a objeto via credencial secundária" server/services/scanners/api/bola.ts` retorna 1 match (título determinístico)
    - `grep -n "api1_bola_2023" server/services/scanners/api/bola.ts` retorna ≥ 2 matches (owaspCategory + remediation)
    - `grep -n "forbidden|unauthorized|permission denied" server/services/scanners/api/bola.ts` retorna ≥ 1 match (FORBIDDEN_PATTERNS)
    - `grep -n "C(n,2)\|i + 1" server/services/scanners/api/bola.ts` retorna ≥ 1 match (pairing logic)
    - `grep -n "/\\^(id|uuid|pk)\\$/i" server/services/scanners/api/bola.ts` retorna ≥ 1 match (harvest regex exata)
    - `grep -n "\\\\{(\\\\w+)\\\\}/g" server/services/scanners/api/bola.ts` retorna ≥ 1 match (path template regex)
    - `npx tsc --noEmit` sem erros em bola.ts
  </acceptance_criteria>
  <done>Scanner BOLA implementado; stubs Wave 0 bola.test.ts têm alvos reais; Wave 2 orchestrator importa pairCredentials + harvestObjectIds + testCrossAccess.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: server/services/scanners/api/bfla.ts (TEST-04 — BFLA privilege escalation)</name>
  <files>server/services/scanners/api/bfla.ts</files>
  <read_first>
    - server/services/scanners/api/authFailure.ts (Phase 12 — template estrutural)
    - .planning/phases/13-security-testing-active/13-CONTEXT.md §"BFLA (TEST-04 / API5)" — regex exata + 3 sinais low-priv + skip conditions
    - .planning/phases/13-security-testing-active/13-RESEARCH.md §"Pitfall 2: BFLA false positive from universal credential"
    - server/__tests__/apiActive/bfla.test.ts (stubs Wave 0 — alvos)
    - shared/apiRemediationTemplates.ts (api5_bfla_2023)
  </read_first>
  <behavior>
    - Test 1: identifyLowPrivCreds flags cred com maior priority int (higher = less privilege)
    - Test 2: identifyLowPrivCreds flags cred com description matching /readonly|read-only|viewer|limited/i
    - Test 3: identifyLowPrivCreds retorna [] quando apenas 1 cred existe (não pode contrastar)
    - Test 4: matchAdminEndpoint retorna true para /admin/users, /manage/roles, /system/config, /internal/debug, /sudo/run, /superuser/promote, /root/settings, /console/admin
    - Test 5: matchAdminEndpoint retorna false para /users, /products, /orders, /administrative-view (word boundary)
    - Test 6: testPrivEscalation emite BflaHit severity=high quando low-priv obtém status < 400 em admin-path (RBAC contrastante)
    - Test 7: testPrivEscalation emite severity=medium quando todas creds retornam mesmo status (RBAC ausente)
    - Test 8: evidence.extractedValues inclui credentialId, priorityLevel, matchedPattern, endpointPath
  </behavior>
  <action>
Criar arquivo novo `server/services/scanners/api/bfla.ts`. Conteúdo:

```typescript
/**
 * Phase 13 TEST-04 — BFLA (Broken Function Level Authorization) scanner.
 *
 * Strategy:
 *   1. identifyLowPrivCreds: OR-logic 3 signals:
 *      a. Highest priority int among creds (higher int = less privilege, Phase 10 convention).
 *      b. description contains /readonly|read-only|viewer|limited/i.
 *      c. Skip entire stage if only 1 cred (cannot contrast).
 *   2. matchAdminEndpoint: regex /(admin|manage|management|system|internal|sudo|superuser|root|console)(\b|\/|$)/i
 *   3. testPrivEscalation: GET admin-path with low-priv cred.
 *      - severity=high when low-priv gets < 400 AND not redirect-to-login (contrasting RBAC)
 *      - severity=medium when all creds return same status (RBAC ambiguous)
 *   4. Universal-cred skip: low-priv cred that passes non-admin control endpoints is likely admin — skip.
 *
 * CONTEXT.md constraints:
 *   - Method-based (PUT/PATCH/DELETE on non-admin-path) requires opts.destructiveEnabled=true.
 *   - Default: GET on admin-path only.
 *   - Request budget: 100 per API.
 *   - Title: "Privilégio administrativo acessível via credencial de baixo privilégio"
 */
import { createLogger } from '../../../lib/logger';
import type { ApiFindingEvidence } from '@shared/schema';
import { API_REMEDIATION_TEMPLATES } from '@shared/apiRemediationTemplates';

const log = createLogger('scanners:api:bfla');

const BFLA_TITLE = 'Privilégio administrativo acessível via credencial de baixo privilégio';
const ADMIN_PATH_REGEX = /(admin|manage|management|system|internal|sudo|superuser|root|console)(\b|\/|$)/i;
const LOW_PRIV_DESC_REGEX = /readonly|read-only|viewer|limited/i;

export interface BflaHit {
  endpointId: string;
  owaspCategory: 'api5_bfla_2023';
  severity: 'high' | 'medium';
  title: string;
  description: string;
  remediation: string;
  evidence: ApiFindingEvidence;
}

export interface BflaCredential {
  id: string;
  priority: number;
  description: string | null;
  authType: string;
  authHeaders: Record<string, string>;
}

/**
 * Identify low-privilege credentials using 3-signal OR-logic.
 * Returns empty array when fewer than 2 creds exist (cannot contrast — skip stage).
 */
export function identifyLowPrivCreds(
  creds: Array<{ id: string; priority: number; description: string | null; authType: string }>,
): Array<{ id: string; priority: number; description: string | null; authType: string; signal: 'priority' | 'description' }> {
  if (creds.length < 2) {
    log.warn({ credCount: creds.length }, 'bfla: insufficient creds to contrast — skipping stage');
    return [];
  }

  const maxPriority = Math.max(...creds.map((c) => c.priority));
  const result = [];

  for (const cred of creds) {
    if (cred.priority === maxPriority) {
      result.push({ ...cred, signal: 'priority' as const });
    } else if (cred.description && LOW_PRIV_DESC_REGEX.test(cred.description)) {
      result.push({ ...cred, signal: 'description' as const });
    }
  }

  return result;
}

/**
 * Match an endpoint path against the admin-path heuristic.
 * Regex: /(admin|manage|management|system|internal|sudo|superuser|root|console)(\b|\/|$)/i
 */
export function matchAdminEndpoint(path: string): string | null {
  const match = ADMIN_PATH_REGEX.exec(path);
  return match ? match[1].toLowerCase() : null;
}

/**
 * Test privilege escalation: low-priv cred accesses admin-path endpoint.
 * Returns BflaHit with severity=high (contrasting RBAC) or medium (RBAC absent).
 */
export async function testPrivEscalation(params: {
  endpointId: string;
  endpointPath: string;
  endpointUrl: string;
  lowPrivCredId: string;
  lowPrivPriority: number;
  matchedPattern: string;
  lowPrivHeaders: Record<string, string>;
  allCredResults?: Array<{ credId: string; status: number }>;
}): Promise<BflaHit | null> {
  let status: number;
  let body: string;
  try {
    const resp = await fetch(params.endpointUrl, {
      method: 'GET',
      headers: params.lowPrivHeaders,
    });
    status = resp.status;
    body = (await resp.text()).slice(0, 8192);
  } catch (err) {
    log.debug({ err: err instanceof Error ? err.message : err, endpointId: params.endpointId }, 'bfla probe failed');
    return null;
  }

  if (status >= 400) return null;

  // Redirect to login check (3xx with Location containing /login)
  if (status >= 300 && status < 400) return null;

  // Determine severity: high if RBAC contrast exists, medium if all creds equal
  let severity: 'high' | 'medium' = 'high';
  if (params.allCredResults && params.allCredResults.length > 0) {
    const allSameStatus = params.allCredResults.every((r) => r.status === status);
    if (allSameStatus) {
      severity = 'medium';
      log.info({ endpointId: params.endpointId }, 'bfla: all creds same status — RBAC ambiguous (medium severity)');
    }
  }

  log.info(
    { endpointId: params.endpointId, status, severity, matchedPattern: params.matchedPattern },
    'bfla privilege escalation finding',
  );

  return {
    endpointId: params.endpointId,
    owaspCategory: 'api5_bfla_2023',
    severity,
    title: BFLA_TITLE,
    description: `Credencial de baixo privilégio (priority=${params.lowPrivPriority}) acessou endpoint administrativo ${params.endpointPath} com status ${status}. Pattern matched: ${params.matchedPattern}.`,
    remediation: API_REMEDIATION_TEMPLATES.api5_bfla_2023,
    evidence: {
      request: { method: 'GET', url: params.endpointUrl },
      response: { status, bodySnippet: body },
      extractedValues: {
        credentialId: params.lowPrivCredId,
        priorityLevel: params.lowPrivPriority,
        matchedPattern: params.matchedPattern,
        endpointPath: params.endpointPath,
      },
      context: 'BFLA — low-privilege credential accessed admin-level endpoint',
    },
  };
}

/**
 * Check if a low-priv credential is "universal" (passes non-admin endpoints too).
 * Samples up to 3 non-admin GET endpoints. Returns true if ≥2/3 succeed.
 * If true, caller should skip BFLA for this cred (false positive risk).
 */
export async function isUniversalCred(params: {
  credHeaders: Record<string, string>;
  nonAdminEndpointUrls: string[];
}): Promise<boolean> {
  const sample = params.nonAdminEndpointUrls.slice(0, 3);
  if (sample.length === 0) return false;

  let successCount = 0;
  for (const url of sample) {
    try {
      const resp = await fetch(url, { method: 'GET', headers: params.credHeaders });
      if (resp.status < 400) successCount++;
    } catch {
      // network error — not a success
    }
  }

  return successCount >= 2;
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "scanners/api/bfla" | head -5 ; npx vitest run server/__tests__/apiActive/bfla.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function identifyLowPrivCreds" server/services/scanners/api/bfla.ts` retorna 1 match
    - `grep -n "export function matchAdminEndpoint" server/services/scanners/api/bfla.ts` retorna 1 match
    - `grep -n "export async function testPrivEscalation" server/services/scanners/api/bfla.ts` retorna 1 match
    - `grep -n "(admin|manage|management|system|internal|sudo|superuser|root|console)" server/services/scanners/api/bfla.ts` retorna ≥ 1 match (regex exata CONTEXT.md)
    - `grep -n "readonly|read-only|viewer|limited" server/services/scanners/api/bfla.ts` retorna ≥ 1 match (LOW_PRIV_DESC_REGEX)
    - `grep -n "Privilégio administrativo acessível via credencial de baixo privilégio" server/services/scanners/api/bfla.ts` retorna 1 match
    - `grep -n "api5_bfla_2023" server/services/scanners/api/bfla.ts` retorna ≥ 2 matches
    - `grep -n "'high' | 'medium'" server/services/scanners/api/bfla.ts` retorna ≥ 1 match (severity union)
    - `npx tsc --noEmit` sem erros em bfla.ts
  </acceptance_criteria>
  <done>Scanner BFLA implementado com 3-signal low-priv identification + admin regex + RBAC contrast severity logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: server/services/scanners/api/bopla.ts + server/services/scanners/api/rateLimit.ts (TEST-05 + TEST-06)</name>
  <files>
    server/services/scanners/api/bopla.ts,
    server/services/scanners/api/rateLimit.ts
  </files>
  <read_first>
    - .planning/phases/13-security-testing-active/13-CONTEXT.md §"BOPLA / Mass Assignment (TEST-05 / API3)" — lista curada 10 chaves, seed strategy, reflection detection
    - .planning/phases/13-security-testing-active/13-CONTEXT.md §"Rate-Limit absence (TEST-06 / API4)" — burst parameters, ALL-3-signals detection
    - .planning/phases/13-security-testing-active/13-RESEARCH.md §"Pattern 4: BOPLA deep-merge and reflection detection"
    - server/__tests__/apiActive/bopla.test.ts (stubs Wave 0)
    - server/__tests__/apiActive/rateLimit.test.ts (stubs Wave 0)
    - shared/schema.ts (BOPLA_SENSITIVE_KEYS const — import)
    - shared/apiRemediationTemplates.ts (api3_bopla_2023, api4_rate_limit_2023)
  </read_first>
  <behavior>
    BOPLA:
    - Test 1: fetchSeedBody retorna body JSON quando GET status < 400; skipa endpoint quando GET retorna 404/401
    - Test 2: injectSensitiveKey produz { ...seedBody, [key]: injectedValue } sem substituir estrutura; boolean→true, string→'admin', array→['admin'], absent→true
    - Test 3: verifyReflection retorna null quando before[key] === after[key] (unchanged); retorna BoplaHit quando after[key] existe e diferente
    - Test 4: stage inteiro skipa quando opts.destructiveEnabled !== true
    - Test 5: severity=critical para keys is_admin/role/superuser; severity=high para outras 7 chaves
    - Test 6: título inclui chave: "Campo sensível aceito em PUT/PATCH sem validação (is_admin)"

    RateLimit:
    - Test 7: buildBurst emite exatamente burstSize fetch calls via Promise.all
    - Test 8: detectThrottling retorna RateLimitHit severity=medium quando: no-429 + no-Retry-After + no-X-RateLimit-* + >=90% status<400
    - Test 9: detectThrottling suprime finding quando ANY response tem status=429
    - Test 10: detectThrottling suprime finding quando ANY response tem Retry-After header
    - Test 11: detectThrottling suprime finding quando ANY response tem header matching /^x-ratelimit-/i
  </behavior>
  <action>
**Arquivo 1 — `server/services/scanners/api/bopla.ts`:**

```typescript
/**
 * Phase 13 TEST-05 — BOPLA / Mass Assignment scanner.
 *
 * DESTRUCTIVE GATE: This scanner modifies server state (PUT/PATCH with injected keys).
 * It MUST ONLY run when opts.destructiveEnabled === true. The gate check is the
 * caller's responsibility (orchestrator) — scanner trusts the gate was checked.
 *
 * Strategy:
 *   1. fetchSeedBody: GET same resource to capture current state (JSON).
 *   2. injectSensitiveKey: PUT/PATCH with { ...seed, [key]: injectedValue }.
 *      One request per key from BOPLA_SENSITIVE_KEYS (10 keys).
 *   3. verifyReflection: GET again; compare key before/after using deep key-path compare.
 *      Finding when key appears in after-body with different value.
 *
 * Severity: critical for is_admin/role/superuser; high for others.
 * Title template: "Campo sensível aceito em PUT/PATCH sem validação ({{key}})"
 * Title includes the specific key for dedupe variance (each key → distinct finding row).
 */
import { createLogger } from '../../../lib/logger';
import { BOPLA_SENSITIVE_KEYS, type ApiFindingEvidence } from '@shared/schema';
import { API_REMEDIATION_TEMPLATES } from '@shared/apiRemediationTemplates';

const log = createLogger('scanners:api:bopla');

const CRITICAL_KEYS = new Set(['is_admin', 'role', 'superuser']);

export interface BoplaHit {
  endpointId: string;
  owaspCategory: 'api3_bopla_2023';
  severity: 'critical' | 'high';
  title: string;
  description: string;
  remediation: string;
  evidence: ApiFindingEvidence;
}

/**
 * Fetch seed GET response body. Returns null when:
 * - GET status >= 400 (endpoint not accessible — test unreliable)
 * - Body is not JSON-parseable (form/XML out of scope Phase 13)
 */
export async function fetchSeedBody(
  resourceUrl: string,
  authHeaders: Record<string, string>,
): Promise<Record<string, unknown> | null> {
  let resp: Response;
  try {
    resp = await fetch(resourceUrl, { method: 'GET', headers: authHeaders });
  } catch (err) {
    log.debug({ err: err instanceof Error ? err.message : err, resourceUrl }, 'bopla seed fetch error');
    return null;
  }

  if (resp.status >= 400) {
    log.debug({ status: resp.status, resourceUrl }, 'bopla seed GET non-success — skipping endpoint');
    return null;
  }

  const text = await resp.text();
  try {
    const parsed = JSON.parse(text);
    if (typeof parsed !== 'object' || parsed === null || Array.isArray(parsed)) {
      log.debug({ resourceUrl }, 'bopla seed body is not a JSON object — skipping');
      return null;
    }
    return parsed as Record<string, unknown>;
  } catch {
    log.debug({ resourceUrl }, 'bopla seed body not JSON-parseable — skipping');
    return null;
  }
}

/**
 * Determine the injected value for a key based on seed body type inference.
 * boolean → true, string → 'admin', array → ['admin'], absent/other → true (default).
 */
export function resolveInjectedValue(
  seedBody: Record<string, unknown>,
  key: string,
): unknown {
  const existing = seedBody[key];
  if (existing === undefined) return true; // default for absent key
  if (typeof existing === 'boolean') return true;
  if (typeof existing === 'string') return 'admin';
  if (Array.isArray(existing)) return ['admin'];
  return true;
}

/**
 * Inject a single sensitive key into the seed body (spread — preserves structure).
 * Returns the new body object with the injected key added/overridden.
 */
export function injectSensitiveKey(
  seedBody: Record<string, unknown>,
  key: string,
): Record<string, unknown> {
  const injectedValue = resolveInjectedValue(seedBody, key);
  return { ...seedBody, [key]: injectedValue };
}

/**
 * Verify if the injected key is reflected in the after-GET response.
 * Uses key-path deep compare (NOT regex text match) to avoid false positives.
 * Returns BoplaHit when: PUT/PATCH status < 400 AND after-GET shows key with changed value.
 */
export async function verifyReflection(params: {
  endpointId: string;
  resourceUrl: string;
  seedBody: Record<string, unknown>;
  key: string;
  authHeaders: Record<string, string>;
  method: 'PUT' | 'PATCH';
}): Promise<BoplaHit | null> {
  const injectedBody = injectSensitiveKey(params.seedBody, params.key);

  // Step 1: PUT/PATCH with injected key
  let putStatus: number;
  try {
    const putResp = await fetch(params.resourceUrl, {
      method: params.method,
      headers: { ...params.authHeaders, 'Content-Type': 'application/json' },
      body: JSON.stringify(injectedBody),
    });
    putStatus = putResp.status;
  } catch (err) {
    log.debug({ err: err instanceof Error ? err.message : err, endpointId: params.endpointId }, 'bopla PUT/PATCH failed');
    return null;
  }

  if (putStatus >= 400) return null;

  // Step 2: GET to verify reflection
  let afterBody: Record<string, unknown> | null;
  try {
    const getResp = await fetch(params.resourceUrl, { method: 'GET', headers: params.authHeaders });
    if (getResp.status >= 400) return null;
    const text = await getResp.text();
    const parsed = JSON.parse(text);
    afterBody = (typeof parsed === 'object' && parsed !== null && !Array.isArray(parsed))
      ? parsed as Record<string, unknown>
      : null;
  } catch {
    return null;
  }

  if (!afterBody) return null;

  // Deep key-path compare: reflection when key in after AND different from seed
  const beforeValue = params.seedBody[params.key];
  const afterValue = afterBody[params.key];

  if (afterValue === undefined) return null;
  if (beforeValue === afterValue) return null; // unchanged — likely pre-existing

  const severity = CRITICAL_KEYS.has(params.key) ? 'critical' : 'high';
  const title = `Campo sensível aceito em PUT/PATCH sem validação (${params.key})`;

  log.info(
    { endpointId: params.endpointId, key: params.key, severity },
    'bopla mass assignment reflection confirmed',
  );

  return {
    endpointId: params.endpointId,
    owaspCategory: 'api3_bopla_2023',
    severity,
    title,
    description: `O campo sensível '${params.key}' foi aceito via ${params.method} e refletido na resposta GET subsequente. Mass assignment confirmado.`,
    remediation: API_REMEDIATION_TEMPLATES.api3_bopla_2023,
    evidence: {
      request: { method: params.method, url: params.resourceUrl, bodySnippet: JSON.stringify(injectedBody).slice(0, 512) },
      response: { status: putStatus },
      extractedValues: {
        injectedKey: params.key,
        originalValue: beforeValue ?? null,
        reflectedValue: afterValue,
        endpointPath: new URL(params.resourceUrl).pathname,
      },
      context: `BOPLA mass assignment — key ${params.key} reflected after ${params.method}`,
    },
  };
}

export { BOPLA_SENSITIVE_KEYS };
```

**Arquivo 2 — `server/services/scanners/api/rateLimit.ts`:**

```typescript
/**
 * Phase 13 TEST-06 — Rate-Limit Absence scanner.
 *
 * OPT-IN: opts.stages.rateLimit must be explicitly true (default false).
 * The orchestrator checks this flag before calling into this scanner.
 *
 * Strategy:
 *   buildBurst: Promise.all of burstSize parallel GET requests (intentional saturation).
 *   detectThrottling: ALL-3-signals must be absent for a finding:
 *     1. No response has status=429.
 *     2. No response has Retry-After header.
 *     3. No response has a header matching /^x-ratelimit-/i.
 *     4. ≥90% responses have status<400 (confirms endpoint accepted load).
 *
 * Severity: medium (rate-limit absence is a posture signal, not an exploit).
 * Title: "Ausência de rate-limiting em endpoint autenticado"
 * burstSize default=20, max=50 (Zod ceiling SAFE-01; Phase 15 enforces globally).
 */
import { createLogger } from '../../../lib/logger';
import type { ApiFindingEvidence } from '@shared/schema';
import { API_REMEDIATION_TEMPLATES } from '@shared/apiRemediationTemplates';

const log = createLogger('scanners:api:rateLimit');

const RATE_LIMIT_TITLE = 'Ausência de rate-limiting em endpoint autenticado';
const X_RATELIMIT_HEADER_REGEX = /^x-ratelimit-/i;
const DEFAULT_BURST_SIZE = 20;
const MAX_BURST_SIZE = 50;

export interface RateLimitHit {
  endpointId: string;
  owaspCategory: 'api4_rate_limit_2023';
  severity: 'medium';
  title: string;
  description: string;
  remediation: string;
  evidence: ApiFindingEvidence;
}

export interface BurstResponse {
  status: number;
  headers: Record<string, string>;
}

/**
 * Issue burstSize parallel GET requests via Promise.all.
 * Returns array of { status, headers } for detectThrottling analysis.
 */
export async function buildBurst(params: {
  url: string;
  authHeaders: Record<string, string>;
  burstSize?: number;
}): Promise<BurstResponse[]> {
  const size = Math.min(params.burstSize ?? DEFAULT_BURST_SIZE, MAX_BURST_SIZE);

  const requests = Array.from({ length: size }, () =>
    fetch(params.url, { method: 'GET', headers: params.authHeaders })
      .then(async (resp) => {
        const headers: Record<string, string> = {};
        resp.headers.forEach((value, key) => { headers[key.toLowerCase()] = value; });
        return { status: resp.status, headers };
      })
      .catch(() => ({ status: 0, headers: {} as Record<string, string> })),
  );

  return Promise.all(requests);
}

/**
 * Analyze burst responses for absence of rate-limiting signals.
 * Returns RateLimitHit when ALL-3-signals absent AND ≥90% success.
 * Returns null when any throttling signal detected.
 */
export function detectThrottling(params: {
  endpointId: string;
  endpointPath: string;
  endpointUrl: string;
  responses: BurstResponse[];
  burstSize: number;
  windowMs: number;
}): RateLimitHit | null {
  const { responses } = params;
  if (responses.length === 0) return null;

  let has429 = false;
  let hasRetryAfter = false;
  let hasXRateLimit = false;
  let successCount = 0;

  for (const r of responses) {
    if (r.status === 429) { has429 = true; break; }
    if (r.headers['retry-after']) hasRetryAfter = true;
    for (const header of Object.keys(r.headers)) {
      if (X_RATELIMIT_HEADER_REGEX.test(header)) { hasXRateLimit = true; break; }
    }
    if (r.status > 0 && r.status < 400) successCount++;
  }

  if (has429 || hasRetryAfter || hasXRateLimit) return null;

  const successRate = successCount / responses.length;
  if (successRate < 0.9) {
    log.debug({ endpointId: params.endpointId, successRate }, 'rateLimit: < 90% success — endpoint unhealthy, no finding');
    return null;
  }

  const throttledCount = responses.length - successCount;
  log.info(
    { endpointId: params.endpointId, successCount, throttledCount, successRate },
    'rate-limit absence finding',
  );

  return {
    endpointId: params.endpointId,
    owaspCategory: 'api4_rate_limit_2023',
    severity: 'medium',
    title: RATE_LIMIT_TITLE,
    description: `Endpoint ${params.endpointPath} aceitou ${successCount}/${responses.length} requisições em burst (${params.windowMs}ms) sem sinalizar 429, Retry-After, ou X-RateLimit-*. Rate limiting ausente ou insuficiente.`,
    remediation: API_REMEDIATION_TEMPLATES.api4_rate_limit_2023,
    evidence: {
      request: { method: 'GET', url: params.endpointUrl },
      response: { status: responses[0]?.status ?? 0 },
      extractedValues: {
        burstSize: params.burstSize,
        successCount,
        throttledCount,
        hasRetryAfter: false,
        hasXRateLimitHeaders: false,
        windowMs: params.windowMs,
        endpointPath: params.endpointPath,
      },
      context: `Rate-limit absence test — ${responses.length} parallel requests, no throttling signals`,
    },
  };
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "scanners/api/bopla|scanners/api/rateLimit" | head -10 ; npx vitest run server/__tests__/apiActive/bopla.test.ts server/__tests__/apiActive/rateLimit.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    BOPLA:
    - `grep -n "export async function fetchSeedBody" server/services/scanners/api/bopla.ts` retorna 1 match
    - `grep -n "export function injectSensitiveKey" server/services/scanners/api/bopla.ts` retorna 1 match
    - `grep -n "export async function verifyReflection" server/services/scanners/api/bopla.ts` retorna 1 match
    - `grep -n "BOPLA_SENSITIVE_KEYS" server/services/scanners/api/bopla.ts` retorna ≥ 2 matches (import + re-export)
    - `grep -n "Campo sensível aceito em PUT/PATCH sem validação" server/services/scanners/api/bopla.ts` retorna ≥ 1 match (título template)
    - `grep -n "api3_bopla_2023" server/services/scanners/api/bopla.ts` retorna ≥ 2 matches
    - `grep -n "critical.*is_admin\|is_admin.*critical\|CRITICAL_KEYS" server/services/scanners/api/bopla.ts` retorna ≥ 1 match
    RateLimit:
    - `grep -n "export async function buildBurst" server/services/scanners/api/rateLimit.ts` retorna 1 match
    - `grep -n "export function detectThrottling" server/services/scanners/api/rateLimit.ts` retorna 1 match
    - `grep -n "Promise.all" server/services/scanners/api/rateLimit.ts` retorna ≥ 1 match (parallelism)
    - `grep -n "Ausência de rate-limiting em endpoint autenticado" server/services/scanners/api/rateLimit.ts` retorna 1 match
    - `grep -n "x-ratelimit-\|X-RateLimit" server/services/scanners/api/rateLimit.ts` retorna ≥ 1 match (header regex)
    - `grep -n "0\.9\|90" server/services/scanners/api/rateLimit.ts` retorna ≥ 1 match (90% success threshold)
    - `grep -n "api4_rate_limit_2023" server/services/scanners/api/rateLimit.ts` retorna ≥ 2 matches
    - `npx tsc --noEmit` sem erros em bopla.ts e rateLimit.ts
  </acceptance_criteria>
  <done>Scanners BOPLA (TEST-05) e RateLimit (TEST-06) implementados; stubs Wave 0 têm alvos reais; Wave 2 orchestrator importa e compõe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: server/services/scanners/api/ssrfNuclei.ts (TEST-07 — SSRF via interactsh)</name>
  <files>server/services/scanners/api/ssrfNuclei.ts</files>
  <read_first>
    - server/services/scanners/api/nucleiApi.ts (Phase 12 — template direto; adaptar para ssrf, sem -ni, com interactsh flags)
    - .planning/phases/13-security-testing-active/13-CONTEXT.md §"SSRF (TEST-07 / API7)" — URL-like regex exata, Nuclei flags exatas, interactsh config
    - .planning/phases/13-security-testing-active/13-RESEARCH.md §"Pattern 2: Nuclei SSRF wrapper" + §"Critical unknowns resolved — Pitfall 6: NucleiFindingSchema camelCase"
    - server/__tests__/apiActive/ssrfNuclei.test.ts (stubs Wave 0)
    - server/__tests__/fixtures/api-active/ssrf-nuclei-interaction.jsonl (fixture shape)
    - shared/apiRemediationTemplates.ts (api7_ssrf_2023)
  </read_first>
  <behavior>
    - Test 1: identifyUrlParams matches name via URL_LIKE_NAME_REGEX (url, redirect, redirect_uri, callback, webhook, target, dest, destination, endpoint, uri, link, image_url, avatar_url, src, href, next, continue, returnTo, return_to, return) case-insensitive
    - Test 2: identifyUrlParams matches type==='url' OR format==='uri' OR format==='url'
    - Test 3: identifyUrlParams matches param.example parseable by new URL() without throw
    - Test 4: identifyUrlParams rejects non-URL params (userId, type=string, no example)
    - Test 5: buildSsrfNucleiArgs inclui -tags ssrf (NÃO misconfig/exposure)
    - Test 6: buildSsrfNucleiArgs NÃO inclui -ni flag (interactsh habilitado)
    - Test 7: buildSsrfNucleiArgs inclui -interactions-poll-duration 5s, -interactions-wait 10s, -interactions-retries-count 3
    - Test 8: buildSsrfNucleiArgs inclui -interactsh-url <url> quando opts.interactshUrl ou env INTERACTSH_URL presente
    - Test 9: buildSsrfNucleiArgs omite -interactsh-url quando nem opts nem env está presente
    - Test 10: finding emitido quando JSONL reports interaction=true OR extracted-results contém callback URL
  </behavior>
  <action>
Criar arquivo novo `server/services/scanners/api/ssrfNuclei.ts`. Adaptar nucleiApi.ts de Phase 12:

```typescript
/**
 * Phase 13 TEST-07 — SSRF scanner via Nuclei + interactsh.
 *
 * KEY DIFFERENCE from nucleiApi.ts (Phase 12):
 *   - Tags: '-tags ssrf' (only SSRF templates)
 *   - NO '-ni' flag (interactsh MUST be enabled for OOB detection)
 *   - Additional flags: '-interactions-poll-duration 5s -interactions-wait 10s
 *     -interactions-retries-count 3'
 *   - '-timeout 30' (longer for OOB callback wait)
 *   - Optional: '-interactsh-url <URL>' when INTERACTSH_URL env or opts.interactshUrl set
 *
 * SCOPE: Only params whose values are URL-like (3 heuristics OR):
 *   1. Name matches URL_LIKE_NAME_REGEX (locked list from CONTEXT.md)
 *   2. type==='url' OR format==='uri' OR format==='url'
 *   3. example parseable by new URL() without throw
 *
 * SAFE-06: log only paramName prefix-3+*** for interactsh URLs; never full callback URL.
 * NucleiFindingSchema camelCase: use safe.data.matchedAt (not 'matched-at').
 */
import { spawn, type ChildProcess } from 'child_process';
import { createLogger } from '../../../lib/logger';
import { preflightNuclei } from '../../journeys/nucleiPreflight';
import { processTracker } from '../../processTracker';
import {
  NucleiFindingSchema,
  type ApiFindingEvidence,
} from '@shared/schema';
import { API_REMEDIATION_TEMPLATES } from '@shared/apiRemediationTemplates';

const log = createLogger('scanners:api:ssrfNuclei');

const NUCLEI_BIN = 'nuclei';
const TEMPLATES_DIR = '/tmp/nuclei/nuclei-templates';
const SSRF_TAGS = 'ssrf';
const BODY_SNIPPET_MAX = 8192;
const DEFAULT_RATE_LIMIT = 10;
const DEFAULT_TIMEOUT_SEC = 30; // longer for OOB callback
const DEFAULT_TOTAL_TIMEOUT_MS = 15 * 60 * 1000; // 15 minutes for SSRF stage

/**
 * URL-like param name regex (locked from CONTEXT.md §SSRF).
 * Matches common param names that accept URLs as values.
 */
export const URL_LIKE_NAME_REGEX =
  /^(url|redirect|redirect_uri|callback|callback_url|webhook|webhook_url|target|dest|destination|endpoint|uri|link|image_url|avatar_url|src|href|next|continue|returnTo|return_to|return)$/i;

export interface SsrfParam {
  name: string;
  type?: string;
  format?: string;
  example?: string;
}

export interface SsrfHit {
  endpointId: string;
  owaspCategory: 'api7_ssrf_2023';
  severity: 'low' | 'medium' | 'high' | 'critical';
  title: string;
  description: string;
  remediation: string;
  evidence: ApiFindingEvidence;
}

export interface SsrfNucleiOpts {
  interactshUrl?: string;
  rateLimit?: number;
  timeoutSec?: number;
}

export interface SsrfNucleiCtx {
  jobId?: string;
  apiId: string;
  signal?: AbortSignal;
}

export interface SsrfNucleiResult {
  findings: SsrfHit[];
  skipped?: { reason: string };
}

/**
 * Identify URL-like parameters using 3-heuristic OR logic.
 * Returns params that match at least one heuristic.
 */
export function identifyUrlParams(params: SsrfParam[]): SsrfParam[] {
  return params.filter((p) => isUrlLikeParam(p));
}

export function isUrlLikeParam(param: SsrfParam): boolean {
  // Heuristic 1: name regex match
  if (URL_LIKE_NAME_REGEX.test(param.name)) return true;
  // Heuristic 2: type/format match
  if (param.type === 'url' || param.format === 'uri' || param.format === 'url') return true;
  // Heuristic 3: example parseable as URL
  if (param.example) {
    try { new URL(param.example); return true; } catch { /* not a URL */ }
  }
  return false;
}

/**
 * Build Nuclei CLI args for SSRF scan.
 * CRITICAL: NO -ni flag. YES -interactions-* flags. -timeout 30 (OOB wait).
 */
export function buildSsrfNucleiArgs(opts: SsrfNucleiOpts): string[] {
  const args = [
    '-tags', SSRF_TAGS,
    '-jsonl',
    '-silent',
    '-retries', '0',
    '-rl', String(opts.rateLimit ?? DEFAULT_RATE_LIMIT),
    '-timeout', String(opts.timeoutSec ?? DEFAULT_TIMEOUT_SEC),
    '-t', TEMPLATES_DIR,
    '-l', '/dev/stdin',
    // interactsh OOB polling config (CONTEXT.md §SSRF)
    '-interactions-poll-duration', '5s',
    '-interactions-wait', '10s',
    '-interactions-retries-count', '3',
  ];

  // Override interactsh server when env or opts provides URL (air-gapped support)
  const interactshUrl = opts.interactshUrl ?? process.env.INTERACTSH_URL;
  if (interactshUrl) {
    args.push('-interactsh-url', interactshUrl);
  }
  // NOTE: NO '-ni' flag here — interactsh MUST be active for SSRF OOB detection.

  return args;
}

/**
 * Map a Nuclei SSRF finding to SsrfHit.
 * CRITICAL: NucleiFindingSchema uses camelCase (safe.data.matchedAt, not 'matched-at').
 */
function mapSsrfFinding(finding: ReturnType<typeof NucleiFindingSchema.parse>, endpointId: string, paramName: string): SsrfHit {
  const nucleiSeverity = finding.info?.severity ?? 'info';
  const severity = (nucleiSeverity === 'info' ? 'low' : nucleiSeverity) as SsrfHit['severity'];
  const title = `SSRF confirmado via interação out-of-band em parâmetro ${paramName}`;

  // Mask interactsh URL: prefix-3 + *** (SAFE-06)
  const matchedAt = finding['matched-at'] ?? '';
  const interactshUrlMasked = matchedAt.slice(0, 3) + '***';

  return {
    endpointId,
    owaspCategory: 'api7_ssrf_2023',
    severity,
    title,
    description: `SSRF out-of-band detectado no parâmetro '${paramName}'. Nuclei confirmou callback interactsh (${finding['interactsh-interaction-type'] ?? 'unknown'}).`,
    remediation: API_REMEDIATION_TEMPLATES.api7_ssrf_2023,
    evidence: {
      request: {
        method: 'GET',
        url: matchedAt || finding.host || 'unknown',
        bodySnippet: typeof finding.request === 'string' ? finding.request.slice(0, BODY_SNIPPET_MAX) : undefined,
      },
      response: {
        status: 200,
        bodySnippet: typeof finding.response === 'string' ? finding.response.slice(0, BODY_SNIPPET_MAX) : undefined,
      },
      extractedValues: {
        paramName,
        interactsh_interaction_type: finding['interactsh-interaction-type'] ?? 'unknown',
        interactshUrl: interactshUrlMasked,
        templateId: finding['template-id'],
      },
      context: 'SSRF OOB interaction confirmed via Nuclei interactsh',
    },
  };
}

/**
 * Run Nuclei SSRF scan against a batch of target URLs.
 * preflightNuclei must pass before spawn. Skip early if no targets.
 */
export async function runSsrfNuclei(
  targetUrls: Array<{ url: string; endpointId: string; paramName: string }>,
  opts: SsrfNucleiOpts,
  ctx: SsrfNucleiCtx,
): Promise<SsrfNucleiResult> {
  if (targetUrls.length === 0) {
    return { findings: [], skipped: { reason: 'no URL-like params found' } };
  }

  const preflight = await preflightNuclei(log);
  if (!preflight.ok) {
    return { findings: [], skipped: { reason: preflight.reason ?? 'nuclei unavailable' } };
  }

  const args = buildSsrfNucleiArgs(opts);

  return new Promise((resolve) => {
    const child: ChildProcess = spawn(NUCLEI_BIN, args, {
      stdio: ['pipe', 'pipe', 'pipe'],
      env: {
        ...process.env,
        HOME: '/tmp/nuclei',
        NUCLEI_CONFIG_DIR: '/tmp/nuclei/.config',
        XDG_CONFIG_HOME: '/tmp/nuclei/.config',
        XDG_CACHE_HOME: '/tmp/nuclei/.cache',
      },
    });

    if (ctx.jobId && child.pid) {
      processTracker.register(ctx.jobId, 'nuclei', child, 'api-ssrf:nuclei');
    }

    // URL → (endpointId, paramName) lookup
    const byUrl = new Map<string, { endpointId: string; paramName: string }>();
    targetUrls.forEach((t) => byUrl.set(t.url, { endpointId: t.endpointId, paramName: t.paramName }));

    child.stdin?.write(targetUrls.map((t) => t.url).join('\n') + '\n');
    child.stdin?.end();

    const findings: SsrfHit[] = [];
    let buf = '';

    child.stdout?.on('data', (chunk: Buffer) => {
      buf += chunk.toString('utf8');
      const lines = buf.split('\n');
      buf = lines.pop() ?? '';
      for (const line of lines) {
        if (!line.trim()) continue;
        try {
          const parsed = JSON.parse(line);
          const safe = NucleiFindingSchema.safeParse(parsed);
          if (!safe.success) {
            log.debug({ issue: safe.error.issues[0] }, 'ssrf nuclei jsonl line rejected by schema');
            continue;
          }
          // Finding criterion: interaction=true OR extracted-results has interactsh callback
          const hasInteraction = (parsed.interaction === true) ||
            (Array.isArray(safe.data['extracted-results']) &&
              safe.data['extracted-results'].some((r: string) => r.includes('oast') || r.includes('interactsh')));
          if (!hasInteraction) continue;

          const matchUrl = safe.data['matched-at'] ?? safe.data.host ?? '';
          const entry = byUrl.get(matchUrl) ?? targetUrls.find((t) => matchUrl.startsWith(t.url));
          if (!entry) {
            log.debug({ matchUrl }, 'ssrf nuclei finding no matching target — skipped');
            continue;
          }
          findings.push(mapSsrfFinding(safe.data as any, entry.endpointId, entry.paramName));
        } catch (err) {
          log.debug({ err: err instanceof Error ? err.message : err }, 'ssrf nuclei jsonl parse error');
        }
      }
    });

    child.stderr?.on('data', (chunk: Buffer) => {
      log.debug({ stderr: chunk.toString('utf8').slice(0, 500) }, 'ssrf nuclei stderr');
    });

    const totalTimeout = setTimeout(() => {
      log.warn({ apiId: ctx.apiId, jobId: ctx.jobId }, 'ssrf nuclei total timeout — SIGTERM');
      child.kill('SIGTERM');
    }, DEFAULT_TOTAL_TIMEOUT_MS);

    const onAbort = () => {
      log.info({ apiId: ctx.apiId, jobId: ctx.jobId }, 'ssrf nuclei aborted via signal');
      child.kill('SIGTERM');
    };
    ctx.signal?.addEventListener('abort', onAbort);

    child.on('close', (code) => {
      clearTimeout(totalTimeout);
      ctx.signal?.removeEventListener('abort', onAbort);
      log.info({ apiId: ctx.apiId, jobId: ctx.jobId, exitCode: code, findingsCount: findings.length }, 'ssrf nuclei scan complete');
      resolve({ findings });
    });

    child.on('error', (err) => {
      clearTimeout(totalTimeout);
      ctx.signal?.removeEventListener('abort', onAbort);
      log.error({ err }, 'ssrf nuclei spawn error');
      resolve({ findings, skipped: { reason: `spawn failed: ${err.message}` } });
    });
  });
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "scanners/api/ssrfNuclei" | head -5 ; npx vitest run server/__tests__/apiActive/ssrfNuclei.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export const URL_LIKE_NAME_REGEX" server/services/scanners/api/ssrfNuclei.ts` retorna 1 match
    - `grep -n "export function identifyUrlParams" server/services/scanners/api/ssrfNuclei.ts` retorna 1 match
    - `grep -n "export function buildSsrfNucleiArgs" server/services/scanners/api/ssrfNuclei.ts` retorna 1 match
    - `grep -n "export async function runSsrfNuclei" server/services/scanners/api/ssrfNuclei.ts` retorna 1 match
    - `grep -n "'-tags', 'ssrf'" server/services/scanners/api/ssrfNuclei.ts` retorna ≥ 1 match (SSRF tag only)
    - `grep -n "\-ni" server/services/scanners/api/ssrfNuclei.ts` retorna 0 matches (NUNCA -ni — CRITICAL)
    - `grep -n "interactions-poll-duration.*5s\|5s.*interactions-poll-duration" server/services/scanners/api/ssrfNuclei.ts` retorna ≥ 1 match
    - `grep -n "interactions-wait.*10s\|10s.*interactions-wait" server/services/scanners/api/ssrfNuclei.ts` retorna ≥ 1 match
    - `grep -n "interactions-retries-count.*3\|3.*interactions-retries-count" server/services/scanners/api/ssrfNuclei.ts` retorna ≥ 1 match
    - `grep -n "INTERACTSH_URL\|interactshUrl" server/services/scanners/api/ssrfNuclei.ts` retorna ≥ 2 matches (env + opts)
    - `grep -n "SSRF confirmado via interação out-of-band em parâmetro" server/services/scanners/api/ssrfNuclei.ts` retorna ≥ 1 match (título template)
    - `grep -n "api7_ssrf_2023" server/services/scanners/api/ssrfNuclei.ts` retorna ≥ 2 matches
    - `grep -n "preflightNuclei" server/services/scanners/api/ssrfNuclei.ts` retorna ≥ 1 match
    - `npx tsc --noEmit` sem erros em ssrfNuclei.ts
  </acceptance_criteria>
  <done>Scanner SSRF implementado com interactsh ativo (sem -ni), 3 heurísticas URL-like, e mapping de findings Nuclei camelCase; stubs Wave 0 têm alvos reais.</done>
</task>

</tasks>

<verification>
Após todas as tasks completarem:

```bash
# 1) Type check — todos os 6 arquivos novos/modificados
npx tsc --noEmit 2>&1 | grep -E "apiRemediationTemplates|bola|bfla|bopla|rateLimit|ssrfNuclei" | head -20

# 2) Suite apiActive (it.todo — 0 fail esperado)
npx vitest run server/__tests__/apiActive --reporter=default 2>&1 | tail -20

# 3) Remediation templates corretas
grep -cE "api[1345789]_" shared/apiRemediationTemplates.ts
# Expected: ≥ 8 matches (3 Phase 12 + 5 Phase 13)

# 4) Inventário de arquivos criados
for f in shared/apiRemediationTemplates.ts server/services/scanners/api/bola.ts server/services/scanners/api/bfla.ts server/services/scanners/api/bopla.ts server/services/scanners/api/rateLimit.ts server/services/scanners/api/ssrfNuclei.ts; do
  echo "== $f =="; wc -l "$f"
done

# 5) Verificar que ssrfNuclei.ts não contém -ni
grep -c "\-ni" server/services/scanners/api/ssrfNuclei.ts
# Expected: 0
```
</verification>

<success_criteria>
- [ ] `shared/apiRemediationTemplates.ts` exporta 5 novas entries Phase 13 + preserva 3 Phase 12
- [ ] `bola.ts` exporta `pairCredentials` (C(n,2)) + `harvestObjectIds` (regex /^(id|uuid|pk)$/i) + `testCrossAccess` + `buildAccessUrl`
- [ ] `bfla.ts` exporta `identifyLowPrivCreds` (3-signal OR) + `matchAdminEndpoint` (regex exata CONTEXT.md) + `testPrivEscalation` (severity high/medium) + `isUniversalCred`
- [ ] `bopla.ts` exporta `fetchSeedBody` + `injectSensitiveKey` (spread) + `verifyReflection` (key-path compare, NÃO regex)
- [ ] `rateLimit.ts` exporta `buildBurst` (Promise.all) + `detectThrottling` (ALL-3-signals)
- [ ] `ssrfNuclei.ts` exporta `identifyUrlParams` + `buildSsrfNucleiArgs` (sem -ni, com interactions-*) + `runSsrfNuclei`
- [ ] ZERO ocorrência de -ni em ssrfNuclei.ts
- [ ] Títulos pt-BR determinísticos idênticos a CONTEXT.md em cada scanner
- [ ] `npx tsc --noEmit` limpo para todos os 6 arquivos
- [ ] Zero novas deps npm
</success_criteria>

<output>
After completion, create `.planning/phases/13-security-testing-active/13-02-SUMMARY.md`
</output>
