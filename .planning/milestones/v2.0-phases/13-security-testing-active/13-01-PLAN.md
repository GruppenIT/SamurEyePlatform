---
phase: 13-security-testing-active
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - shared/schema.ts
  - server/__tests__/apiActive/optsSchema.test.ts
  - server/__tests__/apiActive/bola.test.ts
  - server/__tests__/apiActive/bfla.test.ts
  - server/__tests__/apiActive/bopla.test.ts
  - server/__tests__/apiActive/rateLimit.test.ts
  - server/__tests__/apiActive/ssrfNuclei.test.ts
  - server/__tests__/apiActive/orchestrator.test.ts
  - server/__tests__/apiActive/route.test.ts
  - server/__tests__/apiActive/remediation.test.ts
  - server/__tests__/apiActive/credentialsHelper.test.ts
  - server/__tests__/fixtures/api-active/bola-crossaccess-response.json
  - server/__tests__/fixtures/api-active/bfla-admin-success.json
  - server/__tests__/fixtures/api-active/bopla-reflection-before.json
  - server/__tests__/fixtures/api-active/bopla-reflection-after.json
  - server/__tests__/fixtures/api-active/rate-limit-burst-responses.json
  - server/__tests__/fixtures/api-active/ssrf-nuclei-interaction.jsonl
autonomous: true
requirements:
  - TEST-03
  - TEST-04
  - TEST-05
  - TEST-06
  - TEST-07
must_haves:
  truths:
    - "Test harness exists: 10 test files em server/__tests__/apiActive/ com it.todo stubs"
    - "Fixtures determinísticas existem em server/__tests__/fixtures/api-active/ (6 arquivos)"
    - "apiActiveTestOptsSchema Zod exportada de shared/schema.ts com stages + destructiveEnabled + rateLimit sub + ssrf sub + bola sub + .strict()"
    - "Tipos ApiActiveTestOpts + ActiveTestResult exportados de shared/schema.ts"
    - "Constante BOPLA_SENSITIVE_KEYS exportada de shared/schema.ts com exatamente 10 chaves (is_admin, isAdmin, admin, role, roles, permissions, superuser, owner, verified, email_verified)"
    - "Vitest consegue coletar stubs sem erros: `npx vitest list server/__tests__/apiActive` retorna 10 files"
  artifacts:
    - path: "server/__tests__/apiActive/optsSchema.test.ts"
      provides: "Stub for apiActiveTestOptsSchema (defaults, ceiling caps, destructive gate) — TEST-03..07"
      contains: "it.todo"
    - path: "server/__tests__/apiActive/bola.test.ts"
      provides: "Stub for BOLA pair generation + ID harvest + cross-access finding (TEST-03)"
      contains: "it.todo"
    - path: "server/__tests__/apiActive/bfla.test.ts"
      provides: "Stub for BFLA low-priv heuristics + admin path regex + priv escalation (TEST-04)"
      contains: "it.todo"
    - path: "server/__tests__/apiActive/bopla.test.ts"
      provides: "Stub for BOPLA seed+inject+reflect + destructive gate (TEST-05)"
      contains: "it.todo"
    - path: "server/__tests__/apiActive/rateLimit.test.ts"
      provides: "Stub for rate-limit burst parallelism + 3-signal detection (TEST-06)"
      contains: "it.todo"
    - path: "server/__tests__/apiActive/ssrfNuclei.test.ts"
      provides: "Stub for URL-like param identification + interactsh flag injection (TEST-07)"
      contains: "it.todo"
    - path: "server/__tests__/apiActive/orchestrator.test.ts"
      provides: "Stub for stages order + cancel + dryRun integration (TEST-03..07)"
      contains: "it.todo"
    - path: "server/__tests__/apiActive/route.test.ts"
      provides: "Stub for POST /test/active RBAC + Zod + CLI (TEST-03..07)"
      contains: "it.todo"
    - path: "server/__tests__/apiActive/remediation.test.ts"
      provides: "Stub for 5 new API_REMEDIATION_TEMPLATES entries (api1/api3/api4/api5/api7)"
      contains: "it.todo"
    - path: "server/__tests__/apiActive/credentialsHelper.test.ts"
      provides: "Stub for listApiCredentials({apiId}) consumer contract (BOLA cred pairing)"
      contains: "it.todo"
    - path: "server/__tests__/fixtures/api-active/bola-crossaccess-response.json"
      provides: "Mock response body with foreign object data for cred B (BOLA cross-access positive case)"
    - path: "server/__tests__/fixtures/api-active/bfla-admin-success.json"
      provides: "Mock 200 response on admin-path endpoint with low-priv cred (BFLA positive case)"
    - path: "server/__tests__/fixtures/api-active/bopla-reflection-before.json"
      provides: "Seed GET response body before BOPLA injection"
    - path: "server/__tests__/fixtures/api-active/bopla-reflection-after.json"
      provides: "GET response body after BOPLA PUT/PATCH showing reflected key"
    - path: "server/__tests__/fixtures/api-active/rate-limit-burst-responses.json"
      provides: "Array of 20 x 200 responses without 429/Retry-After/X-RateLimit-* headers"
    - path: "server/__tests__/fixtures/api-active/ssrf-nuclei-interaction.jsonl"
      provides: "1 JSONL Nuclei line with interaction=true + interactsh-server match"
    - path: "shared/schema.ts"
      provides: "apiActiveTestOptsSchema Zod + ApiActiveTestOpts + ActiveTestResult + BOPLA_SENSITIVE_KEYS"
      contains: "apiActiveTestOptsSchema"
  key_links:
    - from: "server/__tests__/apiActive/*.test.ts"
      to: "vitest.config.ts"
      via: "glob include server/**/*.test.ts"
      pattern: "apiActive"
    - from: "shared/schema.ts apiActiveTestOptsSchema"
      to: "Wave 3 orchestrator + Wave 4 route handler"
      via: "Zod import + z.infer"
      pattern: "apiActiveTestOptsSchema"
    - from: "shared/schema.ts BOPLA_SENSITIVE_KEYS"
      to: "Wave 1 bopla.ts scanner"
      via: "import const array for payload iteration"
      pattern: "BOPLA_SENSITIVE_KEYS"
---

<objective>
Wave 0 — Nyquist foundation para Phase 13. Criar 10 test stubs (it.todo) em `server/__tests__/apiActive/`, 6 fixtures determinísticas para dryRun em `server/__tests__/fixtures/api-active/`, e a Zod schema `apiActiveTestOptsSchema` + types `ApiActiveTestOpts`/`ActiveTestResult` + constante `BOPLA_SENSITIVE_KEYS` em `shared/schema.ts`. Nada de implementação em src/server; apenas andaimes que Waves 1-3 consomem.

Purpose: **Sampling continuity**. Waves 1-3 não podem adicionar `<automated>` verify commands sem arquivos de teste pré-existindo. Fixtures permitem dryRun determinístico sem HTTP real nem spawn Nuclei. Zod schema é o contrato público do opts — Wave 2 (orchestrator) e Wave 3 (route + CLI) importam; tipo `ActiveTestResult` é contract consumido por Phase 15. BOPLA_SENSITIVE_KEYS é constante source-of-truth compartilhada entre bopla.ts e test.

Output: 10 stub files + 6 fixture files + 1 Zod schema + 2 types derivados + 1 constante. Zero lógica de negócio.

Structural precedent: espelha plan 12-01-PLAN.md (Phase 12 Wave 0) 1:1 com diferenças: 5 vetores novos em vez de 2, 10 arquivos de teste (vs 10 Phase 12), 6 fixtures (vs 5 Phase 12), Zod schema com 5 stages + 3 sub-objects.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/13-security-testing-active/13-CONTEXT.md
@.planning/phases/13-security-testing-active/13-RESEARCH.md
@.planning/phases/13-security-testing-active/13-VALIDATION.md

@.planning/phases/12-security-testing-passive/12-01-PLAN.md
@.planning/phases/12-security-testing-passive/12-01-SUMMARY.md

<interfaces>
<!-- From shared/schema.ts — types consumed by the new opts/result schema. -->

```typescript
// Already in shared/schema.ts (Phase 9) — 5 OWASP categories Phase 13 uses:
export const owaspApiCategoryEnum = pgEnum('owasp_api_category', [
  'api1_bola_2023', 'api2_broken_auth_2023', 'api3_bopla_2023',
  'api4_rate_limit_2023', 'api5_bfla_2023', 'api6_business_flow_2023',
  'api7_ssrf_2023', 'api8_misconfiguration_2023', 'api9_inventory_2023',
  'api10_unsafe_consumption_2023',
]);
// Phase 13 uses: api1, api3, api4, api5, api7 (all already in enum — zero schema change).

// Already in shared/schema.ts (Phase 12 line ~1900) — TEMPLATE 1:1 for apiActiveTestOptsSchema shape:
export const apiPassiveTestOptsSchema = z.object({
  stages: z.object({
    nucleiPassive: z.boolean().optional(),
    authFailure: z.boolean().optional(),
    api9Inventory: z.boolean().optional(),
  }).strict().optional(),
  credentialIdOverride: z.string().uuid().optional(),
  endpointIds: z.array(z.string().uuid()).optional(),
  dryRun: z.boolean().optional(),
  nuclei: z.object({
    rateLimit: z.number().int().min(1).max(50).optional(),
    timeoutSec: z.number().int().min(1).max(120).optional(),
  }).strict().optional(),
}).strict();
export type ApiPassiveTestOpts = z.infer<typeof apiPassiveTestOptsSchema>;

// Already in shared/schema.ts (Phase 12) — TEMPLATE 1:1 for ActiveTestResult shape:
export interface PassiveTestResult {
  apiId: string;
  stagesRun: Array<'nuclei_passive' | 'auth_failure' | 'api9_inventory'>;
  stagesSkipped: Array<{ stage: string; reason: string }>;
  findingsCreated: number;
  findingsUpdated: number;
  findingsByCategory: Record<string, number>;
  findingsBySeverity: Record<string, number>;
  cancelled: boolean;
  dryRun: boolean;
  durationMs: number;
}

// Already in shared/schema.ts (Phase 9 line ~1741) — used by ssrfNuclei.ts JSONL parse:
export const NucleiFindingSchema = BaseFindingSchema.extend({ /* ... */ });
export type NucleiFinding = z.infer<typeof NucleiFindingSchema>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zod schema apiActiveTestOptsSchema + ActiveTestResult + BOPLA_SENSITIVE_KEYS em shared/schema.ts</name>
  <files>shared/schema.ts</files>
  <read_first>
    - shared/schema.ts (linhas 1890-1932 — apiPassiveTestOptsSchema + PassiveTestResult como template direto)
    - shared/schema.ts (linhas 94-105 — owaspApiCategoryEnum confirma que api1/api3/api4/api5/api7 existem)
    - .planning/phases/13-security-testing-active/13-CONTEXT.md §"Orchestrator + shared infrastructure" — shape ApiActiveTestOpts + ActiveTestResult
    - .planning/phases/13-security-testing-active/13-CONTEXT.md §"BOPLA / Mass Assignment (TEST-05 / API3)" — lista exata 10 chaves
    - .planning/phases/13-security-testing-active/13-RESEARCH.md §"Zod schema for apiActiveTestOptsSchema" (linhas ~670-700)
    - .planning/phases/12-security-testing-passive/12-01-PLAN.md (Task 1 como template 1:1)
  </read_first>
  <behavior>
    - Test 1: apiActiveTestOptsSchema aceita `{}` (todos opts default) e retorna objeto vazio válido
    - Test 2: apiActiveTestOptsSchema aceita `{ stages: { bola: false, bfla: false, bopla: false, rateLimit: false, ssrf: false } }` (explícito off)
    - Test 3: apiActiveTestOptsSchema rejeita campo desconhecido em root (`.strict()`) — ex: `{ foo: 'bar' }` deve falhar
    - Test 4: apiActiveTestOptsSchema rejeita campo desconhecido em stages (`.strict()`) — ex: `{ stages: { unknownVector: true } }` deve falhar
    - Test 5: apiActiveTestOptsSchema aceita `{ destructiveEnabled: true }` e `{ credentialIds: ['uuid1','uuid2'] }` e `{ endpointIds: ['uuid'] }`
    - Test 6: apiActiveTestOptsSchema aceita `{ dryRun: true }`
    - Test 7: apiActiveTestOptsSchema.rateLimit aceita `{ burstSize: 20, windowMs: 2000, endpointIds: ['uuid'] }`; rejeita `burstSize=51` (max 50) e `rateLimit.endpointIds` com 6 uuids (max 5)
    - Test 8: apiActiveTestOptsSchema.ssrf aceita `{ interactshUrl: 'https://oast.me' }`; rejeita URL inválida
    - Test 9: apiActiveTestOptsSchema.bola aceita `{ maxCredentials: 4, maxIdsPerEndpoint: 3 }`; rejeita `maxCredentials=7` (max 6) e `maxIdsPerEndpoint=6` (max 5)
    - Test 10: type ActiveTestResult tem campos: apiId, stagesRun, stagesSkipped, findingsCreated, findingsUpdated, findingsByCategory, findingsBySeverity, cancelled, dryRun, durationMs, credentialsUsed
    - Test 11: stagesRun union é `'bola' | 'bfla' | 'bopla' | 'rate_limit' | 'ssrf'` (rate_limit com underscore, NÃO rateLimit camelCase — contrato estável)
    - Test 12: BOPLA_SENSITIVE_KEYS tem exatamente 10 strings na ordem: is_admin, isAdmin, admin, role, roles, permissions, superuser, owner, verified, email_verified
  </behavior>
  <action>
Adicionar ao final de `shared/schema.ts` (após o bloco `PassiveTestResult` na linha ~1932; procurar âncora `export interface PassiveTestResult {` e inserir APÓS o `}` de fechamento dessa interface):

```typescript

// ============================================================================
// Phase 13: Security Testing — Active — TEST-03, TEST-04, TEST-05, TEST-06, TEST-07
// ============================================================================

/**
 * BOPLA_SENSITIVE_KEYS — lista curada de 10 chaves sensíveis injetadas em
 * bodies de PUT/PATCH pelo scanner bopla.ts. Ordem é carry-forward de
 * 13-CONTEXT.md §"BOPLA / Mass Assignment (TEST-05 / API3)".
 *
 * Severity mapping (aplicado pelo scanner bopla.ts):
 *   - is_admin, role, superuser → critical
 *   - isAdmin, admin, roles, permissions, owner, verified, email_verified → high
 */
export const BOPLA_SENSITIVE_KEYS = [
  'is_admin',
  'isAdmin',
  'admin',
  'role',
  'roles',
  'permissions',
  'superuser',
  'owner',
  'verified',
  'email_verified',
] as const;

export type BoplaSensitiveKey = typeof BOPLA_SENSITIVE_KEYS[number];

/**
 * apiActiveTestOptsSchema — input contract for runApiActiveTests() and POST
 * /api/v1/apis/:id/test/active. Uses `.strict()` on root and every sub-object
 * to reject unknown fields (mirrors apiPassiveTestOptsSchema Phase 12 decision).
 *
 * Defaults (applied at call site by orchestrator):
 *   stages.bola=true, stages.bfla=true, stages.bopla=true (but gated by
 *   destructiveEnabled), stages.rateLimit=false (OPT-IN), stages.ssrf=true.
 *   destructiveEnabled=false (gate for BOPLA entire stage + BFLA method-based).
 *   bola.maxCredentials=4 (max 6), bola.maxIdsPerEndpoint=3 (max 5).
 *   rateLimit.burstSize=20 (max 50), rateLimit.windowMs=2000.
 *   ssrf.interactshUrl=env INTERACTSH_URL || 'oast.me'.
 */
export const apiActiveTestOptsSchema = z.object({
  stages: z.object({
    bola: z.boolean().optional(),          // default true
    bfla: z.boolean().optional(),          // default true
    bopla: z.boolean().optional(),         // default true (gated by destructiveEnabled)
    rateLimit: z.boolean().optional(),     // default false (opt-in)
    ssrf: z.boolean().optional(),          // default true
  }).strict().optional(),
  destructiveEnabled: z.boolean().optional(),  // default false — gates BOPLA + BFLA method-based
  credentialIds: z.array(z.string().uuid()).optional(),  // default = listApiCredentials({apiId})
  endpointIds: z.array(z.string().uuid()).optional(),    // default = all endpoints for API
  dryRun: z.boolean().optional(),                        // default false
  rateLimit: z.object({
    burstSize: z.number().int().min(1).max(50).optional(),  // default 20, max 50 (SAFE-01 ceiling Phase 15 owns global)
    windowMs: z.number().int().min(100).optional(),         // default 2000
    endpointIds: z.array(z.string().uuid()).max(5).optional(),  // default = 1 first GET+200 endpoint
  }).strict().optional(),
  ssrf: z.object({
    interactshUrl: z.string().url().optional(),  // default = env INTERACTSH_URL || 'oast.me'
  }).strict().optional(),
  bola: z.object({
    maxCredentials: z.number().int().min(2).max(6).optional(),        // default 4
    maxIdsPerEndpoint: z.number().int().min(1).max(5).optional(),     // default 3
  }).strict().optional(),
}).strict();

export type ApiActiveTestOpts = z.infer<typeof apiActiveTestOptsSchema>;

/**
 * ActiveTestResult — public contract returned by runApiActiveTests().
 * Consumed by Phase 15 journey executor. Extend without breaking.
 *
 * stagesRun uses snake_case 'rate_limit' (not camelCase rateLimit) for
 * consistency with Phase 12 PassiveTestResult stage-name convention.
 */
export interface ActiveTestResult {
  apiId: string;
  stagesRun: Array<'bola' | 'bfla' | 'bopla' | 'rate_limit' | 'ssrf'>;
  stagesSkipped: Array<{ stage: string; reason: string }>;
  findingsCreated: number;
  findingsUpdated: number;       // dedupe upsert path
  findingsByCategory: Record<string, number>;
  findingsBySeverity: Record<string, number>;
  cancelled: boolean;
  dryRun: boolean;
  durationMs: number;
  credentialsUsed: number;       // how many creds participated (useful for telemetry)
}
```

NÃO modificar outras partes do arquivo. Inserir APENAS o bloco acima. Verificar import `z` já existente no topo (já existe — `import { z } from "zod";`).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "shared/schema\.ts" | head -10 ; grep -c "apiActiveTestOptsSchema\|ActiveTestResult\|BOPLA_SENSITIVE_KEYS" shared/schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export const apiActiveTestOptsSchema" shared/schema.ts` retorna 1 match
    - `grep -n "export type ApiActiveTestOpts" shared/schema.ts` retorna 1 match
    - `grep -n "export interface ActiveTestResult" shared/schema.ts` retorna 1 match
    - `grep -n "export const BOPLA_SENSITIVE_KEYS" shared/schema.ts` retorna 1 match
    - `grep -c "'is_admin'\|'isAdmin'\|'admin'\|'role'\|'roles'\|'permissions'\|'superuser'\|'owner'\|'verified'\|'email_verified'" shared/schema.ts` retorna ≥ 10
    - `grep -n "\.strict()" shared/schema.ts` inclui ≥ 4 novas ocorrências (root + stages + rateLimit + ssrf + bola sub-objects)
    - `grep -n "'bola' | 'bfla' | 'bopla' | 'rate_limit' | 'ssrf'" shared/schema.ts` retorna 1 match (stagesRun union tipada)
    - `npx tsc --noEmit 2>&1 | grep "shared/schema\.ts"` NÃO produz erros novos
    - Bloco tem `credentialsUsed: number;` presente em ActiveTestResult
  </acceptance_criteria>
  <done>apiActiveTestOptsSchema Zod exportado + ApiActiveTestOpts type + ActiveTestResult interface + BOPLA_SENSITIVE_KEYS const; `tsc --noEmit` limpo; Waves 1-3 conseguem importar via `@shared/schema`.</done>
</task>

<task type="auto">
  <name>Task 2: 10 test stubs it.todo em server/__tests__/apiActive/</name>
  <files>
    server/__tests__/apiActive/optsSchema.test.ts,
    server/__tests__/apiActive/bola.test.ts,
    server/__tests__/apiActive/bfla.test.ts,
    server/__tests__/apiActive/bopla.test.ts,
    server/__tests__/apiActive/rateLimit.test.ts,
    server/__tests__/apiActive/ssrfNuclei.test.ts,
    server/__tests__/apiActive/orchestrator.test.ts,
    server/__tests__/apiActive/route.test.ts,
    server/__tests__/apiActive/remediation.test.ts,
    server/__tests__/apiActive/credentialsHelper.test.ts
  </files>
  <read_first>
    - .planning/phases/13-security-testing-active/13-VALIDATION.md §"Wave 0 Requirements" — lista exata 10 stubs
    - .planning/phases/13-security-testing-active/13-CONTEXT.md §"dryRun fixtures + Nyquist coverage" — 15 test stubs distribuídos
    - .planning/phases/13-security-testing-active/13-RESEARCH.md §"Phase Requirements → Test Map"
    - server/__tests__/apiPassive/nucleiArgs.test.ts (canonical it.todo stub pattern)
    - server/__tests__/apiPassive/orchestrator.test.ts (canonical orchestrator it.todo pattern)
    - .planning/codebase/TESTING.md §"Test Structure"
  </read_first>
  <action>
Criar 10 arquivos stub. Cada arquivo segue o pattern exato de Phase 12 (`import { describe, it } from 'vitest';` + `describe(..., () => { it.todo(...) })`). SEM `import { expect }`. SEM `it()` real. Apenas `it.todo`.

Conteúdo exato por arquivo (copiar integralmente):

**1. `server/__tests__/apiActive/optsSchema.test.ts`** (subject: `apiActiveTestOptsSchema`; TEST-03..07; Wave 0 parent of schema task):
```typescript
/**
 * Phase 13 Wave 0 — Nyquist stub for apiActiveTestOptsSchema Zod validation.
 * Implementation of the schema itself is in Wave 0 Task 1 (shared/schema.ts).
 * These stubs get turned into real assertions by Wave 0 Task 1 or earliest consumer.
 * Requirements: TEST-03, TEST-04, TEST-05, TEST-06, TEST-07
 */
import { describe, it } from 'vitest';

describe('shared/schema: apiActiveTestOptsSchema', () => {
  it.todo('accepts empty object {} (all opts default)');
  it.todo('accepts all 5 stages explicitly disabled: bola/bfla/bopla/rateLimit/ssrf = false');
  it.todo('rejects unknown field at root (.strict())');
  it.todo('rejects unknown field inside stages (.strict())');
  it.todo('accepts destructiveEnabled=true + credentialIds array + endpointIds array');
  it.todo('accepts dryRun=true');
  it.todo('rateLimit sub: accepts burstSize=20/windowMs=2000; rejects burstSize=51 (max 50)');
  it.todo('rateLimit sub: rejects rateLimit.endpointIds with 6 entries (max 5)');
  it.todo('ssrf sub: accepts interactshUrl=https://oast.me; rejects invalid URL string');
  it.todo('bola sub: accepts maxCredentials=4/maxIdsPerEndpoint=3; rejects maxCredentials=7 (max 6)');
  it.todo('bola sub: rejects maxIdsPerEndpoint=6 (max 5)');
});

describe('shared/schema: ActiveTestResult interface shape', () => {
  it.todo('has fields apiId, stagesRun, stagesSkipped, findingsCreated, findingsUpdated');
  it.todo('has fields findingsByCategory, findingsBySeverity, cancelled, dryRun, durationMs, credentialsUsed');
  it.todo('stagesRun union type is "bola" | "bfla" | "bopla" | "rate_limit" | "ssrf" (rate_limit with underscore)');
});

describe('shared/schema: BOPLA_SENSITIVE_KEYS constant', () => {
  it.todo('exports exactly 10 keys in order: is_admin, isAdmin, admin, role, roles, permissions, superuser, owner, verified, email_verified');
  it.todo('type BoplaSensitiveKey inferred from const array (not widened to string)');
});
```

**2. `server/__tests__/apiActive/bola.test.ts`** (subject: `BOLA scanner`; TEST-03; Wave 1):
```typescript
/**
 * Phase 13 Wave 0 — Nyquist stub for BOLA scanner (TEST-03 / API1).
 * Implementation comes in Wave 1 (13-02-PLAN — scanners/api/bola.ts).
 * Requirement: TEST-03
 */
import { describe, it } from 'vitest';

describe('scanners/api/bola: pairCredentials', () => {
  it.todo('generates C(n,2) ordered unique pairs from n creds (no mirrored (B,A))');
  it.todo('caps to maxCredentials (default 4) → max 6 pairs');
  it.todo('returns [] when fewer than 2 creds provided');
});

describe('scanners/api/bola: harvestObjectIds', () => {
  it.todo('extracts up to 3 IDs from JSON body fields matching /^(id|uuid|pk)$/i (case-insensitive)');
  it.todo('accepts both arrays (scans each element) and single objects');
  it.todo('returns [] when body is non-object, null, or contains no matching keys');
  it.todo('returns [] when body is not JSON-parseable (caller guards)');
});

describe('scanners/api/bola: testCrossAccess finding criterion', () => {
  it.todo('emits BolaHit severity=high when cred B gets status < 400 AND body.length > 0');
  it.todo('suppresses finding when response body contains "forbidden" / "unauthorized" / "permission denied" (case-insensitive)');
  it.todo('evidence.extractedValues includes credentialAId, credentialBId, objectId, endpointPath');
  it.todo('title is the deterministic pt-BR string "Acesso não autorizado a objeto via credencial secundária"');
  it.todo('substitutePathId replaces OpenAPI {id}/{userId}/{anyParam} tokens with harvested ID');
  it.todo('falls back to ?id=<val> appended when path has no {param} template');
});

describe('scanners/api/bola: scope filters', () => {
  it.todo('filters endpoints to method=GET only');
  it.todo('filters endpoints to requiresAuth=true only');
  it.todo('selects list-like endpoints (path has no {param} OR ends in plural/list) for harvest');
});
```

**3. `server/__tests__/apiActive/bfla.test.ts`** (subject: `BFLA scanner`; TEST-04; Wave 1):
```typescript
/**
 * Phase 13 Wave 0 — Nyquist stub for BFLA scanner (TEST-04 / API5).
 * Implementation comes in Wave 1 (13-02-PLAN — scanners/api/bfla.ts).
 * Requirement: TEST-04
 */
import { describe, it } from 'vitest';

describe('scanners/api/bfla: identifyLowPrivCreds (OR-logic 3 signals)', () => {
  it.todo('flags cred with highest priority int as low-priv (higher priority = less privilege in Phase 10 convention)');
  it.todo('flags cred whose description matches /readonly|read-only|viewer|limited/i');
  it.todo('skips BFLA stage entirely when only 1 cred exists (cannot contrast)');
  it.todo('returns empty low-priv set when all creds have equal priority AND no description match');
});

describe('scanners/api/bfla: matchAdminEndpoint path regex', () => {
  it.todo('matches paths containing /admin, /manage, /management, /system, /internal, /sudo, /superuser, /root, /console (case-insensitive, word boundary)');
  it.todo('rejects non-admin paths (/users, /products, /orders)');
  it.todo('regex anchors via \\b / / / $ — does not match "administrator" inside /administrative-view');
});

describe('scanners/api/bfla: testPrivEscalation finding criterion', () => {
  it.todo('emits BflaHit severity=high when low-priv cred gets status < 400 on admin-path AND not redirect-to-login');
  it.todo('emits severity=medium when all creds return same status (RBAC ambiguous, not contrasting)');
  it.todo('evidence.extractedValues includes credentialId, priorityLevel, matchedPattern, endpointPath');
  it.todo('title is deterministic pt-BR "Privilégio administrativo acessível via credencial de baixo privilégio"');
});

describe('scanners/api/bfla: destructive gate', () => {
  it.todo('only tests GET on admin-path by default (destructiveEnabled=false)');
  it.todo('tests PUT/PATCH/DELETE on non-admin-path only when destructiveEnabled=true');
});

describe('scanners/api/bfla: universal-credential skip', () => {
  it.todo('skips low-priv cred with log when it succeeds on 3 non-admin control endpoints (likely admin universal cred)');
});
```

**4. `server/__tests__/apiActive/bopla.test.ts`** (subject: `BOPLA scanner`; TEST-05; Wave 1):
```typescript
/**
 * Phase 13 Wave 0 — Nyquist stub for BOPLA / Mass Assignment scanner (TEST-05 / API3).
 * Implementation comes in Wave 1 (13-02-PLAN — scanners/api/bopla.ts).
 * Requirement: TEST-05
 */
import { describe, it } from 'vitest';

describe('scanners/api/bopla: destructive gate', () => {
  it.todo('entire stage is skipped when opts.destructiveEnabled !== true (with reason logged)');
  it.todo('stage proceeds when opts.destructiveEnabled === true');
});

describe('scanners/api/bopla: target method filter', () => {
  it.todo('only targets method=PUT or method=PATCH endpoints');
  it.todo('skips GET/POST/DELETE endpoints');
});

describe('scanners/api/bopla: fetchSeedBody + injection', () => {
  it.todo('fetches seed GET response on same path before injection');
  it.todo('skips endpoint with log when seed GET returns 404/401');
  it.todo('skips endpoint when seed body is not JSON-parseable (form/XML not in scope)');
  it.todo('injects one key at a time from BOPLA_SENSITIVE_KEYS (10 requests per endpoint)');
  it.todo('spread pattern: {...seedBody, [key]: injectedValue} preserves existing structure');
  it.todo('injectedValue type matches seed: boolean→true, string→"admin", array→["admin"], absent→true default');
});

describe('scanners/api/bopla: verifyReflection (deep key-path compare)', () => {
  it.todo('emits BoplaHit when PUT/PATCH status < 400 AND subsequent GET shows injected key with reflected value');
  it.todo('suppresses finding when before-value === after-value (unchanged — likely already existed)');
  it.todo('uses deep key-path compare NOT regex text match (avoids false positives)');
});

describe('scanners/api/bopla: severity mapping', () => {
  it.todo('severity=critical for keys is_admin/role/superuser');
  it.todo('severity=high for keys isAdmin/admin/roles/permissions/owner/verified/email_verified');
  it.todo('title includes injected key for dedupe variance: "Campo sensível aceito em PUT/PATCH sem validação ({{key}})"');
});
```

**5. `server/__tests__/apiActive/rateLimit.test.ts`** (subject: `rate-limit scanner`; TEST-06; Wave 1):
```typescript
/**
 * Phase 13 Wave 0 — Nyquist stub for rate-limit absence scanner (TEST-06 / API4).
 * Implementation comes in Wave 1 (13-02-PLAN — scanners/api/rateLimit.ts).
 * Requirement: TEST-06
 */
import { describe, it } from 'vitest';

describe('scanners/api/rateLimit: opt-in gate', () => {
  it.todo('entire stage skipped when opts.stages.rateLimit !== true (default false)');
  it.todo('stage proceeds when opts.stages.rateLimit === true');
});

describe('scanners/api/rateLimit: buildBurst parallelism', () => {
  it.todo('issues exactly burstSize parallel fetch calls via Promise.all (not sequential)');
  it.todo('respects burstSize=20 default, max 50 (Zod ceiling)');
  it.todo('uses method=GET with auth headers from resolveApiCredential');
});

describe('scanners/api/rateLimit: target endpoint selection', () => {
  it.todo('default: exactly 1 endpoint per API (first GET with httpxStatus=200 alphabetically)');
  it.todo('override via opts.rateLimit.endpointIds array (up to 5)');
  it.todo('filters endpoints to method=GET AND requiresAuth=true AND httpxStatus=200');
});

describe('scanners/api/rateLimit: detectThrottling (ALL-3-signals check)', () => {
  it.todo('emits RateLimitHit severity=medium when: no 429 + no Retry-After + no X-RateLimit-* + >=90% status<400');
  it.todo('suppresses finding when ANY response has status=429');
  it.todo('suppresses finding when ANY response has Retry-After header');
  it.todo('suppresses finding when ANY response has header matching /^x-ratelimit-/i');
  it.todo('suppresses finding when <90% responses have status<400 (endpoint unhealthy, not meaningful)');
  it.todo('evidence.extractedValues includes burstSize, successCount, throttledCount, hasRetryAfter, hasXRateLimitHeaders, windowMs, endpointPath');
});
```

**6. `server/__tests__/apiActive/ssrfNuclei.test.ts`** (subject: `SSRF scanner`; TEST-07; Wave 1):
```typescript
/**
 * Phase 13 Wave 0 — Nyquist stub for SSRF scanner with Nuclei + interactsh (TEST-07 / API7).
 * Implementation comes in Wave 1 (13-02-PLAN — scanners/api/ssrfNuclei.ts).
 * Requirement: TEST-07
 */
import { describe, it } from 'vitest';

describe('scanners/api/ssrfNuclei: identifyUrlParams (3 heuristics OR)', () => {
  it.todo('matches param.name via URL_LIKE_NAME_REGEX: url, redirect, redirect_uri, callback, callback_url, webhook, webhook_url, target, dest, destination, endpoint, uri, link, image_url, avatar_url, src, href, next, continue, returnTo, return_to, return (case-insensitive)');
  it.todo('matches param.type === "url" OR param.format === "uri" OR param.format === "url"');
  it.todo('matches param.example parseable by new URL() (without throwing)');
  it.todo('rejects params that match none of the 3 heuristics (e.g., name=userId, type=string, no example)');
});

describe('scanners/api/ssrfNuclei: buildSsrfNucleiArgs', () => {
  it.todo('includes -tags ssrf (ONLY ssrf tag, not misconfig/exposure)');
  it.todo('does NOT include -ni flag (interactsh MUST be enabled for SSRF OOB detection)');
  it.todo('includes -interactions-poll-duration 5s, -interactions-wait 10s, -interactions-retries-count 3');
  it.todo('includes -interactsh-url <url> when opts.interactshUrl OR env INTERACTSH_URL is set');
  it.todo('omits -interactsh-url when neither opts nor env is set (Nuclei defaults to oast.me)');
  it.todo('includes -rl 10 default, -timeout 30 (longer for OOB callback), -retries 0, -silent, -jsonl');
  it.todo('includes -t /tmp/nuclei/nuclei-templates (preflight-managed dir)');
});

describe('scanners/api/ssrfNuclei: finding criterion', () => {
  it.todo('emits SsrfHit when Nuclei JSONL reports interaction=true OR extracted-results contains callback URL match');
  it.todo('severity maps from Nuclei info→low, low→low, medium→medium, high→high, critical→critical');
  it.todo('title is "SSRF confirmado via interação out-of-band em parâmetro {{paramName}}" (paramName substituted)');
  it.todo('evidence.extractedValues includes paramName, interactsh_interaction_type (dns|http), interactshUrl masked (prefix-3 + ***)');
  it.todo('stage runs authenticated via resolveApiCredential (SSRF endpoints typically post-login)');
});

describe('scanners/api/ssrfNuclei: skip early when no URL params', () => {
  it.todo('skips stage with reason "no URL-like params found" when identifyUrlParams returns empty list');
  it.todo('avoids preflightNuclei call when no targets');
});
```

**7. `server/__tests__/apiActive/orchestrator.test.ts`** (subject: `runApiActiveTests orchestrator`; all requirements; Wave 2):
```typescript
/**
 * Phase 13 Wave 0 — Nyquist stub for orchestrator.
 * Implementation comes in Wave 2 (13-03-PLAN — journeys/apiActiveTests.ts).
 * Requirements: TEST-03, TEST-04, TEST-05, TEST-06, TEST-07
 */
import { describe, it } from 'vitest';

describe('journeys/apiActiveTests: runApiActiveTests', () => {
  it.todo('runs 5 stages in order: bola → bfla → bopla → rate_limit → ssrf');
  it.todo('honors opts.stages.X=false to skip stage (stagesSkipped populated with reason)');
  it.todo('dryRun=true: reads fixtures from server/__tests__/fixtures/api-active/, no spawn/fetch, findings prefixed [DRY-RUN]');
  it.todo('cancellation via jobQueue.isJobCancelled between stages sets cancelled=true');
  it.todo('cancellation also checked between endpoints within long-running BOLA loop');
  it.todo('preserves partial findings on cancel (no rollback)');
  it.todo('preflightNuclei called ONLY before ssrf stage (not before bola/bfla/bopla/rateLimit)');
  it.todo('preflightNuclei failure: skip ssrf stage only, continue others');
  it.todo('durationMs populated; findingsByCategory/findingsBySeverity counts match persistHit actions');
  it.todo('credentialsUsed reports how many unique creds participated across stages');
  it.todo('BOPLA stage auto-skips when opts.destructiveEnabled !== true (gate enforced at orchestrator, stage function, or both)');
  it.todo('rateLimit stage auto-skips when opts.stages.rateLimit !== true (default false)');
  it.todo('each stage failure logs + push to stagesSkipped; pipeline continues without abort');
  it.todo('uses storage.upsertApiFindingByKey(endpointId, owaspCategory, title, data) for every hit (dedupe)');
  it.todo('resolves creds via listApiCredentials({apiId}) for BOLA cred pairing');
});
```

**8. `server/__tests__/apiActive/route.test.ts`** (subject: `POST /test/active`; all requirements; Wave 3):
```typescript
/**
 * Phase 13 Wave 0 — Nyquist stub for route handler + CLI.
 * Implementation comes in Wave 3 (13-04-PLAN — routes/apis.ts + scripts/runApiActiveTests.ts).
 * Requirements: TEST-03, TEST-04, TEST-05, TEST-06, TEST-07
 */
import { describe, it } from 'vitest';

describe('POST /api/v1/apis/:id/test/active', () => {
  it.todo('requires authentication (401 if not logged)');
  it.todo('requires operator or global_administrator role (403 if readonly_analyst)');
  it.todo('rejects body with unknown field via Zod .strict() (400)');
  it.todo('returns 404 when apiId does not exist');
  it.todo('parses body as apiActiveTestOptsSchema and passes to runApiActiveTests');
  it.todo('returns 201 with ActiveTestResult shape on success');
  it.todo('writes audit log entry with actorId + action=api_active_test_started + objectType=api + objectId=apiId + before=null + after={jobId, stages, destructiveEnabled}');
  it.todo('returns 500 with pt-BR message when runApiActiveTests throws');
  it.todo('pt-BR error messages ("API não encontrada", "Opções de teste ativo inválidas")');
});

describe('CLI server/scripts/runApiActiveTests.ts', () => {
  it.todo('--help prints usage to stderr and exits 0');
  it.todo('--api=<uuid> required; exits 1 with error when missing');
  it.todo('supports toggle flags --no-bola / --no-bfla / --no-bopla / --no-ssrf / --rate-limit / --destructive / --dry-run');
  it.todo('supports --credential=<uuid> (repeatable) for credentialIds override');
  it.todo('prints ActiveTestResult as JSON to stdout, progress/logs to stderr');
  it.todo('exit code 0 on success, 2 on cancelled, 1 on error');
  it.todo('uses import.meta.url === pathToFileURL(process.argv[1]).href guard (test-safe)');
});
```

**9. `server/__tests__/apiActive/remediation.test.ts`** (subject: `API_REMEDIATION_TEMPLATES extensions`; all requirements; Wave 1):
```typescript
/**
 * Phase 13 Wave 0 — Nyquist stub for 5 new remediation template entries.
 * Implementation comes in Wave 1 (13-02-PLAN — shared/apiRemediationTemplates.ts).
 * Requirements: TEST-03, TEST-04, TEST-05, TEST-06, TEST-07
 */
import { describe, it } from 'vitest';

describe('shared/apiRemediationTemplates: Phase 13 extensions', () => {
  it.todo('exports API_REMEDIATION_TEMPLATES with existing Phase 12 keys preserved (api2_broken_auth_2023, api8_misconfiguration_2023, api9_inventory_2023)');
  it.todo('adds api1_bola_2023 pt-BR string (object-level ACL guidance)');
  it.todo('adds api3_bopla_2023 pt-BR string (allow-list explícita + reject sensitive properties)');
  it.todo('adds api4_rate_limit_2023 pt-BR string (429 + Retry-After guidance)');
  it.todo('adds api5_bfla_2023 pt-BR string (RBAC backend validation)');
  it.todo('adds api7_ssrf_2023 pt-BR string (URL allow-list + block private ranges + cloud metadata 169.254.169.254)');
  it.todo('type ApiRemediationTemplate still derived via typeof (no manual interface)');
});
```

**10. `server/__tests__/apiActive/credentialsHelper.test.ts`** (subject: `listApiCredentials({apiId}) consumer contract`; TEST-03+TEST-04; Wave 1 through orchestrator):
```typescript
/**
 * Phase 13 Wave 0 — Nyquist stub for cred-listing consumer contract.
 * Implementation comes in Wave 1/Wave 2 (13-02-PLAN / 13-03-PLAN — bola.ts / orchestrator).
 * No new storage method is added; this covers the consumer pattern.
 * Requirements: TEST-03, TEST-04
 */
import { describe, it } from 'vitest';

describe('BOLA + BFLA: listApiCredentials({apiId}) consumer pattern', () => {
  it.todo('BOLA scanner calls listApiCredentials({apiId}) to fetch all creds for pairing');
  it.todo('BOLA caps list to bola.maxCredentials (default 4) before pairing');
  it.todo('BFLA scanner consumes same listApiCredentials({apiId}) output for low-priv ranking');
  it.todo('returns list sorted by priority ASC (Phase 10 storage default) — highest priority int = lowest privilege');
  it.todo('filters unsupported auth types (hmac/oauth2_client_credentials/mtls): logs skip + omits from test set');
  it.todo('supported auth types for Phase 13: bearer_jwt, api_key_header, api_key_query, basic');
  it.todo('orchestrator: credentialIds override replaces listApiCredentials result when provided');
});
```

NÃO adicionar `import { expect }` ou assertions reais. Apenas `it.todo` literais.
  </action>
  <verify>
    <automated>ls server/__tests__/apiActive/ 2>&1 | wc -l ; grep -c "it.todo" server/__tests__/apiActive/*.test.ts 2>&1 | tail -15 ; npx vitest list server/__tests__/apiActive 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `ls server/__tests__/apiActive/ | wc -l` retorna `10`
    - `grep -l "it.todo" server/__tests__/apiActive/*.test.ts | wc -l` retorna `10`
    - `grep -c "it.todo" server/__tests__/apiActive/*.test.ts | awk -F: '{s+=$2} END {print s}'` retorna ≥ 70 (cobertura Nyquist; Phase 12 atingiu 69)
    - `grep -l "import.*expect" server/__tests__/apiActive/*.test.ts | wc -l` retorna `0` (sem expect em stubs)
    - `grep -L "it\.todo" server/__tests__/apiActive/*.test.ts | wc -l` retorna `0` (todos arquivos têm ≥ 1 it.todo)
    - `grep -c "describe" server/__tests__/apiActive/*.test.ts | awk -F: '$2 < 1' | wc -l` retorna `0` (todos arquivos têm ≥ 1 describe)
    - `npx vitest list server/__tests__/apiActive 2>&1` enumera 10 arquivos sem erros de parse
    - Todos 10 arquivos exatos conforme lista `<files>`; nenhum arquivo extra além desses
  </acceptance_criteria>
  <done>10 test files criados com `it.todo` cobrindo todas 5 requisitos + orchestrator + route + remediation + credentials helper; vitest coleta sem erros; Waves 1-3 podem referenciar como `<automated>` alvos.</done>
</task>

<task type="auto">
  <name>Task 3: 6 fixtures determinísticas para dryRun em server/__tests__/fixtures/api-active/</name>
  <files>
    server/__tests__/fixtures/api-active/bola-crossaccess-response.json,
    server/__tests__/fixtures/api-active/bfla-admin-success.json,
    server/__tests__/fixtures/api-active/bopla-reflection-before.json,
    server/__tests__/fixtures/api-active/bopla-reflection-after.json,
    server/__tests__/fixtures/api-active/rate-limit-burst-responses.json,
    server/__tests__/fixtures/api-active/ssrf-nuclei-interaction.jsonl
  </files>
  <read_first>
    - .planning/phases/13-security-testing-active/13-VALIDATION.md §"Wave 0 Requirements" — lista 6 fixtures exatas
    - .planning/phases/13-security-testing-active/13-CONTEXT.md §"dryRun fixtures + Nyquist coverage" — shape descritivo de cada
    - server/__tests__/fixtures/api-passive/ (5 arquivos existentes como padrão de formato)
    - server/__tests__/fixtures/api-passive/nuclei-passive-mock.jsonl (JSONL shape canonical)
    - server/__tests__/fixtures/api-passive/api-key-leakage-body.json (JSON response shape canonical)
    - shared/schema.ts linhas 1737-1764 (NucleiFindingSchema — shape de JSONL line)
  </read_first>
  <action>
Criar 6 fixtures. Conteúdo exato:

**1. `server/__tests__/fixtures/api-active/bola-crossaccess-response.json`** — mock de response onde cred B consegue ver objeto alheio:
```json
{
  "status": 200,
  "headers": {
    "content-type": "application/json",
    "x-request-id": "test-bola-crossaccess-001"
  },
  "body": "{\"id\":\"77e54321-aaaa-bbbb-cccc-ddddeeeeffff\",\"email\":\"victim@example.internal\",\"account_balance\":12500.00,\"orders\":[{\"id\":\"ord-abc123\",\"total\":450.00}]}",
  "describedBy": "Response obtained by cred B accessing cred A's object ID — cross-identity BOLA positive",
  "credentialBId": "mock-cred-b-uuid",
  "objectIdHarvested": "77e54321-aaaa-bbbb-cccc-ddddeeeeffff",
  "endpointPath": "/users/{id}"
}
```

**2. `server/__tests__/fixtures/api-active/bfla-admin-success.json`** — mock 200 em admin-path com cred low-priv:
```json
{
  "status": 200,
  "headers": {
    "content-type": "application/json",
    "x-request-id": "test-bfla-admin-001"
  },
  "body": "{\"users\":[{\"id\":1,\"email\":\"admin@example.internal\",\"role\":\"admin\"},{\"id\":2,\"email\":\"alice@example.internal\",\"role\":\"user\"}],\"total\":2}",
  "describedBy": "Admin endpoint accessible via low-priv credential — RBAC failure, BFLA positive",
  "credentialId": "mock-lowpriv-cred-uuid",
  "priorityLevel": 3,
  "matchedPattern": "admin",
  "endpointPath": "/admin/users"
}
```

**3. `server/__tests__/fixtures/api-active/bopla-reflection-before.json`** — seed GET antes da injection:
```json
{
  "status": 200,
  "headers": {
    "content-type": "application/json",
    "x-request-id": "test-bopla-before-001"
  },
  "body": "{\"id\":\"user-42\",\"name\":\"Alice\",\"email\":\"alice@example.internal\",\"verified\":false}",
  "describedBy": "Seed response body before BOPLA sensitive-key injection (no is_admin present)",
  "endpointPath": "/users/{id}"
}
```

**4. `server/__tests__/fixtures/api-active/bopla-reflection-after.json`** — GET após PUT com is_admin injetado:
```json
{
  "status": 200,
  "headers": {
    "content-type": "application/json",
    "x-request-id": "test-bopla-after-001"
  },
  "body": "{\"id\":\"user-42\",\"name\":\"Alice\",\"email\":\"alice@example.internal\",\"verified\":false,\"is_admin\":true}",
  "describedBy": "Response after PUT with injected is_admin:true — mass assignment accepted, reflection confirmed",
  "endpointPath": "/users/{id}",
  "injectedKey": "is_admin",
  "originalValue": null,
  "reflectedValue": true
}
```

**5. `server/__tests__/fixtures/api-active/rate-limit-burst-responses.json`** — array de 20 responses sem throttle signals:
```json
{
  "burstSize": 20,
  "windowMs": 2000,
  "endpointPath": "/users",
  "responses": [
    {"status": 200, "headers": {"content-type": "application/json"}, "body": "{\"ok\":true}"},
    {"status": 200, "headers": {"content-type": "application/json"}, "body": "{\"ok\":true}"},
    {"status": 200, "headers": {"content-type": "application/json"}, "body": "{\"ok\":true}"},
    {"status": 200, "headers": {"content-type": "application/json"}, "body": "{\"ok\":true}"},
    {"status": 200, "headers": {"content-type": "application/json"}, "body": "{\"ok\":true}"},
    {"status": 200, "headers": {"content-type": "application/json"}, "body": "{\"ok\":true}"},
    {"status": 200, "headers": {"content-type": "application/json"}, "body": "{\"ok\":true}"},
    {"status": 200, "headers": {"content-type": "application/json"}, "body": "{\"ok\":true}"},
    {"status": 200, "headers": {"content-type": "application/json"}, "body": "{\"ok\":true}"},
    {"status": 200, "headers": {"content-type": "application/json"}, "body": "{\"ok\":true}"},
    {"status": 200, "headers": {"content-type": "application/json"}, "body": "{\"ok\":true}"},
    {"status": 200, "headers": {"content-type": "application/json"}, "body": "{\"ok\":true}"},
    {"status": 200, "headers": {"content-type": "application/json"}, "body": "{\"ok\":true}"},
    {"status": 200, "headers": {"content-type": "application/json"}, "body": "{\"ok\":true}"},
    {"status": 200, "headers": {"content-type": "application/json"}, "body": "{\"ok\":true}"},
    {"status": 200, "headers": {"content-type": "application/json"}, "body": "{\"ok\":true}"},
    {"status": 200, "headers": {"content-type": "application/json"}, "body": "{\"ok\":true}"},
    {"status": 200, "headers": {"content-type": "application/json"}, "body": "{\"ok\":true}"},
    {"status": 200, "headers": {"content-type": "application/json"}, "body": "{\"ok\":true}"},
    {"status": 200, "headers": {"content-type": "application/json"}, "body": "{\"ok\":true}"}
  ],
  "describedBy": "20 consecutive 200 responses with no 429, no Retry-After, no X-RateLimit-* headers — rate-limit absence positive"
}
```

**6. `server/__tests__/fixtures/api-active/ssrf-nuclei-interaction.jsonl`** — 1 linha JSONL Nuclei com `interaction=true`:
```
{"template":"http/vulnerabilities/generic/ssrf/oast-ssrf.yaml","template-id":"oast-ssrf","template-url":"https://cloud.projectdiscovery.io/public/oast-ssrf","template-path":"/tmp/nuclei/nuclei-templates/http/vulnerabilities/generic/ssrf/oast-ssrf.yaml","info":{"name":"Out-of-band SSRF Detection","author":["pdteam"],"tags":["ssrf","oast","generic"],"description":"Out-of-band SSRF detected via interactsh callback.","severity":"high","reference":["https://owasp.org/www-community/attacks/Server_Side_Request_Forgery"]},"type":"http","host":"https://example.internal","matched-at":"https://example.internal/api/v1/webhooks?url=https://cabc***.oast.me","matcher-name":"interactsh-http","extracted-results":["cabc1234.oast.me"],"request":"GET /api/v1/webhooks?url=https://cabc1234.oast.me HTTP/1.1\nHost: example.internal\nAuthorization: Bearer <redacted>\n","response":"HTTP/1.1 200 OK\nContent-Type: application/json\n\n{\"status\":\"webhook scheduled\"}","ip":"10.0.0.5","timestamp":"2026-04-20T10:00:00Z","curl-command":"curl -H 'Authorization: Bearer <redacted>' 'https://example.internal/api/v1/webhooks?url=https://cabc1234.oast.me'","matcher-status":true,"matched-line":null,"interactsh-server":"oast.me","interactsh-interaction-type":"http"}
```

Criar pasta `server/__tests__/fixtures/api-active/` se não existe. Fixtures versionadas via git SHA (sem checksum explícito — não são binários).
  </action>
  <verify>
    <automated>ls -la server/__tests__/fixtures/api-active/ && echo "--- JSONL ---" && wc -l server/__tests__/fixtures/api-active/ssrf-nuclei-interaction.jsonl && echo "--- JSON validity ---" && for f in server/__tests__/fixtures/api-active/*.json; do node -e "JSON.parse(require('fs').readFileSync('$f','utf8'));console.log('OK: $f');"; done</automated>
  </verify>
  <acceptance_criteria>
    - `ls server/__tests__/fixtures/api-active/ | wc -l` retorna `6`
    - `wc -l server/__tests__/fixtures/api-active/ssrf-nuclei-interaction.jsonl` retorna ≥ 1
    - Cada linha do JSONL é JSON válido — `while read l; do echo "$l" | node -e 'JSON.parse(require("fs").readFileSync(0,"utf8"))'; done < server/__tests__/fixtures/api-active/ssrf-nuclei-interaction.jsonl` sem erro
    - JSONL contém string `"interactsh-server"` — `grep -c "interactsh-server" server/__tests__/fixtures/api-active/ssrf-nuclei-interaction.jsonl` retorna ≥ 1
    - JSONL contém tag `ssrf` — `grep -c '"ssrf"' server/__tests__/fixtures/api-active/ssrf-nuclei-interaction.jsonl` retorna ≥ 1
    - `rate-limit-burst-responses.json` tem ≥ 20 elementos em responses — `node -e "const f=require('./server/__tests__/fixtures/api-active/rate-limit-burst-responses.json'); console.log(f.responses.length)"` imprime `20`
    - Rate-limit fixture NÃO contém `429`, `Retry-After`, nem `X-RateLimit` headers: `grep -c "429\|Retry-After\|X-RateLimit\|x-ratelimit" server/__tests__/fixtures/api-active/rate-limit-burst-responses.json` retorna `0`
    - `bopla-reflection-before.json` NÃO contém `is_admin` no body; `bopla-reflection-after.json` contém `is_admin\":true` — verificado via grep
    - `grep -c "is_admin" server/__tests__/fixtures/api-active/bopla-reflection-before.json` retorna `0`
    - `grep -c "is_admin\\\\\":true" server/__tests__/fixtures/api-active/bopla-reflection-after.json` retorna ≥ 1
    - Todos 5 arquivos `.json` passam `JSON.parse` sem erro
    - `bfla-admin-success.json` body tem `/admin` no describedBy OR endpointPath: `grep -c "admin" server/__tests__/fixtures/api-active/bfla-admin-success.json` retorna ≥ 2
    - `bola-crossaccess-response.json` tem status 200 e body.length > 0: `node -e "const f=require('./server/__tests__/fixtures/api-active/bola-crossaccess-response.json'); console.log(f.status, f.body.length > 0)"` imprime `200 true`
  </acceptance_criteria>
  <done>6 fixtures criadas e versionadas; JSONL tem 1 linha válida com interaction=true + interactsh-server; rate-limit com 20 responses clean (sem throttle signals); BOPLA par before/after mostra injection reflection; BOLA + BFLA mock positives para dryRun determinístico.</done>
</task>

</tasks>

<verification>
Run after all tasks complete:

```bash
# 1) Type check
npx tsc --noEmit 2>&1 | grep -E "shared/schema\.ts|apiActive" | head -10

# 2) Vitest coleta os stubs
npx vitest list server/__tests__/apiActive

# 3) Vitest executa os stubs (todos it.todo = 0 passed, 0 failed, N skipped)
npx vitest run server/__tests__/apiActive --reporter=default

# 4) Contagem final
echo "stubs=$(ls server/__tests__/apiActive/ | wc -l) fixtures=$(ls server/__tests__/fixtures/api-active/ | wc -l)"
# Expected: stubs=10 fixtures=6

# 5) Schema exports
grep -c "apiActiveTestOptsSchema\|ActiveTestResult\|BOPLA_SENSITIVE_KEYS" shared/schema.ts
# Expected: ≥ 4 matches
```
</verification>

<success_criteria>
- [ ] `shared/schema.ts` exporta `apiActiveTestOptsSchema`, `ApiActiveTestOpts`, `ActiveTestResult`, `BOPLA_SENSITIVE_KEYS`, `BoplaSensitiveKey`
- [ ] `npx tsc --noEmit` sem erros novos em `shared/schema.ts`
- [ ] 10 arquivos de teste em `server/__tests__/apiActive/` (apenas `it.todo`)
- [ ] 6 fixtures em `server/__tests__/fixtures/api-active/` (5 JSON + 1 JSONL)
- [ ] `npx vitest run server/__tests__/apiActive` passa (0 fail, ≥70 todo)
- [ ] JSONL tem 1 linha válida com `"interaction-type":"http"` ou `"interactsh-server":"oast.me"`
- [ ] Waves 1-3 podem referenciar stubs como `<automated>` alvos
- [ ] BOPLA_SENSITIVE_KEYS tem exatamente 10 entries na ordem definida em CONTEXT.md
- [ ] Schema `.strict()` aplicado em root + stages + rateLimit + ssrf + bola sub-objects (≥ 5 ocorrências de .strict())
</success_criteria>

<output>
After completion, create `.planning/phases/13-security-testing-active/13-01-SUMMARY.md`
</output>
