---
phase: 13-security-testing-active
plan: 03
type: execute
wave: 2
depends_on:
  - "13-security-testing-active/13-01"
  - "13-security-testing-active/13-02"
files_modified:
  - server/services/journeys/apiActiveTests.ts
autonomous: true
requirements:
  - TEST-03
  - TEST-04
  - TEST-05
  - TEST-06
  - TEST-07
must_haves:
  truths:
    - "runApiActiveTests(apiId, opts, jobId?) executa 5 stages em ordem: bola → bfla → bopla → rate_limit → ssrf"
    - "opts.stages.bopla=true (padrão) mas stage skipa com log se opts.destructiveEnabled=false (padrão) — gate obrigatório"
    - "opts.stages.rateLimit=false (padrão opt-in) — stage sempre skipa a menos que habilitado explicitamente"
    - "opts.dryRun=true lê fixtures de server/__tests__/fixtures/api-active/ e prefixa títulos com '[DRY-RUN]' — sem HTTP real nem spawn Nuclei"
    - "Cancelamento cooperativo via jobQueue.isJobCancelled(jobId) entre cada stage E dentro do loop BOLA credential-pair"
    - "Resultados parciais persistem em cancel (sem rollback — padrão Phase 11/12)"
    - "SSRF stage chama preflightNuclei(log) antes de spawn — falha de preflight skipa stage (não aborta pipeline)"
    - "ActiveTestResult preenchido: stagesRun/stagesSkipped/findingsCreated/findingsUpdated/findingsByCategory/findingsBySeverity/cancelled/dryRun/durationMs/credentialsUsed"
    - "Todos findings persistidos via storage.upsertApiFindingByKey — dedupe em (endpointId, owaspCategory, title)"
    - "NUNCA logar secrets de credenciais (SAFE-06) — credentialId mascara apenas prefix-3 em logs"
  artifacts:
    - path: "server/services/journeys/apiActiveTests.ts"
      provides: "Orchestrator runApiActiveTests — compõe 5 scanners Wave 1"
      exports: ["runApiActiveTests"]
      min_lines: 280
  key_links:
    - from: "apiActiveTests.ts"
      to: "bola.ts testCrossAccess + pairCredentials"
      via: "import + credentialIds via listApiCredentials({apiId}) + stage bola"
      pattern: "testCrossAccess|pairCredentials"
    - from: "apiActiveTests.ts"
      to: "bfla.ts identifyLowPrivCreds + matchAdminEndpoint + testPrivEscalation"
      via: "import + stage bfla"
      pattern: "identifyLowPrivCreds|testPrivEscalation"
    - from: "apiActiveTests.ts"
      to: "bopla.ts fetchSeedBody + injectSensitiveKey + verifyReflection"
      via: "import + destructiveEnabled gate + stage bopla"
      pattern: "fetchSeedBody|injectSensitiveKey|verifyReflection"
    - from: "apiActiveTests.ts"
      to: "rateLimit.ts buildBurst + detectThrottling"
      via: "import + rateLimit opt-in gate + stage rate_limit"
      pattern: "buildBurst|detectThrottling"
    - from: "apiActiveTests.ts"
      to: "ssrfNuclei.ts identifyUrlParams + runSsrfNuclei"
      via: "import + preflightNuclei guard + stage ssrf"
      pattern: "identifyUrlParams|runSsrfNuclei"
    - from: "apiActiveTests.ts"
      to: "storage.upsertApiFindingByKey"
      via: "persistHit helper — dedupe by (endpointId, category, title)"
      pattern: "upsertApiFindingByKey"
    - from: "apiActiveTests.ts"
      to: "storage.listApiCredentials({apiId})"
      via: "credentialIds load at orchestrator entry (shared across stages)"
      pattern: "listApiCredentials"
    - from: "apiActiveTests.ts"
      to: "jobQueue.isJobCancelled(jobId)"
      via: "checkCancel() helper — entre stages + dentro de BOLA pair loop"
      pattern: "isJobCancelled"
    - from: "apiActiveTests.ts"
      to: "server/__tests__/fixtures/api-active/ (dryRun)"
      via: "readFile fixture paths quando opts.dryRun=true"
      pattern: "fixtures/api-active"
---

<objective>
Wave 2 — Orchestrator `runApiActiveTests`. Compõe os 5 scanners de Wave 1 em um único pipeline sequencial, persiste findings via dedupe, suporta dryRun determinístico (fixtures Wave 0), cancelamento cooperativo e gate de BOPLA/rateLimit.

Purpose: Monta a capacidade end-to-end de testing ativo. A função pura `runApiActiveTests(apiId, opts, jobId?)` é o contrato consumido por Wave 3 (rota + CLI) e por Phase 15 (journey executor).

Output: 1 arquivo novo `server/services/journeys/apiActiveTests.ts`. Sem rotas HTTP nem CLI (Wave 3).
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/13-security-testing-active/13-CONTEXT.md
@.planning/phases/13-security-testing-active/13-RESEARCH.md
@.planning/phases/13-security-testing-active/13-01-PLAN.md
@.planning/phases/13-security-testing-active/13-02-PLAN.md

@server/services/journeys/apiPassiveTests.ts
@server/services/journeys/apiDiscovery.ts
@server/services/journeys/nucleiPreflight.ts

<interfaces>
<!-- Key types and contracts para consumo direto. Extraídos de Waves 0-1 e Phase 12. -->

From shared/schema.ts (Wave 0):
```typescript
export const apiActiveTestOptsSchema = z.object({
  stages: z.object({
    bola: z.boolean().default(true),
    bfla: z.boolean().default(true),
    bopla: z.boolean().default(true),
    rateLimit: z.boolean().default(false),   // opt-in obrigatório
    ssrf: z.boolean().default(true),
  }).default({}),
  destructiveEnabled: z.boolean().default(false),
  credentialIds: z.array(z.string().uuid()).optional(),
  endpointIds: z.array(z.string().uuid()).optional(),
  dryRun: z.boolean().default(false),
  rateLimit: z.object({
    burstSize: z.number().int().min(1).max(50).default(20),
    windowMs: z.number().int().min(100).default(2000),
    endpointIds: z.array(z.string().uuid()).optional(),
  }).optional(),
  ssrf: z.object({
    interactshUrl: z.string().url().optional(),
  }).optional(),
  bola: z.object({
    maxCredentials: z.number().int().min(2).max(6).default(4),
    maxIdsPerEndpoint: z.number().int().min(1).max(5).default(3),
  }).optional(),
}).strict();
export type ApiActiveTestOpts = z.infer<typeof apiActiveTestOptsSchema>;

export interface ActiveTestResult {
  apiId: string;
  stagesRun: Array<'bola' | 'bfla' | 'bopla' | 'rate_limit' | 'ssrf'>;
  stagesSkipped: Array<{ stage: string; reason: string }>;
  findingsCreated: number;
  findingsUpdated: number;
  findingsByCategory: Record<string, number>;
  findingsBySeverity: Record<string, number>;
  cancelled: boolean;
  dryRun: boolean;
  durationMs: number;
  credentialsUsed: number;
}
```

From server/services/scanners/api/bola.ts (Wave 1):
```typescript
export interface BolaHit {
  endpointId: string;
  owaspCategory: 'api1_bola_2023';
  severity: 'high';
  title: string;
  description?: string;
  remediation?: string;
  evidence: ApiFindingEvidence;
}
export async function testCrossAccess(params: {
  endpoint: { id: string; path: string; method: string };
  baseUrl: string;
  credentialA: ApiCredentialWithSecret;
  credentialB: ApiCredentialWithSecret;
  harvestedIds: string[];
  dryRun?: boolean;
  dryRunFixturePath?: string;
}): Promise<BolaHit[]>;
export async function harvestObjectIds(params: {
  endpoint: { id: string; path: string };
  baseUrl: string;
  credential: ApiCredentialWithSecret;
  maxIds: number;
}): Promise<string[]>;
export function pairCredentials(creds: ApiCredentialWithSecret[], maxCreds: number): Array<[ApiCredentialWithSecret, ApiCredentialWithSecret]>;
```

From server/services/scanners/api/bfla.ts (Wave 1):
```typescript
export interface BflaHit {
  endpointId: string;
  owaspCategory: 'api5_bfla_2023';
  severity: 'high' | 'medium';
  title: string;
  description?: string;
  remediation?: string;
  evidence: ApiFindingEvidence;
}
export function identifyLowPrivCreds(creds: ApiCredentialWithSecret[]): ApiCredentialWithSecret[];
export function matchAdminEndpoint(endpoint: { path: string }): boolean;
export async function testPrivEscalation(params: {
  endpoint: { id: string; path: string; method: string };
  baseUrl: string;
  lowPrivCred: ApiCredentialWithSecret;
  allCreds: ApiCredentialWithSecret[];
  dryRun?: boolean;
  dryRunFixturePath?: string;
}): Promise<BflaHit | null>;
```

From server/services/scanners/api/bopla.ts (Wave 1):
```typescript
export interface BoplaHit {
  endpointId: string;
  owaspCategory: 'api3_bopla_2023';
  severity: 'critical' | 'high';
  title: string;
  description?: string;
  remediation?: string;
  evidence: ApiFindingEvidence;
}
export async function fetchSeedBody(params: {
  endpoint: { id: string; path: string };
  baseUrl: string;
  credential: ApiCredentialWithSecret;
}): Promise<Record<string, unknown> | null>;
export async function injectSensitiveKey(params: {
  endpoint: { id: string; path: string; method: string };
  baseUrl: string;
  credential: ApiCredentialWithSecret;
  seedBody: Record<string, unknown>;
  key: string;
  value: unknown;
}): Promise<{ status: number; body: string }>;
export async function verifyReflection(params: {
  endpoint: { id: string; path: string };
  baseUrl: string;
  credential: ApiCredentialWithSecret;
  injectedKey: string;
  seedBodyValue: unknown;
}): Promise<{ reflected: boolean; reflectedValue: unknown }>;
```

From server/services/scanners/api/rateLimit.ts (Wave 1):
```typescript
export interface RateLimitHit {
  endpointId: string;
  owaspCategory: 'api4_rate_limit_2023';
  severity: 'medium';
  title: string;
  description?: string;
  remediation?: string;
  evidence: ApiFindingEvidence;
}
export async function buildBurst(params: {
  endpoint: { id: string; path: string };
  baseUrl: string;
  credential: ApiCredentialWithSecret;
  burstSize: number;
}): Promise<Array<{ status: number; headers: Record<string, string> }>>;
export function detectThrottling(responses: Array<{ status: number; headers: Record<string, string> }>): { throttled: boolean; reason?: string };
export function buildRateLimitHit(params: {
  endpointId: string;
  endpointPath: string;
  burstSize: number;
  responses: Array<{ status: number; headers: Record<string, string> }>;
}): RateLimitHit;
```

From server/services/scanners/api/ssrfNuclei.ts (Wave 1):
```typescript
export interface SsrfHit {
  endpointId: string;
  owaspCategory: 'api7_ssrf_2023';
  severity: 'low' | 'medium' | 'high' | 'critical';
  title: string;
  description?: string;
  remediation?: string;
  evidence: ApiFindingEvidence;
}
export function identifyUrlParams(endpoint: {
  query_params?: Array<{ name: string; type?: string; format?: string; example?: unknown }>;
  request_params?: Array<{ name: string; type?: string; format?: string; example?: unknown }>;
  request_schema?: unknown;
}): string[];
export async function runSsrfNuclei(params: {
  endpoints: Array<{ id: string; fullUrl: string; urlParamNames: string[] }>;
  credential?: ApiCredentialWithSecret | null;
  interactshUrl?: string;
  dryRun?: boolean;
  dryRunFixturePath?: string;
  signal?: AbortSignal;
}): Promise<SsrfHit[]>;
```

From server/storage/apiCredentials.ts (Phase 10):
```typescript
export async function listApiCredentials(filter: { apiId: string }): Promise<ApiCredentialSafe[]>;
export async function getApiCredentialWithSecret(id: string): Promise<ApiCredentialWithSecret | undefined>;
```

From server/storage/apiFindings.ts (Phase 12):
```typescript
export async function upsertApiFindingByKey(
  endpointId: string,
  owaspCategory: InsertApiFinding['owaspCategory'],
  title: string,
  data: InsertApiFinding,
): Promise<{ finding: ApiFinding; action: 'inserted' | 'updated' }>;
```

From server/services/jobQueue.ts (Phase 11):
```typescript
export const jobQueue = {
  isJobCancelled(jobId: string | undefined): boolean;
};
```

From server/services/journeys/nucleiPreflight.ts (Phase 12):
```typescript
export async function preflightNuclei(log: Logger): Promise<{ ok: boolean; reason?: string }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: server/services/journeys/apiActiveTests.ts — estágio bola + bfla + bopla + dryRun core</name>
  <files>server/services/journeys/apiActiveTests.ts</files>
  <read_first>
    - server/services/journeys/apiPassiveTests.ts (template direto — replicar padrão: stagesRun, stagesSkipped, cancelled, finalize(), persistHit, checkCancel, FIXTURE_DIR)
    - server/services/journeys/apiDiscovery.ts (padrão Phase 11 — loop de cancelamento cooperativo + resultados parciais em cancel)
    - server/services/scanners/api/bola.ts (criado Wave 1 — pairCredentials, harvestObjectIds, testCrossAccess — consumir conforme interface acima)
    - server/services/scanners/api/bfla.ts (criado Wave 1 — identifyLowPrivCreds, matchAdminEndpoint, testPrivEscalation)
    - server/services/scanners/api/bopla.ts (criado Wave 1 — fetchSeedBody, injectSensitiveKey, verifyReflection)
    - server/storage/apiCredentials.ts (listApiCredentials — importação direta, não storage facade)
    - shared/schema.ts (ApiActiveTestOpts, ActiveTestResult, BOPLA_SENSITIVE_KEYS — Wave 0)
    - .planning/phases/13-security-testing-active/13-CONTEXT.md §"Orchestrator + shared infrastructure" e §"BOPLA/Mass Assignment"
  </read_first>
  <behavior>
    - Test 1: stages executam em ordem fixa: bola → bfla → bopla → rate_limit → ssrf (verificado via stagesRun order)
    - Test 2: opts.stages.bola=false → stagesSkipped inclui { stage: 'bola', reason: 'disabled' }
    - Test 3: opts.stages.bopla=true + opts.destructiveEnabled=false (padrão) → stagesSkipped inclui { stage: 'bopla', reason: 'destructive gate not enabled' }
    - Test 4: opts.stages.rateLimit não definido (padrão false) → stagesSkipped inclui { stage: 'rate_limit', reason: 'opt-in not enabled' }
    - Test 5: checkCancel() entre stages seta cancelled=true + termina cedo quando jobQueue.isJobCancelled retorna true
    - Test 6: dryRun=true lê fixtures api-active/ para cada stage; não chama HTTP real nem spawn Nuclei; findings têm '[DRY-RUN]' no título
    - Test 7: BOLA stage — pairCredentials chamado com cap maxCredentials; checkCancel() dentro do pair loop
    - Test 8: BFLA stage — identifyLowPrivCreds chamado; stage skipa com log quando retorna []
    - Test 9: todos hits persistidos via storage.upsertApiFindingByKey — findingsCreated/findingsUpdated incrementados
    - Test 10: ActiveTestResult contém credentialsUsed = número de creds distintas que participaram
  </behavior>
  <action>
Criar arquivo novo `server/services/journeys/apiActiveTests.ts`. Replicar estrutura de `server/services/journeys/apiPassiveTests.ts` (padrão Phase 12) adaptado para 5 vetores ativos. Conteúdo:

```typescript
/**
 * Phase 13 — API Active Security Testing orchestrator.
 *
 * Composes 5 stages (in order): bola → bfla → bopla → rate_limit → ssrf.
 * Each stage fails independently (log + skip); pipeline continues.
 *
 * Gates:
 *   - BOPLA: always skipped unless opts.destructiveEnabled=true
 *   - rateLimit: always skipped unless opts.stages.rateLimit=true (explicit opt-in)
 *
 * Cooperative cancel: jobQueue.isJobCancelled(jobId) checked between each stage
 * AND inside the BOLA credential-pair loop. Partial results persist on cancel.
 *
 * dryRun=true loads fixtures from server/__tests__/fixtures/api-active/
 * and prefixes finding titles with "[DRY-RUN]". No HTTP requests or Nuclei spawns.
 *
 * SAFE-06: Never log complete secrets. Credential IDs masked as prefix-3 + '***'.
 */
import { readFile } from 'fs/promises';
import { join } from 'path';
import type {
  ApiActiveTestOpts,
  ActiveTestResult,
  InsertApiFinding,
} from '@shared/schema';
import { BOPLA_SENSITIVE_KEYS } from '@shared/schema';
import { createLogger } from '../../lib/logger';
import { storage } from '../../storage';
import { jobQueue } from '../jobQueue';
import { listApiCredentials, getApiCredentialWithSecret } from '../../storage/apiCredentials';
import {
  pairCredentials,
  harvestObjectIds,
  testCrossAccess,
  type BolaHit,
} from '../scanners/api/bola';
import {
  identifyLowPrivCreds,
  matchAdminEndpoint,
  testPrivEscalation,
  type BflaHit,
} from '../scanners/api/bfla';
import {
  fetchSeedBody,
  injectSensitiveKey,
  verifyReflection,
  type BoplaHit,
} from '../scanners/api/bopla';
import {
  buildBurst,
  detectThrottling,
  buildRateLimitHit,
  type RateLimitHit,
} from '../scanners/api/rateLimit';
import {
  identifyUrlParams,
  runSsrfNuclei,
  type SsrfHit,
} from '../scanners/api/ssrfNuclei';
import { preflightNuclei } from '../nucleiPreflight';

const log = createLogger('journeys:apiActiveTests');

const FIXTURE_DIR = join(process.cwd(), 'server/__tests__/fixtures/api-active');
const FIXTURE_BOLA = 'bola-crossaccess-response.json';
const FIXTURE_BFLA = 'bfla-admin-success.json';
const FIXTURE_BOPLA_BEFORE = 'bopla-reflection-before.json';
const FIXTURE_BOPLA_AFTER = 'bopla-reflection-after.json';
const FIXTURE_RATE_LIMIT = 'rate-limit-burst-responses.json';
const FIXTURE_SSRF = 'ssrf-nuclei-interaction.jsonl';

type ActiveStageName = 'bola' | 'bfla' | 'bopla' | 'rate_limit' | 'ssrf';

export async function runApiActiveTests(
  apiId: string,
  opts: ApiActiveTestOpts,
  jobId?: string,
): Promise<ActiveTestResult> {
  const startedAt = Date.now();
  const controller = new AbortController();
  const signal = controller.signal;

  const stagesRun: ActiveStageName[] = [];
  const stagesSkipped: Array<{ stage: string; reason: string }> = [];
  const findingsByCategory: Record<string, number> = {};
  const findingsBySeverity: Record<string, number> = {};
  let findingsCreated = 0;
  let findingsUpdated = 0;
  let cancelled = false;
  const usedCredentialIds = new Set<string>();

  const api = await storage.getApi(apiId);
  if (!api) {
    throw new Error(`API não encontrada: ${apiId}`);
  }

  const endpoints = await storage.listEndpointsByApi(apiId);
  const targetEndpoints = opts.endpointIds?.length
    ? endpoints.filter((e) => opts.endpointIds!.includes(e.id))
    : endpoints;

  // Load credentials once at orchestrator entry — shared across stages.
  const credSafe = await listApiCredentials({ apiId });
  const credIds = opts.credentialIds?.length
    ? opts.credentialIds.filter((id) => credSafe.some((c) => c.id === id))
    : credSafe.map((c) => c.id);

  // Decrypt creds (with secrets) for in-process scanners.
  // SAFE-06: never log full secret — mask ID as prefix-3 + '***' in logs.
  const credsWithSecrets = (
    await Promise.all(credIds.map((id) => getApiCredentialWithSecret(id)))
  ).filter((c): c is NonNullable<typeof c> => c != null);

  const dryRun = opts.dryRun ?? false;
  const titlePrefix = dryRun ? '[DRY-RUN] ' : '';

  // ---- Helper: persist a hit + update counters ----
  type AnyHit = BolaHit | BflaHit | BoplaHit | RateLimitHit | SsrfHit;
  const persistHit = async (hit: AnyHit): Promise<void> => {
    const title = `${titlePrefix}${hit.title}`;
    const insert: InsertApiFinding = {
      apiEndpointId: hit.endpointId,
      jobId: jobId ?? null,
      owaspCategory: hit.owaspCategory,
      severity: hit.severity,
      title,
      description: hit.description ?? null,
      remediation: hit.remediation ?? null,
      evidence: hit.evidence,
      status: 'open',
    };
    try {
      const { action } = await storage.upsertApiFindingByKey(
        hit.endpointId,
        hit.owaspCategory,
        title,
        insert,
      );
      if (action === 'inserted') findingsCreated++;
      else findingsUpdated++;
      findingsByCategory[hit.owaspCategory] = (findingsByCategory[hit.owaspCategory] ?? 0) + 1;
      findingsBySeverity[hit.severity] = (findingsBySeverity[hit.severity] ?? 0) + 1;
    } catch (err) {
      log.error(
        { err, apiId, endpointId: hit.endpointId, owaspCategory: hit.owaspCategory },
        'failed to persist active finding',
      );
    }
  };

  // ---- Helper: cooperative cancel ----
  const checkCancel = (): boolean => {
    if (jobId && jobQueue.isJobCancelled(jobId)) {
      log.info({ apiId, jobId }, 'active tests cancelled — exiting cooperatively');
      cancelled = true;
      controller.abort();
      return true;
    }
    return false;
  };

  // =====================================================================
  // Stage 1: BOLA (API1)
  // =====================================================================
  if (opts.stages?.bola ?? true) {
    if (checkCancel()) return finalize();
    if (credsWithSecrets.length < 2) {
      stagesSkipped.push({ stage: 'bola', reason: 'BOLA requer ≥2 credenciais — menos de 2 disponíveis' });
    } else {
      try {
        stagesRun.push('bola');
        const bolaOpts = opts.bola;
        const maxCreds = bolaOpts?.maxCredentials ?? 4;
        const maxIds = bolaOpts?.maxIdsPerEndpoint ?? 3;
        const pairs = pairCredentials(credsWithSecrets, maxCreds);
        const getEndpoints = targetEndpoints.filter(
          (e) => e.method === 'GET' && e.requiresAuth === true,
        );

        for (const [credA, credB] of pairs) {
          if (checkCancel()) return finalize();
          usedCredentialIds.add(credA.id);
          usedCredentialIds.add(credB.id);

          for (const ep of getEndpoints) {
            if (checkCancel()) return finalize();
            const harvestedIds = dryRun
              ? ['fixture-id-001', 'fixture-id-002']
              : await harvestObjectIds({
                  endpoint: { id: ep.id, path: ep.path },
                  baseUrl: api.baseUrl,
                  credential: credA,
                  maxIds,
                }).catch((err) => {
                  log.debug(
                    { err: err instanceof Error ? err.message : err, endpointId: ep.id },
                    'harvest skipped',
                  );
                  return [];
                });

            if (harvestedIds.length === 0) continue;

            const bolaHits = await testCrossAccess({
              endpoint: { id: ep.id, path: ep.path, method: ep.method },
              baseUrl: api.baseUrl,
              credentialA: credA,
              credentialB: credB,
              harvestedIds,
              dryRun,
              dryRunFixturePath: join(FIXTURE_DIR, FIXTURE_BOLA),
            }).catch((err) => {
              log.error(
                { err: err instanceof Error ? err.message : err, endpointId: ep.id },
                'bola testCrossAccess error',
              );
              return [];
            });

            for (const hit of bolaHits) {
              await persistHit(hit);
            }
          }
        }
        log.info({ apiId, stage: 'bola', pairs: pairs.length }, 'bola stage complete');
      } catch (err) {
        log.error({ err, apiId, stage: 'bola' }, 'bola stage failed');
        stagesSkipped.push({ stage: 'bola', reason: err instanceof Error ? err.message : 'unknown' });
      }
    }
  } else {
    stagesSkipped.push({ stage: 'bola', reason: 'disabled' });
  }

  // =====================================================================
  // Stage 2: BFLA (API5)
  // =====================================================================
  if (opts.stages?.bfla ?? true) {
    if (checkCancel()) return finalize();
    try {
      const lowPrivCreds = identifyLowPrivCreds(credsWithSecrets);
      if (lowPrivCreds.length === 0) {
        stagesSkipped.push({
          stage: 'bfla',
          reason: 'BFLA requer ≥2 creds com privilégios distintos — não identificadas',
        });
      } else {
        stagesRun.push('bfla');
        const adminEndpoints = targetEndpoints.filter((e) => matchAdminEndpoint(e));

        for (const ep of adminEndpoints) {
          if (checkCancel()) return finalize();
          for (const lowPrivCred of lowPrivCreds) {
            if (checkCancel()) return finalize();
            usedCredentialIds.add(lowPrivCred.id);

            const hit = await testPrivEscalation({
              endpoint: { id: ep.id, path: ep.path, method: ep.method },
              baseUrl: api.baseUrl,
              lowPrivCred,
              allCreds: credsWithSecrets,
              dryRun,
              dryRunFixturePath: join(FIXTURE_DIR, FIXTURE_BFLA),
            }).catch((err) => {
              log.error(
                { err: err instanceof Error ? err.message : err, endpointId: ep.id },
                'bfla testPrivEscalation error',
              );
              return null;
            });

            if (hit) await persistHit(hit);
          }
        }
        log.info(
          { apiId, stage: 'bfla', adminEndpointsCount: adminEndpoints.length },
          'bfla stage complete',
        );
      }
    } catch (err) {
      log.error({ err, apiId, stage: 'bfla' }, 'bfla stage failed');
      stagesSkipped.push({ stage: 'bfla', reason: err instanceof Error ? err.message : 'unknown' });
    }
  } else {
    stagesSkipped.push({ stage: 'bfla', reason: 'disabled' });
  }

  // =====================================================================
  // Stage 3: BOPLA / Mass Assignment (API3)
  // Gate: opts.destructiveEnabled MUST be true (default false).
  // =====================================================================
  if (opts.stages?.bopla ?? true) {
    if (checkCancel()) return finalize();
    if (!opts.destructiveEnabled) {
      stagesSkipped.push({ stage: 'bopla', reason: 'destructive gate not enabled' });
      log.info({ apiId }, 'bopla stage skipped — destructiveEnabled=false');
    } else {
      try {
        stagesRun.push('bopla');
        const putPatchEndpoints = targetEndpoints.filter(
          (e) => (e.method === 'PUT' || e.method === 'PATCH') && e.requiresAuth === true,
        );
        const credential = credsWithSecrets[0];

        if (!credential) {
          stagesSkipped.push({ stage: 'bopla', reason: 'nenhuma credencial disponível' });
          const idx = stagesRun.indexOf('bopla');
          if (idx >= 0) stagesRun.splice(idx, 1);
        } else {
          usedCredentialIds.add(credential.id);

          for (const ep of putPatchEndpoints) {
            if (checkCancel()) return finalize();

            const seedBody = dryRun
              ? await readFile(join(FIXTURE_DIR, FIXTURE_BOPLA_BEFORE), 'utf8')
                  .then((raw) => JSON.parse(raw) as Record<string, unknown>)
                  .catch(() => null)
              : await fetchSeedBody({
                  endpoint: { id: ep.id, path: ep.path },
                  baseUrl: api.baseUrl,
                  credential,
                }).catch((err) => {
                  log.debug(
                    { err: err instanceof Error ? err.message : err, endpointId: ep.id },
                    'bopla seed fetch failed — skipping endpoint',
                  );
                  return null;
                });

            if (!seedBody) continue;

            for (const key of BOPLA_SENSITIVE_KEYS) {
              if (checkCancel()) return finalize();
              const injectValue: unknown =
                key === 'roles' || key === 'permissions' ? ['admin'] : true;

              if (dryRun) {
                // dryRun: read reflection-after fixture, treat as reflected
                const afterRaw = await readFile(
                  join(FIXTURE_DIR, FIXTURE_BOPLA_AFTER),
                  'utf8',
                ).catch(() => null);
                if (!afterRaw) continue;
                const afterBody = JSON.parse(afterRaw) as Record<string, unknown>;
                if (key in afterBody) {
                  const CRITICAL_KEYS = new Set(['is_admin', 'role', 'superuser']);
                  const severity: 'critical' | 'high' = CRITICAL_KEYS.has(key) ? 'critical' : 'high';
                  await persistHit({
                    endpointId: ep.id,
                    owaspCategory: 'api3_bopla_2023',
                    severity,
                    title: `Campo sensível aceito em PUT/PATCH sem validação (${key})`,
                    evidence: {
                      request: { method: ep.method, url: `${api.baseUrl}${ep.path}`, headers: {}, bodySnippet: JSON.stringify({ ...seedBody, [key]: injectValue }).slice(0, 8192) },
                      response: { status: 200, headers: {}, bodySnippet: afterRaw.slice(0, 8192) },
                      extractedValues: { injectedKey: key, originalValue: seedBody[key] ?? undefined, reflectedValue: afterBody[key], endpointPath: ep.path },
                    },
                  } as BoplaHit);
                }
              } else {
                await injectSensitiveKey({
                  endpoint: { id: ep.id, path: ep.path, method: ep.method },
                  baseUrl: api.baseUrl,
                  credential,
                  seedBody,
                  key,
                  value: injectValue,
                }).catch(() => null);

                const reflection = await verifyReflection({
                  endpoint: { id: ep.id, path: ep.path },
                  baseUrl: api.baseUrl,
                  credential,
                  injectedKey: key,
                  seedBodyValue: seedBody[key],
                }).catch(() => null);

                if (reflection?.reflected) {
                  const CRITICAL_KEYS = new Set(['is_admin', 'role', 'superuser']);
                  const severity: 'critical' | 'high' = CRITICAL_KEYS.has(key) ? 'critical' : 'high';
                  await persistHit({
                    endpointId: ep.id,
                    owaspCategory: 'api3_bopla_2023',
                    severity,
                    title: `Campo sensível aceito em PUT/PATCH sem validação (${key})`,
                    evidence: {
                      request: { method: ep.method, url: `${api.baseUrl}${ep.path}`, headers: {}, bodySnippet: JSON.stringify({ ...seedBody, [key]: injectValue }).slice(0, 8192) },
                      response: { status: 200, headers: {}, bodySnippet: String(reflection.reflectedValue).slice(0, 8192) },
                      extractedValues: { injectedKey: key, originalValue: seedBody[key] ?? undefined, reflectedValue: reflection.reflectedValue, endpointPath: ep.path },
                    },
                  } as BoplaHit);
                }
              }
            }
          }
          log.info(
            { apiId, stage: 'bopla', endpointsCount: putPatchEndpoints.length },
            'bopla stage complete',
          );
        }
      } catch (err) {
        log.error({ err, apiId, stage: 'bopla' }, 'bopla stage failed');
        stagesSkipped.push({ stage: 'bopla', reason: err instanceof Error ? err.message : 'unknown' });
      }
    }
  } else {
    stagesSkipped.push({ stage: 'bopla', reason: 'disabled' });
  }

  // =====================================================================
  // Stage 4: Rate-limit absence (API4) — opt-in only
  // =====================================================================
  if (opts.stages?.rateLimit === true) {
    if (checkCancel()) return finalize();
    try {
      stagesRun.push('rate_limit');
      const rlOpts = opts.rateLimit;
      const burstSize = Math.min(rlOpts?.burstSize ?? 20, 50);
      const windowMs = rlOpts?.windowMs ?? 2000;

      // 1 GET+200 endpoint per API (or override list)
      const rlEndpointIds = rlOpts?.endpointIds;
      const candidateEndpoints = rlEndpointIds?.length
        ? targetEndpoints.filter((e) => rlEndpointIds.includes(e.id))
        : targetEndpoints
            .filter((e) => e.method === 'GET' && e.requiresAuth === true && e.httpxStatus === 200)
            .sort((a, b) => a.path.localeCompare(b.path))
            .slice(0, 1);

      const credential = credsWithSecrets[0];
      if (!credential) {
        stagesSkipped.push({ stage: 'rate_limit', reason: 'nenhuma credencial disponível' });
        const idx = stagesRun.indexOf('rate_limit');
        if (idx >= 0) stagesRun.splice(idx, 1);
      } else {
        usedCredentialIds.add(credential.id);
        for (const ep of candidateEndpoints) {
          if (checkCancel()) return finalize();

          let responses: Array<{ status: number; headers: Record<string, string> }>;
          if (dryRun) {
            const raw = await readFile(join(FIXTURE_DIR, FIXTURE_RATE_LIMIT), 'utf8');
            responses = JSON.parse(raw) as typeof responses;
          } else {
            responses = await buildBurst({
              endpoint: { id: ep.id, path: ep.path },
              baseUrl: api.baseUrl,
              credential,
              burstSize,
            });
            // Safety net: 30s delay between burst tests
            await new Promise((r) => setTimeout(r, 30_000));
          }

          const { throttled } = detectThrottling(responses);
          if (!throttled) {
            await persistHit(
              buildRateLimitHit({
                endpointId: ep.id,
                endpointPath: ep.path,
                burstSize,
                responses,
              }),
            );
          }
        }
        log.info(
          { apiId, stage: 'rate_limit', burstSize, windowMs, endpointsCount: candidateEndpoints.length },
          'rate_limit stage complete',
        );
      }
    } catch (err) {
      log.error({ err, apiId, stage: 'rate_limit' }, 'rate_limit stage failed');
      stagesSkipped.push({ stage: 'rate_limit', reason: err instanceof Error ? err.message : 'unknown' });
    }
  } else {
    stagesSkipped.push({ stage: 'rate_limit', reason: 'opt-in not enabled' });
  }

  // =====================================================================
  // Stage 5: SSRF (API7) — Nuclei + interactsh
  // =====================================================================
  if (opts.stages?.ssrf ?? true) {
    if (checkCancel()) return finalize();
    try {
      // Preflight Nuclei before any spawn — skip stage on failure, don't abort pipeline.
      const preflight = await preflightNuclei(log);
      if (!preflight.ok) {
        stagesSkipped.push({
          stage: 'ssrf',
          reason: `nuclei preflight failed: ${preflight.reason ?? 'unknown'}`,
        });
      } else {
        stagesRun.push('ssrf');
        const credential = credsWithSecrets[0] ?? null;
        if (credential) usedCredentialIds.add(credential.id);

        const ssrfEndpoints = targetEndpoints
          .filter((e) => e.requiresAuth === true)
          .map((e) => ({
            id: e.id,
            fullUrl: `${api.baseUrl}${e.path}`,
            urlParamNames: identifyUrlParams({
              query_params: e.query_params as Parameters<typeof identifyUrlParams>[0]['query_params'],
              request_params: e.request_params as Parameters<typeof identifyUrlParams>[0]['request_params'],
              request_schema: e.request_schema,
            }),
          }))
          .filter((e) => e.urlParamNames.length > 0);

        const ssrfHits = await runSsrfNuclei({
          endpoints: ssrfEndpoints,
          credential,
          interactshUrl: opts.ssrf?.interactshUrl ?? process.env.INTERACTSH_URL,
          dryRun,
          dryRunFixturePath: join(FIXTURE_DIR, FIXTURE_SSRF),
          signal,
        });

        for (const hit of ssrfHits) {
          await persistHit(hit);
        }
        log.info(
          { apiId, stage: 'ssrf', ssrfEndpointsCount: ssrfEndpoints.length, hitsCount: ssrfHits.length },
          'ssrf stage complete',
        );
      }
    } catch (err) {
      log.error({ err, apiId, stage: 'ssrf' }, 'ssrf stage failed');
      stagesSkipped.push({ stage: 'ssrf', reason: err instanceof Error ? err.message : 'unknown' });
    }
  } else {
    stagesSkipped.push({ stage: 'ssrf', reason: 'disabled' });
  }

  return finalize();

  // ---- Finalizer (inner closure — reads closed-over vars) ----
  function finalize(): ActiveTestResult {
    const durationMs = Date.now() - startedAt;
    const result: ActiveTestResult = {
      apiId,
      stagesRun,
      stagesSkipped,
      findingsCreated,
      findingsUpdated,
      findingsByCategory,
      findingsBySeverity,
      cancelled,
      dryRun,
      durationMs,
      credentialsUsed: usedCredentialIds.size,
    };
    log.info(
      {
        apiId,
        jobId,
        stagesRun: stagesRun.length,
        stagesSkipped: stagesSkipped.length,
        findingsCreated,
        findingsUpdated,
        cancelled,
        dryRun,
        durationMs,
        credentialsUsed: usedCredentialIds.size,
      },
      'api active tests complete',
    );
    return result;
  }
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "journeys/apiActiveTests" | head -10 ; npx vitest run server/__tests__/apiActive/orchestrator.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `server/services/journeys/apiActiveTests.ts` existe com ≥ 280 linhas
    - `grep -n "export async function runApiActiveTests" server/services/journeys/apiActiveTests.ts` retorna 1 match
    - `grep -n "stagesRun.push('bola')\|stagesRun.push('bfla')\|stagesRun.push('bopla')\|stagesRun.push('rate_limit')\|stagesRun.push('ssrf')" server/services/journeys/apiActiveTests.ts` retorna ≥ 5 matches
    - `grep -n "destructive gate not enabled\|destructiveEnabled" server/services/journeys/apiActiveTests.ts` retorna ≥ 2 matches (BOPLA gate)
    - `grep -n "opt-in not enabled" server/services/journeys/apiActiveTests.ts` retorna ≥ 1 match (rateLimit gate)
    - `grep -n "jobQueue.isJobCancelled" server/services/journeys/apiActiveTests.ts` retorna ≥ 3 matches (entre stages + dentro do loop BOLA)
    - `grep -n "upsertApiFindingByKey" server/services/journeys/apiActiveTests.ts` retorna ≥ 1 match
    - `grep -n "pairCredentials\|testCrossAccess\|identifyLowPrivCreds\|testPrivEscalation\|fetchSeedBody\|buildBurst\|detectThrottling\|runSsrfNuclei" server/services/journeys/apiActiveTests.ts` retorna ≥ 8 matches (todos scanners wireados)
    - `grep -n "preflightNuclei" server/services/journeys/apiActiveTests.ts` retorna ≥ 1 match (SSRF preflight)
    - `grep -n "fixtures/api-active" server/services/journeys/apiActiveTests.ts` retorna ≥ 1 match (dryRun fixture dir)
    - `grep -n "\\[DRY-RUN\\]" server/services/journeys/apiActiveTests.ts` retorna ≥ 1 match (titlePrefix)
    - `grep -n "credentialsUsed" server/services/journeys/apiActiveTests.ts` retorna ≥ 2 matches (Set + result field)
    - `grep -n "SAFE-06\|mask\|never log.*secret\|secret.*never" server/services/journeys/apiActiveTests.ts` retorna ≥ 1 match (comentário safety)
    - `npx tsc --noEmit` não produz erros novos em `apiActiveTests.ts`
  </acceptance_criteria>
  <done>Orchestrator completo; Wave 3 (rota + CLI) consome via `import { runApiActiveTests } from '.../journeys/apiActiveTests'`; stubs Wave 0 orchestrator.test.ts têm alvo real.</done>
</task>

</tasks>

<verification>
Após task concluída:

```bash
# 1) Type check
npx tsc --noEmit

# 2) Vitest — stub orchestrator.test.ts deve ter alvo real agora
npx vitest run server/__tests__/apiActive/orchestrator.test.ts --reporter=default

# 3) Inventory
wc -l server/services/journeys/apiActiveTests.ts

# 4) Regression — fases anteriores ainda verdes
npx vitest run server/__tests__/apiPassive server/__tests__/apiDiscovery 2>&1 | tail -10

# 5) Confirmar gates
grep -n "destructive gate\|opt-in not enabled" server/services/journeys/apiActiveTests.ts
```
</verification>

<success_criteria>
- [ ] `server/services/journeys/apiActiveTests.ts` exporta `runApiActiveTests` compondo 5 scanners Wave 1
- [ ] 5 stages em ordem: bola → bfla → bopla → rate_limit → ssrf
- [ ] BOPLA gate: skipa quando `opts.destructiveEnabled=false` (padrão)
- [ ] rateLimit gate: skipa quando `opts.stages.rateLimit` não habilitado (padrão)
- [ ] Cancelamento cooperativo via `jobQueue.isJobCancelled` entre stages + dentro do loop BOLA
- [ ] dryRun funciona via fixtures Wave 0 — prefixa título com `[DRY-RUN]`
- [ ] SSRF stage chama `preflightNuclei` antes de spawn; skipa stage em falha de preflight
- [ ] `credentialsUsed` conta creds distintas que participaram
- [ ] Todos findings persistidos via `upsertApiFindingByKey` — contagens agregadas
- [ ] `npx tsc --noEmit` limpo
</success_criteria>

<output>
After completion, create `.planning/phases/13-security-testing-active/13-03-SUMMARY.md`
</output>
