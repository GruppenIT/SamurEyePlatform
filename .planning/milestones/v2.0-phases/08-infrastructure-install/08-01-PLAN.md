---
phase: 08-infrastructure-install
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/install/binaries.json
  - scripts/install/wordlists.json
  - scripts/install/wordlists/arjun-extended-pt-en.txt
  - scripts/install/wordlists/README.md
  - tests/install/bats.bats
  - tests/install/helpers.bash
  - tests/install/fixtures/.gitkeep
  - vendor/wordlists/.gitkeep
  - package.json
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05
user_setup:
  - service: arjun-extended-pt-en wordlist
    why: "Custom SamurEye pt-BR wordlist is not upstream — user must provide raw content or approve placeholder seed"
    dashboard_config:
      - task: "Review placeholder arjun-extended-pt-en.txt content and either approve as-is or replace with curated pt-BR parameter list before Phase 11"
        location: "scripts/install/wordlists/arjun-extended-pt-en.txt"

must_haves:
  truths:
    - "Wave 0 manifests exist so every downstream plan has a single source of truth for versions and SHA-256"
    - "bats-core test harness runs and produces a green baseline on a clean checkout"
    - "The custom pt-BR Arjun wordlist is committed to the repo with a computed SHA-256 recorded in wordlists.json"
  artifacts:
    - path: "scripts/install/binaries.json"
      provides: "Pinned manifest for Katana 1.5.0, httpx 1.9.0, Kiterunner 1.0.2, Arjun 2.2.7 with exact URLs and SHA-256s"
      contains: "592890e5febaf5706d0a962d96299512418d6eccce6388cf1600e1f078ed359d"
    - path: "scripts/install/wordlists.json"
      provides: "Pinned manifest for routes-large.kite and arjun-extended-pt-en.txt with SHA-256s and install targets"
      contains: "e6f4d78f6e607d0352527dee0123ce1ff7ab18fe845ea898b7ca38e0c6a321f2"
    - path: "scripts/install/wordlists/arjun-extended-pt-en.txt"
      provides: "Custom pt-BR + en parameter wordlist for Arjun (committed in-tree, not fetched at runtime)"
      min_lines: 100
    - path: "tests/install/bats.bats"
      provides: "bats-core smoke test verifying the harness itself loads helpers and fixtures"
    - path: "tests/install/helpers.bash"
      provides: "Shared bats helpers (make_temp_repo, mock_curl_download, assert_sha256_matches)"
  key_links:
    - from: "tests/install/bats.bats"
      to: "tests/install/helpers.bash"
      via: "load 'helpers'"
      pattern: "load +['\"]?helpers"
    - from: "scripts/install/wordlists.json"
      to: "scripts/install/wordlists/arjun-extended-pt-en.txt"
      via: "source='local' path entry with SHA-256"
      pattern: "arjun-extended-pt-en"
---

<objective>
Create the Wave 0 foundation for Phase 8: the pinned binaries manifest, the pinned wordlists manifest, the committed pt-BR Arjun wordlist, and the bats-core test harness. Every downstream plan depends on these artifacts; no binary download, no safe-reset work, and no tarball build can start without them.

Purpose: Single source of truth for versions / SHA-256s (reproducible appliance) and a usable shell-test harness (Nyquist-compliant verification across INFRA-01..05).

Output: Committed manifests (`binaries.json`, `wordlists.json`), committed custom wordlist with computed SHA-256, `tests/install/` directory with bats runner + helpers + placeholder fixtures dir, `vendor/wordlists/` staging directory ready to receive Wave 2 downloads, and bats 1.11 + shellcheck installed on the dev machine.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-infrastructure-install/08-CONTEXT.md
@.planning/phases/08-infrastructure-install/08-RESEARCH.md
@.planning/phases/08-infrastructure-install/08-VALIDATION.md
@install.sh
@package.json

<interfaces>
<!-- binaries.json schema (copied verbatim from RESEARCH.md §"Binaries manifest schema") -->
<!-- The executor MUST write exactly these keys — downstream plans read them with jq. -->

scripts/install/binaries.json top-level keys:
```json
{
  "$schema": "./binaries.schema.json",
  "version": 1,
  "binaries": {
    "katana":    { "version": "...", "url": "...", "sha256": "...", "format": "zip|tar.gz|pip_source", "binary_in_archive": "..." },
    "httpx":     { ... },
    "kiterunner":{ ... },
    "arjun":     { ... }
  }
}
```

scripts/install/wordlists.json top-level keys:
```json
{
  "version": 1,
  "wordlists": {
    "routes-large.kite":         { "source": "remote", "url": "...", "sha256": "...", "format": "tar.gz", "install_to": "wordlists/routes-large.kite" },
    "arjun-extended-pt-en.txt":  { "source": "local",  "path": "scripts/install/wordlists/arjun-extended-pt-en.txt", "sha256": "...", "install_to": "wordlists/arjun-extended-pt-en.txt" }
  }
}
```

tests/install/helpers.bash must export these bash functions (used by bats files in Plans 02-06):
- `make_temp_repo <dir>`  — git init a fake remote + working clone, commit README.md
- `mock_curl_download <url> <dest> <content>` — write <content> to <dest> and record the call
- `assert_sha256_matches <file> <expected_hash>` — `bats assert_equal` shim
- `assert_file_not_mutated <file> <snapshot>` — diff file against snapshot, fail if changed
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pinned manifests (binaries.json + wordlists.json) and seed custom wordlist</name>
  <read_first>
    - .planning/phases/08-infrastructure-install/08-RESEARCH.md (§"Binary Pinning Table", §"Code Examples → Binaries manifest schema")
    - .planning/phases/08-infrastructure-install/08-CONTEXT.md (§"Pinned binaries on disk", §"Wordlists on disk")
    - install.sh (lines 31-41 for INSTALL_DIR defaults and env-var pattern; lines 103 confirming jq is already installed)
  </read_first>
  <behavior>
    - Test 1: `jq -e '.binaries | keys | sort == ["arjun","httpx","katana","kiterunner"]' scripts/install/binaries.json` exits 0
    - Test 2: `jq -r '.binaries.katana.sha256' scripts/install/binaries.json` prints exactly `592890e5febaf5706d0a962d96299512418d6eccce6388cf1600e1f078ed359d`
    - Test 3: `jq -r '.binaries.httpx.sha256' scripts/install/binaries.json` prints exactly `54c6c91d61d3b82ba79f93633df04bb547f0c954d9d9b0fb8bcedf158f85ff2f`
    - Test 4: `jq -r '.binaries.kiterunner.sha256' scripts/install/binaries.json` prints exactly `6f0b70aabf747de592445a690281897eebbc45927e9264185d34ffb11637613b`
    - Test 5: `jq -r '.binaries.arjun.sha256' scripts/install/binaries.json` prints exactly `b193cdaf97bf7b0e8cd91a41da778639e01fd9738d5f666a8161377f475ce72e`
    - Test 6: `jq -e '.wordlists["routes-large.kite"].sha256 == "e6f4d78f6e607d0352527dee0123ce1ff7ab18fe845ea898b7ca38e0c6a321f2"' scripts/install/wordlists.json` exits 0
    - Test 7: `sha256sum scripts/install/wordlists/arjun-extended-pt-en.txt | awk '{print $1}'` output equals value stored in `wordlists.json` at `.wordlists["arjun-extended-pt-en.txt"].sha256`
  </behavior>
  <action>
Create four files and two directory stubs. Use the Write tool; never hand-edit JSON with sed.

**1) Create `scripts/install/binaries.json`** — JSON strict (no comments), exactly this content:

```json
{
  "$schema": "./binaries.schema.json",
  "version": 1,
  "binaries": {
    "katana": {
      "version": "1.5.0",
      "url": "https://github.com/projectdiscovery/katana/releases/download/v1.5.0/katana_1.5.0_linux_amd64.zip",
      "sha256": "592890e5febaf5706d0a962d96299512418d6eccce6388cf1600e1f078ed359d",
      "format": "zip",
      "binary_in_archive": "katana",
      "install_to": "bin/katana"
    },
    "httpx": {
      "version": "1.9.0",
      "url": "https://github.com/projectdiscovery/httpx/releases/download/v1.9.0/httpx_1.9.0_linux_amd64.zip",
      "sha256": "54c6c91d61d3b82ba79f93633df04bb547f0c954d9d9b0fb8bcedf158f85ff2f",
      "format": "zip",
      "binary_in_archive": "httpx",
      "install_to": "bin/httpx"
    },
    "kiterunner": {
      "version": "1.0.2",
      "url": "https://github.com/assetnote/kiterunner/releases/download/v1.0.2/kiterunner_1.0.2_linux_amd64.tar.gz",
      "sha256": "6f0b70aabf747de592445a690281897eebbc45927e9264185d34ffb11637613b",
      "format": "tar.gz",
      "binary_in_archive": "kr",
      "install_to": "bin/kiterunner"
    },
    "arjun": {
      "version": "2.2.7",
      "url": "https://files.pythonhosted.org/packages/04/22/c5b969720d2802de2248c2aac0414ee5ae234887cfe150564d591c73fb23/arjun-2.2.7.tar.gz",
      "sha256": "b193cdaf97bf7b0e8cd91a41da778639e01fd9738d5f666a8161377f475ce72e",
      "format": "pip_source",
      "install_into_venv": "venv-security",
      "install_to": "bin/arjun"
    }
  }
}
```

**2) Create `scripts/install/wordlists/arjun-extended-pt-en.txt`** — SamurEye-curated pt-BR + en parameter wordlist. Seed initial content (committed file; executor writes the full list from the RESEARCH.md intent — at least 100 unique parameter names, one per line, no trailing whitespace). Minimum seed list below (executor may extend but MUST include all of these baseline entries, plus enough lines to reach >= 100):

```
id
token
access_token
refresh_token
api_key
apikey
auth
authorization
senha
password
pwd
usuario
user
username
email
nome
name
cpf
cnpj
telefone
phone
celular
codigo
code
otp
pin
sessao
session
session_id
callback
redirect
redirect_uri
return
return_url
url
next
continue
q
query
search
busca
pesquisa
filter
filtro
sort
order
page
pagina
per_page
limit
offset
start
end
from
to
since
until
format
type
tipo
categoria
category
status
role
perfil
permissao
permission
scope
client_id
client_secret
state
nonce
timestamp
sign
signature
hash
checksum
lang
idioma
locale
country
pais
cidade
city
cep
zipcode
endereco
address
numero
number
valor
value
amount
quantity
qtd
produto
product
cliente
customer
pedido
order_id
transacao
transaction
ref
reference
cupom
coupon
desconto
discount
debug
test
env
ambiente
version
versao
```

**3) Create `scripts/install/wordlists/README.md`** — short note explaining the file is committed, edits require a new SHA-256 in `wordlists.json`, and runtime install.sh is responsible for copying it to `$INSTALL_DIR/wordlists/arjun-extended-pt-en.txt`.

**4) Compute the SHA-256 of the wordlist and write `scripts/install/wordlists.json`:**

Run: `sha256sum scripts/install/wordlists/arjun-extended-pt-en.txt` — capture the first field as `<ARJUN_WORDLIST_SHA>`. Then write the manifest exactly (replacing the placeholder with the computed hash):

```json
{
  "version": 1,
  "wordlists": {
    "routes-large.kite": {
      "source": "remote",
      "url": "https://wordlists-cdn.assetnote.io/data/kiterunner/routes-large.kite.tar.gz",
      "sha256": "e6f4d78f6e607d0352527dee0123ce1ff7ab18fe845ea898b7ca38e0c6a321f2",
      "format": "tar.gz",
      "extract_member": "routes-large.kite",
      "install_to": "wordlists/routes-large.kite",
      "mutable": false
    },
    "arjun-extended-pt-en.txt": {
      "source": "local",
      "path": "scripts/install/wordlists/arjun-extended-pt-en.txt",
      "sha256": "<ARJUN_WORDLIST_SHA>",
      "install_to": "wordlists/arjun-extended-pt-en.txt",
      "mutable": false
    }
  }
}
```

**5) Create `vendor/wordlists/.gitkeep`** — empty file so the staging directory exists for Plan 05 downloads.

Zero tolerance for schema drift: downstream plans read these keys verbatim. Do NOT add trailing commas, do NOT reorder versioned fields inside a binary entry, do NOT use comments.
  </action>
  <verify>
    <automated>test -f scripts/install/binaries.json && test -f scripts/install/wordlists.json && test -f scripts/install/wordlists/arjun-extended-pt-en.txt && jq -e '.binaries.katana.sha256 == "592890e5febaf5706d0a962d96299512418d6eccce6388cf1600e1f078ed359d"' scripts/install/binaries.json && jq -e '.binaries.httpx.sha256 == "54c6c91d61d3b82ba79f93633df04bb547f0c954d9d9b0fb8bcedf158f85ff2f"' scripts/install/binaries.json && jq -e '.binaries.kiterunner.sha256 == "6f0b70aabf747de592445a690281897eebbc45927e9264185d34ffb11637613b"' scripts/install/binaries.json && jq -e '.binaries.arjun.sha256 == "b193cdaf97bf7b0e8cd91a41da778639e01fd9738d5f666a8161377f475ce72e"' scripts/install/binaries.json && jq -e '.wordlists["routes-large.kite"].sha256 == "e6f4d78f6e607d0352527dee0123ce1ff7ab18fe845ea898b7ca38e0c6a321f2"' scripts/install/wordlists.json && COMPUTED=$(sha256sum scripts/install/wordlists/arjun-extended-pt-en.txt | awk '{print $1}') && STORED=$(jq -r '.wordlists["arjun-extended-pt-en.txt"].sha256' scripts/install/wordlists.json) && [ "$COMPUTED" = "$STORED" ] && [ "$(wc -l < scripts/install/wordlists/arjun-extended-pt-en.txt)" -ge 100 ]</automated>
  </verify>
  <done>
    - `scripts/install/binaries.json` contains all 4 pinned binaries with exact SHA-256s from RESEARCH.md §"Binary Pinning Table"
    - `scripts/install/wordlists.json` references routes-large.kite with the upstream SHA and arjun-extended-pt-en.txt with a locally-computed SHA that MATCHES `sha256sum` output
    - `scripts/install/wordlists/arjun-extended-pt-en.txt` exists with >= 100 lines of parameter names
    - `vendor/wordlists/.gitkeep` exists (directory staged for Plan 05)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install bats-core 1.11, shellcheck, and create the bats harness with shared helpers</name>
  <read_first>
    - .planning/phases/08-infrastructure-install/08-RESEARCH.md (§"Validation Architecture", §"bats-core test skeleton")
    - .planning/phases/08-infrastructure-install/08-VALIDATION.md (entire file — per-task verification map is the contract this harness must serve)
    - install.sh (lines 22, 44-54 — reuse `set -Eeuo pipefail` + `log`/`warn`/`error` conventions)
    - package.json (confirm test scripts section and verify bats-adjacent script placement)
  </read_first>
  <behavior>
    - Test 1: `command -v bats` returns path and `bats --version` prints major version >= 1.10
    - Test 2: `command -v shellcheck` returns path
    - Test 3: `bats tests/install/bats.bats` exits 0 with at least 3 passing tests (harness self-check)
    - Test 4: `bash -c 'source tests/install/helpers.bash && type make_temp_repo'` prints "make_temp_repo is a function"
    - Test 5: `bash -c 'source tests/install/helpers.bash && type mock_curl_download'` prints "mock_curl_download is a function"
    - Test 6: `bash -c 'source tests/install/helpers.bash && type assert_sha256_matches'` prints "assert_sha256_matches is a function"
    - Test 7: `npm run test:install` succeeds (proxies to `bats tests/install/`)
  </behavior>
  <action>
**1) Install bats-core and shellcheck on the dev machine.** Run exactly:

```bash
apt-get update
DEBIAN_FRONTEND=noninteractive apt-get install -y bats shellcheck
```

If `bats --version` reports < 1.10 (Ubuntu 22.04 ships 1.2.1), install bats-core 1.11.0 from source instead:

```bash
TMPDIR=$(mktemp -d)
git clone --branch v1.11.0 --depth 1 https://github.com/bats-core/bats-core.git "$TMPDIR/bats-core"
bash "$TMPDIR/bats-core/install.sh" /usr/local
rm -rf "$TMPDIR"
```

After install, verify with `bats --version` — must print `Bats 1.11.0` or similar >= 1.10.

**2) Create `tests/install/helpers.bash`** with these functions (no placeholders — full implementations):

```bash
#!/usr/bin/env bash
# Shared bats helpers for Phase 8 install tests.
# Source this at the top of every .bats file:  load 'helpers'

# Absolute path to the repo root (resolved from the helpers file location)
export REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"

# make_temp_repo <target_dir>
# Creates a bare "origin" and a working clone at <target_dir> with one commit.
make_temp_repo() {
  local dir="$1"
  local bare
  bare="$(mktemp -d)"
  git init --quiet --bare "$bare"
  git clone --quiet "$bare" "$dir"
  (
    cd "$dir"
    git config user.email "test@samureye.local"
    git config user.name  "Phase 8 Test"
    echo "v1" > README.md
    git add README.md
    git commit --quiet -m "init"
    git push --quiet origin HEAD:refs/heads/main
    git branch --set-upstream-to=origin/main main 2>/dev/null || true
  )
  echo "$bare"
}

# mock_curl_download <url> <dest> <content_file>
# Records the requested URL in $MOCK_CURL_LOG and copies <content_file> to <dest>.
mock_curl_download() {
  local url="$1" dest="$2" src="$3"
  : "${MOCK_CURL_LOG:=/tmp/mock-curl-log-$$}"
  printf '%s\n' "$url" >> "$MOCK_CURL_LOG"
  cp "$src" "$dest"
}

# assert_sha256_matches <file> <expected_hex>
assert_sha256_matches() {
  local file="$1" expected="$2"
  local actual
  actual="$(sha256sum "$file" | awk '{print $1}')"
  [[ "$actual" == "$expected" ]] || {
    echo "SHA-256 mismatch for $file: expected=$expected actual=$actual" >&2
    return 1
  }
}

# assert_file_not_mutated <file> <snapshot>
# Fails if <file> differs from <snapshot> (both must exist).
assert_file_not_mutated() {
  local file="$1" snap="$2"
  diff -q "$file" "$snap" >/dev/null 2>&1 || {
    echo "File mutated: $file (diff vs $snap below)" >&2
    diff "$file" "$snap" >&2 || true
    return 1
  }
}
```

**3) Create `tests/install/bats.bats`** as the harness self-check (the file referenced by 08-VALIDATION.md task 08-01-02):

```bash
#!/usr/bin/env bats
# Harness self-check — verifies bats can find helpers and fixtures.

load 'helpers'

@test "harness: REPO_ROOT resolves to repo with install.sh at top level" {
  [ -f "$REPO_ROOT/install.sh" ]
}

@test "harness: helpers.bash exports make_temp_repo" {
  run type make_temp_repo
  [ "$status" -eq 0 ]
  [[ "$output" =~ "function" ]]
}

@test "harness: helpers.bash exports assert_sha256_matches" {
  run type assert_sha256_matches
  [ "$status" -eq 0 ]
  [[ "$output" =~ "function" ]]
}

@test "harness: fixtures directory exists" {
  [ -d "$REPO_ROOT/tests/install/fixtures" ]
}

@test "harness: binaries.json manifest is valid JSON with all 4 binaries" {
  run jq -e '.binaries | length == 4' "$REPO_ROOT/scripts/install/binaries.json"
  [ "$status" -eq 0 ]
}
```

**4) Create `tests/install/fixtures/.gitkeep`** — empty file so the directory exists and is committed.

**5) Add an npm script entry to `package.json`.** Read the file first, then add inside the existing `"scripts"` object a new key:

```json
"test:install": "bats tests/install/"
```

Preserve every other field in `package.json` verbatim. Do NOT reorder existing scripts.
  </action>
  <verify>
    <automated>command -v bats && bats --version | grep -Eq 'Bats 1\.(1[0-9]|[2-9][0-9])' && command -v shellcheck && test -f tests/install/helpers.bash && test -f tests/install/bats.bats && test -f tests/install/fixtures/.gitkeep && bats tests/install/bats.bats && node -e 'const p=require("./package.json"); if(!p.scripts["test:install"]) process.exit(1)'</automated>
  </verify>
  <done>
    - `bats --version` reports >= 1.10
    - `shellcheck --version` succeeds
    - `bats tests/install/bats.bats` passes all self-check tests
    - `tests/install/helpers.bash` exports `make_temp_repo`, `mock_curl_download`, `assert_sha256_matches`, `assert_file_not_mutated`
    - `package.json` has `scripts["test:install"]` set to `bats tests/install/`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human verification</name>
  <files>(human review — no files modified)</files>
  <action>See <what-built> and <how-to-verify> below. This is a blocking checkpoint; execution pauses for user approval before proceeding to the next plan.</action>
  <verify>
    <automated>echo "checkpoint:human-verify — requires user approval (see how-to-verify)"</automated>
  </verify>
  <done>User types the resume-signal (see below).</done>
  <what-built>
    Wave 0 foundation is on disk:
    - `scripts/install/binaries.json` with 4 pinned binaries (katana 1.5.0, httpx 1.9.0, kiterunner 1.0.2, arjun 2.2.7)
    - `scripts/install/wordlists.json` with routes-large.kite (upstream SHA) and arjun-extended-pt-en.txt (locally-computed SHA)
    - `scripts/install/wordlists/arjun-extended-pt-en.txt` (>= 100 lines, seed SamurEye pt-BR parameter list)
    - `tests/install/bats.bats` + `tests/install/helpers.bash` + `tests/install/fixtures/.gitkeep`
    - `vendor/wordlists/.gitkeep`
    - bats-core (>= 1.10) and shellcheck installed
    - `npm run test:install` wired to `bats tests/install/`
  </what-built>
  <how-to-verify>
    User must review the Arjun wordlist seed before Phase 11 consumes it. No wiring depends on the content beyond count, but the quality of parameter discovery downstream is bounded by this list.

    1. Open `scripts/install/wordlists/arjun-extended-pt-en.txt` in an editor.
    2. Confirm the seed list is acceptable as a starting point OR edit it directly (adding/removing lines) — the file is committed in-tree.
    3. If the file was edited in step 2, re-run:  `sha256sum scripts/install/wordlists/arjun-extended-pt-en.txt`  and update the `sha256` field in `scripts/install/wordlists.json` accordingly.
    4. Re-run `bats tests/install/bats.bats` — must still pass.

    Output expected: `ok 1..5` from bats and a human-approved wordlist.
  </how-to-verify>
  <resume-signal>Type "approved" to confirm the wordlist seed is acceptable for v2.0 — or describe the edit you made and the new SHA-256 value.</resume-signal>
</task>

</tasks>

<verification>
Wave 0 is complete when:
- `jq -e '.binaries | length == 4' scripts/install/binaries.json` exits 0
- `sha256sum scripts/install/wordlists/arjun-extended-pt-en.txt` output equals `jq -r '.wordlists["arjun-extended-pt-en.txt"].sha256' scripts/install/wordlists.json`
- `bats tests/install/bats.bats` passes all self-check tests
- `bats --version` and `shellcheck --version` both succeed
- `vendor/wordlists/` directory exists (empty, ready for Plan 05)
- User approved the Arjun wordlist seed (checkpoint)
</verification>

<success_criteria>
Wave 0 ready: all downstream plans (02-06) can begin in parallel where dependencies allow. No plan in Wave 1+ needs to create manifests, install bats, or bootstrap helpers — all are on disk and green.
</success_criteria>

<output>
After completion, create `.planning/phases/08-infrastructure-install/08-01-SUMMARY.md` with: manifest file paths, computed Arjun wordlist SHA-256, bats version installed, and confirmation that all downstream plans can now read `binaries.json` / `wordlists.json` via `jq -r '.binaries.<name>.<field>' scripts/install/binaries.json`.
</output>
