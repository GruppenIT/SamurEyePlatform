---
phase: 15-journey-orchestration-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/__tests__/journeyOrchestration.test.ts
  - server/__tests__/rateLimiter.test.ts
  - server/__tests__/abortRoute.test.ts
  - server/__tests__/healthzTarget.test.ts
autonomous: true
requirements:
  - JRNY-01
  - JRNY-02
  - JRNY-03
  - JRNY-05
  - SAFE-01
  - SAFE-02
  - SAFE-03
  - SAFE-04
  - SAFE-05
  - SAFE-06

must_haves:
  truths:
    - "4 test files existem com it.todo stubs cobrindo 11 requisitos da Phase 15"
    - "Cada stub nomeia explicitamente o requisito (JRNY-XX / SAFE-XX) no título do test"
    - "Executar `npx vitest run server/__tests__/journeyOrchestration.test.ts server/__tests__/rateLimiter.test.ts server/__tests__/abortRoute.test.ts server/__tests__/healthzTarget.test.ts` retorna 0 (todos it.todo passam como pending)"
    - "Nenhum stub contém imports de código de produção que ainda não existe (stubs são pure placeholders)"
  artifacts:
    - path: "server/__tests__/journeyOrchestration.test.ts"
      provides: "Nyquist stubs para JRNY-01, JRNY-02, JRNY-03, SAFE-03, SAFE-04, SAFE-06"
      min_lines: 40
    - path: "server/__tests__/rateLimiter.test.ts"
      provides: "Nyquist stubs para SAFE-01, SAFE-02"
      min_lines: 25
    - path: "server/__tests__/abortRoute.test.ts"
      provides: "Nyquist stubs para JRNY-05"
      min_lines: 20
    - path: "server/__tests__/healthzTarget.test.ts"
      provides: "Nyquist stubs para SAFE-05"
      min_lines: 20
  key_links:
    - from: "15-VALIDATION.md §Per-Task Verification Map"
      to: "server/__tests__/*.test.ts"
      via: "Cada Task ID (15-0X-XX) aponta para um test no arquivo de stubs"
      pattern: "it\\.todo\\(['\"].*?(JRNY|SAFE)-\\d+"
---

<objective>
Criar 4 arquivos de teste com `it.todo` stubs (convenção Nyquist do projeto, estabelecida nas Phases 12-14) que servirão como alvos de `<verify>` para os Planos 02-04 da Phase 15.

Purpose: Garantir que Waves 2-3 só sejam consideradas completas quando os testes correspondentes passarem. Como Phase 15 é fase de wiring, não há lógica algorítmica nova — os stubs existem para ancorar o contrato de cada requisito e evitar que implementação ocorra sem verificação.

Output: 4 arquivos de teste com stubs nomeados que referenciam explicitamente JRNY-01..05 e SAFE-01..06. Nenhum `.test.ts` importa código que ainda não existe (stubs puros).
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/15-journey-orchestration-safety/15-CONTEXT.md
@.planning/phases/15-journey-orchestration-safety/15-RESEARCH.md
@.planning/phases/15-journey-orchestration-safety/15-VALIDATION.md

# Reference: convenção Nyquist estabelecida
@server/__tests__/apiCredentials/guard.test.ts
</context>

<interfaces>
<!-- Convenção Nyquist confirmada em Phases 12-14 via busca no histórico: -->
<!-- Arquivos .test.ts em server/__tests__/ usam vitest describe + it.todo -->
<!-- Estrutura: describe('Requisito XXX-YY', () => { it.todo('behavior'); }); -->

Vitest it.todo semantics (vitest >= 0.30):
- `it.todo('descrição')` marca test como pendente — NÃO falha a suíte, reporta como "pending"
- Arquivo .test.ts só com it.todo executa em < 100ms e retorna exit code 0
- Quando implementação chegar, substitui `it.todo(...)` por `it(..., () => { ... })` com assertions

Imports permitidos em stubs:
- `import { describe, it } from 'vitest'` — SEMPRE OK
- NÃO importar código de produção que ainda não existe (TokenBucketRateLimiter, executeApiSecurity, etc.)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Criar server/__tests__/journeyOrchestration.test.ts com 6 describe blocks (JRNY-01, JRNY-02, JRNY-03, SAFE-03, SAFE-04, SAFE-06)</name>
  <files>server/__tests__/journeyOrchestration.test.ts</files>
  <read_first>
    - server/__tests__/apiCredentials/guard.test.ts (convenção Nyquist: estrutura de describe + it.todo usada em Phases 10-14)
    - .planning/phases/15-journey-orchestration-safety/15-VALIDATION.md (lista exata dos test IDs esperados: 15-02-01, 15-02-02, 15-03-02, 15-04-02, 15-04-04)
    - .planning/phases/15-journey-orchestration-safety/15-RESEARCH.md §Phase Requirements → Test Map (descrição da behavior de cada requisito)
  </read_first>
  <action>
Criar arquivo `server/__tests__/journeyOrchestration.test.ts` com exatamente este conteúdo:

```typescript
/**
 * Phase 15 — Nyquist stubs for journey orchestration requirements.
 * Stubs use `it.todo` — replaced with real `it(..., () => {})` by Plan 02-04 executors.
 *
 * Test files that DO NOT import production code yet (pure placeholders).
 * Convention: Phases 10-14 established this pattern.
 */
import { describe, it } from 'vitest';

describe('JRNY-01 — journey_type enum contains api_security', () => {
  it.todo('shared/schema.ts journeyTypeEnum array inclui "api_security" como 5o valor');
  it.todo('switch statement em journeyExecutor.ts roteia case api_security para executeApiSecurity()');
});

describe('JRNY-02 — authorizationAck é obrigatório antes de iniciar api_security', () => {
  it.todo('executeApiSecurity lança Error "requer acknowledgment" se journey.authorizationAck !== true');
  it.todo('migration adiciona coluna authorization_ack boolean NOT NULL DEFAULT false em journeys');
  it.todo('insertJourneySchema (derivado via createInsertSchema) inclui authorizationAck como boolean');
});

describe('JRNY-03 — discovery+testing opts fluem do journey.params para os sub-orchestrators', () => {
  it.todo('executeApiSecurity lê journey.params.discoveryOpts e passa para discoverApi()');
  it.todo('executeApiSecurity lê journey.params.passiveOpts e passa para runApiPassiveTests()');
  it.todo('executeApiSecurity lê journey.params.activeOpts e passa para runApiActiveTests()');
});

describe('SAFE-03 — destructive gate antes de active tests', () => {
  it.todo('quando opts.destructiveEnabled=false, activeOpts sanitizados antes de runApiActiveTests');
  it.todo('quando opts.destructiveEnabled=true, activeOpts passam inalterados para runApiActiveTests');
});

describe('SAFE-04 — audit log ao início e fim da execução', () => {
  it.todo('logAudit chamado com action="start", objectType="api_security_journey" no início');
  it.todo('logAudit chamado com action="complete" (ou "failed") com outcome + findingsCount + duration no fim');
  it.todo('audit log after.credentialIds contém apenas UUIDs (sem secretEncrypted/dekEncrypted)');
});

describe('SAFE-06 — logs nunca contêm request body, credenciais ou tokens', () => {
  it.todo('durante dryRun, pino output não contém regex /(password|token|apiKey|authorization|bearer|secret)/i com valores reais');
  it.todo('logs emitidos em executeApiSecurity só contêm campos permitidos: jobId, apiId, endpointId, stage, duration, statusCode, findingId, severity, category');
});
```

NOTAS DE EXECUÇÃO:
- Arquivo NÃO importa nenhum módulo de server/services/ ou shared/schema.ts — são stubs puros.
- Cada `describe` cita o ID do requisito e uma descrição curta.
- Cada `it.todo` descreve a behavior esperada em português (convenção do projeto).
- Total: 14 stubs cobrindo 6 requisitos.
- NÃO usar `.only`, `.skip`, `.each` — apenas `describe` + `it.todo`.
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/journeyOrchestration.test.ts --reporter=verbose 2>&1 | grep -E "^(Tests|Test Files)"</automated>
  </verify>
  <acceptance_criteria>
    - File `server/__tests__/journeyOrchestration.test.ts` exists
    - `grep -c "it.todo" server/__tests__/journeyOrchestration.test.ts` returns 14
    - `grep -c "describe" server/__tests__/journeyOrchestration.test.ts` returns exactly 6
    - File contains exact string `describe('JRNY-01 —`
    - File contains exact string `describe('JRNY-02 —`
    - File contains exact string `describe('JRNY-03 —`
    - File contains exact string `describe('SAFE-03 —`
    - File contains exact string `describe('SAFE-04 —`
    - File contains exact string `describe('SAFE-06 —`
    - File does NOT contain `import ... from '../services/` (no production imports)
    - File does NOT contain `import ... from '@shared/schema'` (no production imports)
    - Command `npx vitest run server/__tests__/journeyOrchestration.test.ts --reporter=verbose` exits with code 0
    - vitest output reports "14 todo" (or equivalent marker for pending tests)
  </acceptance_criteria>
  <done>Arquivo existe com 14 it.todo stubs, 6 describe blocks nomeando JRNY-01, JRNY-02, JRNY-03, SAFE-03, SAFE-04, SAFE-06 e vitest run retorna exit 0 sem erros.</done>
</task>

<task type="auto">
  <name>Task 2: Criar server/__tests__/rateLimiter.test.ts (SAFE-01, SAFE-02) + server/__tests__/abortRoute.test.ts (JRNY-05) + server/__tests__/healthzTarget.test.ts (SAFE-05)</name>
  <files>server/__tests__/rateLimiter.test.ts, server/__tests__/abortRoute.test.ts, server/__tests__/healthzTarget.test.ts</files>
  <read_first>
    - server/__tests__/apiCredentials/guard.test.ts (convenção Nyquist)
    - .planning/phases/15-journey-orchestration-safety/15-VALIDATION.md §Per-Task Verification Map (IDs 15-03-01, 15-04-01, 15-04-03)
    - .planning/phases/15-journey-orchestration-safety/15-RESEARCH.md §Pattern 3 (TokenBucketRateLimiter signature), §Pattern 4 (Abort Route), §Pattern 5 (healthz endpoint)
  </read_first>
  <action>
Criar 3 arquivos:

### A) `server/__tests__/rateLimiter.test.ts`

```typescript
/**
 * Phase 15 — SAFE-01/SAFE-02 Nyquist stubs.
 * Implementação chega no Plano 15-03.
 */
import { describe, it } from 'vitest';

describe('SAFE-01 — TokenBucketRateLimiter ceiling + default', () => {
  it.todo('MAX_API_RATE_LIMIT constant exported from rateLimiter.ts equals 50');
  it.todo('new TokenBucketRateLimiter() com arg undefined usa ratePerSecond=10 (default)');
  it.todo('new TokenBucketRateLimiter(100) clampa silenciosamente para 50 (sem throw)');
  it.todo('new TokenBucketRateLimiter(25) respeita 25 req/s (dentro do ceiling)');
  it.todo('acquire() bloqueia até token disponível quando bucket vazio');
  it.todo('acquire() retorna imediatamente quando tokens >= 1 (bucket tem capacity)');
});

describe('SAFE-02 — Retry-After + exponential backoff', () => {
  it.todo('handleRetryAfter(headers) com Retry-After: "2" aguarda 2000ms');
  it.todo('handleRetryAfter(headers) sem header Retry-After aguarda 1000ms (default)');
  it.todo('exponentialBackoff(0) aguarda ~1000ms (base 1s)');
  it.todo('exponentialBackoff(3) aguarda ~8000ms (base 1s * 2^3)');
  it.todo('exponentialBackoff(10) clampa em 30_000ms (max jitter-adjusted)');
  it.todo('exponentialBackoff aplica jitter ±20% do base delay');
});
```

### B) `server/__tests__/abortRoute.test.ts`

```typescript
/**
 * Phase 15 — JRNY-05 Nyquist stubs.
 * Implementação chega no Plano 15-04.
 */
import { describe, it } from 'vitest';

describe('JRNY-05 — POST /api/v1/jobs/:id/abort', () => {
  it.todo('rota registrada em server/routes/jobs.ts responde 404 para jobId inexistente');
  it.todo('rota responde 400 quando job.status !== "running"');
  it.todo('rota chama jobQueue.markJobAsCancelled(id) antes de killAll');
  it.todo('rota chama processTracker.killAll(id) e retorna killedProcesses count');
  it.todo('rota atualiza storage.updateJob(id, { status: "failed", error, finishedAt })');
  it.todo('rota emite jobQueue.emit("jobUpdate", ...) com status "failed"');
  it.todo('rota chama storage.logAudit com action="abort", objectType="job"');
  it.todo('rota retorna JSON { message: "Jornada abortada", killedProcesses: N }');
  it.todo('rota original POST /api/jobs/:id/cancel-process permanece funcional (backward compat)');
});
```

### C) `server/__tests__/healthzTarget.test.ts`

```typescript
/**
 * Phase 15 — SAFE-05 Nyquist stubs.
 * Implementação chega no Plano 15-03.
 */
import { describe, it } from 'vitest';

describe('SAFE-05 — GET /healthz/api-test-target', () => {
  it.todo('rota registrada em server/routes/index.ts NÃO exige autenticação session');
  it.todo('GET /healthz/api-test-target retorna 200 com Content-Type application/json');
  it.todo('response body tem { status: "ok", dryRun: true, mockFindings: [...] }');
  it.todo('mockFindings contém exatamente 4 itens cobrindo severidades low/medium/high/critical');
  it.todo('mockFindings[*].category são valores válidos do owaspApiCategoryEnum');
  it.todo('mockFindings[*].title começa com prefixo "Mock: " (indica dados fictícios)');
  it.todo('rota NÃO faz nenhuma query ao DB (response é hardcoded)');
});
```

NOTAS:
- Todos os 3 arquivos usam apenas `import { describe, it } from 'vitest'`.
- Nenhum importa produção.
- Cada descrição é auto-explicativa em pt-BR (convenção do projeto).
  </action>
  <verify>
    <automated>npx vitest run server/__tests__/rateLimiter.test.ts server/__tests__/abortRoute.test.ts server/__tests__/healthzTarget.test.ts --reporter=verbose 2>&1 | grep -E "^(Tests|Test Files)"</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: `server/__tests__/rateLimiter.test.ts`, `server/__tests__/abortRoute.test.ts`, `server/__tests__/healthzTarget.test.ts`
    - `grep -c "it.todo" server/__tests__/rateLimiter.test.ts` returns 12
    - `grep -c "it.todo" server/__tests__/abortRoute.test.ts` returns 9
    - `grep -c "it.todo" server/__tests__/healthzTarget.test.ts` returns 7
    - `rateLimiter.test.ts` contains `describe('SAFE-01 —` and `describe('SAFE-02 —`
    - `abortRoute.test.ts` contains `describe('JRNY-05 —`
    - `healthzTarget.test.ts` contains `describe('SAFE-05 —`
    - Nenhum dos 3 arquivos contém `import ... from '../services/` ou `import ... from '@shared/schema'`
    - Command `npx vitest run server/__tests__/rateLimiter.test.ts server/__tests__/abortRoute.test.ts server/__tests__/healthzTarget.test.ts` exits code 0
    - Output reporta total de 28 pending (12+9+7)
  </acceptance_criteria>
  <done>Os 3 arquivos de teste existem com 28 stubs it.todo total, todos pt-BR, e vitest run de todos juntos retorna exit 0.</done>
</task>

</tasks>

<verification>
# Smoke test final — todos os 4 arquivos Wave 0 em uma execução
npx vitest run \
  server/__tests__/journeyOrchestration.test.ts \
  server/__tests__/rateLimiter.test.ts \
  server/__tests__/abortRoute.test.ts \
  server/__tests__/healthzTarget.test.ts \
  --reporter=verbose

# Deve reportar 42 tests todo (14 + 12 + 9 + 7) e exit code 0.
</verification>

<success_criteria>
- Os 4 arquivos Nyquist existem e são executáveis via vitest run
- Total agregado: exatamente 42 it.todo stubs cobrindo JRNY-01..05, SAFE-01..06 (11 requisitos)
- Nenhum stub importa código de produção (puro placeholder)
- Comando validador único do <verification> block retorna exit code 0
</success_criteria>

<output>
Após completar, criar `.planning/phases/15-journey-orchestration-safety/15-01-SUMMARY.md` descrevendo os 4 arquivos criados, contagem final de stubs, e confirmação de que nenhum import de produção foi adicionado.
</output>
