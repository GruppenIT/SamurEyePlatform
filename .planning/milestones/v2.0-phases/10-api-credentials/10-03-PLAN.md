---
phase: 10-api-credentials
plan: 03
type: execute
wave: 1
depends_on: [10-01]
files_modified:
  - server/services/credentials/matchUrlPattern.ts
  - server/services/credentials/decodeJwtExp.ts
autonomous: true
requirements: [CRED-03, CRED-04]
must_haves:
  truths:
    - "matchUrlPattern(pattern, url) converte glob em regex e retorna boolean determinístico"
    - "isValidUrlPattern(pattern) rejeita patterns inválidos (`**`, vazio) e aceita os válidos"
    - "decodeJwtExp(jwt) retorna `Date` quando JWT tem claim `exp`, e `null` para JWT opaco/malformado (NUNCA lança)"
    - "Os helpers são puros (sem I/O, sem dependências de banco) e testáveis isoladamente"
  artifacts:
    - path: "server/services/credentials/matchUrlPattern.ts"
      provides: "Helper glob→regex + validação de pattern (consumido por resolveApiCredential no Plan 04)"
      exports: ["matchUrlPattern", "isValidUrlPattern"]
    - path: "server/services/credentials/decodeJwtExp.ts"
      provides: "Helper de parse do exp claim para popular bearerExpiresAt no POST"
      exports: ["decodeJwtExp"]
  key_links:
    - from: "server/services/credentials/matchUrlPattern.ts"
      to: "server/__tests__/apiCredentials/urlPattern.test.ts"
      via: "import { matchUrlPattern, isValidUrlPattern } from '../../services/credentials/matchUrlPattern'"
      pattern: "from ['\"]\\.\\./\\.\\./services/credentials/matchUrlPattern['\"]"
---

<objective>
Wave 1 — Implementar os 2 helpers stateless do Phase 10: `matchUrlPattern` (glob→regex) e `decodeJwtExp`. Ambos são funções puras consumidas no Plan 04 (storage facade — `resolveApiCredential` e `createApiCredential`).

Purpose: Helpers podem ser implementados em paralelo com o schema (Plan 02) porque NÃO dependem de schema/DB. São puro TypeScript com `node:buffer` para o JWT decode.
Output: 2 arquivos em `server/services/credentials/` totalizando ~50 linhas, mais ~100 linhas de testes ativados.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-api-credentials/10-CONTEXT.md
@.planning/phases/10-api-credentials/10-RESEARCH.md
@.planning/phases/10-api-credentials/10-VALIDATION.md

@.planning/phases/10-api-credentials/10-01-SUMMARY.md

# Stubs Wave 0 que este plan ATIVA (substitui it.todo por it real)
@server/__tests__/apiCredentials/urlPattern.test.ts
@server/__tests__/helpers/apiCredentialFactory.ts

<interfaces>
<!-- Contratos exatos a implementar -->

// server/services/credentials/matchUrlPattern.ts
export function matchUrlPattern(pattern: string, url: string): boolean;
export function isValidUrlPattern(pattern: string): boolean;

// server/services/credentials/decodeJwtExp.ts
export function decodeJwtExp(jwt: string): Date | null;

// Algoritmo de conversão glob→regex (10-RESEARCH.md §"Padrão 5"):
//   - Escapa todos caracteres regex EXCETO `*`
//   - Substitui `*` por `[^/]*` (não cruza `/`)
//   - Wraps com `^` e `$`

// Whitelist de pattern válido:
//   /^[a-zA-Z0-9:/.*?=&_\-{}~!$'()+,;%@#]+$/
//   Rejeita: vazio, contém `**`, caractere fora da whitelist
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar matchUrlPattern + isValidUrlPattern em server/services/credentials/matchUrlPattern.ts</name>
  <files>server/services/credentials/matchUrlPattern.ts, server/__tests__/apiCredentials/urlPattern.test.ts</files>
  <read_first>
    - .planning/phases/10-api-credentials/10-RESEARCH.md (seção "Padrão 5: Helper matchUrlPattern.ts" linhas 419-444 — código verbatim)
    - .planning/phases/10-api-credentials/10-RESEARCH.md (seção "SC3" linhas 798-810 + tabela URL_PATTERN_MATRIX — todos os casos a passar)
    - server/__tests__/helpers/apiCredentialFactory.ts (ver `URL_PATTERN_MATRIX` e `VALID_PATTERN_CASES` — testes vão consumir)
    - server/__tests__/apiCredentials/urlPattern.test.ts (stubs Wave 0 que precisam ser ativados)
    - server/services/ (Bash `ls server/services/` para confirmar que `credentials/` ainda não existe — precisa criar a pasta)
  </read_first>
  <behavior>
    - Test (CRED-03): URL_PATTERN_MATRIX — todas as 9+ entradas passam (cada [pattern, url, expected] confirmado)
    - Test (CRED-03): VALID_PATTERN_CASES — `**` retorna false, vazio retorna false, `*` e `https://api.corp.com/*` retornam true
    - Test (CRED-03): match exato sem glob — trailing slash NÃO casa
    - Test (CRED-03): glob `*` em path NÃO cruza `/` (`https://api.corp.com/*` casa `/v2/users` mas não `/v2/users/123`)
    - Test (CRED-03): caracteres regex (`.`, `(`, `)`, `[`, `]`, `{`, `}`) são escapados para que sejam tratados como literais
  </behavior>
  <action>
**Sub-task A: Criar `server/services/credentials/matchUrlPattern.ts`**

Verificar via Bash `ls server/services/credentials/` se a pasta existe; se não, criar via `mkdir -p server/services/credentials`.

Criar o arquivo com EXATAMENTE este conteúdo (copiar verbatim de 10-RESEARCH.md §"Padrão 5"):

```ts
// Phase 10 — CRED-03: helper de matching glob→regex para URL patterns.
// Função pura, sem I/O, sem dependências externas. Consumida por resolveApiCredential
// (Plan 04) e potencialmente pelo runtime do Phase 11.
//
// Algoritmo (CONTEXT.md decisões):
//   - `*` no path = `[^/]*` (não cruza barra)
//   - `*` no host = `[^/]*` (idem; URL completa é tratada uniformemente)
//   - Demais caracteres regex são escapados (literais)

const REGEX_SPECIAL = /[.+^${}()|[\]\\]/g;

export function matchUrlPattern(pattern: string, url: string): boolean {
  if (!pattern || !url) return false;
  const escaped = pattern
    .replace(REGEX_SPECIAL, "\\$&") // escapa caracteres regex especiais (exceto *)
    .replace(/\*/g, "[^/]*");        // wildcards não cruzam barra
  const regex = new RegExp(`^${escaped}$`);
  return regex.test(url);
}

// Whitelist conservadora — rejeita patterns inválidos no POST.
// Runtime (Phase 11) confia que o pattern armazenado é válido.
const VALID_PATTERN = /^[a-zA-Z0-9:/.*?=&_\-{}~!$'()+,;%@#]+$/;

export function isValidUrlPattern(pattern: string): boolean {
  if (!pattern || pattern.length === 0) return false;
  if (pattern.includes("**")) return false; // ambíguo
  return VALID_PATTERN.test(pattern);
}
```

**Sub-task B: Ativar stubs em `server/__tests__/apiCredentials/urlPattern.test.ts`**

ABRIR o arquivo (criado no Plan 01) e SUBSTITUIR `it.todo` por `it` real, implementando os asserts. Estrutura final:

```ts
import { describe, it, expect } from "vitest";
import { matchUrlPattern, isValidUrlPattern } from "../../services/credentials/matchUrlPattern";
import { URL_PATTERN_MATRIX, VALID_PATTERN_CASES } from "../helpers/apiCredentialFactory";

describe("matchUrlPattern (Phase 10 — CRED-03)", () => {
  it.each(URL_PATTERN_MATRIX)(
    "%s vs %s → %s (%s)",
    (pattern, url, expected, _description) => {
      expect(matchUrlPattern(pattern, url)).toBe(expected);
    }
  );

  describe("casos individuais explicitos", () => {
    it("* casa qualquer URL", () => {
      expect(matchUrlPattern("*", "https://any.url/path")).toBe(true);
    });
    it("https://api.corp.com/* casa /v2/users mas nao /v2/users/123", () => {
      expect(matchUrlPattern("https://api.corp.com/*", "https://api.corp.com/v2/users")).toBe(true);
      expect(matchUrlPattern("https://api.corp.com/*", "https://api.corp.com/v2/users/123")).toBe(false);
    });
    it("*.prod.example.com/* casa api.prod.example.com mas nao api.staging.example.com", () => {
      expect(matchUrlPattern("*.prod.example.com/*", "https://api.prod.example.com/v1")).toBe(true);
      expect(matchUrlPattern("*.prod.example.com/*", "https://api.staging.example.com/v1")).toBe(false);
    });
    it("trailing slash NAO casa em match exato", () => {
      expect(matchUrlPattern("https://api.corp.com/v2/users", "https://api.corp.com/v2/users")).toBe(true);
      expect(matchUrlPattern("https://api.corp.com/v2/users", "https://api.corp.com/v2/users/")).toBe(false);
    });
    it("path params {id} sao caracteres literais", () => {
      expect(matchUrlPattern("https://api.corp.com/v2/users/{id}", "https://api.corp.com/v2/users/{id}")).toBe(true);
      expect(matchUrlPattern("https://api.corp.com/v2/users/{id}", "https://api.corp.com/v2/users/123")).toBe(false);
    });
  });

  describe("escape de caracteres regex", () => {
    it("ponto em pattern e tratado como literal (nao .)", () => {
      expect(matchUrlPattern("a.b", "aXb")).toBe(false);
      expect(matchUrlPattern("a.b", "a.b")).toBe(true);
    });
    it("parenteses e colchetes sao escapados", () => {
      expect(matchUrlPattern("foo(bar)", "foo(bar)")).toBe(true);
      expect(matchUrlPattern("foo[bar]", "foo[bar]")).toBe(true);
    });
  });

  describe("guards de input vazio", () => {
    it("pattern vazio retorna false", () => {
      expect(matchUrlPattern("", "https://api.corp.com")).toBe(false);
    });
    it("url vazia retorna false", () => {
      expect(matchUrlPattern("*", "")).toBe(false);
    });
  });
});

describe("isValidUrlPattern (Phase 10 — CRED-03)", () => {
  it.each(VALID_PATTERN_CASES)(
    "isValidUrlPattern(%s) → %s (%s)",
    (pattern, expected, _description) => {
      expect(isValidUrlPattern(pattern)).toBe(expected);
    }
  );
  it("** retorna false (ambiguo)", () => {
    expect(isValidUrlPattern("**")).toBe(false);
  });
  it("string vazia retorna false", () => {
    expect(isValidUrlPattern("")).toBe(false);
  });
  it("* retorna true", () => {
    expect(isValidUrlPattern("*")).toBe(true);
  });
  it("padrao com wildcard retorna true", () => {
    expect(isValidUrlPattern("https://api.corp.com/*")).toBe(true);
  });
  it("rejeita pattern com caractere fora da whitelist", () => {
    expect(isValidUrlPattern("https://api.corp.com/<script>")).toBe(false);
  });
});
```

Rodar `npm run test -- server/__tests__/apiCredentials/urlPattern.test.ts` e confirmar exit 0 com todos os testes verdes (não mais skipped).
  </action>
  <verify>
    <automated>npm run test -- server/__tests__/apiCredentials/urlPattern.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `server/services/credentials/matchUrlPattern.ts` exists
    - Grep `export function matchUrlPattern\(pattern: string, url: string\): boolean` returns 1 match
    - Grep `export function isValidUrlPattern\(pattern: string\): boolean` returns 1 match
    - Grep `\.replace\(/\\\*/g, ['"]\[\^/\]\*['"]\)` returns 1 match (algoritmo de conversão * → [^/]*)
    - Grep `pattern\.includes\(['"]\\\*\\\*['"]\)` returns 1 match (rejeição de **)
    - File `server/__tests__/apiCredentials/urlPattern.test.ts` no longer contains `it\.todo` (grep returns 0 matches)
    - `npm run test -- server/__tests__/apiCredentials/urlPattern.test.ts` exits 0
    - Test output shows ≥ 20 passing tests for urlPattern (URL_PATTERN_MATRIX expansion + individual cases + isValidUrlPattern)
  </acceptance_criteria>
  <done>matchUrlPattern + isValidUrlPattern implementados, todos asserts da matriz passam, 0 it.todo restantes em urlPattern.test.ts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar decodeJwtExp em server/services/credentials/decodeJwtExp.ts</name>
  <files>server/services/credentials/decodeJwtExp.ts, server/__tests__/apiCredentials/jwtExp.test.ts</files>
  <read_first>
    - .planning/phases/10-api-credentials/10-RESEARCH.md (seção "Padrão 7" linhas 484-498 — código verbatim)
    - .planning/phases/10-api-credentials/10-CONTEXT.md (seção "Bearer JWT" linhas 130-133 — comportamento de falha silenciosa)
    - server/services/credentials/matchUrlPattern.ts (criado na Task 1 — mesma pasta destino)
  </read_first>
  <behavior>
    - Test (CRED-01): JWT com `exp` numérico válido retorna `Date` correto (`exp * 1000`)
    - Test (CRED-01): JWT opaco (1 segmento) retorna `null` sem lançar
    - Test (CRED-01): JWT com payload inválido (não-base64url) retorna `null` sem lançar
    - Test (CRED-01): JWT com payload sem `exp` retorna `null`
    - Test (CRED-01): JWT com `exp` não-numérico (string) retorna `null`
  </behavior>
  <action>
**Sub-task A: Criar `server/services/credentials/decodeJwtExp.ts`**

```ts
// Phase 10 — CRED-01: helper de parse do claim `exp` de um JWT.
// Usado pelo POST /api/v1/api-credentials para popular bearerExpiresAt
// quando o usuário cadastra credencial bearer_jwt.
//
// Falha silenciosa por design (CONTEXT.md): JWT opaco/malformado retorna null,
// não lança — o backend aceita o JWT mesmo assim.

export function decodeJwtExp(jwt: string): Date | null {
  try {
    if (typeof jwt !== "string" || jwt.length === 0) return null;
    const parts = jwt.split(".");
    if (parts.length < 2) return null;
    const payload = JSON.parse(Buffer.from(parts[1], "base64url").toString("utf8"));
    if (typeof payload.exp !== "number" || !Number.isFinite(payload.exp)) return null;
    return new Date(payload.exp * 1000);
  } catch {
    return null;
  }
}
```

**Sub-task B: Criar `server/__tests__/apiCredentials/jwtExp.test.ts`** (novo arquivo de teste — não estava no Wave 0 porque é um helper menor; criar agora junto da implementação para fechar o ciclo).

```ts
import { describe, it, expect } from "vitest";
import { decodeJwtExp } from "../../services/credentials/decodeJwtExp";

function makeJwt(payload: Record<string, unknown>): string {
  const header = Buffer.from(JSON.stringify({ alg: "HS256", typ: "JWT" })).toString("base64url");
  const body = Buffer.from(JSON.stringify(payload)).toString("base64url");
  return `${header}.${body}.signature`;
}

describe("decodeJwtExp (Phase 10 — CRED-01)", () => {
  it("JWT com exp numerico retorna Date correto (exp * 1000)", () => {
    const epochSeconds = 9999999999;
    const jwt = makeJwt({ sub: "user", exp: epochSeconds });
    const result = decodeJwtExp(jwt);
    expect(result).toBeInstanceOf(Date);
    expect(result?.getTime()).toBe(epochSeconds * 1000);
  });

  it("JWT sem claim exp retorna null", () => {
    const jwt = makeJwt({ sub: "user" });
    expect(decodeJwtExp(jwt)).toBeNull();
  });

  it("JWT com exp string nao-numerico retorna null", () => {
    const jwt = makeJwt({ sub: "user", exp: "tomorrow" });
    expect(decodeJwtExp(jwt)).toBeNull();
  });

  it("JWT opaco (1 segmento) retorna null sem lancar", () => {
    expect(() => decodeJwtExp("opaco-sem-pontos")).not.toThrow();
    expect(decodeJwtExp("opaco-sem-pontos")).toBeNull();
  });

  it("JWT com payload nao-base64url retorna null sem lancar", () => {
    expect(() => decodeJwtExp("h.notValidBase64URL!!!.s")).not.toThrow();
    expect(decodeJwtExp("h.notValidBase64URL!!!.s")).toBeNull();
  });

  it("string vazia retorna null", () => {
    expect(decodeJwtExp("")).toBeNull();
  });

  it("exp = NaN ou Infinity retorna null", () => {
    const jwtNaN = makeJwt({ exp: NaN });
    expect(decodeJwtExp(jwtNaN)).toBeNull();
  });
});
```

Rodar `npm run test -- server/__tests__/apiCredentials/jwtExp.test.ts` e confirmar exit 0.
  </action>
  <verify>
    <automated>npm run test -- server/__tests__/apiCredentials/jwtExp.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `server/services/credentials/decodeJwtExp.ts` exists
    - Grep `export function decodeJwtExp\(jwt: string\): Date \| null` returns 1 match
    - Grep `Buffer\.from\(parts\[1\], ['"]base64url['"]\)` returns 1 match
    - Grep `typeof payload\.exp !== ['"]number['"]` returns 1 match
    - Grep `} catch ` returns 1 match (try/catch para falha silenciosa)
    - File `server/__tests__/apiCredentials/jwtExp.test.ts` exists
    - `npm run test -- server/__tests__/apiCredentials/jwtExp.test.ts` exits 0
    - Test output shows ≥ 7 passing tests
  </acceptance_criteria>
  <done>decodeJwtExp implementado com falha silenciosa, 7+ testes verdes.</done>
</task>

</tasks>

<verification>
- `npm run test -- server/__tests__/apiCredentials` exit 0 (urlPattern + jwtExp + outros stubs Wave 0 ainda skipped)
- `npm run test` exit 0 (suite completa)
- `ls server/services/credentials/` lista 2 arquivos
- Nenhum import de `@shared` ou `db` nos helpers (grep prova pureza)
</verification>

<success_criteria>
- 2 helpers puros criados, ambos completamente testados (URL_PATTERN_MATRIX + JWT exp casos)
- 0 dependências de banco/schema — Plan 04 pode importar sem ciclo
- ~25 testes ativos nos 2 arquivos de teste deste plan
</success_criteria>

<output>
Após completion, criar `.planning/phases/10-api-credentials/10-03-SUMMARY.md` documentando:
- Arquivos criados (2 helpers + 1 teste novo do JWT)
- Quantidade de testes verdes por arquivo
- Confirmação de pureza (zero imports de produção além de `node:buffer`)
</output>
