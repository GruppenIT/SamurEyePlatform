---
phase: 10-api-credentials
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - server/__tests__/apiCredentials/schema.test.ts
  - server/__tests__/apiCredentials/storage.test.ts
  - server/__tests__/apiCredentials/route.test.ts
  - server/__tests__/apiCredentials/urlPattern.test.ts
  - server/__tests__/apiCredentials/resolveCredential.test.ts
  - server/__tests__/apiCredentials/guard.test.ts
  - server/__tests__/helpers/apiCredentialFactory.ts
autonomous: true
requirements: [CRED-01, CRED-02, CRED-03, CRED-04, CRED-05]
must_haves:
  truths:
    - "Os 6 test files do Phase 10 existem e podem ser invocados pelo vitest sem erro de import"
    - "O factory compartilhado `createTestApiCredential(authType, overrides)` existe e expõe payload válido para os 7 auth types"
    - "Constantes `URL_PATTERN_MATRIX`, `TEST_PEM_CERT`, `TEST_PEM_KEY` são exportadas pelo factory e consumíveis pelos demais testes"
    - "`npm run test -- server/__tests__/apiCredentials` exit code 0 (todos os stubs `it.todo` ou skip controlado)"
  artifacts:
    - path: "server/__tests__/apiCredentials/schema.test.ts"
      provides: "Stubs Nyquist do insertApiCredentialSchema (CRED-01)"
      contains: "describe('insertApiCredentialSchema'"
    - path: "server/__tests__/apiCredentials/storage.test.ts"
      provides: "Stubs Nyquist do storage facade + encryption round-trip (CRED-01, CRED-02, CRED-04)"
      contains: "describe('apiCredentials storage'"
    - path: "server/__tests__/apiCredentials/route.test.ts"
      provides: "Stubs Nyquist do contrato dos 7 auth types via POST /api/v1/api-credentials (CRED-01, CRED-05)"
      contains: "describe('POST /api/v1/api-credentials'"
    - path: "server/__tests__/apiCredentials/urlPattern.test.ts"
      provides: "Stubs Nyquist do helper glob→regex (CRED-03)"
      contains: "describe('matchUrlPattern'"
    - path: "server/__tests__/apiCredentials/resolveCredential.test.ts"
      provides: "Stubs Nyquist da resolução por prioridade (CRED-04)"
      contains: "describe('resolveApiCredential'"
    - path: "server/__tests__/apiCredentials/guard.test.ts"
      provides: "Stubs Nyquist da idempotência de ensureApiCredentialTables()"
      contains: "describe('ensureApiCredentialTables'"
    - path: "server/__tests__/helpers/apiCredentialFactory.ts"
      provides: "Factory de payload + URL_PATTERN_MATRIX + PEM constants compartilhados pelos testes do phase"
      exports: ["createTestApiCredential", "URL_PATTERN_MATRIX", "TEST_PEM_CERT", "TEST_PEM_KEY"]
  key_links:
    - from: "server/__tests__/apiCredentials/schema.test.ts"
      to: "server/__tests__/helpers/apiCredentialFactory.ts"
      via: "import { createTestApiCredential, TEST_PEM_CERT, TEST_PEM_KEY }"
      pattern: "from ['\"]\\.\\./helpers/apiCredentialFactory['\"]"
    - from: "server/__tests__/apiCredentials/urlPattern.test.ts"
      to: "server/__tests__/helpers/apiCredentialFactory.ts"
      via: "import { URL_PATTERN_MATRIX }"
      pattern: "URL_PATTERN_MATRIX"
---

<objective>
Wave 0 — Criar TODA a infraestrutura de testes Nyquist do Phase 10 antes de qualquer linha de código de produção.

Purpose: Garantir que cada task subsequente (Plans 02–05) tenha um comando `<automated>` válido para referenciar em `acceptance_criteria`, fechando o ciclo de validação Nyquist obrigatório (`workflow.nyquist_validation: true`).
Output: 6 arquivos de teste em `server/__tests__/apiCredentials/` (todos com `describe` + `it.todo` placeholders) + 1 helper compartilhado em `server/__tests__/helpers/apiCredentialFactory.ts`. Nenhum código de produção é tocado neste plan.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-api-credentials/10-CONTEXT.md
@.planning/phases/10-api-credentials/10-RESEARCH.md
@.planning/phases/10-api-credentials/10-VALIDATION.md

# Templates Nyquist do Phase 9 (precedente exato)
@server/__tests__/apiSchema.test.ts
@server/__tests__/ensureApiTables.test.ts
@server/__tests__/apisRoute.test.ts
@server/__tests__/apiStorage.test.ts

# Vitest config (já inclui server/**/*.test.ts e shared/**/*.test.ts)
@vitest.config.ts

<interfaces>
<!-- Interfaces que os stubs vão referenciar (ainda não existem; este plan usa it.todo). -->
<!-- A factory deve produzir payloads compatíveis com a discriminated union descrita em 10-RESEARCH.md §"Padrão 2". -->

Auth types suportados (ordem fixa, do CONTEXT.md):
  type ApiAuthType =
    | 'api_key_header'
    | 'api_key_query'
    | 'bearer_jwt'
    | 'basic'
    | 'oauth2_client_credentials'
    | 'hmac'
    | 'mtls';

Shape do payload válido por auth type (10-RESEARCH.md §Validation Architecture > Fixtures):
  api_key_header           → { authType, apiKeyHeaderName: 'X-API-Key', secret: 'test-api-key-value' }
  api_key_query            → { authType, apiKeyQueryParam: 'api_key', secret: 'test-api-key-value' }
  bearer_jwt               → { authType, secret: '<JWT 3-segment string>' }
  basic                    → { authType, basicUsername: 'testuser', secret: 'testpassword' }
  oauth2_client_credentials → { authType, oauth2ClientId, oauth2TokenUrl, oauth2Scope?, oauth2Audience?, secret }
  hmac                     → { authType, hmacKeyId, hmacAlgorithm: 'HMAC-SHA256', hmacSignatureHeader, hmacSignedHeaders[], hmacCanonicalTemplate?, secret }
  mtls                     → { authType, mtlsCert: PEM, mtlsKey: PEM, mtlsCa?: PEM }

Base comum a todos: { name, description?, urlPattern, priority, apiId? }

URL_PATTERN_MATRIX: 4-tuplas [pattern, url, expected (boolean), description (string)] cobrindo todos os casos
da tabela em 10-RESEARCH.md §"SC3 — URL pattern (glob) match" (mínimo 9 entradas).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar helper compartilhado apiCredentialFactory.ts</name>
  <files>server/__tests__/helpers/apiCredentialFactory.ts</files>
  <read_first>
    - .planning/phases/10-api-credentials/10-RESEARCH.md (seções "Fixtures Reutilizáveis Recomendadas", linhas ~975-1050; "URL_PATTERN_MATRIX" linhas ~1039-1050)
    - .planning/phases/10-api-credentials/10-CONTEXT.md (seção "Auth-type specifics" para campos por tipo)
    - server/__tests__/fixtures/ (listar via Bash `ls server/__tests__/fixtures/` para ver convenção de nomes existente)
  </read_first>
  <action>
Criar `server/__tests__/helpers/apiCredentialFactory.ts` com TODAS as exportações listadas abaixo (copiar verbatim do RESEARCH.md §"Fixtures Reutilizáveis Recomendadas").

EXPORTS obrigatórios:

1. `export type ApiAuthType` — union literal dos 7 tipos na ordem:
   `'api_key_header' | 'api_key_query' | 'bearer_jwt' | 'basic' | 'oauth2_client_credentials' | 'hmac' | 'mtls'`

2. `export const TEST_PEM_CERT = "-----BEGIN CERTIFICATE-----\\nMIIBpTCCAQ+gAwIBAgIUtest\\n-----END CERTIFICATE-----"`
   `export const TEST_PEM_KEY  = "-----BEGIN PRIVATE KEY-----\\nMIIBVAIBADANBgkqtest\\n-----END PRIVATE KEY-----"`
   (Ambos passam o regex `/-----BEGIN [A-Z ]+-----[\s\S]+-----END [A-Z ]+-----/`. Strings dummy — não são chaves reais.)

3. `export function createTestApiCredential(authType: ApiAuthType, overrides: Record<string, unknown> = {}): Record<string, unknown>` — switch/Record exatamente como em 10-RESEARCH.md linhas 996-1036. Valores literais por tipo:
   - `api_key_header`: `{ authType, apiKeyHeaderName: 'X-API-Key', secret: 'test-api-key-value' }`
   - `api_key_query`: `{ authType, apiKeyQueryParam: 'api_key', secret: 'test-api-key-value' }`
   - `bearer_jwt`: `{ authType, secret: 'eyJhbGciOiJSUzI1NiJ9.eyJzdWIiOiJ1c2VyIiwiZXhwIjo5OTk5OTk5OTk5fQ.sig' }` (JWT com `exp` muito futuro para teste de bearerExpiresAt)
   - `basic`: `{ authType, basicUsername: 'testuser', secret: 'testpassword' }`
   - `oauth2_client_credentials`: `{ authType, oauth2ClientId: 'test-client-id', oauth2TokenUrl: 'https://auth.example.com/oauth/token', oauth2Scope: 'read:api', oauth2Audience: 'https://api.example.com', secret: 'test-client-secret' }`
   - `hmac`: `{ authType, hmacKeyId: 'key-2024-01', hmacAlgorithm: 'HMAC-SHA256', hmacSignatureHeader: 'Authorization', hmacSignedHeaders: ['host', 'x-date'], hmacCanonicalTemplate: null, secret: 'test-hmac-secret-key' }`
   - `mtls`: `{ authType, mtlsCert: TEST_PEM_CERT, mtlsKey: TEST_PEM_KEY, mtlsCa: null }`
   Base prepended a tudo: `{ name: \`test-${authType}-cred\`, description: null, urlPattern: '*', priority: 100, apiId: null }`.
   Spread overrides por último.

4. `export const URL_PATTERN_MATRIX: Array<[string, string, boolean, string]>` — copiar verbatim do RESEARCH.md §"SC3 — URL pattern (glob) match" (linhas ~798-810 + 1041-1050). Mínimo 9 entradas, cobrindo:
   - `['*', 'https://any.url/path', true, 'wildcard global casa qualquer URL']`
   - `['https://api.corp.com/*', 'https://api.corp.com/v2/users', true, 'glob path simples casa']`
   - `['https://api.corp.com/*', 'https://api.corp.com/v2/users/123', false, 'glob nao cruza barra']`
   - `['https://api.corp.com/v2/*', 'https://api.corp.com/v2/users/123', false, 'glob nao cruza barra (deep)']`
   - `['*.prod.example.com/*', 'https://api.prod.example.com/v1', true, 'glob no host casa']`
   - `['*.prod.example.com/*', 'https://api.staging.example.com/v1', false, 'host diferente nao casa']`
   - `['https://api.corp.com/v2/users', 'https://api.corp.com/v2/users', true, 'match exato casa']`
   - `['https://api.corp.com/v2/users', 'https://api.corp.com/v2/users/', false, 'trailing slash nao casa']`
   - `['https://api.corp.com/v2/users/{id}', 'https://api.corp.com/v2/users/{id}', true, 'path params literais casam literalmente']`

5. `export const VALID_PATTERN_CASES: Array<[string, boolean, string]>` — para `isValidUrlPattern`:
   - `['**', false, 'duplo asterisco rejeitado por ambiguidade']`
   - `['', false, 'string vazia rejeitada']`
   - `['https://api.corp.com/*', true, 'pattern com wildcard valido']`
   - `['*', true, 'wildcard global valido']`

NÃO importar nada de produção (`@shared/schema`, `server/services/...`) — este arquivo é puro factory de dados.

Cabeçalho do arquivo:
```ts
// Phase 10 Wave 0 — fixtures compartilhadas para testes de api_credentials.
// Não importa código de produção; serve como fonte única de verdade para payloads de teste.
```
  </action>
  <verify>
    <automated>npm run test -- server/__tests__/apiCredentials/urlPattern.test.ts 2>&1 | grep -E "no test files|0 tests|Test Files"</automated>
  </verify>
  <acceptance_criteria>
    - File `server/__tests__/helpers/apiCredentialFactory.ts` exists
    - Grep `export function createTestApiCredential` returns 1 match in the file
    - Grep `export const URL_PATTERN_MATRIX` returns 1 match in the file
    - Grep `export const TEST_PEM_CERT` returns 1 match in the file
    - Grep `export const TEST_PEM_KEY` returns 1 match in the file
    - Grep `export const VALID_PATTERN_CASES` returns 1 match in the file
    - Grep `'api_key_header'` and `'api_key_query'` and `'bearer_jwt'` and `'basic'` and `'oauth2_client_credentials'` and `'hmac'` and `'mtls'` each appear at least once in the file
    - File has zero imports from `@shared/schema` or `server/services/` (grep `from ['"]@shared|from ['"]\.\./\.\./services` returns 0 matches)
  </acceptance_criteria>
  <done>Helper file existe, exporta as 5 entidades obrigatórias, contém os 7 auth types literais e zero imports de produção.</done>
</task>

<task type="auto">
  <name>Task 2: Criar 6 stubs de teste com it.todo cobrindo todos os requirements CRED-01..05</name>
  <files>
    server/__tests__/apiCredentials/schema.test.ts,
    server/__tests__/apiCredentials/storage.test.ts,
    server/__tests__/apiCredentials/route.test.ts,
    server/__tests__/apiCredentials/urlPattern.test.ts,
    server/__tests__/apiCredentials/resolveCredential.test.ts,
    server/__tests__/apiCredentials/guard.test.ts
  </files>
  <read_first>
    - server/__tests__/helpers/apiCredentialFactory.ts (criado na Task 1 — ver exports disponíveis)
    - server/__tests__/apiSchema.test.ts (template Phase 9 de stubs Nyquist com it.todo — copiar estilo)
    - server/__tests__/ensureApiTables.test.ts (template do guard test do Phase 9)
    - .planning/phases/10-api-credentials/10-VALIDATION.md (seção "Wave 0 Requirements" — lista exata dos 6 stubs obrigatórios)
    - .planning/phases/10-api-credentials/10-RESEARCH.md (seção "Validation Architecture" linhas 760-960 — casos exaustivos por arquivo)
  </read_first>
  <action>
Criar OS 6 ARQUIVOS abaixo, cada um com `describe` + N `it.todo` (zero implementação real). Todo `it.todo` deve ter título descritivo em pt-BR. Importar `vitest` (`import { describe, it, expect } from 'vitest'`) e o factory quando necessário.

────────────────────────────────────────────────────────────────────
**Arquivo 1: `server/__tests__/apiCredentials/schema.test.ts`** (CRED-01)

Imports: `describe, it, expect` de `vitest`; `createTestApiCredential, TEST_PEM_CERT, TEST_PEM_KEY` de `../helpers/apiCredentialFactory`.

Estrutura:
```ts
describe('insertApiCredentialSchema (Phase 10 — CRED-01)', () => {
  describe('aceita os 7 auth types com payload valido', () => {
    it.todo('aceita api_key_header com apiKeyHeaderName + secret');
    it.todo('aceita api_key_query com apiKeyQueryParam + secret');
    it.todo('aceita bearer_jwt com secret (JWT string)');
    it.todo('aceita basic com basicUsername + secret');
    it.todo('aceita oauth2_client_credentials com oauth2ClientId + oauth2TokenUrl + secret');
    it.todo('aceita hmac com hmacKeyId + hmacAlgorithm HMAC-SHA256 + secret');
    it.todo('aceita mtls com mtlsCert + mtlsKey PEM (sem mtlsCa)');
    it.todo('aceita mtls com mtlsCert + mtlsKey + mtlsCa todos PEM');
  });

  describe('rejeita payload invalido', () => {
    it.todo('rejeita authType desconhecido (ex: saml)');
    it.todo('rejeita api_key_header sem apiKeyHeaderName');
    it.todo('rejeita api_key_header com secret string vazia');
    it.todo('rejeita api_key_query sem apiKeyQueryParam');
    it.todo('rejeita bearer_jwt sem secret');
    it.todo('rejeita basic sem basicUsername');
    it.todo('rejeita basic sem secret');
    it.todo('rejeita oauth2_client_credentials sem oauth2ClientId');
    it.todo('rejeita oauth2_client_credentials com oauth2TokenUrl nao-URL');
    it.todo('rejeita hmac sem hmacKeyId');
    it.todo('rejeita hmac com hmacAlgorithm nao suportado (ex: HMAC-MD5)');
    it.todo('rejeita mtls sem mtlsCert');
    it.todo('rejeita mtls com mtlsCert string nao-PEM');
    it.todo('rejeita cross-type: bearer_jwt + apiKeyHeaderName presente');
  });
});
```

────────────────────────────────────────────────────────────────────
**Arquivo 2: `server/__tests__/apiCredentials/storage.test.ts`** (CRED-01, CRED-02)

Imports: `describe, it, expect` de `vitest`; `createTestApiCredential, TEST_PEM_CERT, TEST_PEM_KEY` de `../helpers/apiCredentialFactory`.

Estrutura:
```ts
describe('apiCredentials storage facade (Phase 10 — CRED-01, CRED-02)', () => {
  describe('createApiCredential — encryption round-trip por auth type', () => {
    it.todo('api_key_header: persiste secret cifrado via encryptionService.encryptCredential');
    it.todo('api_key_query: persiste secret cifrado');
    it.todo('bearer_jwt: persiste JWT inteiro como secret cifrado');
    it.todo('basic: persiste senha cifrada (basicUsername em coluna plain)');
    it.todo('oauth2_client_credentials: persiste client_secret cifrado (clientId em coluna plain)');
    it.todo('hmac: persiste hmacSecretKey cifrada (hmacKeyId em coluna plain)');
    it.todo('mtls: persiste JSON.stringify({cert,key,ca}) cifrado (multi-part composite)');
  });

  describe('decryptCredential round-trip', () => {
    it.todo('api_key_header: decryptCredential retorna o API key original');
    it.todo('mtls: decrypt + JSON.parse retorna { cert, key, ca } com 3 PEMs originais');
    it.todo('mtls sem ca: decrypt + JSON.parse retorna objeto sem chave ca definida');
  });

  describe('sanitizacao de secrets na resposta', () => {
    it.todo('listApiCredentials() nunca retorna secretEncrypted nem dekEncrypted');
    it.todo('getApiCredential(id) nunca retorna secretEncrypted nem dekEncrypted');
    it.todo('getApiCredentialWithSecret(id) RETORNA secretEncrypted e dekEncrypted (uso interno do executor)');
  });

  describe('bearerExpiresAt derivado do JWT', () => {
    it.todo('bearer_jwt com exp valido popula bearerExpiresAt no insert');
    it.todo('bearer_jwt opaco (sem exp ou nao-decodavel) aceita sem erro com bearerExpiresAt null');
  });

  describe('UNIQUE constraint', () => {
    it.todo('createApiCredential com (name, createdBy) duplicado lanca erro 23505');
    it.todo('createApiCredential com mesmo name por usuarios diferentes nao conflita');
  });

  describe('FK ON DELETE SET NULL', () => {
    it.todo('deletar API referenciada nao deleta a credencial — apiId vira NULL');
  });
});
```

────────────────────────────────────────────────────────────────────
**Arquivo 3: `server/__tests__/apiCredentials/route.test.ts`** (CRED-01, CRED-05)

Imports: `describe, it, expect` de `vitest`; `createTestApiCredential` de `../helpers/apiCredentialFactory`.

Estrutura:
```ts
describe('POST /api/v1/api-credentials (Phase 10 — CRED-01, CRED-05)', () => {
  describe('contrato dos 7 auth types — POST retorna 201 com ApiCredentialSafe', () => {
    it.todo('POST api_key_header retorna 201 e response inclui id, name, authType, urlPattern, priority, createdAt');
    it.todo('POST api_key_query retorna 201 com shape ApiCredentialSafe');
    it.todo('POST bearer_jwt retorna 201 e popula bearerExpiresAt quando JWT tem exp');
    it.todo('POST bearer_jwt opaco retorna 201 com bearerExpiresAt null');
    it.todo('POST basic retorna 201 com basicUsername no response e secret ausente');
    it.todo('POST oauth2_client_credentials retorna 201 com oauth2ClientId, oauth2TokenUrl no response');
    it.todo('POST hmac retorna 201 com hmacKeyId, hmacAlgorithm, hmacSignatureHeader no response');
    it.todo('POST mtls retorna 201 sem expor cert/key no response (so id, name, authType)');
  });

  describe('sanitizacao da resposta', () => {
    it.todo('Response 201 NUNCA contem secretEncrypted nem dekEncrypted');
    it.todo('Response 201 NUNCA contem secret/mtlsCert/mtlsKey/mtlsCa em texto puro');
  });

  describe('codigos de erro', () => {
    it.todo('POST com payload invalido (Zod fail) retorna 400 com message pt-BR');
    it.todo('POST com nome duplicado para o mesmo createdBy retorna 409 com message "Credencial ja cadastrada com esse nome"');
    it.todo('POST sem autenticacao retorna 401');
    it.todo('POST com role read_only retorna 403');
    it.todo('POST com role operator retorna 201');
    it.todo('POST com role global_administrator retorna 201');
  });

  describe('GET /api/v1/api-credentials', () => {
    it.todo('GET lista todas credenciais sanitizadas (sem secret*/dek*)');
    it.todo('GET com filter ?apiId=X lista somente credenciais com apiId=X');
    it.todo('GET com filter ?authType=basic lista somente credenciais basic');
  });

  describe('GET /api/v1/api-credentials/:id', () => {
    it.todo('GET por id retorna 200 sanitizado');
    it.todo('GET por id inexistente retorna 404');
  });

  describe('PATCH /api/v1/api-credentials/:id', () => {
    it.todo('PATCH atualiza name + description sem tocar secret');
    it.todo('PATCH com secret novo re-criptografa e atualiza secretEncrypted/dekEncrypted');
    it.todo('PATCH com urlPattern invalido retorna 400');
  });

  describe('DELETE /api/v1/api-credentials/:id', () => {
    it.todo('DELETE remove credencial e retorna 204');
    it.todo('DELETE de id inexistente retorna 404');
  });

  describe('logging seguro', () => {
    it.todo('log.info de criacao inclui apiCredentialId e authType — nunca secret nem mtlsCert');
  });
});
```

────────────────────────────────────────────────────────────────────
**Arquivo 4: `server/__tests__/apiCredentials/urlPattern.test.ts`** (CRED-03)

Imports: `describe, it, expect` de `vitest`; `URL_PATTERN_MATRIX, VALID_PATTERN_CASES` de `../helpers/apiCredentialFactory`.

Estrutura:
```ts
describe('matchUrlPattern (Phase 10 — CRED-03)', () => {
  it.todo('cobre todos os casos da URL_PATTERN_MATRIX [pattern,url,expected]');
  describe('casos individuais explicitos da matriz', () => {
    it.todo('* casa qualquer URL (wildcard global)');
    it.todo('https://api.corp.com/* casa /v2/users mas nao /v2/users/123 (* nao cruza /)');
    it.todo('*.prod.example.com/* casa api.prod.example.com mas nao api.staging.example.com');
    it.todo('match exato sem glob: trailing slash NAO casa');
    it.todo('path params {id} sao tratados como caracteres literais');
  });
  describe('escape de caracteres regex', () => {
    it.todo('ponto em pattern e escapado (literal . no regex resultante)');
    it.todo('parenteses, colchetes, chaves regex sao escapados');
  });
});

describe('isValidUrlPattern (Phase 10 — CRED-03)', () => {
  it.todo('cobre todos os casos da VALID_PATTERN_CASES [pattern,expected]');
  it.todo('** retorna false (ambiguo)');
  it.todo('string vazia retorna false');
  it.todo('* retorna true');
  it.todo('https://api.corp.com/* retorna true');
});
```

────────────────────────────────────────────────────────────────────
**Arquivo 5: `server/__tests__/apiCredentials/resolveCredential.test.ts`** (CRED-04)

Imports: `describe, it, expect` de `vitest`.

Estrutura:
```ts
describe('resolveApiCredential (Phase 10 — CRED-04)', () => {
  describe('priority como tie-break primario', () => {
    it.todo('multiplas credenciais casam → retorna a com menor priority');
    it.todo('priority 50 vence priority 100 quando ambas casam o mesmo URL');
  });

  describe('specificity como tie-break secundario (mais literais ganha)', () => {
    it.todo('mesma priority — pattern mais especifico (mais literais) ganha');
    it.todo('https://api.corp.com/v2/users vence https://api.corp.com/* quando ambos casam');
  });

  describe('createdAt como tie-break terciario', () => {
    it.todo('mesma priority + mesma specificity → mais antiga ganha (createdAt ASC)');
  });

  describe('escopo apiId vs global', () => {
    it.todo('credencial com apiId=X candidata para query apiId=X');
    it.todo('credencial global (apiId IS NULL) candidata para qualquer apiId');
    it.todo('credencial com apiId=Y NAO candidata para query apiId=X');
  });

  describe('caso negativo', () => {
    it.todo('nenhuma credencial casa o URL → retorna null (nao lanca erro)');
  });

  describe('shape de retorno', () => {
    it.todo('resolveApiCredential retorna ApiCredentialSafe (sem secret*/dek*)');
  });
});
```

────────────────────────────────────────────────────────────────────
**Arquivo 6: `server/__tests__/apiCredentials/guard.test.ts`** (CRED-01..05 infra)

Imports: `describe, it, expect` de `vitest`.

Estrutura:
```ts
describe('ensureApiCredentialTables (Phase 10 — guard idempotente)', () => {
  describe('idempotencia', () => {
    it.todo('primeira execucao cria enum api_auth_type via CREATE TYPE');
    it.todo('segunda execucao detecta enum existente em pg_type e nao recria');
    it.todo('primeira execucao cria tabela api_credentials');
    it.todo('segunda execucao detecta tabela em pg_tables e nao recria');
    it.todo('primeira execucao cria todos indexes (IDX_api_credentials_api_id, IDX_api_credentials_priority, UQ_api_credentials_name_created_by)');
    it.todo('segunda execucao detecta cada index em pg_indexes e nao recria');
  });

  describe('fallback mode (nao relancar erro)', () => {
    it.todo('erro durante criacao da enum nao propaga — log.error + return');
    it.todo('erro durante criacao da tabela nao propaga — app continua bootando');
  });

  describe('ordem de invocacao no boot', () => {
    it.todo('ensureApiCredentialTables() e chamado APOS ensureApiTables() em initializeDatabaseStructure');
  });
});
```

────────────────────────────────────────────────────────────────────

Após criar os 6 arquivos, rodar `npm run test -- server/__tests__/apiCredentials` e confirmar exit 0 (todos `it.todo` aparecem como `↓ skipped`).
  </action>
  <verify>
    <automated>npm run test -- server/__tests__/apiCredentials</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files exist under `server/__tests__/apiCredentials/`
    - `npm run test -- server/__tests__/apiCredentials` exits 0
    - Grep `it\.todo` in `server/__tests__/apiCredentials/schema.test.ts` returns ≥ 21 matches (8 positive + 13 negative cases minimum)
    - Grep `it\.todo` in `server/__tests__/apiCredentials/storage.test.ts` returns ≥ 15 matches
    - Grep `it\.todo` in `server/__tests__/apiCredentials/route.test.ts` returns ≥ 24 matches
    - Grep `it\.todo` in `server/__tests__/apiCredentials/urlPattern.test.ts` returns ≥ 9 matches
    - Grep `it\.todo` in `server/__tests__/apiCredentials/resolveCredential.test.ts` returns ≥ 9 matches
    - Grep `it\.todo` in `server/__tests__/apiCredentials/guard.test.ts` returns ≥ 9 matches
    - Each file imports from `'vitest'` (grep `from ['"]vitest['"]` returns ≥ 1 in every file)
    - Each non-guard test file references the factory (grep `from ['"]\.\./helpers/apiCredentialFactory['"]` returns ≥ 1 in schema, storage, route, urlPattern test files)
  </acceptance_criteria>
  <done>Os 6 stubs existem, todos os títulos `it.todo` estão em pt-BR conforme especificado, suite roda com exit 0 em < 30 segundos.</done>
</task>

</tasks>

<verification>
- Suite Nyquist do Phase 10 invocável: `npm run test -- server/__tests__/apiCredentials` exit 0
- Suite completa não regride: `npm run test` exit 0 (a adição de stubs todo não afeta nada)
- Helper compartilhado é o único arquivo que outros testes importam
</verification>

<success_criteria>
- 6 test files + 1 helper criados
- 0 testes ativos (todos `it.todo` ou skip)
- Vitest reporta suíte como "passed" com X tests skipped
- Próximas waves podem referenciar `npm run test -- server/__tests__/apiCredentials/<file>.test.ts` em seus `acceptance_criteria` sem precisar criar nada
</success_criteria>

<output>
Após completion, criar `.planning/phases/10-api-credentials/10-01-SUMMARY.md` documentando:
- Lista exata dos 7 arquivos criados (6 stubs + 1 factory)
- Quantidade de `it.todo` por arquivo
- Comando exato para rodar a suíte do phase
</output>
