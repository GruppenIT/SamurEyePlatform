---
phase: 10-api-credentials
plan: 02
type: execute
wave: 1
depends_on: [10-01]
files_modified:
  - shared/schema.ts
autonomous: true
requirements: [CRED-01, CRED-02]
must_haves:
  truths:
    - "shared/schema.ts exporta `apiAuthTypeEnum` pgEnum com EXATAMENTE os 7 valores na ordem fixa: api_key_header, api_key_query, bearer_jwt, basic, oauth2_client_credentials, hmac, mtls"
    - "shared/schema.ts exporta `apiCredentials` pgTable com colunas comuns + por-tipo + crypto + auditoria"
    - "shared/schema.ts exporta `insertApiCredentialSchema` como `z.discriminatedUnion('authType', [...7 variantes])` que valida shape correto por auth type"
    - "shared/schema.ts exporta tipos `ApiCredential`, `ApiCredentialSafe`, `ApiCredentialWithSecret`, `InsertApiCredential`"
    - "Stubs Wave 0 do schema.test.ts compilam sem erro de import"
    - "Tabela tem UNIQUE constraint em (name, createdBy) e FK apiId → apis(id) com ON DELETE SET NULL"
  artifacts:
    - path: "shared/schema.ts"
      provides: "apiAuthTypeEnum pgEnum + apiCredentials pgTable + insertApiCredentialSchema discriminated union + tipos exportados"
      contains: "pgEnum('api_auth_type'"
      min_lines_added: 200
  key_links:
    - from: "shared/schema.ts"
      to: "shared/schema.ts (apis table do Phase 9)"
      via: "FK apiCredentials.apiId references apis.id ON DELETE SET NULL"
      pattern: "references\\(\\(\\) => apis\\.id"
    - from: "shared/schema.ts"
      to: "shared/schema.ts (users table)"
      via: "FK createdBy/updatedBy references users.id"
      pattern: "references\\(\\(\\) => users\\.id"
    - from: "shared/schema.ts apiAuthTypeEnum"
      to: "shared/schema.ts apiCredentials.authType column"
      via: "apiAuthTypeEnum('auth_type').notNull()"
      pattern: "apiAuthTypeEnum\\('auth_type'\\)"
---

<objective>
Wave 1 — Adicionar TODA a definição de schema do Phase 10 em `shared/schema.ts`: pgEnum, pgTable, relations, discriminated union de inserção, e tipos derivados.

Purpose: Bloqueia desbloqueio de Plan 04 (storage facade) e Plan 05 (route). Toda a forma de dados do Phase 10 vive aqui — única fonte de verdade.
Output: Adições em `shared/schema.ts` (~200 linhas novas), zero modificações em código existente.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/10-api-credentials/10-CONTEXT.md
@.planning/phases/10-api-credentials/10-RESEARCH.md
@.planning/phases/10-api-credentials/10-VALIDATION.md
@.planning/phases/10-api-credentials/10-01-SUMMARY.md

# Stub Wave 0 que este plan ativa (deve continuar passando como skipped)
@server/__tests__/apiCredentials/schema.test.ts
@server/__tests__/helpers/apiCredentialFactory.ts

# Templates verificados no codebase
@shared/schema.ts

<interfaces>
<!-- Pontos de inserção em shared/schema.ts (verificados via Read em 2026-04-19) -->

Padrão de pgEnum existente (linha 38):
  export const credentialTypeEnum = pgEnum("credential_type", ["ssh", "wmi", "omi", "ad"]);

Padrão de pgTable de credenciais (linhas 147-159) — referência para campos secret:
  secretEncrypted: text("secret_encrypted").notNull(),
  dekEncrypted: text("dek_encrypted").notNull(),

Padrão de insertSchema (linhas 877-884):
  const baseInsert = createInsertSchema(credentials).omit({ id: true, secretEncrypted: true, dekEncrypted: true, createdAt: true });

Padrão Phase 9 — apis table (referência para FK + index):
  apis pgTable já existe; apiCredentials.apiId references apis.id ON DELETE SET NULL

Padrão Phase 9 — uso de `index()` e `uniqueIndex()`:
  (table) => [
    index('IDX_<table>_<col>').on(table.<col>),
    uniqueIndex('UQ_<table>_<cols>').on(table.<col1>, table.<col2>),
  ]

Padrão Phase 9 — array column:
  text('col_name').array() (Drizzle) — DDL Postgres `TEXT[]`

Tipos derivados (padrão do projeto):
  export type Xxx = typeof xxxTable.$inferSelect;
  export type InsertXxx = typeof xxxTable.$inferInsert;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar apiAuthTypeEnum + apiCredentials pgTable + relations em shared/schema.ts</name>
  <files>shared/schema.ts</files>
  <read_first>
    - shared/schema.ts (LER COMPLETO — precisa entender ordem das tabelas: apiCredentials deve vir APÓS o bloco do Phase 9 `apis`/`apiEndpoints`/`apiFindings`)
    - .planning/phases/10-api-credentials/10-CONTEXT.md (seção "Tabela `api_credentials`" linhas 34-62 — colunas obrigatórias + tipos)
    - .planning/phases/10-api-credentials/10-RESEARCH.md (seção "Padrão 1: pgEnum + pgTable" linhas 157-217 — código verbatim)
    - server/__tests__/apiCredentials/schema.test.ts (stubs Wave 0 — futura implementação consumirá esta forma)
  </read_first>
  <behavior>
    - Test 1 (CRED-01): pgEnum `api_auth_type` existe com 7 valores na ordem fixa
    - Test 2 (CRED-01): pgTable `api_credentials` exporta 23+ colunas (10 comuns + 13 por-tipo)
    - Test 3 (CRED-02): tabela tem `secretEncrypted` (text notNull) + `dekEncrypted` (text notNull)
    - Test 4: FK `apiId` references `apis.id` ON DELETE SET NULL (nullable)
    - Test 5: FK `createdBy` references `users.id` notNull; `updatedBy` references `users.id` nullable
    - Test 6: 3 indexes: IDX_api_credentials_api_id, IDX_api_credentials_priority, UQ_api_credentials_name_created_by
  </behavior>
  <action>
ABRIR `shared/schema.ts` e adicionar as seguintes definições. Localizar o bloco do Phase 9 (`apis`, `apiEndpoints`, `apiFindings`) via grep (`grep -n "export const apis = pgTable\|export const apiFindings"`) e inserir o novo bloco IMEDIATAMENTE APÓS as relations do Phase 9 (mas antes da seção de insertSchemas).

────────────────────────────────────────────────────────────────────
**Inserção 1: pgEnum** (após `credentialTypeEnum` ou ao lado dos enums do Phase 9):

```ts
// Phase 10 — API Credentials (CRED-01)
// Ordem fixa — qualquer adição futura cria nova enum em vez de ALTER TYPE.
export const apiAuthTypeEnum = pgEnum("api_auth_type", [
  "api_key_header",
  "api_key_query",
  "bearer_jwt",
  "basic",
  "oauth2_client_credentials",
  "hmac",
  "mtls",
]);
```

────────────────────────────────────────────────────────────────────
**Inserção 2: pgTable apiCredentials** (após o bloco do Phase 9):

```ts
// Phase 10 — API Credentials (CRED-01..05)
// Tabela isolada — NÃO estende `credentials` legada (ssh/wmi/omi/ad).
// Secrets cifrados via encryptionService (KEK/DEK existente, AES-256-GCM).
export const apiCredentials = pgTable("api_credentials", {
  // --- identidade ---
  id: varchar("id").primaryKey().default(sql`gen_random_uuid()`),
  name: text("name").notNull(),
  description: text("description"),
  authType: apiAuthTypeEnum("auth_type").notNull(),

  // --- mapeamento (CRED-03, CRED-04) ---
  urlPattern: text("url_pattern").notNull().default("*"),
  priority: integer("priority").notNull().default(100),
  apiId: varchar("api_id").references(() => apis.id, { onDelete: "set null" }),

  // --- crypto (CRED-02) ---
  secretEncrypted: text("secret_encrypted").notNull(),
  dekEncrypted: text("dek_encrypted").notNull(),

  // --- por auth type (nullable, validados via Zod discriminated union) ---
  apiKeyHeaderName: text("api_key_header_name"),
  apiKeyQueryParam: text("api_key_query_param"),
  basicUsername: text("basic_username"),
  bearerExpiresAt: timestamp("bearer_expires_at"),
  oauth2ClientId: text("oauth2_client_id"),
  oauth2TokenUrl: text("oauth2_token_url"),
  oauth2Scope: text("oauth2_scope"),
  oauth2Audience: text("oauth2_audience"),
  hmacKeyId: text("hmac_key_id"),
  hmacAlgorithm: text("hmac_algorithm"), // HMAC-SHA1 | HMAC-SHA256 | HMAC-SHA512
  hmacSignatureHeader: text("hmac_signature_header"),
  hmacSignedHeaders: text("hmac_signed_headers").array(),
  hmacCanonicalTemplate: text("hmac_canonical_template"),

  // --- auditoria ---
  createdAt: timestamp("created_at").defaultNow().notNull(),
  updatedAt: timestamp("updated_at").defaultNow().notNull(),
  createdBy: varchar("created_by").references(() => users.id).notNull(),
  updatedBy: varchar("updated_by").references(() => users.id),
}, (table) => [
  index("IDX_api_credentials_api_id").on(table.apiId),
  index("IDX_api_credentials_priority").on(table.priority),
  uniqueIndex("UQ_api_credentials_name_created_by").on(table.name, table.createdBy),
]);
```

────────────────────────────────────────────────────────────────────
**Inserção 3: relations** (junto com outras relations existentes do projeto):

```ts
export const apiCredentialsRelations = relations(apiCredentials, ({ one }) => ({
  api: one(apis, {
    fields: [apiCredentials.apiId],
    references: [apis.id],
  }),
  creator: one(users, {
    fields: [apiCredentials.createdBy],
    references: [users.id],
    relationName: "apiCredentialCreator",
  }),
  updater: one(users, {
    fields: [apiCredentials.updatedBy],
    references: [users.id],
    relationName: "apiCredentialUpdater",
  }),
}));
```

────────────────────────────────────────────────────────────────────
Garantir que TODOS os imports necessários já existem no topo do arquivo:
- `pgEnum, pgTable, text, varchar, integer, timestamp, index, uniqueIndex` de `drizzle-orm/pg-core`
- `relations` de `drizzle-orm`
- `sql` de `drizzle-orm`

Se algum estiver faltando, adicionar ao import existente. NÃO criar novo import block.

NÃO TOCAR EM NADA QUE JÁ EXISTE no arquivo — apenas adições aditivas.

Validar localmente que `tsc --noEmit` (ou `npm run typecheck` se existir) passa sem erro relacionado a apiCredentials.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "apiCredentials|api_auth_type" | head -10; npm run test -- server/__tests__/apiCredentials/schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `shared/schema.ts` modified
    - Grep `pgEnum\(['"]api_auth_type['"]` in `shared/schema.ts` returns 1 match
    - Grep `'api_key_header'` AND `'api_key_query'` AND `'bearer_jwt'` AND `'basic'` AND `'oauth2_client_credentials'` AND `'hmac'` AND `'mtls'` each return ≥ 1 match in `shared/schema.ts`
    - Grep `pgTable\(['"]api_credentials['"]` in `shared/schema.ts` returns 1 match
    - Grep `secretEncrypted: text\(['"]secret_encrypted['"]\)\.notNull\(\)` returns 1 match
    - Grep `dekEncrypted: text\(['"]dek_encrypted['"]\)\.notNull\(\)` returns 1 match
    - Grep `apiId: varchar\(['"]api_id['"]\)\.references\(\(\) => apis\.id, \{ onDelete: ['"]set null['"]` returns 1 match
    - Grep `IDX_api_credentials_api_id` returns ≥ 1 match
    - Grep `IDX_api_credentials_priority` returns ≥ 1 match
    - Grep `UQ_api_credentials_name_created_by` returns ≥ 1 match
    - Grep `apiCredentialsRelations` returns 1 match
    - Grep `hmacSignedHeaders: text\(['"]hmac_signed_headers['"]\)\.array\(\)` returns 1 match
    - `npm run test -- server/__tests__/apiCredentials/schema.test.ts` exits 0
  </acceptance_criteria>
  <done>pgEnum + pgTable + relations adicionados, todos indexes corretos, FKs corretas, stubs Wave 0 continuam verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar insertApiCredentialSchema (discriminated union) + tipos derivados</name>
  <files>shared/schema.ts</files>
  <read_first>
    - shared/schema.ts (ver bloco de insertSchemas existentes — procurar `insertCredentialSchema` em ~linha 877 e o bloco do Phase 9 `insertApiSchema`)
    - .planning/phases/10-api-credentials/10-RESEARCH.md (seção "Padrão 2: Zod discriminated union" linhas 219-291 — código verbatim das 7 variantes)
    - .planning/phases/10-api-credentials/10-RESEARCH.md (seção "Armadilha 2" linhas 538-558 — campos por-tipo DEVEM ser omitidos do baseInsert)
    - server/__tests__/apiCredentials/schema.test.ts (stubs que serão habilitados em Plan 04)
  </read_first>
  <behavior>
    - Test 1 (CRED-01): insertApiCredentialSchema é discriminated union em `authType`
    - Test 2 (CRED-01): cada variante exige seus campos próprios (e.g. mtls exige mtlsCert + mtlsKey PEM)
    - Test 3 (Armadilha 2 do RESEARCH): variante `bearer_jwt` REJEITA `apiKeyHeaderName` (Zod não aceita campo de outro tipo)
    - Test 4: tipos `ApiCredential`, `ApiCredentialSafe`, `ApiCredentialWithSecret`, `InsertApiCredential` exportados
  </behavior>
  <action>
ADICIONAR no bloco de insertSchemas de `shared/schema.ts` (junto com `insertApiSchema` do Phase 9):

────────────────────────────────────────────────────────────────────
**Inserção 1: baseInsert + discriminated union**:

```ts
// Phase 10 — Discriminated union para insert de api_credentials (CRED-01)
// Armadilha 2 do RESEARCH: TODOS os campos por-tipo são omitidos do baseInsert
// para que cada variante REJEITE campos de outros tipos.
const baseInsertApiCredential = createInsertSchema(apiCredentials).omit({
  id: true,
  secretEncrypted: true,
  dekEncrypted: true,
  createdAt: true,
  updatedAt: true,
  createdBy: true,
  updatedBy: true,
  bearerExpiresAt: true,         // derivado no backend a partir do JWT exp
  // Campos por-tipo — omitir do base, adicionar SO na variante correta:
  apiKeyHeaderName: true,
  apiKeyQueryParam: true,
  basicUsername: true,
  oauth2ClientId: true,
  oauth2TokenUrl: true,
  oauth2Scope: true,
  oauth2Audience: true,
  hmacKeyId: true,
  hmacAlgorithm: true,
  hmacSignatureHeader: true,
  hmacSignedHeaders: true,
  hmacCanonicalTemplate: true,
});

// Regex PEM compartilhado (mTLS)
const PEM_REGEX = /-----BEGIN [A-Z ]+-----[\s\S]+-----END [A-Z ]+-----/;

export const insertApiCredentialSchema = z.discriminatedUnion("authType", [
  baseInsertApiCredential.extend({
    authType: z.literal("api_key_header"),
    apiKeyHeaderName: z.string().min(1, "Nome do header é obrigatório"),
    secret: z.string().min(1, "API key é obrigatória"),
  }),
  baseInsertApiCredential.extend({
    authType: z.literal("api_key_query"),
    apiKeyQueryParam: z.string().min(1, "Parâmetro de query é obrigatório"),
    secret: z.string().min(1, "API key é obrigatória"),
  }),
  baseInsertApiCredential.extend({
    authType: z.literal("bearer_jwt"),
    secret: z.string().min(1, "JWT é obrigatório"),
  }),
  baseInsertApiCredential.extend({
    authType: z.literal("basic"),
    basicUsername: z.string().min(1, "Username é obrigatório"),
    secret: z.string().min(1, "Senha é obrigatória"),
  }),
  baseInsertApiCredential.extend({
    authType: z.literal("oauth2_client_credentials"),
    oauth2ClientId: z.string().min(1, "Client ID é obrigatório"),
    oauth2TokenUrl: z.string().url("Token URL inválida"),
    oauth2Scope: z.string().optional(),
    oauth2Audience: z.string().optional(),
    secret: z.string().min(1, "Client secret é obrigatório"),
  }),
  baseInsertApiCredential.extend({
    authType: z.literal("hmac"),
    hmacKeyId: z.string().min(1, "Key ID é obrigatório"),
    hmacAlgorithm: z.enum(["HMAC-SHA1", "HMAC-SHA256", "HMAC-SHA512"]),
    hmacSignatureHeader: z.string().default("Authorization"),
    hmacSignedHeaders: z.array(z.string()).default([]),
    hmacCanonicalTemplate: z.string().nullable().optional(),
    secret: z.string().min(1, "HMAC secret key é obrigatória"),
  }),
  baseInsertApiCredential.extend({
    authType: z.literal("mtls"),
    mtlsCert: z.string().regex(PEM_REGEX, "Certificado PEM inválido"),
    mtlsKey: z.string().regex(PEM_REGEX, "Chave PEM inválida"),
    mtlsCa: z.string().regex(PEM_REGEX, "CA PEM inválida").nullable().optional(),
  }),
]);

// Schema de PATCH — flat com todos campos opcionais, exceto authType (imutável).
// Ver Pergunta em Aberto #3 do RESEARCH: discriminated union .partial() não é nativo.
export const patchApiCredentialSchema = z.object({
  name: z.string().min(1).optional(),
  description: z.string().nullable().optional(),
  urlPattern: z.string().min(1).optional(),
  priority: z.number().int().optional(),
  apiId: z.string().nullable().optional(),
  // Campos por-tipo — todos opcionais; rota valida que não cruzam authType
  apiKeyHeaderName: z.string().min(1).optional(),
  apiKeyQueryParam: z.string().min(1).optional(),
  basicUsername: z.string().min(1).optional(),
  oauth2ClientId: z.string().min(1).optional(),
  oauth2TokenUrl: z.string().url().optional(),
  oauth2Scope: z.string().optional(),
  oauth2Audience: z.string().optional(),
  hmacKeyId: z.string().min(1).optional(),
  hmacAlgorithm: z.enum(["HMAC-SHA1", "HMAC-SHA256", "HMAC-SHA512"]).optional(),
  hmacSignatureHeader: z.string().optional(),
  hmacSignedHeaders: z.array(z.string()).optional(),
  hmacCanonicalTemplate: z.string().nullable().optional(),
  // Re-encrypt do secret se passado
  secret: z.string().min(1).optional(),
  mtlsCert: z.string().regex(PEM_REGEX).optional(),
  mtlsKey: z.string().regex(PEM_REGEX).optional(),
  mtlsCa: z.string().regex(PEM_REGEX).nullable().optional(),
});
```

────────────────────────────────────────────────────────────────────
**Inserção 2: tipos derivados** (junto com outros `export type` no fim do arquivo):

```ts
// Phase 10 — Tipos derivados de api_credentials
export type ApiCredential = typeof apiCredentials.$inferSelect;
export type InsertApiCredential = z.infer<typeof insertApiCredentialSchema>;
export type PatchApiCredential = z.infer<typeof patchApiCredentialSchema>;
export type ApiAuthType = ApiCredential["authType"];

// Shape sanitizado retornado por listApiCredentials/getApiCredential (sem secret*/dek*)
export type ApiCredentialSafe = Omit<ApiCredential, "secretEncrypted" | "dekEncrypted">;

// Shape interno usado SO pelo executor (Phase 11+) via getApiCredentialWithSecret
export type ApiCredentialWithSecret = ApiCredential;
```

────────────────────────────────────────────────────────────────────
Garantir imports já existentes no topo do arquivo:
- `z` de `zod`
- `createInsertSchema` de `drizzle-zod`

NÃO TOCAR em código existente. Apenas adições aditivas.

Após editar, rodar typecheck local e verificar stub Wave 0.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "insertApiCredentialSchema|ApiCredentialSafe|patchApiCredentialSchema" | head -5; npm run test -- server/__tests__/apiCredentials/schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Grep `export const insertApiCredentialSchema = z\.discriminatedUnion\(["']authType["']` in `shared/schema.ts` returns 1 match
    - Grep `export const patchApiCredentialSchema = z\.object` returns 1 match
    - Grep `z\.literal\(["']api_key_header["']\)` returns 1 match
    - Grep `z\.literal\(["']api_key_query["']\)` returns 1 match
    - Grep `z\.literal\(["']bearer_jwt["']\)` returns 1 match
    - Grep `z\.literal\(["']basic["']\)` returns 1 match
    - Grep `z\.literal\(["']oauth2_client_credentials["']\)` returns 1 match
    - Grep `z\.literal\(["']hmac["']\)` returns 1 match
    - Grep `z\.literal\(["']mtls["']\)` returns 1 match
    - Grep `export type ApiCredential = typeof apiCredentials\.\$inferSelect` returns 1 match
    - Grep `export type InsertApiCredential = z\.infer<typeof insertApiCredentialSchema>` returns 1 match
    - Grep `export type ApiCredentialSafe = Omit<ApiCredential, ["']secretEncrypted["'] \| ["']dekEncrypted["']>` returns 1 match
    - Grep `export type ApiCredentialWithSecret = ApiCredential` returns 1 match
    - Grep `PEM_REGEX` returns ≥ 1 match
    - `npm run test -- server/__tests__/apiCredentials/schema.test.ts` exits 0
    - `npx tsc --noEmit -p tsconfig.json 2>&1 | grep "apiCredential" | grep "error TS"` returns 0 matches (no TS errors related to apiCredentials)
  </acceptance_criteria>
  <done>Discriminated union com 7 variantes + patch schema + tipos derivados exportados, typecheck verde, stubs continuam skipped.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` retorna sem erro relacionado a `apiCredentials` ou `api_auth_type`
- `npm run test -- server/__tests__/apiCredentials/schema.test.ts` exit 0 (stubs continuam todo/skipped)
- `npm run test` exit 0 (suite completa não regride)
</verification>

<success_criteria>
- pgEnum + pgTable + relations + insertSchema + patchSchema + 5 tipos exportados — todos em `shared/schema.ts`
- Zero modificações em código pré-existente
- Plan 03 (helpers) pode importar `ApiCredentialSafe` se precisar
- Plan 04 (storage) pode importar tudo o que precisa daqui
</success_criteria>

<output>
Após completion, criar `.planning/phases/10-api-credentials/10-02-SUMMARY.md` documentando:
- Linhas adicionadas em `shared/schema.ts` (referência de onde foram inseridas)
- Lista exata de exports criados
- Confirmação que `npm run test` continua verde
</output>
